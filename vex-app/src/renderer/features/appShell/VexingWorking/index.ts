/**
 * The vexing scene's public gate. One export: the composed view the transcript
 * mounts. The canvas renderer and the two pure modules are implementation
 * details their own unit tests import directly.
 */

export { VexingWorking } from "./VexingWorking.js";
