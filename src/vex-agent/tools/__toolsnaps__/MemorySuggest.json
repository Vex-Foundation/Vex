{
  "definition": {
    "function": {
      "description": "Propose a durable, cross-session LESSON for long-term memory - a trading insight, a strategy/risk lesson, a stable user preference, or a project fact or constraint. Write title and summary in English (embedding retrieval is significantly stronger on English). Use this when something just learned would change how a LATER session acts: a strategy that worked or failed for a stated reason, a risk rule the user set, a preference they expressed, or a protocol constraint you had to discover the hard way. A fact that only matters for the rest of this conversation does not belong here. This does NOT write memory directly. It STAGES a candidate; an async manager later reviews it, dedupes it, and decides whether to promote it into long-term memory. You get back a candidateId and status, not a stored memory. Never include secrets (keys, seeds, API tokens) - a candidate carrying one is REJECTED and nothing is stored. Do NOT record live values (current balances, prices, gas, amounts, open quotes) - memory is for the durable LESSON, not the snapshot, and a candidate that reads as live state is REJECTED. Persisted memory text is English-only: a candidate whose title/summary/content does not read as English is REJECTED - translate the durable lesson into English and re-suggest. Wallet and transaction addresses are auto-masked, so the lesson survives without the raw value. Attach evidence_refs (protocol execution / capture ids, with optional instrumentKey / positionKey) when the lesson came from a real trade - it makes the lesson far stronger downstream. source_refs is pointer-only (messageIds / toolCallIds) provenance from this session. response_format: 'concise' (default) returns the candidate id + status; 'detailed' adds redaction counts, the derived source tier, and the dual-trace retrieval window.",
      "name": "MemorySuggest",
      "parameters": {
        "additionalProperties": false,
        "properties": {
          "confidence": {
            "description": "Your confidence in the lesson, 0..1.",
            "type": "number"
          },
          "content_md": {
            "description": "Optional fuller markdown body in English (defaults to empty). Stored verbatim (after redaction).",
            "type": "string"
          },
          "entities": {
            "description": "Optional entity tokens this lesson is about (e.g. ['SOL', 'kyberswap']).",
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "event_time": {
            "description": "Optional ISO 8601 timestamp of when the underlying event happened.",
            "type": "string"
          },
          "evidence_refs": {
            "description": "Immutable evidence anchors when the lesson came from a real trade. Each: { executionId:int (required), captureItemId:int?, instrumentKey:string?, positionKey:string? }.",
            "items": {
              "additionalProperties": false,
              "properties": {
                "captureItemId": {
                  "type": "number"
                },
                "executionId": {
                  "type": "number"
                },
                "instrumentKey": {
                  "type": "string"
                },
                "positionKey": {
                  "type": "string"
                }
              },
              "required": [
                "executionId"
              ],
              "type": "object"
            },
            "type": "array"
          },
          "importance": {
            "description": "Importance 1..10 (default 5).",
            "type": "number"
          },
          "kind": {
            "description": "Free-form snake_case kind, English (e.g. trade_lesson, risk_rule, user_preference, protocol_fact). Reuse an existing kind before inventing a new one.",
            "type": "string"
          },
          "observed_at": {
            "description": "Optional ISO 8601 timestamp of when you observed the lesson.",
            "type": "string"
          },
          "response_format": {
            "description": "Response shape, 'concise' or 'detailed'. Default 'concise'. 'detailed' adds redaction counts, source tier and the dual-trace window to the id and status 'concise' returns.",
            "enum": [
              "concise",
              "detailed"
            ],
            "type": "string"
          },
          "source_refs": {
            "additionalProperties": false,
            "description": "Pointer-only provenance from THIS session: { messageIds:[int], toolCallIds:[string] }. No free text.",
            "properties": {
              "messageIds": {
                "items": {
                  "type": "number"
                },
                "type": "array"
              },
              "toolCallIds": {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            },
            "type": "object"
          },
          "summary": {
            "description": "1-3 sentences in English stating the durable lesson. This is ALSO the retrieval representation - title + summary are the embedding input, so write retrieval-quality semantic text using stable protocol/ticker names (and their common synonyms), never live balances, prices, amounts, or transient quotes.",
            "type": "string"
          },
          "tags": {
            "description": "Optional string tags (e.g. ['risk', 'memecoin']).",
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "title": {
            "description": "Single thesis or lesson, in English. Embedding input together with summary.",
            "type": "string"
          }
        },
        "required": [
          "kind",
          "title",
          "summary"
        ],
        "type": "object"
      }
    },
    "type": "function"
  },
  "metadata": {
    "actionKind": "local_write",
    "gates": {
      "declaredVisibility": {},
      "measured": {
        "hiddenWithoutEnvGates": false,
        "visibleInMissionSetup": true
      },
      "proactive": false,
      "requiresEnv": null,
      "showOnlyWhenEnvMissing": null
    },
    "kind": "internal",
    "mutating": false,
    "pressureSafety": "mutating",
    "withheld": false
  }
}
