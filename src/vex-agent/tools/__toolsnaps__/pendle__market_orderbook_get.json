{
  "definition": {
    "function": {
      "description": "Show the resting LIMIT-ORDER depth on one Pendle market: implied-APY levels with their sizes on the long-yield and short-yield sides, plus the best level on each side. Vex CANNOT FILL these orders - every Pendle quote and trade Vex builds routes through the AMM only, so this is the price quality being forgone, not a price you can take. Use this when you want to judge whether an AMM quote is competitive, how deep the market is around your size, or how much price quality a limit order elsewhere would capture. RETURNS `summary`, `chain`, `market`, `marketName`, `matured`, `precision`, `sizeUnit`, `asOf`, `note`, `whitelisted`, `nextStep`, and, when the market is whitelisted, `best` with a `bestNote`, `levelCounts` for the longYield and shortYield sides, `truncated` with a `truncationNote`, an `amountsNote`, and `levels` split into `longYield` and `shortYield`, each level carrying `impliedApyPercent`, `size` and `incentiveQualifiedSize`. A market Pendle has not whitelisted for limit orders comes back with `whitelisted` false and `levels` and `best` null, which means all of its liquidity is AMM liquidity and the quote already sees it - that is an answer, not an error. A market address that resolves to nothing returns the absence shape instead: `summary`, `resolution`, `absenceProven`, `chain`, `queried`, `asOf` and `nextStep`. Sizes are the market's PT/YT unit as a raw amount with its `decimals` and an `exact` human value, flagged `unreadable` rather than guessed when Vex cannot resolve the decimals. `limit` bounds the levels per side and `levelCounts` always says how many exist, so nothing is dropped silently and there is no continuation to fetch. Read-only; Vex never places, signs or cancels a limit order.",
      "name": "pendle__market_orderbook_get",
      "parameters": {
        "additionalProperties": false,
        "properties": {
          "chain": {
            "description": "Chain slug or numeric id (e.g. 'ethereum', 'arbitrum', 42161). An unsupported chain is rejected by name.",
            "type": "string"
          },
          "limit": {
            "description": "Levels to show per side: whole number 1-50 (default 10); above that it is rejected by name. The response always reports how many levels exist, so nothing is dropped silently.",
            "type": "number"
          },
          "market": {
            "description": "Market (LP) CONTRACT ADDRESS, 0x-prefixed 40-hex. Not the PT or YT - pendle__market_get resolves either to its market.",
            "type": "string"
          },
          "precision": {
            "description": "Decimal places the implied-APY axis is rounded to: whole number 0-3 (default 2). Provider-required; anything outside the range is rejected by name. Coarser precision merges nearby levels into fewer, larger ones.",
            "type": "number"
          }
        },
        "required": [
          "chain",
          "market"
        ],
        "type": "object"
      }
    },
    "type": "function"
  },
  "metadata": {
    "actionKind": "read",
    "advertised": true,
    "kind": "protocol",
    "lifecycle": "active",
    "mutating": false,
    "namespace": "pendle",
    "requiresEnv": null,
    "toolId": "pendle.orderbook"
  }
}
