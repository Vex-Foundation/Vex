{
  "definition": {
    "function": {
      "description": "Browse the Virtuals genesis launch calendar — points-sale events that precede agent-token launches (mostly Base), newest first, with start/end windows, participant counts, and the linked agent. Use to track upcoming or past launches. Suspicious far-future dates are spam. Read-only.",
      "name": "virtuals__geneses",
      "parameters": {
        "additionalProperties": false,
        "properties": {
          "limit": {
            "description": "Max geneses to return (default 20, max 100). Out of range is rejected, not clamped.",
            "type": "number"
          },
          "page": {
            "description": "1-based page for older genesis events (default 1).",
            "type": "number"
          },
          "pageSize": {
            "description": "Rows fetched from the provider for this page (default 100, max 200).",
            "type": "number"
          }
        },
        "type": "object"
      }
    },
    "type": "function"
  },
  "metadata": {
    "actionKind": "read",
    "advertised": true,
    "kind": "protocol",
    "lifecycle": "active",
    "mutating": false,
    "namespace": "virtuals",
    "requiresEnv": null,
    "toolId": "virtuals.geneses"
  }
}
