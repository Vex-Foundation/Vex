{
  "definition": {
    "function": {
      "description": "Pin an ERC-20 token on a LOCAL chain (e.g. Robinhood 4663) so `WalletBalances` and the portfolio track it. Local chains scan a fixed set (seed tokens + pins), so a token outside that set is invisible until it is pinned. Use this when a token arrived on a local chain by transfer or airdrop and does not show up in balances; swap and bridge executes auto-pin what they buy, so you rarely need it after a trade. It writes a DB-only bookmark against your wallet and that chain - no on-chain transaction, nothing signed, nothing spent - and the effect the user sees is the extra token appearing in `WalletBalances` and the portfolio. Khalani-covered chains discover tokens on their own and are refused by name here. RETURNS chain and chainId with, for pin, the token and `pinned`, plus a note when the address was already a seed token; for unpin, the token and `unpinned`; and for list, the wallet with `seedTokens` (address, label) and `pinned` (address, source, createdAt).",
      "name": "WalletTrackToken",
      "parameters": {
        "additionalProperties": false,
        "properties": {
          "action": {
            "description": "pin adds a token to tracking, unpin removes it, list shows the chain's seed + pinned set.",
            "enum": [
              "pin",
              "unpin",
              "list"
            ],
            "type": "string"
          },
          "chain": {
            "description": "Local chain alias or id (e.g. 'robinhood', '4663').",
            "type": "string"
          },
          "token": {
            "description": "ERC-20 contract ADDRESS (0x…). Required for pin/unpin; ignored for list. Resolve a symbol to its address first.",
            "type": "string"
          }
        },
        "required": [
          "action",
          "chain"
        ],
        "type": "object"
      }
    },
    "type": "function"
  },
  "metadata": {
    "actionKind": "local_write",
    "gates": {
      "declaredVisibility": {},
      "measured": {
        "hiddenWithoutEnvGates": false,
        "visibleInMissionSetup": true
      },
      "proactive": false,
      "requiresEnv": null,
      "showOnlyWhenEnvMissing": null
    },
    "kind": "internal",
    "mutating": false,
    "pressureSafety": "read_only",
    "withheld": false
  }
}
