{
  "definition": {
    "function": {
      "description": "Price candles (open/high/low/close/volume) for ONE Pendle asset — a PT, a YT or an LP token — at hourly, daily or weekly resolution. Use it to see how a PT's price has moved before buying or exiting one. Timestamps are returned as ISO instants. A candle with no recorded trades reports volume null, never 0, and for LP assets Pendle reports volume as 0 on this endpoint regardless — use pendle.market.history with the tradingVolume field for a market's real traded volume. At most 1440 rows are returned and any truncation is stated explicitly. These are provider price marks, not executable quotes: what you would receive depends on the market's depth at your size. Read-only.",
      "name": "pendle__market__candles",
      "parameters": {
        "additionalProperties": false,
        "properties": {
          "asset": {
            "description": "PT, YT or LP CONTRACT ADDRESS, 0x-prefixed 40-hex. This endpoint covers those three only — it does not price SY or ordinary tokens. pendle.market.get lists a market's legs.",
            "type": "string"
          },
          "chain": {
            "description": "Chain slug or numeric id (e.g. 'ethereum', 'base', 8453). An unsupported chain is rejected by name.",
            "type": "string"
          },
          "from": {
            "description": "Window start as an ISO-8601 instant (e.g. '2026-07-20T00:00:00Z'). Omit to let Pendle choose the window. Combined with timeFrame it must cover at most 1440 candles.",
            "type": "string"
          },
          "timeFrame": {
            "description": "Candle size: 'hour', 'day' (default) or 'week'. Anything else is rejected by name.",
            "type": "string"
          },
          "to": {
            "description": "Window end as an ISO-8601 instant. Must be later than `from`. Omit for 'up to now'.",
            "type": "string"
          }
        },
        "required": [
          "chain",
          "asset"
        ],
        "type": "object"
      }
    },
    "type": "function"
  },
  "metadata": {
    "actionKind": "read",
    "advertised": true,
    "kind": "protocol",
    "lifecycle": "active",
    "mutating": false,
    "namespace": "pendle",
    "requiresEnv": null,
    "toolId": "pendle.market.candles"
  }
}
