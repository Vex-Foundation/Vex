{
  "definition": {
    "function": {
      "description": "Trace the full version chain (root → head) of a long-term memory entry from any id in the chain, plus its reinforcement timeline (when it was first promoted, last reinforced, and its outcome version). Use this when you have a historical id (e.g. from MemoryGet's supersededBy/supersedesId) and want to see how the lesson evolved and whether the current head is still active. Returns compact metadata (no full content — use MemoryGet for that). Read-only. Does not require the embeddings service.",
      "name": "MemoryHistory",
      "parameters": {
        "additionalProperties": false,
        "properties": {
          "id": {
            "description": "Any long-term memory entry id in the chain (root, middle, or head).",
            "type": "number"
          },
          "response_format": {
            "description": "Reserved — the chain + reinforcement timeline are returned the same way for both.",
            "enum": [
              "concise",
              "detailed"
            ],
            "type": "string"
          }
        },
        "required": [
          "id"
        ],
        "type": "object"
      }
    },
    "type": "function"
  },
  "metadata": {
    "actionKind": "read",
    "gates": {
      "declaredVisibility": {},
      "measured": {
        "hiddenWithoutEnvGates": false,
        "visibleInMissionSetup": true
      },
      "proactive": false,
      "requiresEnv": null,
      "showOnlyWhenEnvMissing": null
    },
    "kind": "internal",
    "mutating": false,
    "pressureSafety": "read_only",
    "withheld": false
  }
}
