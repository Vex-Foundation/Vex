{
  "definition": {
    "function": {
      "description": "Check if a token is a honeypot or has fee-on-transfer tax. Essential safety check before trading.",
      "name": "kyberswap__tokens__check",
      "parameters": {
        "additionalProperties": false,
        "properties": {
          "chain": {
            "description": "Chain slug or alias.",
            "type": "string"
          },
          "tokenAddress": {
            "description": "Token contract address to inspect (0x… on the named chain). Resolve it with token_find first — a symbol is not accepted. The former key `address` is retired and is rejected by name.",
            "type": "string"
          }
        },
        "required": [
          "chain",
          "tokenAddress"
        ],
        "type": "object"
      }
    },
    "type": "function"
  },
  "metadata": {
    "actionKind": "read",
    "advertised": true,
    "kind": "protocol",
    "lifecycle": "active",
    "mutating": false,
    "namespace": "kyberswap",
    "requiresEnv": null,
    "toolId": "kyberswap.tokens.check"
  }
}
