{
  "definition": {
    "function": {
      "description": "Get detailed public metadata for one Lighter market on Core or Robinhood Chain by numeric marketId. Use when the user already has a market id and needs last trade price, daily activity, volume, open interest, fees, decimals, funding fields, minimums, and status before inspecting depth, trades, or candles. Returns matching detail rows and fails clearly if the market id is not found. Base and quote asset ids are numeric provider identifiers, not symbols; never infer labels for them from the market symbol. Read-only: no account data, signing, order placement, deposit, or withdrawal support.",
      "name": "lighter__market_get",
      "parameters": {
        "additionalProperties": false,
        "properties": {
          "environment": {
            "description": "Optional public Lighter environment to read: core for Lighter Core, rhc for Robinhood Chain. Defaults to rhc for conversational Lighter requests. Any other value is rejected.",
            "enum": [
              "core",
              "rhc"
            ],
            "type": "string"
          },
          "filter": {
            "description": "Optional market-type filter accepted by Lighter: all, spot, or perp. The value is exact and unsupported values are rejected.",
            "enum": [
              "all",
              "spot",
              "perp"
            ],
            "type": "string"
          },
          "marketId": {
            "description": "Numeric Lighter market id from lighter__markets_list, as an integer from 0 to 65535. It names the order book to read.",
            "type": "number"
          }
        },
        "required": [
          "marketId"
        ],
        "type": "object"
      }
    },
    "type": "function"
  },
  "metadata": {
    "actionKind": "read",
    "advertised": true,
    "kind": "protocol",
    "lifecycle": "active",
    "mutating": false,
    "namespace": "lighter",
    "requiresEnv": null,
    "toolId": "lighter.market.get"
  }
}
