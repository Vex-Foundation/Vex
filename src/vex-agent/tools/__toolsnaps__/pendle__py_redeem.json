{
  "definition": {
    "function": {
      "description": "Redeem a Pendle PT and YT pair back to a token BEFORE expiry — burns an EQUAL amount of principal token (PT) and yield token (YT) and returns the output token. This needs BOTH legs in equal amount; a MATURED PT with no YT uses pendle.pt.redeem instead. Approval-gated; pins the canonical Pendle Router. REQUIRES a fresh matching pendle.py.quote (direction redeem) first.",
      "name": "pendle__py_redeem",
      "parameters": {
        "additionalProperties": false,
        "properties": {
          "amountIn": {
            "description": "Amount of the PT+YT pair to burn in human-readable units (equal PT and YT).",
            "type": "string"
          },
          "chain": {
            "description": "Chain slug or id — one of Pendle's 11 chains (e.g. 'ethereum', 'arbitrum', 'base', 'bsc').",
            "type": "string"
          },
          "dryRun": {
            "description": "Preview without executing.",
            "type": "boolean"
          },
          "pt": {
            "description": "The market's PT (principal token) CONTRACT ADDRESS — the anchor that resolves the market and its YT.",
            "type": "string"
          },
          "slippageBps": {
            "description": "Slippage tolerance in basis points (default 100 = 1%).",
            "type": "number"
          },
          "tokenOut": {
            "description": "The output token CONTRACT ADDRESS. Defaults to the market's underlying asset.",
            "type": "string"
          }
        },
        "required": [
          "chain",
          "pt",
          "amountIn"
        ],
        "type": "object"
      }
    },
    "type": "function"
  },
  "metadata": {
    "actionKind": "user_wallet_broadcast",
    "advertised": true,
    "kind": "protocol",
    "lifecycle": "active",
    "mutating": true,
    "namespace": "pendle",
    "requiresEnv": null,
    "toolId": "pendle.py.redeem"
  }
}
