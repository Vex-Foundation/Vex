{
  "definition": {
    "function": {
      "description": "Safety-check an EVM token before trading it: detects honeypots and fee-on-transfer (tax) tokens via KyberSwap. Pass the chain and the token contract `tokenAddress` (resolve it with TokenFind first). The former key `address` is retired and is rejected by name. Read-only.",
      "name": "TokenCheck",
      "parameters": {
        "additionalProperties": false,
        "properties": {
          "chain": {
            "description": "EVM chain slug or alias (ethereum, base, arbitrum, …). Accepts a chain slug/alias or the numeric chain id TokenFind returns (e.g. base or 8453).",
            "type": "string"
          },
          "tokenAddress": {
            "description": "Token contract address to inspect (0x… on the named chain). Resolve it with TokenFind first — a symbol is not accepted. The former key `address` is retired and is rejected by name.",
            "type": "string"
          }
        },
        "required": [
          "chain",
          "tokenAddress"
        ],
        "type": "object"
      }
    },
    "type": "function"
  },
  "metadata": {
    "actionKind": "read",
    "gates": {
      "declaredVisibility": {},
      "measured": {
        "hiddenWithoutEnvGates": false,
        "visibleInMissionSetup": true
      },
      "proactive": false,
      "requiresEnv": null,
      "showOnlyWhenEnvMissing": null
    },
    "kind": "internal",
    "mutating": false,
    "pressureSafety": "read_only",
    "withheld": false
  }
}
