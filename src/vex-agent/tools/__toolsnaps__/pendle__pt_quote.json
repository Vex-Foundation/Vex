{
  "definition": {
    "function": {
      "description": "Preview a Pendle PT trade - quote buying a PT with a payment token, selling a PT early, or redeeming a matured PT. Call this before every pendle__pt_buy, pendle__pt_sell and pendle__pt_redeem, and whenever the user asks what locking a fixed rate to this market's expiry would cost or return. RETURNS `action` (swap or redeem), `direction` (buy, sell or redeem), `chainId`, `tokenIn` and `tokenOut`, the resolved `pt`, `yt` and `market`, `receiver` (always the session wallet), `expiry`, `liquidityUsd`, `priceImpact`, `feeUsdEstimate` (Pendle's own estimated route fee in USD), `amountIn`, `amountOut`, `aggregator` and `slippageBps`. Quotes route through Pendle's AMM only - limit-order liquidity is excluded, so a better resting price may exist. Exact-output is impossible: you specify amountIn and receive an estimate, never a guaranteed amountOut. This call has a SIDE EFFECT - it records the prequote authorization the matching broadcast tool requires, so quoting arms that tool for ~15 minutes. Read-only.",
      "name": "pendle__pt_quote",
      "parameters": {
        "additionalProperties": false,
        "properties": {
          "amountIn": {
            "description": "Amount of tokenIn in human-readable units.",
            "type": "string"
          },
          "chain": {
            "description": "Chain slug or id — one of Pendle's 11 chains (e.g. 'ethereum', 'arbitrum', 'base', 'bsc').",
            "type": "string"
          },
          "slippageBps": {
            "description": "Slippage tolerance in basis points (default 100 = 1%).",
            "type": "number"
          },
          "tokenIn": {
            "description": "Input token address (payment token for a buy; PT address for a sell/redeem).",
            "type": "string"
          },
          "tokenOut": {
            "description": "Output token address (PT for a buy; payment/underlying for a sell/redeem).",
            "type": "string"
          }
        },
        "required": [
          "chain",
          "tokenIn",
          "tokenOut",
          "amountIn"
        ],
        "type": "object"
      }
    },
    "type": "function"
  },
  "metadata": {
    "actionKind": "read",
    "advertised": true,
    "kind": "protocol",
    "lifecycle": "active",
    "mutating": false,
    "namespace": "pendle",
    "requiresEnv": null,
    "toolId": "pendle.pt.quote"
  }
}
