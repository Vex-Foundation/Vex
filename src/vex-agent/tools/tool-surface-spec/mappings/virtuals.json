{
  "namespace": "virtuals",
  "entries": [
    {
      "toolId": "virtuals.list",
      "publicName": "virtuals__agents_discover",
      "rationale": "Names the resource and the operation. `virtuals.list` said neither what it lists nor that it screens: it takes chain, status, sortBy, limit, page and pageSize and returns a filtered, ranked, bounded window of agent tokens (manifests/agents.ts:33-55), which is exactly the canonical `discover` verb rather than `list`."
    },
    {
      "toolId": "virtuals.get",
      "publicName": "virtuals__agent_get",
      "rationale": "Names the resource; singular for the by-id read, matching the plural discover above."
    },
    {
      "toolId": "virtuals.graduations",
      "publicName": "virtuals__graduations_list",
      "rationale": "Adds the missing canonical verb. Mapped 1:1 here, but see the merge proposal in protocol-naming-2.md: the handler is virtuals.list with sort lpCreatedAt plus an AVAILABLE-and-lpCreatedAt predicate over the same projector (handlers.ts:139-178 vs :81-120)."
    },
    {
      "toolId": "virtuals.geneses",
      "publicName": "virtuals__genesis_launches_list",
      "rationale": "Provider jargon expanded. `geneses` is an unusual plural that carries no meaning for a model scanning the namespace; the tool browses the genesis launch calendar, points-sale events preceding agent-token launches (manifests/agents.ts:93). Kept in the singular-noun-plus-plural-resource form with the canonical verb last."
    },
    {
      "toolId": "virtuals.trades",
      "publicName": "virtuals__agent_trades_list",
      "rationale": "Names the resource and adds the canonical verb. The tool reads ONE agent's bonding-curve trade tape from the provider's vp-api feed, so the resource is the agent's trades and the operation is a bounded list (manifests/market.ts)."
    },
    {
      "toolId": "virtuals.candles",
      "publicName": "virtuals__agent_candles_list",
      "rationale": "Same shape as the trades read: one agent's OHLCV history, listed with a bound and a backwards cursor. `candles` is the term every other price-history tool in the tree uses (pools ohlcv, dexscreener bars), so the noun stays consistent across namespaces."
    },
    {
      "toolId": "virtuals.creator_fees",
      "publicName": "virtuals__creator_fees_get",
      "rationale": "Existing snake words preserved, canonical by-id verb appended. The read answers for ONE agent on ONE chain from AgentTaxV2 at a pinned block (manifests/creator-fees.ts); it stays a `get` and never a claim verb, because the collection path is gated on SWAP_ROLE that the creator wallet does not hold, which the tool reports as `unsupported` with the measurement."
    },
    {
      "toolId": "virtuals.trade.quote",
      "publicName": "virtuals__agent_trade_quote",
      "rationale": "Names the resource and the operation, by FAMILY rather than by chain or side. One curve surface serves base and robinhood and both directions, so `agent_trade` is the resource and `quote` the canonical read verb - the same pairing every other venue uses for the priced half of a two-step trade."
    },
    {
      "toolId": "virtuals.trade.execute",
      "publicName": "virtuals__agent_trade_execute",
      "rationale": "The signing half of the same pair, under the canonical `execute` verb. Deliberately NOT split into buy/sell tools: the side is a parameter the prequote identity binds, and two tools would double the surface while making a buy quote look able to authorize a sell execute."
    },
    {
      "toolId": "virtuals.launch.preview",
      "publicName": "virtuals__agent_launch_preview",
      "rationale": "Names the resource (an AGENT, which is what Virtuals calls the thing being launched) and the operation, and keeps the `agent_` prefix every mutating tool in this namespace already carries. `preview` rather than `quote` because the pair it belongs to is a launch, not a trade: `quote` is reserved in this tree for a priced swap, and the pools.fun launch family already established `preview` as the verb for the read-only half of a launch."
    },
    {
      "toolId": "virtuals.launch.execute",
      "publicName": "virtuals__agent_launch_execute",
      "rationale": "The `_execute` suffix matches every other signing tool in the namespace and in the tree, so a model that has read the trade pair knows which half spends money. `launch` rather than `create` or `deploy` because it is the venue's own word for the operation and for the contract event (`PreLaunched`, `Launched`)."
    },
    {
      "toolId": "virtuals.launch.status",
      "publicName": "virtuals__agent_launch_status",
      "rationale": "`status` names the question the venue's two-transaction shape creates: whether the keeper has run `launch()` yet. It is deliberately not `_get`, which in this namespace reads an AGENT's profile; this reads a LAUNCH's progress and answers three states no profile read distinguishes."
    },
    {
      "toolId": "virtuals.launch.cancel",
      "publicName": "virtuals__agent_launch_cancel",
      "rationale": "`cancel` is the contract's own verb (`cancelLaunch`, `CancelledLaunch`), so the tool name and the on-chain event a user would look up agree. The `agent_launch_` prefix keeps it beside the three tools it shares a lifecycle with rather than filing it under a generic refund verb."
    }
  ]
}
