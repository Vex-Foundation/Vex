{
  "toolCount": 159,
  "tools": [
    {
      "alwaysLoad": true,
      "descriptionBytes": 3661,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "cursor": {
            "description": "Opaque pagination cursor (transactions) — pass the prior response's nextCursor to fetch the next page",
            "type": "string"
          },
          "limit": {
            "description": "Max rows (default 20)",
            "type": "number"
          },
          "namespace": {
            "description": "Protocol filter (e.g. kyberswap, khalani)",
            "type": "string"
          },
          "productType": {
            "description": "Product filter (e.g. spot, perps, prediction, bridge, order, lend)",
            "type": "string"
          },
          "txHash": {
            "description": "Transaction hash anchor (transactions) — return rows matching this txHash",
            "type": "string"
          },
          "view": {
            "description": "What to inspect (see description for group breakdown)",
            "enum": [
              "transactions",
              "activity",
              "balances",
              "snapshots",
              "summary",
              "executions",
              "mission_baseline"
            ],
            "type": "string"
          }
        },
        "required": [
          "view"
        ],
        "type": "object"
      },
      "kind": "internal",
      "publicName": "AgentScan",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Review this wallet's recorded activity"
    },
    {
      "alwaysLoad": true,
      "descriptionBytes": 1926,
      "destructiveHint": true,
      "inputSchema": {
        "properties": {
          "amountRaw": {
            "description": "Amount in raw atomic units of fromToken (e.g. USDC has 6 decimals, so 1 USDC = \"1000000\"). Get the token's decimals from TokenFind / khalani.tokens.search, which returns decimals per chain — a raw amount next to a token whose decimals you have not read is a thousandfold error waiting to happen.",
            "type": "string"
          },
          "filler": {
            "description": "Restrict quotes to a specific filler.",
            "type": "string"
          },
          "fromAddress": {
            "description": "Source wallet address override.",
            "type": "string"
          },
          "fromChain": {
            "description": "Source chain ID or alias.",
            "type": "string"
          },
          "fromToken": {
            "description": "Source token address.",
            "type": "string"
          },
          "recipient": {
            "description": "Destination recipient override (defaults to your dest-chain wallet).",
            "type": "string"
          },
          "toChain": {
            "description": "Destination chain ID or alias.",
            "type": "string"
          },
          "toToken": {
            "description": "Destination token address.",
            "type": "string"
          },
          "tradeType": {
            "description": "EXACT_INPUT or EXACT_OUTPUT (default: EXACT_INPUT).",
            "type": "string"
          }
        },
        "required": [
          "fromChain",
          "fromToken",
          "toChain",
          "toToken",
          "amountRaw"
        ],
        "type": "object"
      },
      "kind": "internal",
      "publicName": "BridgeExecute",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Execute a cross-chain bridge"
    },
    {
      "alwaysLoad": true,
      "descriptionBytes": 723,
      "destructiveHint": true,
      "inputSchema": {
        "properties": {
          "amountRaw": {
            "description": "Amount in raw atomic units of fromToken (e.g. USDC has 6 decimals, so 1 USDC = \"1000000\"). Get the token's decimals from TokenFind / khalani.tokens.search, which returns decimals per chain — a raw amount next to a token whose decimals you have not read is a thousandfold error waiting to happen.",
            "type": "string"
          },
          "fromChain": {
            "description": "Source chain ID or alias.",
            "type": "string"
          },
          "fromToken": {
            "description": "Source token address, or native ETH/native.",
            "type": "string"
          },
          "recipient": {
            "description": "Destination recipient override (defaults to your dest-chain wallet).",
            "type": "string"
          },
          "slippageBps": {
            "description": "Slippage tolerance in basis points for the destination-side fill (1 bps = 0.01%). Higher tolerance widens the worst-case received amount on the destination chain. When a bridge quote fails or fills keep expiring, re-quote with a higher value rather than retrying the same one. Vex caps it at 1000 (10%) and rejects anything above rather than clamping.",
            "type": "number"
          },
          "toChain": {
            "description": "Destination chain ID or alias.",
            "type": "string"
          },
          "toToken": {
            "description": "Destination token address, or native ETH/native.",
            "type": "string"
          },
          "tradeType": {
            "description": "EXACT_INPUT or EXACT_OUTPUT (default: EXACT_INPUT).",
            "type": "string"
          }
        },
        "required": [
          "fromChain",
          "fromToken",
          "toChain",
          "toToken",
          "amountRaw"
        ],
        "type": "object"
      },
      "kind": "internal",
      "publicName": "BridgeExecuteRelay",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Execute a Relay bridge"
    },
    {
      "alwaysLoad": true,
      "descriptionBytes": 1260,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "amountRaw": {
            "description": "Amount in raw atomic units of fromToken (e.g. USDC has 6 decimals, so 1 USDC = \"1000000\"). Get the token's decimals from TokenFind / khalani.tokens.search, which returns decimals per chain — a raw amount next to a token whose decimals you have not read is a thousandfold error waiting to happen.",
            "type": "string"
          },
          "filler": {
            "description": "Restrict quotes to a specific filler.",
            "type": "string"
          },
          "fromAddress": {
            "description": "Source wallet address override.",
            "type": "string"
          },
          "fromChain": {
            "description": "Source chain ID or alias.",
            "type": "string"
          },
          "fromToken": {
            "description": "Source token address.",
            "type": "string"
          },
          "recipient": {
            "description": "Destination recipient override.",
            "type": "string"
          },
          "toChain": {
            "description": "Destination chain ID or alias.",
            "type": "string"
          },
          "toToken": {
            "description": "Destination token address.",
            "type": "string"
          },
          "tradeType": {
            "description": "EXACT_INPUT or EXACT_OUTPUT (default: EXACT_INPUT).",
            "type": "string"
          }
        },
        "required": [
          "fromChain",
          "fromToken",
          "toChain",
          "toToken",
          "amountRaw"
        ],
        "type": "object"
      },
      "kind": "internal",
      "publicName": "BridgeQuote",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Quote a cross-chain bridge"
    },
    {
      "alwaysLoad": true,
      "descriptionBytes": 578,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "amountRaw": {
            "description": "Amount in raw atomic units of fromToken (e.g. USDC has 6 decimals, so 1 USDC = \"1000000\"). Get the token's decimals from TokenFind / khalani.tokens.search, which returns decimals per chain — a raw amount next to a token whose decimals you have not read is a thousandfold error waiting to happen.",
            "type": "string"
          },
          "fromChain": {
            "description": "Source chain ID or alias.",
            "type": "string"
          },
          "fromToken": {
            "description": "Source token address, or native ETH/native.",
            "type": "string"
          },
          "recipient": {
            "description": "Destination recipient override (defaults to your dest-chain wallet).",
            "type": "string"
          },
          "slippageBps": {
            "description": "Slippage tolerance in basis points for the destination-side fill (1 bps = 0.01%). Higher tolerance widens the worst-case received amount on the destination chain. When a bridge quote fails or fills keep expiring, re-quote with a higher value rather than retrying the same one. Vex caps it at 1000 (10%) and rejects anything above rather than clamping.",
            "type": "number"
          },
          "toChain": {
            "description": "Destination chain ID or alias.",
            "type": "string"
          },
          "toToken": {
            "description": "Destination token address, or native ETH/native.",
            "type": "string"
          },
          "tradeType": {
            "description": "EXACT_INPUT or EXACT_OUTPUT (default: EXACT_INPUT).",
            "type": "string"
          }
        },
        "required": [
          "fromChain",
          "fromToken",
          "toChain",
          "toToken",
          "amountRaw"
        ],
        "type": "object"
      },
      "kind": "internal",
      "publicName": "BridgeQuoteRelay",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Quote a Relay bridge"
    },
    {
      "alwaysLoad": true,
      "descriptionBytes": 1297,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "cursor": {
            "description": "List mode: the provider's own opaque continuation value, echoed back as `cursor` on the previous reply. Send back exactly what that reply returned; a reply with no `cursor` is the end of the list.",
            "type": "number"
          },
          "fromChain": {
            "description": "List mode: source chain filter (ID or alias).",
            "type": "string"
          },
          "limit": {
            "description": "List mode: max results.",
            "type": "number"
          },
          "orderId": {
            "description": "Khalani order ID. Provide to fetch a single order; omit to list your orders.",
            "type": "string"
          },
          "orderIds": {
            "description": "List mode: comma-separated order IDs to filter.",
            "type": "string"
          },
          "toChain": {
            "description": "List mode: destination chain filter (ID or alias).",
            "type": "string"
          },
          "txHashSearch": {
            "description": "List mode: search by transaction hash.",
            "type": "string"
          },
          "walletAddress": {
            "description": "List mode: the ACCOUNT address whose orders to list (optional - omit to use your configured wallet).",
            "type": "string"
          },
          "walletFamily": {
            "description": "List mode: wallet FAMILY, never a chain - eip155 or solana.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "kind": "internal",
      "publicName": "BridgeStatus",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Check a bridge order's status"
    },
    {
      "alwaysLoad": true,
      "descriptionBytes": 1326,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "action": {
            "description": "What to read",
            "enum": [
              "tx_receipt",
              "erc721_mint",
              "erc20_balance"
            ],
            "type": "string"
          },
          "address": {
            "description": "Recipient address — optional mint filter for erc721_mint (only mints to this address are returned)",
            "type": "string"
          },
          "chain": {
            "description": "Chain slug/alias, or the numeric chain id `TokenFind` returns (e.g. `base` or `8453`). Local-only chains work too: 'robinhood' / '4663'.",
            "type": "string"
          },
          "owner": {
            "description": "Address to read the ERC-20 balance of (erc20_balance). Omit to read your own selected wallet.",
            "type": "string"
          },
          "tokenAddress": {
            "description": "ERC-20 contract address (REQUIRED for erc20_balance)",
            "type": "string"
          },
          "txHash": {
            "description": "Transaction hash (for tx_receipt, erc721_mint)",
            "type": "string"
          }
        },
        "required": [
          "action",
          "chain"
        ],
        "type": "object"
      },
      "kind": "internal",
      "publicName": "ChainRead",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Read raw EVM chain data"
    },
    {
      "alwaysLoad": true,
      "descriptionBytes": 1630,
      "destructiveHint": true,
      "inputSchema": {
        "properties": {
          "amountIn": {
            "description": "Amount of tokenIn to swap, in HUMAN decimal units (e.g. \"1.5\"). Not wei/lamports.",
            "type": "string"
          },
          "chain": {
            "description": "Chain to swap on. EVM slugs/aliases route to KyberSwap (ethereum, base, arbitrum, robinhood, …); the literal \"solana\" routes to Jupiter. Accepts a chain slug/alias or the numeric chain id TokenFind returns (e.g. base or 8453).",
            "type": "string"
          },
          "slippageBps": {
            "description": "Slippage tolerance in basis points (1 bps = 0.01%); default 100 = 1%, which fits deep, liquid pairs. It is the ONLY price protection on the trade. Pass the SAME value to the quote and the execute, or omit it on both — a mismatch blocks the execute. On a thin or volatile pair (new listings, memecoins, small pools) 100 bps often fails. When it fails, the message says so and names this parameter: re-quote with a higher slippageBps and pass the same value to the execute. Do not read a slippage failure as \"this pair is untradeable\" and do not switch venue for it — another venue at 100 bps fails the same way. Vex caps it at 1000 (10%) and REJECTS anything above rather than clamping; every increase widens the worst-case price you accept, so raise it in steps.",
            "type": "number"
          },
          "tokenIn": {
            "description": "Input token. EVM: the token CONTRACT ADDRESS (resolve a symbol with TokenFind first) or native ETH/native. Solana: symbol or mint.",
            "type": "string"
          },
          "tokenOut": {
            "description": "Output token. EVM: the token CONTRACT ADDRESS (resolve a symbol with TokenFind first) or native ETH/native. Solana: symbol or mint.",
            "type": "string"
          }
        },
        "required": [
          "chain",
          "tokenIn",
          "tokenOut",
          "amountIn"
        ],
        "type": "object"
      },
      "kind": "internal",
      "publicName": "SwapExecute",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Execute a token swap"
    },
    {
      "alwaysLoad": true,
      "descriptionBytes": 701,
      "destructiveHint": true,
      "inputSchema": {
        "properties": {
          "amountIn": {
            "description": "Amount of tokenIn to swap, in HUMAN decimal units (e.g. \"1.5\"). Not wei.",
            "type": "string"
          },
          "chain": {
            "description": "EVM chain to swap on (ethereum, base, arbitrum, robinhood, …) — Uniswap ONLY, no venue routing. Solana is NOT supported by this tool.",
            "type": "string"
          },
          "slippageBps": {
            "description": "Slippage tolerance in basis points (1 bps = 0.01%); default 100 = 1%, which fits deep, liquid pairs. It is the ONLY price protection on the trade. Pass the SAME value to the quote and the execute, or omit it on both — a mismatch blocks the execute. On a thin or volatile pair (new listings, memecoins, small pools) 100 bps often fails. When it fails, the message says so and names this parameter: re-quote with a higher slippageBps and pass the same value to the execute. Do not read a slippage failure as \"this pair is untradeable\" and do not switch venue for it — another venue at 100 bps fails the same way. Vex caps it at 1000 (10%) and REJECTS anything above rather than clamping; every increase widens the worst-case price you accept, so raise it in steps.",
            "type": "number"
          },
          "tokenIn": {
            "description": "Input token — the token CONTRACT ADDRESS (resolve a symbol with TokenFind first) or native ETH/native.",
            "type": "string"
          },
          "tokenOut": {
            "description": "Output token — the token CONTRACT ADDRESS (resolve a symbol with TokenFind first) or native ETH/native.",
            "type": "string"
          }
        },
        "required": [
          "chain",
          "tokenIn",
          "tokenOut",
          "amountIn"
        ],
        "type": "object"
      },
      "kind": "internal",
      "publicName": "SwapExecuteUniswap",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Execute a Uniswap swap"
    },
    {
      "alwaysLoad": true,
      "descriptionBytes": 1321,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "amountIn": {
            "description": "Amount of tokenIn to swap, in HUMAN decimal units (e.g. \"1.5\"). Not wei/lamports.",
            "type": "string"
          },
          "chain": {
            "description": "Chain to swap on. EVM slugs/aliases route to KyberSwap (ethereum, base, arbitrum, robinhood, …); the literal \"solana\" routes to Jupiter. Accepts a chain slug/alias or the numeric chain id TokenFind returns (e.g. base or 8453).",
            "type": "string"
          },
          "slippageBps": {
            "description": "Slippage tolerance in basis points (1 bps = 0.01%); default 100 = 1%, which fits deep, liquid pairs. It is the ONLY price protection on the trade. Pass the SAME value to the quote and the execute, or omit it on both — a mismatch blocks the execute. On a thin or volatile pair (new listings, memecoins, small pools) 100 bps often fails. When it fails, the message says so and names this parameter: re-quote with a higher slippageBps and pass the same value to the execute. Do not read a slippage failure as \"this pair is untradeable\" and do not switch venue for it — another venue at 100 bps fails the same way. Vex caps it at 1000 (10%) and REJECTS anything above rather than clamping; every increase widens the worst-case price you accept, so raise it in steps.",
            "type": "number"
          },
          "tokenIn": {
            "description": "Input token. EVM: the token CONTRACT ADDRESS (resolve a symbol with TokenFind first) or native ETH/native. Solana: symbol or mint.",
            "type": "string"
          },
          "tokenOut": {
            "description": "Output token. EVM: the token CONTRACT ADDRESS (resolve a symbol with TokenFind first) or native ETH/native. Solana: symbol or mint.",
            "type": "string"
          }
        },
        "required": [
          "chain",
          "tokenIn",
          "tokenOut",
          "amountIn"
        ],
        "type": "object"
      },
      "kind": "internal",
      "publicName": "SwapQuote",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Quote a token swap"
    },
    {
      "alwaysLoad": true,
      "descriptionBytes": 656,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "amountIn": {
            "description": "Amount of tokenIn to swap, in HUMAN decimal units (e.g. \"1.5\"). Not wei.",
            "type": "string"
          },
          "chain": {
            "description": "EVM chain to swap on (ethereum, base, arbitrum, robinhood, …) — Uniswap ONLY, no venue routing. Solana is NOT supported by this tool.",
            "type": "string"
          },
          "slippageBps": {
            "description": "Slippage tolerance in basis points (1 bps = 0.01%); default 100 = 1%, which fits deep, liquid pairs. It is the ONLY price protection on the trade. Pass the SAME value to the quote and the execute, or omit it on both — a mismatch blocks the execute. On a thin or volatile pair (new listings, memecoins, small pools) 100 bps often fails. When it fails, the message says so and names this parameter: re-quote with a higher slippageBps and pass the same value to the execute. Do not read a slippage failure as \"this pair is untradeable\" and do not switch venue for it — another venue at 100 bps fails the same way. Vex caps it at 1000 (10%) and REJECTS anything above rather than clamping; every increase widens the worst-case price you accept, so raise it in steps.",
            "type": "number"
          },
          "tokenIn": {
            "description": "Input token — the token CONTRACT ADDRESS (resolve a symbol with TokenFind first) or native ETH/native.",
            "type": "string"
          },
          "tokenOut": {
            "description": "Output token — the token CONTRACT ADDRESS (resolve a symbol with TokenFind first) or native ETH/native.",
            "type": "string"
          }
        },
        "required": [
          "chain",
          "tokenIn",
          "tokenOut",
          "amountIn"
        ],
        "type": "object"
      },
      "kind": "internal",
      "publicName": "SwapQuoteUniswap",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Quote a Uniswap swap"
    },
    {
      "alwaysLoad": true,
      "descriptionBytes": 553,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "chain": {
            "description": "EVM chain slug or alias (ethereum, base, arbitrum, …). Accepts a chain slug/alias or the numeric chain id TokenFind returns (e.g. base or 8453).",
            "type": "string"
          },
          "tokenAddress": {
            "description": "Token contract address to inspect (0x… on the named chain). Resolve it with TokenFind first — a symbol is not accepted. The former key `address` is retired and is rejected by name.",
            "type": "string"
          }
        },
        "required": [
          "chain",
          "tokenAddress"
        ],
        "type": "object"
      },
      "kind": "internal",
      "publicName": "TokenCheck",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Check an EVM token for honeypot and tax"
    },
    {
      "alwaysLoad": true,
      "descriptionBytes": 1051,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "chainIds": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Comma-separated chain IDs or aliases. Accepts either a comma-separated string (\"1,8453\") or an array of strings ([\"1\",\"8453\"]) — the two are equivalent."
          },
          "query": {
            "description": "Search phrase or token address.",
            "type": "string"
          }
        },
        "required": [
          "query"
        ],
        "type": "object"
      },
      "kind": "internal",
      "publicName": "TokenFind",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Find a token's address and decimals"
    },
    {
      "alwaysLoad": true,
      "descriptionBytes": 2803,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "action": {
            "description": "Read-only Twitter/X operation. tweet_search: search posts by cashtag/hashtag/words with filters — the momentum workhorse (max 20 per call); user_timeline: recent posts by one account, newest first (max 20); user_details: one account's profile: follower count, verification, bio — author credibility; tweet_details: one post by id, with its engagement counts; tweet_replies: replies under one post — sentiment around a call; tweet_likers: accounts that liked a post (max 100) — who is behind a signal; tweet_retweeters: accounts that reposted a post (max 100); user_search: find accounts by name/handle text (max 20); user_replies: one account's replies, i.e. what it says under other posts (max 20); user_followers: an account's followers (max 100); user_following: who an account follows (max 100); space_details: one Space's metadata and participant counts; account_status: the configured research account itself — use it to check the session works.",
            "enum": [
              "tweet_search",
              "user_timeline",
              "user_details",
              "tweet_details",
              "tweet_replies",
              "tweet_likers",
              "tweet_retweeters",
              "user_search",
              "user_replies",
              "user_followers",
              "user_following",
              "space_details",
              "account_status"
            ],
            "type": "string"
          },
          "count": {
            "description": "Rows per call. Max 20 for search/timelines, max 100 for follower/liker lists. Budget: ~650 bytes per post row measured, so 20 post rows is ~12 KB in one response. `count` and `cursor` are what bound it.",
            "type": "number"
          },
          "cursor": {
            "description": "Cursor returned by a previous call (`next`).",
            "type": "string"
          },
          "filter": {
            "additionalProperties": false,
            "description": "tweet_search filter. Provide query or at least one filter field. For a momentum question the useful trio is cashtags + minLikes + withinHours.",
            "properties": {
              "cashtags": {
                "description": "Tickers to search as X cashtags, with or without the $ (e.g. [\"WIF\", \"$BONK\"]). Normalized to $TICKER and merged into includeWords. This is THE param for \"who is talking about this token\".",
                "items": {
                  "type": "string"
                },
                "type": "array"
              },
              "endDate": {
                "description": "Inclusive ISO datetime end of the window.",
                "type": "string"
              },
              "excludeWords": {
                "description": "Words that must not appear — useful against airdrop/giveaway spam.",
                "items": {
                  "type": "string"
                },
                "type": "array"
              },
              "fromUsers": {
                "description": "Only posts by these accounts (with or without @).",
                "items": {
                  "type": "string"
                },
                "type": "array"
              },
              "hashtags": {
                "description": "Hashtags, with or without #.",
                "items": {
                  "type": "string"
                },
                "type": "array"
              },
              "includePhrase": {
                "description": "Exact phrase to search.",
                "type": "string"
              },
              "includeWords": {
                "description": "Words that must ALL appear. Passed to X verbatim, so operators and $TICKERS work here directly.",
                "items": {
                  "type": "string"
                },
                "type": "array"
              },
              "language": {
                "description": "Language code such as en or pl. Honoured by X.",
                "type": "string"
              },
              "list": {
                "description": "Twitter/X list id to search within.",
                "type": "string"
              },
              "maxId": {
                "description": "Tweet id before which to search.",
                "type": "string"
              },
              "mentions": {
                "description": "Only posts mentioning these accounts.",
                "items": {
                  "type": "string"
                },
                "type": "array"
              },
              "minLikes": {
                "description": "Minimum likes, 1 or more. The single most useful filter for momentum: an unfiltered cashtag sweep measured 15 of 19 rows at zero engagement. 10 is a useful floor. 0 is rejected — X's query builder drops a 0 floor instead of applying it.",
                "type": "number"
              },
              "minReplies": {
                "description": "Minimum replies, 1 or more (0 is rejected — it would be silently dropped).",
                "type": "number"
              },
              "minRetweets": {
                "description": "Minimum reposts, 1 or more (0 is rejected — it would be silently dropped).",
                "type": "number"
              },
              "onlyLinks": {
                "description": "Only posts with links/media.",
                "type": "boolean"
              },
              "onlyOriginal": {
                "description": "Exclude replies. It does NOT exclude quote-tweets — measured, 7 of 19 \"originals\" were quote-tweets. Check each row's `quoted` field.",
                "type": "boolean"
              },
              "onlyReplies": {
                "description": "Only replies.",
                "type": "boolean"
              },
              "onlyText": {
                "description": "Only text-only posts.",
                "type": "boolean"
              },
              "optionalWords": {
                "description": "Words where any may appear.",
                "items": {
                  "type": "string"
                },
                "type": "array"
              },
              "quoted": {
                "description": "Only posts quoting this tweet id.",
                "type": "string"
              },
              "sinceId": {
                "description": "Tweet id after which to search.",
                "type": "string"
              },
              "startDate": {
                "description": "Inclusive ISO datetime start of the window. Honoured by X. Use withinHours instead for a relative window.",
                "type": "string"
              },
              "toUsers": {
                "description": "Only posts addressed to these accounts.",
                "items": {
                  "type": "string"
                },
                "type": "array"
              },
              "top": {
                "description": "Select from X's TOP (relevance) result set instead of LATEST. It changes WHICH posts match, NOT their order — the client re-sorts every result newest-first regardless.",
                "type": "boolean"
              },
              "withinHours": {
                "description": "Only posts from the last N hours (1-720). Derives startDate for you and echoes the resolved UTC instant in filtersApplied. Rejected together with an explicit startDate — pass one or the other.",
                "type": "number"
              }
            },
            "type": "object"
          },
          "query": {
            "description": "Required for user_search. For tweet_search, shortcut text split on spaces into filter.includeWords (each word must appear).",
            "type": "string"
          },
          "sortBy": {
            "description": "Sort order for tweet_replies.",
            "enum": [
              "LATEST",
              "LIKES",
              "RELEVANCE"
            ],
            "type": "string"
          },
          "spaceId": {
            "description": "Twitter/X Space id.",
            "type": "string"
          },
          "tweetId": {
            "description": "Numeric tweet id.",
            "type": "string"
          },
          "userId": {
            "description": "Numeric Twitter/X user id.",
            "type": "string"
          },
          "username": {
            "description": "Twitter/X username, with or without @.",
            "type": "string"
          },
          "withListeners": {
            "description": "space_details only: include listener information.",
            "type": "boolean"
          },
          "withReplays": {
            "description": "space_details only: include replay information.",
            "type": "boolean"
          }
        },
        "required": [
          "action"
        ],
        "type": "object"
      },
      "kind": "internal",
      "publicName": "TwitterAccount",
      "readOnlyHint": true,
      "requiresEnv": "RETTIWT_API_KEY",
      "title": "Read Twitter accounts and posts"
    },
    {
      "alwaysLoad": true,
      "descriptionBytes": 1397,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "amountRaw": {
            "description": "apply_bps / token_amount_to_usd. The amount in BASE units as a digits-only string, read together with `decimals`.",
            "type": "string"
          },
          "bps": {
            "description": "apply_bps only. The rate in basis points, 0 to 10000 (1 bps = 0.01%, so 25 bps = 0.25% and 10000 bps = 100%).",
            "type": "number"
          },
          "decimals": {
            "description": "The token's decimals, 0 to 36 (get them from TokenFind — 6 for USDC, 18 for most ERC-20s). Required for apply_bps, usd_to_token_amount and token_amount_to_usd, and for unit_convert whenever `from` or `to` is 'raw' or 'human'.",
            "type": "number"
          },
          "from": {
            "description": "unit_convert only. The unit `value` is currently in. Must be in the same family as `to`: {wei,gwei,eth}, {lamports,sol}, or {raw,human}.",
            "enum": [
              "wei",
              "gwei",
              "eth",
              "lamports",
              "sol",
              "raw",
              "human"
            ],
            "type": "string"
          },
          "gasPriceWei": {
            "description": "gas_cost only. The gas PRICE in wei as a digits-only string (e.g. '22518000', which is 0.022518 gwei).",
            "type": "string"
          },
          "gasUnits": {
            "description": "gas_cost only. The gas UNIT count as a digits-only string (a unitless count, e.g. '1634838') — never a gas price.",
            "type": "string"
          },
          "op": {
            "description": "Which calculation to run. 'unit_convert' — restate one amount in another unit of the SAME family ({wei,gwei,eth} or {lamports,sol} or {raw,human}); exact, never floors, so it returns no remainder. 'gas_cost' — gasUnits x gasPriceWei, answered in wei, gwei and eth at once; exact, no remainder. 'apply_bps' — split an amount into fee and net at a basis-point rate; fee.remainder counts TEN-THOUSANDTHS of one base unit that the floor dropped (numerator over denominator 10000). 'usd_to_token_amount' — how many base units a USD figure buys; remainder is the leftover numerator over the divisor (priceUsd scaled to an integer), always smaller than the cost of one base unit. 'token_amount_to_usd' — what a base-unit amount is worth; usd is floored to 6 decimals (usdPrecision) and remainder is the leftover numerator over 10^(priceUsd decimals + token decimals), i.e. the value below one micro-dollar.",
            "enum": [
              "unit_convert",
              "gas_cost",
              "apply_bps",
              "usd_to_token_amount",
              "token_amount_to_usd"
            ],
            "type": "string"
          },
          "priceUsd": {
            "description": "usd_to_token_amount / token_amount_to_usd. The price of ONE whole token in USD, as a decimal string greater than zero, e.g. '0.00031415'.",
            "type": "string"
          },
          "to": {
            "description": "unit_convert only. The unit to restate the amount in. Same family as `from` — there is no fixed rate between wei and lamports, so a cross-family request is refused.",
            "enum": [
              "wei",
              "gwei",
              "eth",
              "lamports",
              "sol",
              "raw",
              "human"
            ],
            "type": "string"
          },
          "usd": {
            "description": "usd_to_token_amount only. The USD figure as a decimal string, e.g. '250.75'. No currency symbol and no thousands separators.",
            "type": "string"
          },
          "value": {
            "description": "unit_convert only. The amount to restate, as a string. Digits only when `from` is an atomic unit (wei/lamports/raw); a decimal like '0.0225' is allowed for gwei/eth/sol/human, but never finer than one atomic unit.",
            "type": "string"
          }
        },
        "required": [
          "op"
        ],
        "type": "object"
      },
      "kind": "internal",
      "publicName": "UnitsConvert",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Convert token amounts and units"
    },
    {
      "alwaysLoad": true,
      "descriptionBytes": 1576,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "chainIds": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Optional. Omit (or pass empty) to scan all supported chains (Khalani + local). To restrict, pass chain IDs/aliases either comma-separated ('ethereum,base,solana') or as an array (['ethereum','base'])."
          },
          "limit": {
            "description": "Optional. Caps the PRICED rows per wallet snapshot, top-N by held USD value. Held tokens with no usable price are appended after them, outside this limit, marked `priceUnavailable` so 'no price feed' never reads as 'not held'; they are capped at 20 with the surplus counted in `unpricedOmitted`. Only applied when response_format='concise'; ignored under the default 'detailed'. `tokenCount`/`totalUsd` always reflect the full scan.",
            "type": "number"
          },
          "response_format": {
            "description": "Response shape, 'concise' or 'detailed'. Default 'detailed'. 'detailed' returns every token per wallet; 'concise' is what enables the `limit` trim (top rows by held USD value) and sets `truncated` when rows were left out.",
            "enum": [
              "concise",
              "detailed"
            ],
            "type": "string"
          },
          "walletFamily": {
            "description": "Which wallet FAMILY to read — 'eip155', 'solana', or 'all'. Default 'all' aggregates your EVM + Solana wallets. This is a wallet family, not a chain: chains go in `chainIds`.",
            "enum": [
              "eip155",
              "solana",
              "all"
            ],
            "type": "string"
          }
        },
        "type": "object"
      },
      "kind": "internal",
      "publicName": "WalletBalances",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Read wallet balances across chains"
    },
    {
      "alwaysLoad": true,
      "descriptionBytes": 1285,
      "destructiveHint": true,
      "inputSchema": {
        "properties": {
          "intentId": {
            "description": "The intent id that `WalletEvmTransactionPrepare` returned. It is scoped to this session and to the exact proposal that was approved.",
            "type": "string"
          }
        },
        "required": [
          "intentId"
        ],
        "type": "object"
      },
      "kind": "internal",
      "publicName": "WalletEvmTransactionConfirm",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Broadcast a prepared EVM transaction"
    },
    {
      "alwaysLoad": true,
      "descriptionBytes": 1475,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "chain": {
            "description": "The EVM chain the transaction executes on. Chain slug/alias, or the numeric chain id `TokenFind` returns (e.g. `base` or `8453`).",
            "type": "string"
          },
          "data": {
            "description": "Transaction calldata as 0x-prefixed hex. Pass `0x` (the default) for a plain native transfer, which is accepted only when `to` has no contract code.",
            "type": "string"
          },
          "gasLimit": {
            "description": "Maximum gas UNITS, as a decimal integer string. REQUIRED fee cap. Vex never derives a spending limit from a network estimate, so calling without the caps refuses by name and returns the current estimate as a labelled hint you can choose from.",
            "type": "string"
          },
          "gasPriceWei": {
            "description": "LEGACY cap on the price per gas unit, in RAW wei as a decimal integer string, for chains without EIP-1559. Mutually exclusive with the 1559 pair. REQUIRED fee cap. Vex never derives a spending limit from a network estimate, so calling without the caps refuses by name and returns the current estimate as a labelled hint you can choose from.",
            "type": "string"
          },
          "maxFeePerGasWei": {
            "description": "EIP-1559 cap on the total price per gas unit, in RAW wei as a decimal integer string. Pass this with maxPriorityFeePerGasWei, or pass gasPriceWei instead, never both. REQUIRED fee cap. Vex never derives a spending limit from a network estimate, so calling without the caps refuses by name and returns the current estimate as a labelled hint you can choose from.",
            "type": "string"
          },
          "maxPriorityFeePerGasWei": {
            "description": "EIP-1559 cap on the validator tip per gas unit, in RAW wei as a decimal integer string. Cannot exceed maxFeePerGasWei. REQUIRED fee cap. Vex never derives a spending limit from a network estimate, so calling without the caps refuses by name and returns the current estimate as a labelled hint you can choose from.",
            "type": "string"
          },
          "to": {
            "description": "Destination address (0x, 20 bytes): the contract being called, or the recipient for a plain native transfer.",
            "type": "string"
          },
          "valueWei": {
            "description": "Native coin to send with the call, in RAW wei as a decimal integer STRING (never a human decimal such as \"0.1\", never a JSON number). Defaults to \"0\". Must be \"0\" for a contract call: every function in the v1 decode set is non-payable.",
            "type": "string"
          }
        },
        "required": [
          "chain",
          "to",
          "gasLimit"
        ],
        "type": "object"
      },
      "kind": "internal",
      "publicName": "WalletEvmTransactionPrepare",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Prepare an EVM transaction"
    },
    {
      "alwaysLoad": true,
      "descriptionBytes": 1503,
      "destructiveHint": true,
      "inputSchema": {
        "properties": {
          "intentId": {
            "description": "Prepared intent ID",
            "type": "string"
          },
          "walletFamily": {
            "description": "Which wallet FAMILY to act on — 'eip155' for your EVM wallet or 'solana' for your Solana wallet. This is a wallet family, not a chain: the EVM chain goes in `chain`.",
            "enum": [
              "eip155",
              "solana"
            ],
            "type": "string"
          }
        },
        "required": [
          "walletFamily",
          "intentId"
        ],
        "type": "object"
      },
      "kind": "internal",
      "publicName": "WalletSendConfirm",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Broadcast a prepared wallet transfer"
    },
    {
      "alwaysLoad": true,
      "descriptionBytes": 1252,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "amountIn": {
            "description": "Amount of the token to send (for native/ERC-20), or \"1\" for an ERC-721. In HUMAN decimal units (e.g. \"1.5\") — not wei, lamports, or any other base unit.",
            "type": "string"
          },
          "chain": {
            "description": "Required for eip155. EVM chain ID or alias (e.g. 'polygon', '137', 'base'). Ignored for solana.",
            "type": "string"
          },
          "to": {
            "description": "Recipient address",
            "type": "string"
          },
          "token": {
            "description": "Token: 'native' for chain native, contract address for ERC-20, 'nft:{contract}:{tokenId}' for ERC-721. Solana: symbol or mint (SOL + SPL only, NFT not supported).",
            "type": "string"
          },
          "walletFamily": {
            "description": "Which wallet FAMILY to act on — 'eip155' for your EVM wallet or 'solana' for your Solana wallet. This is a wallet family, not a chain: the EVM chain goes in `chain`.",
            "enum": [
              "eip155",
              "solana"
            ],
            "type": "string"
          }
        },
        "required": [
          "walletFamily",
          "to",
          "amountIn"
        ],
        "type": "object"
      },
      "kind": "internal",
      "publicName": "WalletSendPrepare",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Prepare a wallet transfer"
    },
    {
      "alwaysLoad": true,
      "descriptionBytes": 1273,
      "destructiveHint": true,
      "inputSchema": {
        "properties": {
          "intentId": {
            "description": "The intent id that `WalletSolanaTransactionPrepare` returned. It is scoped to this session and to the exact canonical message that was approved.",
            "type": "string"
          }
        },
        "required": [
          "intentId"
        ],
        "type": "object"
      },
      "kind": "internal",
      "publicName": "WalletSolanaTransactionConfirm",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Broadcast a prepared Solana transaction"
    },
    {
      "alwaysLoad": true,
      "descriptionBytes": 1505,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "computeUnitLimit": {
            "description": "REQUIRED cap on the requested COMPUTE UNITS, as a decimal integer string. The priority fee is charged on the requested limit rather than on units actually used, so this is part of what you authorize. Calling without the caps refuses by name and returns the current network estimate as a labelled hint.",
            "type": "string"
          },
          "computeUnitPriceMicroLamports": {
            "description": "REQUIRED cap on the priority price per compute unit, in RAW micro-lamports as a decimal integer string. Together with computeUnitLimit it fixes the maximum priority fee in lamports, which is echoed back in approvedFeeBounds.",
            "type": "string"
          },
          "transactionBase64": {
            "description": "The UNSIGNED Solana transaction or transaction message, base64 encoded. A proposal that already carries a signature is refused, because preparing installs a fresh blockhash and would invalidate it.",
            "type": "string"
          }
        },
        "required": [
          "transactionBase64",
          "computeUnitLimit",
          "computeUnitPriceMicroLamports"
        ],
        "type": "object"
      },
      "kind": "internal",
      "publicName": "WalletSolanaTransactionPrepare",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Prepare a Solana transaction"
    },
    {
      "alwaysLoad": true,
      "descriptionBytes": 971,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "action": {
            "description": "pin adds a token to tracking, unpin removes it, list shows the chain's seed + pinned set.",
            "enum": [
              "pin",
              "unpin",
              "list"
            ],
            "type": "string"
          },
          "chain": {
            "description": "Local chain alias or id (e.g. 'robinhood', '4663').",
            "type": "string"
          },
          "token": {
            "description": "ERC-20 contract ADDRESS (0x…). Required for pin/unpin; ignored for list. Resolve a symbol to its address first.",
            "type": "string"
          }
        },
        "required": [
          "action",
          "chain"
        ],
        "type": "object"
      },
      "kind": "internal",
      "publicName": "WalletTrackToken",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Track a token in the local wallet view"
    },
    {
      "alwaysLoad": true,
      "descriptionBytes": 2057,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "chunksPerSource": {
            "description": "Search-only, and REQUIRES searchDepth: \"advanced\" — measured live, basic depth ignores it. Passages Tavily selects per source, 1-5. This is provider-side selection, not truncation of what we received: at advanced depth chunksPerSource: 1 held every row to 720-755 B against 646-2,203 B without it.",
            "type": "number"
          },
          "fetchTop": {
            "description": "Search-only. How many of the top hits to read in full, 0-10 (default 3); 0 = snippets only. Budget: each page read costs ~2.1 KB (max observed 2,508 B), so fetchTop: 10 is ~21 KB of page text alone. The default shape measures ~12 KB.",
            "type": "number"
          },
          "maxResults": {
            "description": "Search-only. Hits to request from Tavily, 1-10 (default 6). This changes the provider request, not a local trim. Budget: a hit you do not read costs ~1.0 KB of snippet (measured 466-1,891 B).",
            "type": "number"
          },
          "query": {
            "description": "Search query. Pass this OR `url`, not both.",
            "type": "string"
          },
          "searchDepth": {
            "description": "Search-only. `advanced` improves recall and costs 2 Tavily credits instead of 1 (measured). Default: `basic`.",
            "enum": [
              "basic",
              "advanced"
            ],
            "type": "string"
          },
          "timeRange": {
            "description": "Search-only. Restrict hits to the last day/week/month/year. Verified live on `topic: \"news\"` (with `day`, all 5 rows were published that day against a 5-day spread without it). On `topic: \"general\"` the provider accepts it but returns no dates, so its effect cannot be verified from the payload.",
            "enum": [
              "day",
              "week",
              "month",
              "year"
            ],
            "type": "string"
          },
          "topic": {
            "description": "Search-only. `news` is the ONLY way to get publication dates (measured: 10/10 news rows carried one, 0/9 general rows did) and is the right choice for \"why is this token moving today\". Scores are NOT comparable across topics — measured 0.53-0.71 on general vs 0.02-0.33 on news for the same query. Default: `general`.",
            "enum": [
              "general",
              "news"
            ],
            "type": "string"
          },
          "url": {
            "description": "Absolute http:// or https:// URL to read as markdown. Unlike a search page read this is NOT query-targeted - you get the whole document, measured at 21,319 B for one documentation page, and no parameter bounds it. Other schemes (ftp, file, mailto, data) rejected. Pass this OR `query`, not both.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "kind": "internal",
      "publicName": "WebResearch",
      "readOnlyHint": true,
      "requiresEnv": "TAVILY_API_KEY",
      "title": "Search and read the web"
    },
    {
      "alwaysLoad": true,
      "descriptionBytes": 903,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "limit": {
            "description": "Maximum rows to return, 1 to 20 (default 5). Out-of-range values are refused, not clamped.",
            "type": "number"
          },
          "namespace": {
            "description": "A protocol namespace to list in full, for example \"pendle\". Mutually exclusive with `query`; exactly one of the two is required.",
            "type": "string"
          },
          "query": {
            "description": "An intent phrase to search for. Mutually exclusive with `namespace`; exactly one of the two is required.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "kind": "internal",
      "publicName": "vex_ToolSearch",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Search the protocol tool catalog"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1651,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "chainIds": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Comma-separated chain slugs to keep (e.g. base,solana,robinhood), matched case-insensitively. Accepts either a comma-separated string (\"a,b\") or an array of strings ([\"a\",\"b\"]) — the two are equivalent. Applied by Vex to the at most 30 rows DexScreener returned — it cannot reach rows outside that window, and an empty result means this window held none, not that none exist. Every feed mixes chains — one live window held base, solana, bsc, pulsechain, ethereum, arbitrum, robinhood and berachain at once — so this is the filter that makes a per-chain hunt possible at all. Read droppedByFilter.chainIds to see how much of the window it removed."
          },
          "explainDrops": {
            "description": "Also emit droppedRows[]: at most 10 rejected rows, each carrying the FIRST filter that rejected it, that row's own value, and the threshold you passed — which answers 'missed by 0.1 or by 100x?' in the same call. droppedByFilter stays the full census; this is a sample, and droppedRowsTruncated states whether more were dropped than shown. providerRowIndex is the row's 0-based position in THIS response before filtering. It is deterministic inside this answer and NOT stable across calls, since DexScreener re-chooses its window every time. rowId carries the row's own identifier — a pool address, a listed-asset address, or a narrative slug — and that is the value worth keeping. Off by default, and it costs nothing when off.",
            "type": "boolean"
          },
          "fields": {
            "description": "Comma-separated extra output fields, ADDED to the default row (chainId and tokenAddress are never projected away, and each feed's own signal fields are always emitted). Use 'full' for every field — that is also how to discover the names, since a misspelled one is rejected with the complete accepted list. Opt-in covers dexScreenerUrl, the issuer's link list, and two CDN image URLs which are excluded by default because a model cannot see an image and they were 5-7 KB of every call.",
            "type": "string"
          },
          "limit": {
            "description": "Max rows to return (1-200). Profile feeds default to 20 to bound issuer-authored text; other feeds return their provider window unless set. Any applied value is echoed in filtersApplied.limit and hasMore tells whether this provider window has additional rows. Use offset to read them. 0 is rejected because it cannot mean both 'none' and 'all'.",
            "type": "number"
          },
          "offset": {
            "description": "Skip this many rows of the SAME provider window (default 0). It cannot reach rows beyond DexScreener's 30 — there is no pagination on any feed.",
            "type": "number"
          },
          "omitFields": {
            "description": "Comma-separated output fields to REMOVE from every row. Accepts one name: description — the issuer-authored text that dominates these payloads (30 descriptions measured at 18,473 characters on the recent-profiles feed) and carries the whole untrusted surface. Use it when you want the chainId/tokenAddress identities and the feed's signal, and will resolve details with dexscreener__token_pairs_list. Identity and signal fields are never omittable; everything else is already opt-in via \"fields\", so there is nothing there to subtract. Echoed back as fieldsOmitted, and externalContentFields reflects what actually remains.",
            "type": "string"
          },
          "placedWithinSeconds": {
            "description": "Keep ads placed within this many seconds, computed from the ad's date against asOfMs and reported as eventAgeSeconds. Rows with no date are excluded and counted in droppedByFilter.unknownEventAge. Whole number.",
            "type": "number"
          },
          "sortBy": {
            "description": "relevance | eventAgeSeconds | boostCount | boostCountTotal | adImpressionCount, restricted per feed to the keys its rows actually carry — a key this feed cannot order by is rejected naming the ones it can. 'relevance' means 'as DexScreener returned': its feed order is undisclosed, is not a ranking and is not stable. eventAgeSeconds is seconds since the event THIS feed reports (profile update, takeover claim, ad placement).",
            "type": "string"
          },
          "sortDir": {
            "description": "desc (default) or asc. Rows whose sort metric is unknown always sort last, in BOTH directions — an unknown value is unranked, not smallest.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "dexscreener",
      "publicName": "dexscreener__ads_list",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "List DEX Screener ad placements",
      "toolId": "dexscreener.ads"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1973,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "chainIds": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Comma-separated chain slugs to keep (e.g. base,solana,robinhood), matched case-insensitively. Accepts either a comma-separated string (\"a,b\") or an array of strings ([\"a\",\"b\"]) — the two are equivalent. Applied by Vex to the at most 30 rows DexScreener returned — it cannot reach rows outside that window, and an empty result means this window held none, not that none exist. Every feed mixes chains — one live window held base, solana, bsc, pulsechain, ethereum, arbitrum, robinhood and berachain at once — so this is the filter that makes a per-chain hunt possible at all. Read droppedByFilter.chainIds to see how much of the window it removed."
          },
          "explainDrops": {
            "description": "Also emit droppedRows[]: at most 10 rejected rows, each carrying the FIRST filter that rejected it, that row's own value, and the threshold you passed — which answers 'missed by 0.1 or by 100x?' in the same call. droppedByFilter stays the full census; this is a sample, and droppedRowsTruncated states whether more were dropped than shown. providerRowIndex is the row's 0-based position in THIS response before filtering. It is deterministic inside this answer and NOT stable across calls, since DexScreener re-chooses its window every time. rowId carries the row's own identifier — a pool address, a listed-asset address, or a narrative slug — and that is the value worth keeping. Off by default, and it costs nothing when off.",
            "type": "boolean"
          },
          "fields": {
            "description": "Comma-separated extra output fields, ADDED to the default row (chainId and tokenAddress are never projected away, and each feed's own signal fields are always emitted). Use 'full' for every field — that is also how to discover the names, since a misspelled one is rejected with the complete accepted list. Opt-in covers dexScreenerUrl, the issuer's link list, and two CDN image URLs which are excluded by default because a model cannot see an image and they were 5-7 KB of every call.",
            "type": "string"
          },
          "limit": {
            "description": "Max rows to return (1-200). Profile feeds default to 20 to bound issuer-authored text; other feeds return their provider window unless set. Any applied value is echoed in filtersApplied.limit and hasMore tells whether this provider window has additional rows. Use offset to read them. 0 is rejected because it cannot mean both 'none' and 'all'.",
            "type": "number"
          },
          "minBoostCountTotal": {
            "description": "Keep rows with at least this many CUMULATIVE active boost units. These are counts of 12-24h promotion packs and are NEVER a currency amount — observed values are exactly 10, 20, 30, 50, 100, 300 and 500. Reads boostCountTotal, not the per-purchase boostCount, which the top feed omits on every row. Rows with no reported total are dropped and counted in droppedByFilter; 0 is a genuine no-op. Whole number.",
            "type": "number"
          },
          "offset": {
            "description": "Skip this many rows of the SAME provider window (default 0). It cannot reach rows beyond DexScreener's 30 — there is no pagination on any feed.",
            "type": "number"
          },
          "omitFields": {
            "description": "Comma-separated output fields to REMOVE from every row. Accepts one name: description — the issuer-authored text that dominates these payloads (30 descriptions measured at 18,473 characters on the recent-profiles feed) and carries the whole untrusted surface. Use it when you want the chainId/tokenAddress identities and the feed's signal, and will resolve details with dexscreener__token_pairs_list. Identity and signal fields are never omittable; everything else is already opt-in via \"fields\", so there is nothing there to subtract. Echoed back as fieldsOmitted, and externalContentFields reflects what actually remains.",
            "type": "string"
          },
          "sortBy": {
            "description": "relevance | eventAgeSeconds | boostCount | boostCountTotal | adImpressionCount, restricted per feed to the keys its rows actually carry — a key this feed cannot order by is rejected naming the ones it can. 'relevance' means 'as DexScreener returned': its feed order is undisclosed, is not a ranking and is not stable. eventAgeSeconds is seconds since the event THIS feed reports (profile update, takeover claim, ad placement).",
            "type": "string"
          },
          "sortDir": {
            "description": "desc (default) or asc. Rows whose sort metric is unknown always sort last, in BOTH directions — an unknown value is unranked, not smallest.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "dexscreener",
      "publicName": "dexscreener__attention_list",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Rank tokens by DEX Screener attention",
      "toolId": "dexscreener.attention"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1990,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "chainIds": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Comma-separated chain slugs to keep (e.g. base,solana,robinhood), matched case-insensitively. Accepts either a comma-separated string (\"a,b\") or an array of strings ([\"a\",\"b\"]) — the two are equivalent. Applied by Vex to the at most 30 rows DexScreener returned — it cannot reach rows outside that window, and an empty result means this window held none, not that none exist. Every feed mixes chains — one live window held base, solana, bsc, pulsechain, ethereum, arbitrum, robinhood and berachain at once — so this is the filter that makes a per-chain hunt possible at all. Read droppedByFilter.chainIds to see how much of the window it removed."
          },
          "explainDrops": {
            "description": "Also emit droppedRows[]: at most 10 rejected rows, each carrying the FIRST filter that rejected it, that row's own value, and the threshold you passed — which answers 'missed by 0.1 or by 100x?' in the same call. droppedByFilter stays the full census; this is a sample, and droppedRowsTruncated states whether more were dropped than shown. providerRowIndex is the row's 0-based position in THIS response before filtering. It is deterministic inside this answer and NOT stable across calls, since DexScreener re-chooses its window every time. rowId carries the row's own identifier — a pool address, a listed-asset address, or a narrative slug — and that is the value worth keeping. Off by default, and it costs nothing when off.",
            "type": "boolean"
          },
          "feed": {
            "description": "Which boost endpoint to read (default latest). latest = /token-boosts/latest/v1, the most recent boost PURCHASES, carrying both the per-purchase boostCount and the cumulative boostCountTotal. top = /token-boosts/top/v1, the tokens holding the most active boosts, where boostCount is null on every row (the provider omits it) and only boostCountTotal is readable; sortBy defaults to boostCountTotal there instead of relevance. Neither feed carries a timestamp of any kind.",
            "enum": [
              "latest",
              "top"
            ],
            "type": "string"
          },
          "fields": {
            "description": "Comma-separated extra output fields, ADDED to the default row (chainId and tokenAddress are never projected away, and each feed's own signal fields are always emitted). Use 'full' for every field — that is also how to discover the names, since a misspelled one is rejected with the complete accepted list. Opt-in covers dexScreenerUrl, the issuer's link list, and two CDN image URLs which are excluded by default because a model cannot see an image and they were 5-7 KB of every call.",
            "type": "string"
          },
          "limit": {
            "description": "Max rows to return (1-200). Profile feeds default to 20 to bound issuer-authored text; other feeds return their provider window unless set. Any applied value is echoed in filtersApplied.limit and hasMore tells whether this provider window has additional rows. Use offset to read them. 0 is rejected because it cannot mean both 'none' and 'all'.",
            "type": "number"
          },
          "minBoostCountTotal": {
            "description": "Keep rows with at least this many CUMULATIVE active boost units. These are counts of 12-24h promotion packs and are NEVER a currency amount — observed values are exactly 10, 20, 30, 50, 100, 300 and 500. Reads boostCountTotal, not the per-purchase boostCount, which the top feed omits on every row. Rows with no reported total are dropped and counted in droppedByFilter; 0 is a genuine no-op. Whole number.",
            "type": "number"
          },
          "offset": {
            "description": "Skip this many rows of the SAME provider window (default 0). It cannot reach rows beyond DexScreener's 30 — there is no pagination on any feed.",
            "type": "number"
          },
          "omitFields": {
            "description": "Comma-separated output fields to REMOVE from every row. Accepts one name: description — the issuer-authored text that dominates these payloads (30 descriptions measured at 18,473 characters on the recent-profiles feed) and carries the whole untrusted surface. Use it when you want the chainId/tokenAddress identities and the feed's signal, and will resolve details with dexscreener__token_pairs_list. Identity and signal fields are never omittable; everything else is already opt-in via \"fields\", so there is nothing there to subtract. Echoed back as fieldsOmitted, and externalContentFields reflects what actually remains.",
            "type": "string"
          },
          "sortBy": {
            "description": "relevance | eventAgeSeconds | boostCount | boostCountTotal | adImpressionCount, restricted per feed to the keys its rows actually carry — a key this feed cannot order by is rejected naming the ones it can. 'relevance' means 'as DexScreener returned': its feed order is undisclosed, is not a ranking and is not stable. eventAgeSeconds is seconds since the event THIS feed reports (profile update, takeover claim, ad placement).",
            "type": "string"
          },
          "sortDir": {
            "description": "desc (default) or asc. Rows whose sort metric is unknown always sort last, in BOTH directions — an unknown value is unranked, not smallest.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "dexscreener",
      "publicName": "dexscreener__boosts_list",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Read DEX Screener paid boosts",
      "toolId": "dexscreener.boosts"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 2087,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "chainIds": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Comma-separated chain slugs to keep (e.g. base,solana,robinhood), matched case-insensitively. Accepts either a comma-separated string (\"a,b\") or an array of strings ([\"a\",\"b\"]) — the two are equivalent. Applied by Vex to the at most 30 rows DexScreener returned — it cannot reach rows outside that window, and an empty result means this window held none, not that none exist. Every feed mixes chains — one live window held base, solana, bsc, pulsechain, ethereum, arbitrum, robinhood and berachain at once — so this is the filter that makes a per-chain hunt possible at all. Read droppedByFilter.chainIds to see how much of the window it removed."
          },
          "claimedWithinSeconds": {
            "description": "Keep takeovers claimed within this many seconds, computed from claimDate against asOfMs and reported as eventAgeSeconds. Rows with no claimDate are excluded and counted in droppedByFilter.unknownEventAge. Whole number.",
            "type": "number"
          },
          "explainDrops": {
            "description": "Also emit droppedRows[]: at most 10 rejected rows, each carrying the FIRST filter that rejected it, that row's own value, and the threshold you passed — which answers 'missed by 0.1 or by 100x?' in the same call. droppedByFilter stays the full census; this is a sample, and droppedRowsTruncated states whether more were dropped than shown. providerRowIndex is the row's 0-based position in THIS response before filtering. It is deterministic inside this answer and NOT stable across calls, since DexScreener re-chooses its window every time. rowId carries the row's own identifier — a pool address, a listed-asset address, or a narrative slug — and that is the value worth keeping. Off by default, and it costs nothing when off.",
            "type": "boolean"
          },
          "fields": {
            "description": "Comma-separated extra output fields, ADDED to the default row (chainId and tokenAddress are never projected away, and each feed's own signal fields are always emitted). Use 'full' for every field — that is also how to discover the names, since a misspelled one is rejected with the complete accepted list. Opt-in covers dexScreenerUrl, the issuer's link list, and two CDN image URLs which are excluded by default because a model cannot see an image and they were 5-7 KB of every call.",
            "type": "string"
          },
          "limit": {
            "description": "Max rows to return (1-200). Profile feeds default to 20 to bound issuer-authored text; other feeds return their provider window unless set. Any applied value is echoed in filtersApplied.limit and hasMore tells whether this provider window has additional rows. Use offset to read them. 0 is rejected because it cannot mean both 'none' and 'all'.",
            "type": "number"
          },
          "offset": {
            "description": "Skip this many rows of the SAME provider window (default 0). It cannot reach rows beyond DexScreener's 30 — there is no pagination on any feed.",
            "type": "number"
          },
          "omitFields": {
            "description": "Comma-separated output fields to REMOVE from every row. Accepts one name: description — the issuer-authored text that dominates these payloads (30 descriptions measured at 18,473 characters on the recent-profiles feed) and carries the whole untrusted surface. Use it when you want the chainId/tokenAddress identities and the feed's signal, and will resolve details with dexscreener__token_pairs_list. Identity and signal fields are never omittable; everything else is already opt-in via \"fields\", so there is nothing there to subtract. Echoed back as fieldsOmitted, and externalContentFields reflects what actually remains.",
            "type": "string"
          },
          "sortBy": {
            "description": "relevance | eventAgeSeconds | boostCount | boostCountTotal | adImpressionCount, restricted per feed to the keys its rows actually carry — a key this feed cannot order by is rejected naming the ones it can. 'relevance' means 'as DexScreener returned': its feed order is undisclosed, is not a ranking and is not stable. eventAgeSeconds is seconds since the event THIS feed reports (profile update, takeover claim, ad placement).",
            "type": "string"
          },
          "sortDir": {
            "description": "desc (default) or asc. Rows whose sort metric is unknown always sort last, in BOTH directions — an unknown value is unranked, not smallest.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "dexscreener",
      "publicName": "dexscreener__community_takeovers_list",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "List community-takeover tokens",
      "toolId": "dexscreener.communityTakeovers"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1529,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "chainIds": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Comma-separated slugs to keep (e.g. ethereum,base,solana), matched case-insensitively and echoed lowercase. Accepts either a comma-separated string (\"a,b\") or an array of strings ([\"a\",\"b\"]) — the two are equivalent. DexScreener applies no server-side equivalent, so this only subtracts from the at most 30 rows it already chose: an EMPTY RESULT DOES NOT MEAN THERE IS NO POOL THERE. Read droppedByFilter to tell the two apart, then use dexscreener__token_pairs_list to ask the question properly."
          },
          "dexIds": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Comma-separated venue slugs to keep, matched case-insensitively. Accepts either a comma-separated string (\"a,b\") or an array of strings ([\"a\",\"b\"]) — the two are equivalent. Applied by Vex to the at most 30 rows DexScreener returned — it cannot reach rows outside that window. 52 distinct values observed and 4 of them are raw contract addresses for unnamed venues, so an unrecognised value is not necessarily a mistake."
          },
          "excludeDexIds": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Comma-separated venue slugs to drop, matched case-insensitively. Accepts either a comma-separated string (\"a,b\") or an array of strings ([\"a\",\"b\"]) — the two are equivalent. Applied by Vex to the at most 30 rows DexScreener returned — it cannot reach rows outside that window."
          },
          "explainDrops": {
            "description": "Also emit droppedRows[]: at most 10 rejected rows, each carrying the FIRST filter that rejected it, that row's own value, and the threshold you passed — which answers 'missed by 0.1 or by 100x?' in the same call. droppedByFilter stays the full census; this is a sample, and droppedRowsTruncated states whether more were dropped than shown. providerRowIndex is the row's 0-based position in THIS response before filtering. It is deterministic inside this answer and NOT stable across calls, since DexScreener re-chooses its window every time. rowId carries the row's own identifier — a pool address, a listed-asset address, or a narrative slug — and that is the value worth keeping. Off by default, and it costs nothing when off.",
            "type": "boolean"
          },
          "fields": {
            "description": "Comma-separated extra output fields, ADDED to the default lean row (identity is never projected away). Use 'full' for every field — that is also how to discover the names, since a misspelled one is rejected with the complete accepted list. Opt-in covers the market-cap and FDV figures, the reserve amounts on each side of the pool, every individual m5/h1/h6/h24 window, the info/social flags, the DexScreener link, and the issuer-authored display names — which are the single most expensive field in this API (one live pool's name is 34,090 characters).",
            "type": "string"
          },
          "includeAllWindows": {
            "description": "Emit all four windows of volume, txns, price change, buy/sell ratio and turnover on every row instead of just the selected one (adds roughly 250 B per row).",
            "type": "boolean"
          },
          "labels": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Comma-separated pool-variant tags to keep, matched case-insensitively because the provider emits both V2 and v2. Accepts either a comma-separated string (\"a,b\") or an array of strings ([\"a\",\"b\"]) — the two are equivalent. Observed: v1 v2 v3 v4 V0.5 CLMM CPMM DLMM DYN DYN2 tri wp. Undocumented set; it may grow."
          },
          "limit": {
            "description": "Max rows to return (1-200). Core tools apply an agent-safe default stated in their tool description and echo it in filtersApplied.limit; use offset to read the rest of the same at-most-30-row provider window. 0 is rejected because it cannot mean both 'none' and 'all'.",
            "type": "number"
          },
          "maxBuySellRatio": {
            "description": "Keep rows whose buys / sells in the selected `window` is at or below this. Read it back as buySellRatio<Window>.",
            "type": "number"
          },
          "maxFdvUsd": {
            "description": "Keep rows whose fdvUsd is at or below this.",
            "type": "number"
          },
          "maxLiquidityUsd": {
            "description": "Keep rows whose liquidityUsd is at or below this.",
            "type": "number"
          },
          "maxMarketCapUsd": {
            "description": "Keep rows whose marketCapUsd is at or below this.",
            "type": "number"
          },
          "maxPairAgeSeconds": {
            "description": "Keep pools whose pairAgeSeconds is at or below this, computed from pairCreatedAt against asOfMs. pairCreatedAt is absent on about 9% of rows and those rows are EXCLUDED and counted in droppedByFilter.unknownAge. How fresh a pool you can reach at all depends on the entry point: arriving via dexscreener__profiles_list with feed: recentUpdates, a 5-minute-old pool has been observed (4 samples - a proven floor, not a typical value) - and the window's HORIZON is just as bounded: one live 30-row window's oldest reachable row measured about 85 minutes, so a feed sweep is a sample of a narrow live window, never a survey of 'the last N hours' - whereas dexscreener__pairs_search returns established pools by its own relevance and will not surface them. DexScreener publishes no new-pair stream and every response is edge-cached about 30s, so nothing here is real-time. And there is no category field anywhere in this API: pair age, turnover, venue and whether socials exist are the only proxies available, and none of them means 'memecoin'.",
            "type": "number"
          },
          "maxPriceChangePct": {
            "description": "Keep rows whose priceChangePctSelected is at or below this, already in percent. May be negative.",
            "type": "number"
          },
          "maxTurnoverRatio": {
            "description": "Keep rows whose turnoverRatioH24 is at or below this.",
            "type": "number"
          },
          "maxVolumeUsd": {
            "description": "Keep rows whose volumeUsdSelected is at or below this.",
            "type": "number"
          },
          "minBuySellRatio": {
            "description": "Keep rows whose buys / sells in the selected `window` is at least this — emitted only per window, as buySellRatio<Window> (e.g. buySellRatioH24), never as a *Selected field. Rows with zero sells have no ratio and are dropped rather than treated as infinite. 0 is a genuine no-op.",
            "type": "number"
          },
          "minFdvUsd": {
            "description": "Keep rows whose fdvUsd is at least this. Fake when the pool's price is fake. 0 is a genuine no-op.",
            "type": "number"
          },
          "minLiquidityUsd": {
            "description": "Keep rows whose liquidityUsd is at least this. WARNING: USD liquidity is derived from a price the pool itself sets and can be fabricated — measured on one live search, 13 of 30 rows reported over $1M while holding fewer than 1,000 quote tokens, the worst $1.63B against 59.72. Pair this with minTurnoverRatio or minQuoteDepthTokens. 0 is a genuine no-op.",
            "type": "number"
          },
          "minMarketCapUsd": {
            "description": "Keep rows whose marketCapUsd is at least this. Measured, market cap equals FDV on 58% of rows, which means circulating supply is UNKNOWN rather than fully circulating — request the marketCapEqualsFdv field to see which rows those are. 0 is a genuine no-op.",
            "type": "number"
          },
          "minPairAgeSeconds": {
            "description": "Keep pools whose pairAgeSeconds is at least this. Rows with no pairCreatedAt are excluded and counted in droppedByFilter.unknownAge. Whole number.",
            "type": "number"
          },
          "minPriceChangePct": {
            "description": "Keep rows whose priceChangePctSelected is at least this, ALREADY IN PERCENT (1.08 = +1.08%). May be negative. Unlike the USD floors, 0 is a real threshold here ('flat or up'), not a no-op.",
            "type": "number"
          },
          "minQuoteDepthTokens": {
            "description": "Keep rows whose liquidityQuoteTokens is at least this — QUOTE TOKENS, not USD. Request liquidityQuoteTokens in `fields` and the number itself comes back. The pool's real exit liquidity, in the quote asset's own units (quoteSymbol names it). Priced at the quote token's independent value, this is the one depth number a pool cannot inflate by mispricing itself. 0 is a genuine no-op.",
            "type": "number"
          },
          "minTurnoverRatio": {
            "description": "Keep rows whose turnoverRatioH24 (24h volume divided by USD liquidity) is at least this. The cheapest real defence against fabricated depth: a genuine Ethereum WETH/USDC pool measured about 0.22, an impostor reporting $1.63B measured 0.00013. INVERSION ON DEEP POOLS: the canonical $20M PEPE/WETH pool measured 0.018 - deep blue-chip pools legitimately sit far below thresholds that catch impostors, so this filter can delete exactly the genuine token. Pair it with explainDrops and read droppedByFilter before concluding anything from an empty result. Rows where either input is missing are dropped and counted in droppedByFilter. 0 is a genuine no-op.",
            "type": "number"
          },
          "minTxnCount": {
            "description": "Keep rows with at least this many buys + sells in the selected `window`. Vex computes the sum and does NOT emit it as one field — request txnBuyCount<Window> and txnSellCount<Window> (e.g. txnBuyCountH24) in `fields`, which carry the two parts. Whole number. 0 is a genuine no-op.",
            "type": "number"
          },
          "minVolumeUsd": {
            "description": "Keep rows whose volumeUsdSelected is at least this — USD traded in the selected `window`. 0 is a genuine no-op.",
            "type": "number"
          },
          "offset": {
            "description": "Skip this many rows of the SAME provider window (default 0). It cannot reach rows beyond DexScreener's 30 — there is no pagination.",
            "type": "number"
          },
          "onlyBoosted": {
            "description": "Keep only rows with an active paid boost. Nearly useless over pair rows — measured on 1 of 489 - use dexscreener__boosts_list or dexscreener__ads_list for paid-visibility feeds.",
            "type": "boolean"
          },
          "quoteSymbols": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Comma-separated quote-asset symbols to keep (e.g. USDC,USDT,WETH,SOL), matched case-insensitively. Accepts either a comma-separated string (\"a,b\") or an array of strings ([\"a\",\"b\"]) — the two are equivalent."
          },
          "requireLiquidityUsd": {
            "description": "Keep only rows that carry a USD liquidity figure. Pre-graduation bonding-curve pools (measured: every pumpfun row in a live 20-row batch, 9 of 20) legitimately report liquidityUsd: null - null means UNKNOWN reserves, not zero - and turnoverRatioH24 is null with it, so liquidity-based sorting and the turnover defence are unavailable on exactly those rows. Dropped rows are counted in droppedByFilter.requireLiquidityUsd.",
            "type": "boolean"
          },
          "requirePriceUsd": {
            "description": "Keep only rows that carry a USD price.",
            "type": "boolean"
          },
          "requireSocials": {
            "description": "Keep only rows with at least one social link. The provider's info block is present on only 67% of rows, so a row with no info block is UNKNOWN rather than social-less — it is dropped, because the flag asks us to prove presence.",
            "type": "boolean"
          },
          "requireWebsite": {
            "description": "Keep only rows with at least one website. Same unknown-is-dropped rule as requireSocials.",
            "type": "boolean"
          },
          "slug": {
            "description": "Narrative slug from dexscreener__narratives_list results (e.g. 'ai', 'dog', 'knockoff-legends'). NOT a chain slug.",
            "type": "string"
          },
          "sortBy": {
            "description": "relevance | liquidityUsd | volumeUsd | turnoverRatio | marketCapUsd | fdvUsd | pairAgeSeconds | priceChangePct | txnCount | buySellRatio. 'relevance' means 'as DexScreener returned' — its order is neither a ranking nor stable. Sorting cannot recover a pool outside the 30-row window.",
            "type": "string"
          },
          "sortDir": {
            "description": "desc (default) or asc. Rows whose sort metric is unknown always sort last.",
            "type": "string"
          },
          "window": {
            "description": "m5 | h1 | h6 | h24 (default h24). Selects the window behind volumeUsdSelected, priceChangePctSelected and the volume/txn/price-change filters. Measured coverage: volume and txns carry all four windows on 100% of rows, but priceChange carries m5 on only 31% of rows, h1 61%, h6 74%, h24 87% — a price-change filter has nothing to compare on the rest, and drops them.",
            "type": "string"
          }
        },
        "required": [
          "slug"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "dexscreener",
      "publicName": "dexscreener__narrative_get",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Read one trending narrative",
      "toolId": "dexscreener.meta"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 2161,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "explainDrops": {
            "description": "Also emit droppedRows[]: at most 10 rejected rows, each carrying the FIRST filter that rejected it, that row's own value, and the threshold you passed — which answers 'missed by 0.1 or by 100x?' in the same call. droppedByFilter stays the full census; this is a sample, and droppedRowsTruncated states whether more were dropped than shown. providerRowIndex is the row's 0-based position in THIS response before filtering. It is deterministic inside this answer and NOT stable across calls, since DexScreener re-chooses its window every time. rowId carries the row's own identifier — a pool address, a listed-asset address, or a narrative slug — and that is the value worth keeping. Off by default, and it costs nothing when off.",
            "type": "boolean"
          },
          "fields": {
            "description": "Comma-separated extra output fields, ADDED to the default row. Use 'full' for every field; a misspelled name is rejected with the complete accepted list. Opt-in covers the narrative description and each individual m5/h1/h6/h24 window of market-cap change (percent) and market-cap delta (absolute USD) — two different units the provider ships in two identically-shaped, unnamed maps.",
            "type": "string"
          },
          "limit": {
            "description": "Max narratives to return (1-200). Omit to receive EVERY narrative DexScreener returned — 19 on the live feed, and the whole payload is under 7 KB, so there is rarely a reason to set this. 0 is rejected because it cannot mean both 'none' and 'all'.",
            "type": "number"
          },
          "minLiquidityUsd": {
            "description": "Keep narratives whose liquidityUsd is at least this. Note the provider's aggregate is NOT the sum of the pools it will show you for that narrative — measured 23,354,089.77 reported against 21,774,508.92 summed. 0 is a genuine no-op.",
            "type": "number"
          },
          "minMarketCapUsd": {
            "description": "Keep narratives whose marketCapUsd is at least this, in USD. Aggregated by DexScreener over tokens whose individual market caps are derived from pool prices a pool can set itself, so a fabricated token price inflates its narrative too. 0 is a genuine no-op.",
            "type": "number"
          },
          "minTokenCount": {
            "description": "Keep narratives whose narrativeTokenCount — how many tokens DexScreener counts in the narrative — is at least this. This is the narrative-level count and it disagrees with what dexscreener__narrative_get reports for the same slug — measured 67 here against 31 there, in the same minute. This tool owns the narrative-level number. 0 is a genuine no-op; whole number.",
            "type": "number"
          },
          "minVolumeUsd": {
            "description": "Keep narratives whose volumeUsdH24 is at least this. Same caveat as minLiquidityUsd: the aggregate is not a sum over the pools dexscreener__narrative_get returns. 0 is a genuine no-op.",
            "type": "number"
          },
          "offset": {
            "description": "Skip this many narratives of the same provider window (default 0). The PROVIDER has no pagination: this walks the single trending list Vex already fetched, so the reply's `hasMore` and `totalMatched` are about that list. Continue by adding the reply's `returned` to this value while hasMore is true.",
            "type": "number"
          },
          "omitFields": {
            "description": "Comma-separated output fields to REMOVE. Accepts one name: description — DexScreener's editorial prose, which is unbounded and which no decision reads. It is opt-in here, so this is only meaningful together with fields: \"full\": that pairing means 'every number, none of the prose'. slug and the aggregates are what this feed exists to report and are never omittable. Echoed back as fieldsOmitted.",
            "type": "string"
          },
          "sortBy": {
            "description": "relevance | marketCapUsd | liquidityUsd | volumeUsdH24 | narrativeTokenCount | marketCapChangePct. Default 'relevance', meaning 'as DexScreener returned'. DexScreener DOES NOT DISCLOSE ITS ORDER and it is not a ranking: nine candidate sorts (market cap, liquidity, volume, token count and each change window, both directions) were tested against the live feed and none reproduces it, so position carries no meaning. Sort by marketCapChangePct to see what is ROTATING; the largest narrative is rarely the moving one.",
            "type": "string"
          },
          "sortDir": {
            "description": "desc (default) or asc. Narratives whose sort metric is unknown always sort last, in both directions.",
            "type": "string"
          },
          "window": {
            "description": "m5 | h1 | h6 | h24 (default h24). Selects the window behind marketCapChangePctSelected and behind a marketCapChangePct sort. Unlike the pair tools, all four windows were present on every live narrative row.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "dexscreener",
      "publicName": "dexscreener__narratives_list",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "List trending DEX Screener narratives",
      "toolId": "dexscreener.trending"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1271,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "chain": {
            "description": "The chain this pool/token lives on, as a DexScreener chain slug (e.g. ethereum, base, solana, bsc, arbitrum, robinhood). Chain slug/alias, or the numeric chain id `TokenFind` returns (e.g. `base` or `8453`).",
            "type": "string"
          },
          "fields": {
            "description": "Comma-separated extra output fields, ADDED to the default lean row (identity is never projected away). Use 'full' for every field — that is also how to discover the names, since a misspelled one is rejected with the complete accepted list. Opt-in covers the market-cap and FDV figures, the reserve amounts on each side of the pool, every individual m5/h1/h6/h24 window, the info/social flags, the DexScreener link, and the issuer-authored display names — which are the single most expensive field in this API (one live pool's name is 34,090 characters).",
            "type": "string"
          },
          "includeAllWindows": {
            "description": "Emit all four windows of volume, txns, price change, buy/sell ratio and turnover on every row instead of just the selected one (adds roughly 250 B per row).",
            "type": "boolean"
          },
          "pairAddress": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Up to 60 DEX pool/pair contract addresses. Vex deduplicates them, splits them into provider-safe calls of at most 30, and merges all responses — cheaper than one call each. Accepts either a comma-separated string (\"a,b\") or an array of strings ([\"a\",\"b\"]) — the two are equivalent. The reply reconciles every address: requestedPairAddresses, resolvedPairAddresses, unresolvedPairAddresses (asked, not returned), unreachedPairAddresses (their batch failed - retry those), batchRequestCount, failedBatchCount, and `found`."
          },
          "window": {
            "description": "m5 | h1 | h6 | h24 (default h24). Selects the window behind volumeUsdSelected, priceChangePctSelected and the volume/txn/price-change filters. Measured coverage: volume and txns carry all four windows on 100% of rows, but priceChange carries m5 on only 31% of rows, h1 61%, h6 74%, h24 87% — a price-change filter has nothing to compare on the rest, and drops them.",
            "type": "string"
          }
        },
        "required": [
          "chain",
          "pairAddress"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "dexscreener",
      "publicName": "dexscreener__pairs_get",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Read one DEX pair",
      "toolId": "dexscreener.pairs"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1941,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "chainIds": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Comma-separated slugs to keep (e.g. ethereum,base,solana), matched case-insensitively and echoed lowercase. Accepts either a comma-separated string (\"a,b\") or an array of strings ([\"a\",\"b\"]) — the two are equivalent. DexScreener applies no server-side equivalent, so this only subtracts from the at most 30 rows it already chose: an EMPTY RESULT DOES NOT MEAN THERE IS NO POOL THERE. Read droppedByFilter to tell the two apart, then use dexscreener__token_pairs_list to ask the question properly."
          },
          "explainDrops": {
            "description": "Also emit droppedRows[]: at most 10 rejected rows, each carrying the FIRST filter that rejected it, that row's own value, and the threshold you passed — which answers 'missed by 0.1 or by 100x?' in the same call. droppedByFilter stays the full census; this is a sample, and droppedRowsTruncated states whether more were dropped than shown. providerRowIndex is the row's 0-based position in THIS response before filtering. It is deterministic inside this answer and NOT stable across calls, since DexScreener re-chooses its window every time. rowId carries the row's own identifier — a pool address, a listed-asset address, or a narrative slug — and that is the value worth keeping. Off by default, and it costs nothing when off.",
            "type": "boolean"
          },
          "fields": {
            "description": "Comma-separated extra output fields, ADDED to the default lean row (identity is never projected away). Use 'full' for every field — that is also how to discover the names, since a misspelled one is rejected with the complete accepted list. Opt-in covers the market-cap and FDV figures, the reserve amounts on each side of the pool, every individual m5/h1/h6/h24 window, the info/social flags, the DexScreener link, and the issuer-authored display names — which are the single most expensive field in this API (one live pool's name is 34,090 characters).",
            "type": "string"
          },
          "limit": {
            "description": "Max rows to return (1-200). Core tools apply an agent-safe default stated in their tool description and echo it in filtersApplied.limit; use offset to read the rest of the same at-most-30-row provider window. 0 is rejected because it cannot mean both 'none' and 'all'. MEASURED: one bare pair-notation query with no limit returned 24,139 B. The widest queries are genuinely large, and `limit` is what bounds them.",
            "type": "number"
          },
          "minLiquidityUsd": {
            "description": "Keep rows whose liquidityUsd is at least this. WARNING: USD liquidity is derived from a price the pool itself sets and can be fabricated — measured on one live search, 13 of 30 rows reported over $1M while holding fewer than 1,000 quote tokens, the worst $1.63B against 59.72. Pair this with minTurnoverRatio or minQuoteDepthTokens. 0 is a genuine no-op.",
            "type": "number"
          },
          "minTurnoverRatio": {
            "description": "Keep rows whose turnoverRatioH24 (24h volume divided by USD liquidity) is at least this. The cheapest real defence against fabricated depth: a genuine Ethereum WETH/USDC pool measured about 0.22, an impostor reporting $1.63B measured 0.00013. INVERSION ON DEEP POOLS: the canonical $20M PEPE/WETH pool measured 0.018 - deep blue-chip pools legitimately sit far below thresholds that catch impostors, so this filter can delete exactly the genuine token. Pair it with explainDrops and read droppedByFilter before concluding anything from an empty result. Rows where either input is missing are dropped and counted in droppedByFilter. 0 is a genuine no-op.",
            "type": "number"
          },
          "offset": {
            "description": "Skip this many rows of the SAME provider window (default 0). It cannot reach rows beyond DexScreener's 30 — there is no pagination.",
            "type": "number"
          },
          "query": {
            "description": "What to match, minimum 2 characters (DexScreener answers 1 character with an HTTP 400 it does not explain). Matches a token name (spaces are fine), a symbol, a token address, pair notation like SOL/USDC, and a venue name such as raydium — which acts as a de-facto venue filter. A POOL address is the cheapest way to identify an address of unknown provenance, but it can return matching rows on more than one chain because EVM addresses recur — inspect chainId on every returned row or use `chainIds` to narrow. TRAP: matching is purely textual and never against the chain field, so q=arbitrum returns the ARB token on Solana, not pools on Arbitrum — use `chainIds` for that.",
            "type": "string"
          },
          "quoteSymbols": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Comma-separated quote-asset symbols to keep (e.g. USDC,USDT,WETH,SOL), matched case-insensitively. Accepts either a comma-separated string (\"a,b\") or an array of strings ([\"a\",\"b\"]) — the two are equivalent."
          },
          "requireLiquidityUsd": {
            "description": "Keep only rows that carry a USD liquidity figure. Pre-graduation bonding-curve pools (measured: every pumpfun row in a live 20-row batch, 9 of 20) legitimately report liquidityUsd: null - null means UNKNOWN reserves, not zero - and turnoverRatioH24 is null with it, so liquidity-based sorting and the turnover defence are unavailable on exactly those rows. Dropped rows are counted in droppedByFilter.requireLiquidityUsd.",
            "type": "boolean"
          },
          "requirePriceUsd": {
            "description": "Keep only rows that carry a USD price.",
            "type": "boolean"
          }
        },
        "required": [
          "query"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "dexscreener",
      "publicName": "dexscreener__pairs_search",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Search DEX pairs",
      "toolId": "dexscreener.search"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 2338,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "chainIds": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Comma-separated chain slugs to keep (e.g. base,solana,robinhood), matched case-insensitively. Accepts either a comma-separated string (\"a,b\") or an array of strings ([\"a\",\"b\"]) — the two are equivalent. Applied by Vex to the at most 30 rows DexScreener returned — it cannot reach rows outside that window, and an empty result means this window held none, not that none exist. Every feed mixes chains — one live window held base, solana, bsc, pulsechain, ethereum, arbitrum, robinhood and berachain at once — so this is the filter that makes a per-chain hunt possible at all. Read droppedByFilter.chainIds to see how much of the window it removed."
          },
          "ctoOnly": {
            "description": "Keep only profiles flagged as a community takeover (the provider's cto field, emitted as communityTakeover). Present on 30/30 rows of both profile feeds — 2 of 30 were true on one live window, 0 of 30 on the other, so expect this to empty the result often. A row where the feed sent no flag is UNKNOWN and is dropped, because the filter asks us to prove the takeover. Rejected on any tool whose feed does not carry the flag.",
            "type": "boolean"
          },
          "explainDrops": {
            "description": "Also emit droppedRows[]: at most 10 rejected rows, each carrying the FIRST filter that rejected it, that row's own value, and the threshold you passed — which answers 'missed by 0.1 or by 100x?' in the same call. droppedByFilter stays the full census; this is a sample, and droppedRowsTruncated states whether more were dropped than shown. providerRowIndex is the row's 0-based position in THIS response before filtering. It is deterministic inside this answer and NOT stable across calls, since DexScreener re-chooses its window every time. rowId carries the row's own identifier — a pool address, a listed-asset address, or a narrative slug — and that is the value worth keeping. Off by default, and it costs nothing when off.",
            "type": "boolean"
          },
          "feed": {
            "description": "Which profile endpoint to read (default latest). latest = /token-profiles/latest/v1, the plain newest-profiles window; the provider currently sends NO updatedAt on it (drift measured 2026-08-21), so rows there carry no age and updatedWithinSeconds drops all of them. recentUpdates = /token-profiles/recent-updates/v1, the change feed of projects that just refreshed their description/socials/branding; it still sends updatedAt on 30/30 rows, so it is the one to ask for freshness or a time sort. It is a narrow recency SAMPLE (one live window's oldest row measured about 85 minutes), never a survey of the last N hours, and it is a live but undocumented API surface: if it changes the call fails with the real reason rather than returning an empty success.",
            "enum": [
              "latest",
              "recentUpdates"
            ],
            "type": "string"
          },
          "fields": {
            "description": "Comma-separated extra output fields, ADDED to the default row (chainId and tokenAddress are never projected away, and each feed's own signal fields are always emitted). Use 'full' for every field — that is also how to discover the names, since a misspelled one is rejected with the complete accepted list. Opt-in covers dexScreenerUrl, the issuer's link list, and two CDN image URLs which are excluded by default because a model cannot see an image and they were 5-7 KB of every call.",
            "type": "string"
          },
          "limit": {
            "description": "Max rows to return (1-200). Profile feeds default to 20 to bound issuer-authored text; other feeds return their provider window unless set. Any applied value is echoed in filtersApplied.limit and hasMore tells whether this provider window has additional rows. Use offset to read them. 0 is rejected because it cannot mean both 'none' and 'all'.",
            "type": "number"
          },
          "offset": {
            "description": "Skip this many rows of the SAME provider window (default 0). It cannot reach rows beyond DexScreener's 30 — there is no pagination on any feed.",
            "type": "number"
          },
          "omitFields": {
            "description": "Comma-separated output fields to REMOVE from every row. Accepts one name: description — the issuer-authored text that dominates these payloads (30 descriptions measured at 18,473 characters on the recent-profiles feed) and carries the whole untrusted surface. Use it when you want the chainId/tokenAddress identities and the feed's signal, and will resolve details with dexscreener__token_pairs_list. Identity and signal fields are never omittable; everything else is already opt-in via \"fields\", so there is nothing there to subtract. Echoed back as fieldsOmitted, and externalContentFields reflects what actually remains.",
            "type": "string"
          },
          "sortBy": {
            "description": "relevance | eventAgeSeconds | boostCount | boostCountTotal | adImpressionCount, restricted per feed to the keys its rows actually carry — a key this feed cannot order by is rejected naming the ones it can. 'relevance' means 'as DexScreener returned': its feed order is undisclosed, is not a ranking and is not stable. eventAgeSeconds is seconds since the event THIS feed reports (profile update, takeover claim, ad placement).",
            "type": "string"
          },
          "sortDir": {
            "description": "desc (default) or asc. Rows whose sort metric is unknown always sort last, in BOTH directions — an unknown value is unranked, not smallest.",
            "type": "string"
          },
          "updatedWithinSeconds": {
            "description": "Keep profiles updated within this many seconds, computed from updatedAt against asOfMs and reported as eventAgeSeconds on every row. Rows with no updatedAt are excluded and counted in droppedByFilter.unknownEventAge — 'we cannot tell when' is a different answer from 'too old'. The latest-profiles feed currently sends no updatedAt at all (provider drift, measured 2026-08-21), so this filter there drops every row - read the recentUpdates feed for freshness. Every response is edge-cached about 30s, so nothing here is real-time. Whole number.",
            "type": "number"
          }
        },
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "dexscreener",
      "publicName": "dexscreener__profiles_list",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Read DEX Screener token profiles",
      "toolId": "dexscreener.profiles"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1594,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "chain": {
            "description": "The chain this pool/token lives on, as a DexScreener chain slug (e.g. ethereum, base, solana, bsc, arbitrum, robinhood). Chain slug/alias, or the numeric chain id `TokenFind` returns (e.g. `base` or `8453`).",
            "type": "string"
          },
          "tokenAddress": {
            "description": "Token contract address.",
            "type": "string"
          }
        },
        "required": [
          "chain",
          "tokenAddress"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "dexscreener",
      "publicName": "dexscreener__token_orders_list",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "List a token's paid promotion orders",
      "toolId": "dexscreener.orders"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 2000,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "chain": {
            "description": "The chain this pool/token lives on, as a DexScreener chain slug (e.g. ethereum, base, solana, bsc, arbitrum, robinhood). Chain slug/alias, or the numeric chain id `TokenFind` returns (e.g. `base` or `8453`).",
            "type": "string"
          },
          "dexIds": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Comma-separated venue slugs to keep, matched case-insensitively. Accepts either a comma-separated string (\"a,b\") or an array of strings ([\"a\",\"b\"]) — the two are equivalent. Applied by Vex to the at most 30 rows DexScreener returned — it cannot reach rows outside that window. 52 distinct values observed and 4 of them are raw contract addresses for unnamed venues, so an unrecognised value is not necessarily a mistake."
          },
          "excludeDexIds": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Comma-separated venue slugs to drop, matched case-insensitively. Accepts either a comma-separated string (\"a,b\") or an array of strings ([\"a\",\"b\"]) — the two are equivalent. Applied by Vex to the at most 30 rows DexScreener returned — it cannot reach rows outside that window."
          },
          "explainDrops": {
            "description": "Also emit droppedRows[]: at most 10 rejected rows, each carrying the FIRST filter that rejected it, that row's own value, and the threshold you passed — which answers 'missed by 0.1 or by 100x?' in the same call. droppedByFilter stays the full census; this is a sample, and droppedRowsTruncated states whether more were dropped than shown. providerRowIndex is the row's 0-based position in THIS response before filtering. It is deterministic inside this answer and NOT stable across calls, since DexScreener re-chooses its window every time. rowId carries the row's own identifier — a pool address, a listed-asset address, or a narrative slug — and that is the value worth keeping. Off by default, and it costs nothing when off.",
            "type": "boolean"
          },
          "fields": {
            "description": "Comma-separated extra output fields, ADDED to the default lean row (identity is never projected away). Use 'full' for every field — that is also how to discover the names, since a misspelled one is rejected with the complete accepted list. Opt-in covers the market-cap and FDV figures, the reserve amounts on each side of the pool, every individual m5/h1/h6/h24 window, the info/social flags, the DexScreener link, and the issuer-authored display names — which are the single most expensive field in this API (one live pool's name is 34,090 characters).",
            "type": "string"
          },
          "includeAllWindows": {
            "description": "Emit all four windows of volume, txns, price change, buy/sell ratio and turnover on every row instead of just the selected one (adds roughly 250 B per row).",
            "type": "boolean"
          },
          "labels": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Comma-separated pool-variant tags to keep, matched case-insensitively because the provider emits both V2 and v2. Accepts either a comma-separated string (\"a,b\") or an array of strings ([\"a\",\"b\"]) — the two are equivalent. Observed: v1 v2 v3 v4 V0.5 CLMM CPMM DLMM DYN DYN2 tri wp. Undocumented set; it may grow."
          },
          "limit": {
            "description": "Max rows to return (1-200). Core tools apply an agent-safe default stated in their tool description and echo it in filtersApplied.limit; use offset to read the rest of the same at-most-30-row provider window. 0 is rejected because it cannot mean both 'none' and 'all'.",
            "type": "number"
          },
          "maxBuySellRatio": {
            "description": "Keep rows whose buys / sells in the selected `window` is at or below this. Read it back as buySellRatio<Window>.",
            "type": "number"
          },
          "maxFdvUsd": {
            "description": "Keep rows whose fdvUsd is at or below this.",
            "type": "number"
          },
          "maxLiquidityUsd": {
            "description": "Keep rows whose liquidityUsd is at or below this.",
            "type": "number"
          },
          "maxMarketCapUsd": {
            "description": "Keep rows whose marketCapUsd is at or below this.",
            "type": "number"
          },
          "maxPairAgeSeconds": {
            "description": "Keep pools whose pairAgeSeconds is at or below this, computed from pairCreatedAt against asOfMs. pairCreatedAt is absent on about 9% of rows and those rows are EXCLUDED and counted in droppedByFilter.unknownAge. How fresh a pool you can reach at all depends on the entry point: arriving via dexscreener__profiles_list with feed: recentUpdates, a 5-minute-old pool has been observed (4 samples - a proven floor, not a typical value) - and the window's HORIZON is just as bounded: one live 30-row window's oldest reachable row measured about 85 minutes, so a feed sweep is a sample of a narrow live window, never a survey of 'the last N hours' - whereas dexscreener__pairs_search returns established pools by its own relevance and will not surface them. DexScreener publishes no new-pair stream and every response is edge-cached about 30s, so nothing here is real-time. And there is no category field anywhere in this API: pair age, turnover, venue and whether socials exist are the only proxies available, and none of them means 'memecoin'.",
            "type": "number"
          },
          "maxPriceChangePct": {
            "description": "Keep rows whose priceChangePctSelected is at or below this, already in percent. May be negative.",
            "type": "number"
          },
          "maxTurnoverRatio": {
            "description": "Keep rows whose turnoverRatioH24 is at or below this.",
            "type": "number"
          },
          "maxVolumeUsd": {
            "description": "Keep rows whose volumeUsdSelected is at or below this.",
            "type": "number"
          },
          "minBuySellRatio": {
            "description": "Keep rows whose buys / sells in the selected `window` is at least this — emitted only per window, as buySellRatio<Window> (e.g. buySellRatioH24), never as a *Selected field. Rows with zero sells have no ratio and are dropped rather than treated as infinite. 0 is a genuine no-op.",
            "type": "number"
          },
          "minFdvUsd": {
            "description": "Keep rows whose fdvUsd is at least this. Fake when the pool's price is fake. 0 is a genuine no-op.",
            "type": "number"
          },
          "minLiquidityUsd": {
            "description": "Keep rows whose liquidityUsd is at least this. WARNING: USD liquidity is derived from a price the pool itself sets and can be fabricated — measured on one live search, 13 of 30 rows reported over $1M while holding fewer than 1,000 quote tokens, the worst $1.63B against 59.72. Pair this with minTurnoverRatio or minQuoteDepthTokens. 0 is a genuine no-op.",
            "type": "number"
          },
          "minMarketCapUsd": {
            "description": "Keep rows whose marketCapUsd is at least this. Measured, market cap equals FDV on 58% of rows, which means circulating supply is UNKNOWN rather than fully circulating — request the marketCapEqualsFdv field to see which rows those are. 0 is a genuine no-op.",
            "type": "number"
          },
          "minPairAgeSeconds": {
            "description": "Keep pools whose pairAgeSeconds is at least this. Rows with no pairCreatedAt are excluded and counted in droppedByFilter.unknownAge. Whole number.",
            "type": "number"
          },
          "minPriceChangePct": {
            "description": "Keep rows whose priceChangePctSelected is at least this, ALREADY IN PERCENT (1.08 = +1.08%). May be negative. Unlike the USD floors, 0 is a real threshold here ('flat or up'), not a no-op.",
            "type": "number"
          },
          "minQuoteDepthTokens": {
            "description": "Keep rows whose liquidityQuoteTokens is at least this — QUOTE TOKENS, not USD. Request liquidityQuoteTokens in `fields` and the number itself comes back. The pool's real exit liquidity, in the quote asset's own units (quoteSymbol names it). Priced at the quote token's independent value, this is the one depth number a pool cannot inflate by mispricing itself. 0 is a genuine no-op.",
            "type": "number"
          },
          "minTurnoverRatio": {
            "description": "Keep rows whose turnoverRatioH24 (24h volume divided by USD liquidity) is at least this. The cheapest real defence against fabricated depth: a genuine Ethereum WETH/USDC pool measured about 0.22, an impostor reporting $1.63B measured 0.00013. INVERSION ON DEEP POOLS: the canonical $20M PEPE/WETH pool measured 0.018 - deep blue-chip pools legitimately sit far below thresholds that catch impostors, so this filter can delete exactly the genuine token. Pair it with explainDrops and read droppedByFilter before concluding anything from an empty result. Rows where either input is missing are dropped and counted in droppedByFilter. 0 is a genuine no-op.",
            "type": "number"
          },
          "minTxnCount": {
            "description": "Keep rows with at least this many buys + sells in the selected `window`. Vex computes the sum and does NOT emit it as one field — request txnBuyCount<Window> and txnSellCount<Window> (e.g. txnBuyCountH24) in `fields`, which carry the two parts. Whole number. 0 is a genuine no-op.",
            "type": "number"
          },
          "minVolumeUsd": {
            "description": "Keep rows whose volumeUsdSelected is at least this — USD traded in the selected `window`. 0 is a genuine no-op.",
            "type": "number"
          },
          "offset": {
            "description": "Skip this many rows of the SAME provider window (default 0). It cannot reach rows beyond DexScreener's 30 — there is no pagination.",
            "type": "number"
          },
          "onlyBoosted": {
            "description": "Keep only rows with an active paid boost. Nearly useless over pair rows — measured on 1 of 489 - use dexscreener__boosts_list or dexscreener__ads_list for paid-visibility feeds.",
            "type": "boolean"
          },
          "quoteSymbols": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Comma-separated quote-asset symbols to keep (e.g. USDC,USDT,WETH,SOL), matched case-insensitively. Accepts either a comma-separated string (\"a,b\") or an array of strings ([\"a\",\"b\"]) — the two are equivalent."
          },
          "requireLiquidityUsd": {
            "description": "Keep only rows that carry a USD liquidity figure. Pre-graduation bonding-curve pools (measured: every pumpfun row in a live 20-row batch, 9 of 20) legitimately report liquidityUsd: null - null means UNKNOWN reserves, not zero - and turnoverRatioH24 is null with it, so liquidity-based sorting and the turnover defence are unavailable on exactly those rows. Dropped rows are counted in droppedByFilter.requireLiquidityUsd.",
            "type": "boolean"
          },
          "requirePriceUsd": {
            "description": "Keep only rows that carry a USD price.",
            "type": "boolean"
          },
          "requireSocials": {
            "description": "Keep only rows with at least one social link. The provider's info block is present on only 67% of rows, so a row with no info block is UNKNOWN rather than social-less — it is dropped, because the flag asks us to prove presence.",
            "type": "boolean"
          },
          "requireWebsite": {
            "description": "Keep only rows with at least one website. Same unknown-is-dropped rule as requireSocials.",
            "type": "boolean"
          },
          "sortBy": {
            "description": "relevance | liquidityUsd | volumeUsd | turnoverRatio | marketCapUsd | fdvUsd | pairAgeSeconds | priceChangePct | txnCount | buySellRatio. 'relevance' means 'as DexScreener returned' — its order is neither a ranking nor stable. Sorting cannot recover a pool outside the 30-row window.",
            "type": "string"
          },
          "sortDir": {
            "description": "desc (default) or asc. Rows whose sort metric is unknown always sort last.",
            "type": "string"
          },
          "tokenAddress": {
            "description": "Token contract address.",
            "type": "string"
          },
          "window": {
            "description": "m5 | h1 | h6 | h24 (default h24). Selects the window behind volumeUsdSelected, priceChangePctSelected and the volume/txn/price-change filters. Measured coverage: volume and txns carry all four windows on 100% of rows, but priceChange carries m5 on only 31% of rows, h1 61%, h6 74%, h24 87% — a price-change filter has nothing to compare on the rest, and drops them.",
            "type": "string"
          }
        },
        "required": [
          "chain",
          "tokenAddress"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "dexscreener",
      "publicName": "dexscreener__token_pairs_list",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "List a token's DEX pools",
      "toolId": "dexscreener.tokenPairs"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 2418,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "chain": {
            "description": "The chain this pool/token lives on, as a DexScreener chain slug (e.g. ethereum, base, solana, bsc, arbitrum, robinhood). Chain slug/alias, or the numeric chain id `TokenFind` returns (e.g. `base` or `8453`).",
            "type": "string"
          },
          "fields": {
            "description": "Comma-separated extra output fields, ADDED to the default lean row (identity is never projected away). Use 'full' for every field — that is also how to discover the names, since a misspelled one is rejected with the complete accepted list. Opt-in covers the market-cap and FDV figures, the reserve amounts on each side of the pool, every individual m5/h1/h6/h24 window, the info/social flags, the DexScreener link, and the issuer-authored display names — which are the single most expensive field in this API (one live pool's name is 34,090 characters).",
            "type": "string"
          },
          "includeAllWindows": {
            "description": "Emit all four windows of volume, txns, price change, buy/sell ratio and turnover on every row instead of just the selected one (adds roughly 250 B per row).",
            "type": "boolean"
          },
          "limit": {
            "description": "Max rows to return (1-200). Core tools apply an agent-safe default stated in their tool description and echo it in filtersApplied.limit; use offset to read the rest of the same at-most-30-row provider window. 0 is rejected because it cannot mean both 'none' and 'all'. MEASURED: 35 resolved addresses at the default lean projection returned 22,378 B (live batch, 2026-08-17, engine byte measure) - about 640 B per row, so a full 60-address batch is ~38 KB in one response and offset paging is how you bound it.",
            "type": "number"
          },
          "maxPairAgeSeconds": {
            "description": "Keep pools whose pairAgeSeconds is at or below this, computed from pairCreatedAt against asOfMs. pairCreatedAt is absent on about 9% of rows and those rows are EXCLUDED and counted in droppedByFilter.unknownAge. How fresh a pool you can reach at all depends on the entry point: arriving via dexscreener__profiles_list with feed: recentUpdates, a 5-minute-old pool has been observed (4 samples - a proven floor, not a typical value) - and the window's HORIZON is just as bounded: one live 30-row window's oldest reachable row measured about 85 minutes, so a feed sweep is a sample of a narrow live window, never a survey of 'the last N hours' - whereas dexscreener__pairs_search returns established pools by its own relevance and will not surface them. DexScreener publishes no new-pair stream and every response is edge-cached about 30s, so nothing here is real-time. And there is no category field anywhere in this API: pair age, turnover, venue and whether socials exist are the only proxies available, and none of them means 'memecoin'.",
            "type": "number"
          },
          "minPairAgeSeconds": {
            "description": "Keep pools whose pairAgeSeconds is at least this. Rows with no pairCreatedAt are excluded and counted in droppedByFilter.unknownAge. Whole number.",
            "type": "number"
          },
          "offset": {
            "description": "Skip this many rows of the SAME provider window (default 0). It cannot reach rows beyond DexScreener's 30 — there is no pagination.",
            "type": "number"
          },
          "requireLiquidityUsd": {
            "description": "Keep only rows that carry a USD liquidity figure. Pre-graduation bonding-curve pools (measured: every pumpfun row in a live 20-row batch, 9 of 20) legitimately report liquidityUsd: null - null means UNKNOWN reserves, not zero - and turnoverRatioH24 is null with it, so liquidity-based sorting and the turnover defence are unavailable on exactly those rows. Dropped rows are counted in droppedByFilter.requireLiquidityUsd.",
            "type": "boolean"
          },
          "sortBy": {
            "description": "relevance | liquidityUsd | volumeUsd | turnoverRatio | marketCapUsd | fdvUsd | pairAgeSeconds | priceChangePct | txnCount | buySellRatio. 'relevance' means 'as DexScreener returned' — its order is neither a ranking nor stable. Sorting cannot recover a pool outside the 30-row window.",
            "type": "string"
          },
          "sortDir": {
            "description": "desc (default) or asc. Rows whose sort metric is unknown always sort last.",
            "type": "string"
          },
          "tokenAddresses": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Up to 60 comma-separated token addresses. Vex splits them into calls of at most 30, then reports batchRequestCount, failedBatchCount and every requested/resolved/unresolved/unreached address. Always inspect unresolvedAddresses and unreachedAddresses. Accepts either a comma-separated string (\"a,b\") or an array of strings ([\"a\",\"b\"]) — the two are equivalent. Address casing is preserved on both spellings (Solana base58 is case-sensitive). Each address yields ONE arbitrary pool, often not the deepest; use dexscreener__token_pairs_list for depth."
          },
          "window": {
            "description": "m5 | h1 | h6 | h24 (default h24). Selects the window behind volumeUsdSelected, priceChangePctSelected and the volume/txn/price-change filters. Measured coverage: volume and txns carry all four windows on 100% of rows, but priceChange carries m5 on only 31% of rows, h1 61%, h6 74%, h24 87% — a price-change filter has nothing to compare on the rest, and drops them.",
            "type": "string"
          }
        },
        "required": [
          "chain",
          "tokenAddresses"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "dexscreener",
      "publicName": "dexscreener__tokens_get",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Batch-price tokens on one chain",
      "toolId": "dexscreener.tokens"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 2556,
      "destructiveHint": true,
      "inputSchema": {
        "properties": {
          "amountRaw": {
            "description": "Amount to bridge, in raw atomic units of fromToken (e.g. USDC has 6 decimals, so 1 USDC = \"1000000\"). Raw base units as an integer string (not human decimals) — read the token's decimals from `TokenFind` first.",
            "type": "string"
          },
          "depositMethod": {
            "description": "CONTRACT_CALL, PERMIT2, or TRANSFER.",
            "type": "string"
          },
          "dryRun": {
            "description": "If true, build deposit plan without executing.",
            "type": "boolean"
          },
          "filler": {
            "description": "Restrict quotes to a specific filler.",
            "type": "string"
          },
          "fromAddress": {
            "description": "Source wallet address override.",
            "type": "string"
          },
          "fromChain": {
            "description": "Source chain ID or alias.",
            "type": "string"
          },
          "fromToken": {
            "description": "Source token address.",
            "type": "string"
          },
          "recipient": {
            "description": "Destination recipient override.",
            "type": "string"
          },
          "routeId": {
            "description": "Specific route ID (default: best route).",
            "type": "string"
          },
          "toChain": {
            "description": "Destination chain ID or alias.",
            "type": "string"
          },
          "toToken": {
            "description": "Destination token address.",
            "type": "string"
          },
          "tradeType": {
            "description": "EXACT_INPUT or EXACT_OUTPUT.",
            "type": "string"
          }
        },
        "required": [
          "fromChain",
          "fromToken",
          "toChain",
          "toToken",
          "amountRaw"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "khalani",
      "publicName": "khalani__bridge_execute",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Execute a Khalani bridge",
      "toolId": "khalani.bridge"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1844,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "amountRaw": {
            "description": "Amount to bridge, in raw atomic units of fromToken (e.g. USDC has 6 decimals, so 1 USDC = \"1000000\"). Raw base units as an integer string (not human decimals) — read the token's decimals from `TokenFind` first.",
            "type": "string"
          },
          "filler": {
            "description": "Restrict quotes to a specific filler.",
            "type": "string"
          },
          "fromAddress": {
            "description": "Source wallet address override.",
            "type": "string"
          },
          "fromChain": {
            "description": "Source chain ID or alias.",
            "type": "string"
          },
          "fromToken": {
            "description": "Source token address.",
            "type": "string"
          },
          "recipient": {
            "description": "Destination recipient override.",
            "type": "string"
          },
          "toChain": {
            "description": "Destination chain ID or alias.",
            "type": "string"
          },
          "toToken": {
            "description": "Destination token address.",
            "type": "string"
          },
          "tradeType": {
            "description": "EXACT_INPUT or EXACT_OUTPUT (default: EXACT_INPUT).",
            "type": "string"
          }
        },
        "required": [
          "fromChain",
          "fromToken",
          "toChain",
          "toToken",
          "amountRaw"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "khalani",
      "publicName": "khalani__bridge_quote_get",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Quote a Khalani bridge",
      "toolId": "khalani.quote.get"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 850,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "refresh": {
            "description": "Force refresh chain cache.",
            "type": "boolean"
          }
        },
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "khalani",
      "publicName": "khalani__chains_list",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "List Khalani bridge chains",
      "toolId": "khalani.chains.list"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 990,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "orderId": {
            "description": "Khalani order ID.",
            "type": "string"
          }
        },
        "required": [
          "orderId"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "khalani",
      "publicName": "khalani__order_get",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Read one Khalani bridge order",
      "toolId": "khalani.orders.get"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1066,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "cursor": {
            "description": "The provider's own continuation value, echoed back as `cursor` on the previous reply. Pass back exactly what that reply returned; never compute one. Omit for the first page, and when a reply carries no `cursor` there is no next page.",
            "type": "number"
          },
          "fromChain": {
            "description": "Source chain filter (ID or alias).",
            "type": "string"
          },
          "limit": {
            "description": "Max results. Range 1-20 (provider maximum, default 10); anything else is rejected before the call.",
            "type": "number"
          },
          "orderIds": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Comma-separated order IDs to filter. Accepts either a comma-separated string (\"1,8453\") or an array of strings ([\"1\",\"8453\"]) — the two are equivalent."
          },
          "toChain": {
            "description": "Destination chain filter (ID or alias).",
            "type": "string"
          },
          "txHashSearch": {
            "description": "Search by transaction hash. At most 66 characters (0x + 64 hex, the provider maximum); a longer value is rejected before the call.",
            "type": "string"
          },
          "walletAddress": {
            "description": "Optional. The ACCOUNT address whose orders to list; omit to use your personal wallet. Under a session-scoped wallet it must equal the selected wallet.",
            "type": "string"
          },
          "walletFamily": {
            "description": "Wallet FAMILY, never a chain: eip155 or solana.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "khalani",
      "publicName": "khalani__orders_list",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "List Khalani bridge orders",
      "toolId": "khalani.orders.list"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1109,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "chainIds": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Comma-separated chain IDs or aliases. Omit to scan all chains in the family. Accepts either a comma-separated string (\"1,8453\") or an array of strings ([\"1\",\"8453\"]) — the two are equivalent."
          },
          "walletAddress": {
            "description": "Optional. The ACCOUNT address whose balances to read; omit to use your personal wallet. Under a session-scoped wallet it must equal the selected wallet, or the call fails with WALLET_SCOPE_MISMATCH.",
            "type": "string"
          },
          "walletFamily": {
            "description": "Wallet FAMILY, never a chain: eip155 or solana (default: eip155).",
            "type": "string"
          }
        },
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "khalani",
      "publicName": "khalani__token_balances_get",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Read Khalani-chain wallet balances",
      "toolId": "khalani.tokens.balances"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 964,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "chainIds": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Comma-separated chain IDs or aliases. Accepts either a comma-separated string (\"1,8453\") or an array of strings ([\"1\",\"8453\"]) — the two are equivalent."
          },
          "keyword": {
            "description": "Autocomplete keyword.",
            "type": "string"
          },
          "limit": {
            "description": "Max results. Range 1-20 (provider maximum, default 10); anything else is rejected before the call.",
            "type": "number"
          }
        },
        "required": [
          "keyword"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "khalani",
      "publicName": "khalani__tokens_autocomplete",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Parse a token phrase into Khalani tokens",
      "toolId": "khalani.tokens.autocomplete"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1253,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "chainIds": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Comma-separated chain IDs or aliases. Accepts either a comma-separated string (\"1,8453\") or an array of strings ([\"1\",\"8453\"]) — the two are equivalent."
          },
          "query": {
            "description": "Search phrase or token address.",
            "type": "string"
          }
        },
        "required": [
          "query"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "khalani",
      "publicName": "khalani__tokens_search",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Search Khalani tokens",
      "toolId": "khalani.tokens.search"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 794,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "chainIds": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Comma-separated chain IDs or aliases (e.g. '1,solana'). Accepts either a comma-separated string (\"1,8453\") or an array of strings ([\"1\",\"8453\"]) — the two are equivalent."
          }
        },
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "khalani",
      "publicName": "khalani__tokens_top_list",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "List top bridged tokens on Khalani",
      "toolId": "khalani.tokens.top"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 776,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "liveStatus": {
            "description": "Join KyberSwap Common Service's live per-chain state onto the registry rows (default false, which makes no network call). A chain the live list does not carry, and every chain when the service is unavailable, comes back with state null and a stateReason naming which of the two happened - the call still returns the registry rows.",
            "type": "boolean"
          }
        },
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "kyberswap",
      "publicName": "kyberswap__chains_list",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "List KyberSwap chains",
      "toolId": "kyberswap.chains"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 2534,
      "destructiveHint": true,
      "inputSchema": {
        "properties": {
          "amountIn": {
            "description": "Amount in human-readable units.",
            "type": "string"
          },
          "chain": {
            "description": "Chain slug or alias.",
            "type": "string"
          },
          "slippageBps": {
            "description": "Slippage tolerance in basis points (1 bps = 0.01%); default 100 = 1%, which fits deep, liquid pairs. Must match the kyberswap__swap_quote value exactly (or be omitted on both) - a mismatch blocks execution. It is the ONLY price protection on the trade. On a thin or volatile pair (new listings, memecoins, small pools) 100 bps often fails, in one of two ways. USUALLY it fails for FREE: the pre-sign gas estimate is refused before anything is signed, NO gas is spent, this call returns status \"not_attempted\" with retryable true and failureCode slippage, and the message names the price guard. LESS OFTEN the pool moves after that estimate passes: the transaction broadcasts, then REVERTS, this call returns status \"reverted\", the activity row records mined_revert, and the gas IS spent. Read either as \"this pair needs more tolerance\", not as \"this pair is untradeable\": re-quote with a higher slippageBps and pass the same value here. Vex caps it at 1000 (10%) and REJECTS anything above rather than clamping. Raise it in steps, starting tight since the usual failure costs nothing - no tolerance is known to fit a given pair in advance, and every increase widens the worst-case price you accept.",
            "type": "number"
          },
          "tokenIn": {
            "description": "Input token CONTRACT ADDRESS (resolve a symbol with TokenFind first) or native ETH/native. Symbols are rejected here.",
            "type": "string"
          },
          "tokenOut": {
            "description": "Output token CONTRACT ADDRESS (resolve a symbol with TokenFind first) or native ETH/native. Symbols are rejected here.",
            "type": "string"
          }
        },
        "required": [
          "chain",
          "tokenIn",
          "tokenOut",
          "amountIn"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "kyberswap",
      "publicName": "kyberswap__swap_execute",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Execute a KyberSwap swap",
      "toolId": "kyberswap.swap.execute"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1675,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "amountIn": {
            "description": "Amount in human-readable units.",
            "type": "string"
          },
          "chain": {
            "description": "Chain slug or alias.",
            "type": "string"
          },
          "slippageBps": {
            "description": "Slippage tolerance in basis points (1 bps = 0.01%); default 100 = 1%, which fits deep, liquid pairs. Pass the SAME value on kyberswap__swap_execute, or omit it on both - a mismatch blocks the execute (the prequote match requires identical params). Not sent to the quote route; it only pins the tolerance this quote authorizes. It is the ONLY price protection the resulting trade has. On a thin or volatile pair (new listings, memecoins, small pools) 100 bps often fails at execute time, in one of two ways. USUALLY it fails for FREE: the pre-sign gas estimate is refused before anything is signed, NO gas is spent, and the execute returns status \"not_attempted\" with failureCode slippage. LESS OFTEN the pool moves after that estimate passes: the transaction REVERTS, the activity row records mined_revert, and the gas IS spent. After either, re-quote with a higher slippageBps rather than abandoning the pair - and since the usual failure costs nothing, a tighter tolerance is the cheap thing to try first. Vex caps it at 1000 (10%) and REJECTS anything above rather than clamping; every increase widens the worst-case price you accept.",
            "type": "number"
          },
          "tokenIn": {
            "description": "Input token CONTRACT ADDRESS (resolve a symbol with TokenFind first) or native ETH/native. Symbols are not resolved here.",
            "type": "string"
          },
          "tokenOut": {
            "description": "Output token CONTRACT ADDRESS (resolve a symbol with TokenFind first) or native ETH/native. Symbols are not resolved here.",
            "type": "string"
          }
        },
        "required": [
          "chain",
          "tokenIn",
          "tokenOut",
          "amountIn"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "kyberswap",
      "publicName": "kyberswap__swap_quote",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Quote a KyberSwap swap",
      "toolId": "kyberswap.swap.quote"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1138,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "chain": {
            "description": "Chain slug or alias.",
            "type": "string"
          },
          "tokenAddress": {
            "description": "Token contract address to inspect (0x… on the named chain). Resolve it with TokenFind first — a symbol is not accepted. The former key `address` is retired and is rejected by name.",
            "type": "string"
          }
        },
        "required": [
          "chain",
          "tokenAddress"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "kyberswap",
      "publicName": "kyberswap__token_safety_check",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Audit an EVM token with KyberSwap",
      "toolId": "kyberswap.tokens.check"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 2892,
      "destructiveHint": true,
      "inputSchema": {
        "properties": {
          "borrowAmountRaw": {
            "description": "How much to borrow, in the LOAN token's RAW base units as a whole-number string. THE SCALE IS THE LOAN TOKEN'S OWN, which is usually NOT the collateral token's: read `loanAsset.decimals` from `morpho__market_get`. A human decimal amount is refused, not rounded. Another operation's amount key is refused by name. Quote first to see where this leaves the health factor before committing to a size.",
            "type": "string"
          },
          "chain": {
            "description": "The chain the market lives on. Required: a market id is chain-scoped. Chain slug/alias, or the numeric chain id `TokenFind` returns (e.g. `base` or `8453`).",
            "type": "string"
          },
          "dryRun": {
            "description": "Set true to get the FULL preview and sign nothing: the vouching verdict, the health factor before and after, the free liquidity, the allowance plan, the decoded transaction, the gas bound and the simulation verdict.",
            "type": "boolean"
          },
          "marketId": {
            "description": "The market's 0x-prefixed 64-hex id, from `morpho__markets_discover` or `morpho__positions_get`. A 40-hex value is a CONTRACT ADDRESS and is rejected by name. Chain-scoped.",
            "type": "string"
          },
          "slippageBps": {
            "description": "Price protection in basis points (1 bps = 0.01%). Default 100, capped at 1000, and a higher value is REJECTED rather than clamped. It bounds a full-debt repayment's per-share price of the TWO operations that move shares: a market supply and a repayment. On each, Vex derives a `maxSharePrice` ceiling from it that the CHAIN enforces, so a worse price cannot mine; on a full-debt (shares-mode) repayment it also caps the approval. The other four - supply_collateral, withdraw, withdraw_collateral and borrow - are direct calls naming their own exact token amount, carry NO on-chain price guard, and are unaffected by this value.",
            "type": "number"
          }
        },
        "required": [
          "marketId",
          "chain",
          "borrowAmountRaw"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "morpho",
      "publicName": "morpho__market_borrow",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Borrow from a Morpho market",
      "toolId": "morpho.market.borrow"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 2042,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "chain": {
            "description": "The chain the market lives on. Chain slug/alias, or the numeric chain id `TokenFind` returns (e.g. `base` or `8453`). Required because a Morpho market id is chain-scoped, so the same id on the wrong chain resolves to nothing. Discovery ships the supported slugs on this tool's `chains` metadata, and an unsupported chain is rejected with the full set spelled out.",
            "type": "string"
          },
          "includeHistory": {
            "description": "Add the averaged APY window selected by `lookback` (default false). Use it to judge whether today's rate is high or low for this market - a rate is never high or low on its own.",
            "type": "boolean"
          },
          "includeSupplyingVaults": {
            "description": "Also list the curated Morpho vaults supplying this market, with each vault's NET APY (default false). Covers BOTH generations and tags every row with its `version`; a curator commonly runs a v1 and a v2 vault under the same name at different addresses, so identify a route by address. A vault APY is net of the vault fee and is a different basis from this market's supply APY.",
            "type": "boolean"
          },
          "lookback": {
            "description": "Which averaging window `includeHistory` returns, one of: one_day, seven_days (default), thirty_days, ninety_days, one_year, inception. Ignored unless includeHistory is true. Anything else is rejected by name.",
            "enum": [
              "one_day",
              "seven_days",
              "thirty_days",
              "ninety_days",
              "one_year",
              "inception"
            ],
            "type": "string"
          },
          "marketId": {
            "description": "The Morpho Blue market id: a 0x-prefixed 64-hex hash, NOT a 40-hex contract address. Read one from `morpho__markets_discover`. An address is rejected by name, because Morpho answers an address-shaped id with an empty result rather than an error.",
            "type": "string"
          }
        },
        "required": [
          "marketId",
          "chain"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "morpho",
      "publicName": "morpho__market_get",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Read one Morpho Blue market",
      "toolId": "morpho.market.get"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 2425,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "borrowAmountRaw": {
            "description": "Required when `direction` is `borrow`. RAW base units of the LOAN token, at that token's own decimals (`loanAsset.decimals` from `morpho__market_get`).",
            "type": "string"
          },
          "chain": {
            "description": "The chain the market lives on. Required: a market id is chain-scoped. Chain slug/alias, or the numeric chain id `TokenFind` returns (e.g. `base` or `8453`).",
            "type": "string"
          },
          "direction": {
            "description": "Which operation to price. `supplyCollateral` moves the collateral token onto the market (raises the health factor), `borrow` takes the loan token out against it (lowers it, and is the only one that creates liquidation risk), `repay` pays debt down (raises it), `withdrawCollateral` takes collateral back off (lowers it whenever debt remains). `supply` and `withdraw` are the LENDER'S side and move the LOAN token: `supply` lends into the market to earn its borrow rate and `withdraw` takes that back out. Neither moves any health factor, and neither is `supplyCollateral`/`withdrawCollateral`, which move the other token. The quote authorizes only the execute matching this direction.",
            "enum": [
              "supplyCollateral",
              "withdrawCollateral",
              "borrow",
              "repay",
              "supply",
              "withdraw"
            ],
            "type": "string"
          },
          "marketId": {
            "description": "The market's 0x-prefixed 64-hex id, from `morpho__markets_discover` or `morpho__positions_get`. A 40-hex value is a CONTRACT ADDRESS and is rejected by name. Chain-scoped.",
            "type": "string"
          },
          "repayAmountRaw": {
            "description": "Used when `direction` is `repay` and the repayment is PARTIAL. RAW base units of the LOAN token, at that token's own decimals: read `loanAsset.decimals` from `morpho__market_get`, not from TokenFind, because the market names its own loan asset. Omit it and send `repayFullDebt: true` to price closing the debt completely; sending both is refused.",
            "type": "string"
          },
          "repayFullDebt": {
            "description": "Set true with `direction: repay` to price closing the debt COMPLETELY. It reads the position's own borrow shares from the chain, which is the only denomination that reaches zero: an amount always leaves accruing dust behind. Only meaningful for a repayment.",
            "type": "boolean"
          },
          "slippageBps": {
            "description": "Price protection in basis points (1 bps = 0.01%). Default 100, capped at 1000, and a higher value is REJECTED rather than clamped. It bounds a full-debt repayment's per-share price of the TWO operations that move shares: a market supply and a repayment. On each, Vex derives a `maxSharePrice` ceiling from it that the CHAIN enforces, so a worse price cannot mine; on a full-debt (shares-mode) repayment it also caps the approval. The other four - supply_collateral, withdraw, withdraw_collateral and borrow - are direct calls naming their own exact token amount, carry NO on-chain price guard, and are unaffected by this value.",
            "type": "number"
          },
          "supplyAmountRaw": {
            "description": "Required when `direction` is `supply`. RAW base units of the LOAN token being LENT into the market, at that token's own decimals (`loanAsset.decimals` from `morpho__market_get`). This is the lender's side; `supplyCollateralAmountRaw` is a different operation on a different token and is refused by name.",
            "type": "string"
          },
          "supplyCollateralAmountRaw": {
            "description": "Required when `direction` is `supplyCollateral`. RAW base units of the COLLATERAL token, whose decimals are usually NOT the loan token's: read `collateralAsset.decimals` from `morpho__market_get`. Another direction's amount key is refused by name rather than dropped.",
            "type": "string"
          },
          "walletAddress": {
            "description": "Whose position to price this against. A health factor has no meaning without one, so when this is omitted the quote uses the session's selected EVM wallet, and when there is none it uses a stand-in with NO position and says so. Read-only: it never selects a signer, and the executes refuse this parameter outright.",
            "type": "string"
          },
          "withdrawAmountRaw": {
            "description": "Required when `direction` is `withdraw`. RAW base units of the LOAN token being taken back out of the market, at that token's own decimals. Bounded by the wallet's own supplied position AND by the market's free liquidity, each refused by name rather than clamped.",
            "type": "string"
          },
          "withdrawCollateralAmountRaw": {
            "description": "Required when `direction` is `withdrawCollateral`. RAW base units of the COLLATERAL token, at that token's own decimals.",
            "type": "string"
          }
        },
        "required": [
          "direction",
          "marketId",
          "chain"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "morpho",
      "publicName": "morpho__market_quote",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Quote a Morpho Blue market operation",
      "toolId": "morpho.market.quote"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 3455,
      "destructiveHint": true,
      "inputSchema": {
        "properties": {
          "chain": {
            "description": "The chain the market lives on. Required: a market id is chain-scoped. Chain slug/alias, or the numeric chain id `TokenFind` returns (e.g. `base` or `8453`).",
            "type": "string"
          },
          "dryRun": {
            "description": "Set true to get the FULL preview and sign nothing: the vouching verdict, the health factor before and after, the free liquidity, the allowance plan, the decoded transaction, the gas bound and the simulation verdict.",
            "type": "boolean"
          },
          "marketId": {
            "description": "The market's 0x-prefixed 64-hex id, from `morpho__markets_discover` or `morpho__positions_get`. A 40-hex value is a CONTRACT ADDRESS and is rejected by name. Chain-scoped.",
            "type": "string"
          },
          "repayAmountRaw": {
            "description": "How much debt to repay, in the LOAN token's RAW base units as a whole-number string. Read `loanAsset.decimals` from `morpho__market_get` for the scale. Required UNLESS `repayFullDebt` is true, and supplying BOTH is refused because they disagree about how much debt to clear. A human decimal amount is refused, not rounded. An amount at or above the current debt is refused by name: use `repayFullDebt` instead, because an assets repayment cannot reach zero.",
            "type": "string"
          },
          "repayFullDebt": {
            "description": "Set true to close the debt COMPLETELY. It routes the repayment through borrow SHARES, reading the position's own share count from the chain and burning exactly that, which is the only denomination that lands at zero. Do not combine it with `repayAmountRaw`. The wallet must hold enough of the loan token to cover the ceiling those shares can cost; the excess is swept back in the same transaction.",
            "type": "boolean"
          },
          "slippageBps": {
            "description": "Price protection in basis points (1 bps = 0.01%). Default 100, capped at 1000, and a higher value is REJECTED rather than clamped. It bounds a full-debt repayment's per-share price of the TWO operations that move shares: a market supply and a repayment. On each, Vex derives a `maxSharePrice` ceiling from it that the CHAIN enforces, so a worse price cannot mine; on a full-debt (shares-mode) repayment it also caps the approval. The other four - supply_collateral, withdraw, withdraw_collateral and borrow - are direct calls naming their own exact token amount, carry NO on-chain price guard, and are unaffected by this value.",
            "type": "number"
          }
        },
        "required": [
          "marketId",
          "chain"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "morpho",
      "publicName": "morpho__market_repay",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Repay debt on a Morpho market",
      "toolId": "morpho.market.repay"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 3959,
      "destructiveHint": true,
      "inputSchema": {
        "properties": {
          "chain": {
            "description": "The chain the market lives on. Required: a market id is chain-scoped. Chain slug/alias, or the numeric chain id `TokenFind` returns (e.g. `base` or `8453`).",
            "type": "string"
          },
          "dryRun": {
            "description": "Set true to get the FULL preview and sign nothing: the vouching verdict, the health factor before and after, the free liquidity, the allowance plan, the decoded transaction, the gas bound and the simulation verdict.",
            "type": "boolean"
          },
          "marketId": {
            "description": "The market's 0x-prefixed 64-hex id, from `morpho__markets_discover` or `morpho__positions_get`. A 40-hex value is a CONTRACT ADDRESS and is rejected by name. Chain-scoped.",
            "type": "string"
          },
          "slippageBps": {
            "description": "Price protection in basis points (1 bps = 0.01%). Default 100, capped at 1000, and a higher value is REJECTED rather than clamped. It bounds a full-debt repayment's per-share price of the TWO operations that move shares: a market supply and a repayment. On each, Vex derives a `maxSharePrice` ceiling from it that the CHAIN enforces, so a worse price cannot mine; on a full-debt (shares-mode) repayment it also caps the approval. The other four - supply_collateral, withdraw, withdraw_collateral and borrow - are direct calls naming their own exact token amount, carry NO on-chain price guard, and are unaffected by this value.",
            "type": "number"
          },
          "supplyAmountRaw": {
            "description": "How much of the LOAN asset to lend, in the LOAN token's RAW base units as a whole-number string. THE SCALE IS THE LOAN TOKEN'S OWN, which is usually NOT the collateral token's: read `loanAsset.decimals` from `morpho__market_get`. A human decimal amount is refused, not rounded. `supplyCollateralAmountRaw` is a DIFFERENT operation on a DIFFERENT token and is refused by name rather than accepted here.",
            "type": "string"
          }
        },
        "required": [
          "marketId",
          "chain",
          "supplyAmountRaw"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "morpho",
      "publicName": "morpho__market_supply",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Supply the loan asset to a Morpho market",
      "toolId": "morpho.market.supply"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 2862,
      "destructiveHint": true,
      "inputSchema": {
        "properties": {
          "chain": {
            "description": "The chain the market lives on. Required: a market id is chain-scoped. Chain slug/alias, or the numeric chain id `TokenFind` returns (e.g. `base` or `8453`).",
            "type": "string"
          },
          "dryRun": {
            "description": "Set true to get the FULL preview and sign nothing: the vouching verdict, the health factor before and after, the free liquidity, the allowance plan, the decoded transaction, the gas bound and the simulation verdict.",
            "type": "boolean"
          },
          "marketId": {
            "description": "The market's 0x-prefixed 64-hex id, from `morpho__markets_discover` or `morpho__positions_get`. A 40-hex value is a CONTRACT ADDRESS and is rejected by name. Chain-scoped.",
            "type": "string"
          },
          "slippageBps": {
            "description": "Price protection in basis points (1 bps = 0.01%). Default 100, capped at 1000, and a higher value is REJECTED rather than clamped. It bounds a full-debt repayment's per-share price of the TWO operations that move shares: a market supply and a repayment. On each, Vex derives a `maxSharePrice` ceiling from it that the CHAIN enforces, so a worse price cannot mine; on a full-debt (shares-mode) repayment it also caps the approval. The other four - supply_collateral, withdraw, withdraw_collateral and borrow - are direct calls naming their own exact token amount, carry NO on-chain price guard, and are unaffected by this value.",
            "type": "number"
          },
          "supplyCollateralAmountRaw": {
            "description": "How much COLLATERAL to supply, in the COLLATERAL token's RAW base units as a whole-number string. THE SCALE IS THE COLLATERAL TOKEN'S OWN, which is usually NOT the loan token's: read `collateralAsset.decimals` from `morpho__market_get`. A market pairing 8-decimal cbBTC against 6-decimal USDC will silently accept a loan-scaled number as a hundredfold wrong amount, so read the scale rather than assuming it. A human decimal amount is refused, not rounded. Another operation's amount key is refused by name.",
            "type": "string"
          }
        },
        "required": [
          "marketId",
          "chain",
          "supplyCollateralAmountRaw"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "morpho",
      "publicName": "morpho__market_supply_collateral",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Supply collateral to a Morpho market",
      "toolId": "morpho.market.supplyCollateral"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 3270,
      "destructiveHint": true,
      "inputSchema": {
        "properties": {
          "chain": {
            "description": "The chain the market lives on. Required: a market id is chain-scoped. Chain slug/alias, or the numeric chain id `TokenFind` returns (e.g. `base` or `8453`).",
            "type": "string"
          },
          "dryRun": {
            "description": "Set true to get the FULL preview and sign nothing: the vouching verdict, the health factor before and after, the free liquidity, the allowance plan, the decoded transaction, the gas bound and the simulation verdict.",
            "type": "boolean"
          },
          "marketId": {
            "description": "The market's 0x-prefixed 64-hex id, from `morpho__markets_discover` or `morpho__positions_get`. A 40-hex value is a CONTRACT ADDRESS and is rejected by name. Chain-scoped.",
            "type": "string"
          },
          "slippageBps": {
            "description": "Price protection in basis points (1 bps = 0.01%). Default 100, capped at 1000, and a higher value is REJECTED rather than clamped. It bounds a full-debt repayment's per-share price of the TWO operations that move shares: a market supply and a repayment. On each, Vex derives a `maxSharePrice` ceiling from it that the CHAIN enforces, so a worse price cannot mine; on a full-debt (shares-mode) repayment it also caps the approval. The other four - supply_collateral, withdraw, withdraw_collateral and borrow - are direct calls naming their own exact token amount, carry NO on-chain price guard, and are unaffected by this value.",
            "type": "number"
          },
          "withdrawAmountRaw": {
            "description": "How much of the LOAN asset to withdraw, in the LOAN token's RAW base units as a whole-number string. THE SCALE IS THE LOAN TOKEN'S OWN: read `loanAsset.decimals` from `morpho__market_get`. A human decimal amount is refused, not rounded. `withdrawCollateralAmountRaw` is a DIFFERENT operation on a DIFFERENT token and is refused by name. An amount above the supplied position, or above the market's free liquidity, is refused by name rather than reduced to what would fit.",
            "type": "string"
          }
        },
        "required": [
          "marketId",
          "chain",
          "withdrawAmountRaw"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "morpho",
      "publicName": "morpho__market_withdraw",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Withdraw a supply from a Morpho market",
      "toolId": "morpho.market.withdraw"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 2894,
      "destructiveHint": true,
      "inputSchema": {
        "properties": {
          "chain": {
            "description": "The chain the market lives on. Required: a market id is chain-scoped. Chain slug/alias, or the numeric chain id `TokenFind` returns (e.g. `base` or `8453`).",
            "type": "string"
          },
          "dryRun": {
            "description": "Set true to get the FULL preview and sign nothing: the vouching verdict, the health factor before and after, the free liquidity, the allowance plan, the decoded transaction, the gas bound and the simulation verdict.",
            "type": "boolean"
          },
          "marketId": {
            "description": "The market's 0x-prefixed 64-hex id, from `morpho__markets_discover` or `morpho__positions_get`. A 40-hex value is a CONTRACT ADDRESS and is rejected by name. Chain-scoped.",
            "type": "string"
          },
          "slippageBps": {
            "description": "Price protection in basis points (1 bps = 0.01%). Default 100, capped at 1000, and a higher value is REJECTED rather than clamped. It bounds a full-debt repayment's per-share price of the TWO operations that move shares: a market supply and a repayment. On each, Vex derives a `maxSharePrice` ceiling from it that the CHAIN enforces, so a worse price cannot mine; on a full-debt (shares-mode) repayment it also caps the approval. The other four - supply_collateral, withdraw, withdraw_collateral and borrow - are direct calls naming their own exact token amount, carry NO on-chain price guard, and are unaffected by this value.",
            "type": "number"
          },
          "withdrawCollateralAmountRaw": {
            "description": "How much COLLATERAL to withdraw, in the COLLATERAL token's RAW base units as a whole-number string. THE SCALE IS THE COLLATERAL TOKEN'S OWN, not the loan token's: read `collateralAsset.decimals` from `morpho__market_get`. A human decimal amount is refused, not rounded. Another operation's amount key is refused by name. Quote first: on a position with debt this lowers the health factor.",
            "type": "string"
          }
        },
        "required": [
          "marketId",
          "chain",
          "withdrawCollateralAmountRaw"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "morpho",
      "publicName": "morpho__market_withdraw_collateral",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Withdraw collateral from a Morpho market",
      "toolId": "morpho.market.withdrawCollateral"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 3263,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "chainIds": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Comma-separated chains, an array of the same, or 'all'. Slugs or numeric chain ids; discovery ships the supported slugs on this tool's `chains` metadata and an unsupported entry is rejected with the full set."
          },
          "limit": {
            "description": "Max transactions (default 25, maximum 100). A larger value is REJECTED by name rather than clamped; `hasMore` and `nextOffset` report what is left.",
            "type": "number"
          },
          "liquidatorAddress": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Comma list or array of LIQUIDATOR addresses, up to 20. This is the other side of `walletAddress`, which matches the borrower who was liquidated: use this one to profile who is doing the liquidating on a market. It only ever matches liquidation rows, so pair it with `types: 'liquidation'` for a clean page."
          },
          "marketIds": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Morpho Blue market ids as a comma list OR an array, up to 20. A market id is a 0x-prefixed 64-hex hash, NOT a contract address, and it is chain-scoped; read one from morpho__markets_discover. An address here is rejected by name and told what it actually is."
          },
          "minBadDebtAssetsRaw": {
            "description": "Keep only liquidations that left at least this much BAD DEBT, in the LOAN asset's RAW base units as a quoted string (\"1000000\" is 1 USDC at 6 decimals). Bad debt is loss socialised across everyone supplying the market, so this is the direct way to ask 'has this market ever lost lenders money'. Scope it to one market or one asset, because a raw floor means different money in different markets. A human decimal amount, or an unquoted number, is rejected by name rather than rounded.",
            "type": "string"
          },
          "minSeizedAssetsRaw": {
            "description": "Keep only liquidations that seized at least this much COLLATERAL, in the COLLATERAL asset's RAW base units as a quoted string. Note the different asset from `minBadDebtAssetsRaw`: on one live row the two legs sat at 18 and 6 decimals, so the same integer means wildly different money on each. Same by-name refusal of a human decimal.",
            "type": "string"
          },
          "offset": {
            "description": "Row offset for paging (default 0). Pair it with the `nextOffset` the reply returns.",
            "type": "number"
          },
          "order": {
            "description": "Ranking direction, one of: desc (default, newest or largest first), asc. Anything else is rejected by name.",
            "enum": [
              "asc",
              "desc"
            ],
            "type": "string"
          },
          "since": {
            "description": "Only events at or after this unix timestamp, in SECONDS (10 digits today), not milliseconds. A milliseconds value is rejected by name: it would select a window thousands of years out and return an empty history that reads as a dead market.",
            "type": "number"
          },
          "sort": {
            "description": "Ranking key, one of: timestamp, assets, repaidAssets, seizedAssets, badDebtAssets. Default timestamp. The last three rank liquidations by size, the fastest way to the worst ones; they order non-liquidation rows arbitrarily, so pair them with types 'liquidation'. Applied server-side over ALL matches.",
            "enum": [
              "timestamp",
              "assets",
              "repaidAssets",
              "seizedAssets",
              "badDebtAssets"
            ],
            "type": "string"
          },
          "txHash": {
            "description": "Look up ONE transaction by its 0x-prefixed 64-hex hash. Use it when the user pastes a hash and asks what it did on Morpho; a single transaction can produce several rows, so expect more than one. Morpho declares no list form of this filter, so it takes exactly one hash.",
            "type": "string"
          },
          "types": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Event types as a comma list or an array: supply, withdraw, borrow, repay, supplyCollateral, withdrawCollateral, liquidation. Omit for every type. 'liquidation' alone is the risk lens; 'borrow' and 'repay' show whether anyone actually uses the market. A value outside this set is rejected by name."
          },
          "until": {
            "description": "Only events at or before this unix timestamp, in SECONDS, not milliseconds. Earlier than `since` is rejected by name, because nothing could match.",
            "type": "number"
          },
          "walletAddress": {
            "description": "Scope the history to ONE actor, 0x-prefixed and 40 hex. On liquidation rows this matches the BORROWER whose position was taken, not the liquidator - which is what makes it the way to see who liquidated an address and when.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "morpho",
      "publicName": "morpho__markets_activity_list",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Read Morpho market activity",
      "toolId": "morpho.markets.activity"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 2961,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "chainIds": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Comma-separated chains to scope to, an array of the same, or 'all'. Each entry is a chain slug or a numeric chain id; discovery ships the supported slugs on this tool's `chains` metadata, and an unsupported entry is rejected with the full set spelled out. Omit for every supported chain. A chain Morpho serves but Vex does not operate on is rejected by name, so a coverage gap is never reported as an absence of markets."
          },
          "collateralAssetTags": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Comma list or array of Morpho's own asset-class TAGS the COLLATERAL asset must carry - the way to ask what can be borrowed against a whole class of collateral rather than one named token. Same accepted set as `loanAssetTags`, and the same reason it is not enumerated here; an unknown tag is rejected by name with the full set."
          },
          "collateralTokenAddress": {
            "description": "Comma-separated contract addresses of the COLLATERAL asset - what a borrower locks up. Up to 20 entries. Use it to answer 'where can I borrow against X'. Addresses only.",
            "type": "string"
          },
          "fields": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Row field groups to keep: a comma list, an array of the same, or 'all' (default). Groups: identity, assets, apy, size, risk. Keeps a large result small; marketId, chain and chainId are always present. An unknown group is rejected by name."
          },
          "irmAddress": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Comma list or array of INTEREST RATE MODEL contract addresses to scope to. Markets sharing an IRM share the shape of how their rate responds to utilization. Read an address from any row's `irmAddress`."
          },
          "isIdle": {
            "description": "True keeps ONLY idle markets, false excludes them. An idle market has no collateral asset and cannot be borrowed from: it is where a vault parks uninvested cash, so it earns nothing and is never a lending recommendation. Omit to include both kinds.",
            "type": "boolean"
          },
          "limit": {
            "description": "Max markets to return (default 20, maximum 50). A larger value is REJECTED by name rather than clamped, so what you asked for is always what was applied; `hasMore` and `nextOffset` report what is left.",
            "type": "number"
          },
          "listedOnly": {
            "description": "Keep only markets Morpho itself curates (default true). Set false to include permissionless deployments - expect unusable oracles and enormous headline rates on near-empty markets, and check every row's warnings.",
            "type": "boolean"
          },
          "loanAssetTags": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Comma list or array of Morpho's own asset-class TAGS the BORROWABLE asset must carry. The accepted set is Morpho's, not ours, and it is NOT enumerated here on purpose: discovery indexes this prose, and repeating a list of asset-class words in a screening tool's parameter made it outrank other namespaces on queries that merely NAMED an asset. Send a tag; a wrong one is rejected by name with the whole accepted set spelled out, which is where that list belongs."
          },
          "loanTokenAddress": {
            "description": "Comma-separated contract addresses of the BORROWABLE asset - the token a lender supplies and a borrower receives. Up to 20 entries. Addresses only; a symbol is rejected by name.",
            "type": "string"
          },
          "marketIds": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Comma list or array of up to 20 known 64-hex market ids, to re-read a specific set in ONE call instead of one morpho__market_get per market. Use it to refresh markets you already hold or already shortlisted. A 40-hex contract address here is rejected by name as an address."
          },
          "maxBorrowUsd": {
            "description": "Maximum total borrowed value in USD. USD is the market oracle's estimate.",
            "type": "number"
          },
          "maxLltvPercent": {
            "description": "Maximum liquidation loan-to-value threshold as a PERCENT, 0-100. Cap it for a wider safety margin before liquidation.",
            "type": "number"
          },
          "maxNetBorrowApyPercent": {
            "description": "Maximum NET borrow APY as a PERCENT (8 means 8%), incentives INCLUDED. The filter for 'cheapest place to borrow'. Not comparable with the incentive-free `borrowApyPercent`.",
            "type": "number"
          },
          "maxSupplyUsd": {
            "description": "Maximum total supplied value in USD. USD is the market oracle's estimate, not a traded price.",
            "type": "number"
          },
          "maxUtilizationPercent": {
            "description": "Maximum borrowed/supplied as a PERCENT, 0-100. Cap it to keep markets you could actually exit; a market at 100% has no withdrawable liquidity at all.",
            "type": "number"
          },
          "minBorrowUsd": {
            "description": "Minimum total borrowed value in USD - real borrowing demand, as opposed to a market that merely holds deposits. USD is the market oracle's estimate.",
            "type": "number"
          },
          "minLltvPercent": {
            "description": "Minimum liquidation loan-to-value threshold as a PERCENT, 0-100 (86 means 86%). Higher lets a borrower take more debt per unit of collateral and liquidates sooner in a drawdown.",
            "type": "number"
          },
          "minNetSupplyApyPercent": {
            "description": "Minimum NET supply APY as a PERCENT (5 means 5%), incentives INCLUDED. Not comparable with the incentive-free `supplyApyPercent` in the result; pair it with minSupplyUsd or the top hits will be dust.",
            "type": "number"
          },
          "minSupplyUsd": {
            "description": "Minimum total supplied value in USD - the main way to screen out dust markets whose headline rate is meaningless. USD is the market oracle's estimate, not a traded price.",
            "type": "number"
          },
          "minUtilizationPercent": {
            "description": "Minimum borrowed/supplied as a PERCENT, 0-100 (80 means 80%), not a fraction. High utilization means a high supply rate but little free liquidity to withdraw.",
            "type": "number"
          },
          "offset": {
            "description": "Row offset for paging (default 0). Pair it with the `nextOffset` the reply returns.",
            "type": "number"
          },
          "oracleAddress": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Comma list or array of ORACLE contract addresses to scope to. Every market names the oracle that prices its collateral, so this is how you find the other markets exposed to one price feed - the blast radius when an oracle is flagged unusable or is known to be stale. Read an address from any row's `oracle.address`."
          },
          "order": {
            "description": "Ranking direction, one of: desc (default), asc. Anything else is rejected by name.",
            "enum": [
              "asc",
              "desc"
            ],
            "type": "string"
          },
          "query": {
            "description": "Free-text substring match on the market's asset symbols and name (for example 'usdc', 'wsteth'). Use it when the user names an asset informally instead of by contract address. It is a FILTER, not a ranker: it narrows the set and `sort` decides the order.",
            "type": "string"
          },
          "sort": {
            "description": "Ranking key, one of: supplyUsd, borrowUsd, sizeUsd, liquidityUsd, supplyApy, netSupplyApy, borrowApy, netBorrowApy, avgBorrowApy, avgNetBorrowApy, dailyBorrowApy, dailyNetBorrowApy, apyAtTarget, utilization, lltv, fee, loanAssetSymbol, collateralAssetSymbol (default supplyUsd). `supplyApy` and `borrowApy` rank by the incentive-FREE rate while `netSupplyApy` and `netBorrowApy` include incentives; `avg*` and `daily*` rank by a trailing average instead of the spot rate, which is the way to avoid ranking on a one-block spike. `sizeUsd` is Morpho's own combined-size measure, `fee` is the market's protocol fee share, and the two symbol keys sort alphabetically for a readable listing. Applied server-side over ALL matches, not just the returned page. Anything else is rejected by name.",
            "enum": [
              "supplyUsd",
              "borrowUsd",
              "sizeUsd",
              "liquidityUsd",
              "supplyApy",
              "netSupplyApy",
              "borrowApy",
              "netBorrowApy",
              "avgBorrowApy",
              "avgNetBorrowApy",
              "dailyBorrowApy",
              "dailyNetBorrowApy",
              "apyAtTarget",
              "utilization",
              "lltv",
              "fee",
              "loanAssetSymbol",
              "collateralAssetSymbol"
            ],
            "type": "string"
          }
        },
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "morpho",
      "publicName": "morpho__markets_discover",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Screen Morpho Blue lending markets",
      "toolId": "morpho.markets.discover"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 4446,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "chainIds": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Comma-separated chains, an array of the same, or 'all'. Slugs or numeric chain ids; discovery ships the supported slugs on this tool's `chains` metadata and an unsupported entry is rejected with the full set. Omit for every chain, which is the right default when the user asks what they hold."
          },
          "includeVaultV2": {
            "description": "Sweep for VaultV2 positions as well as V1 (default true). Morpho publishes no per-user V2 list, so this costs one read to find candidate vaults from the wallet's V2 transaction history plus one per vault, and its coverage is reported in `vaultV2Coverage`. Set false to skip it; the reply then claims nothing about V2.",
            "type": "boolean"
          },
          "limit": {
            "description": "Max positions per section (default 20, maximum 50). A larger value is REJECTED by name rather than clamped. Market and vault positions page independently, each with its own `hasMore`. In the vault section it bounds the V1 page only: the V2 sweep adds its rows on top, so `returned` there can exceed it and `v1Returned` and `v2Returned` say which part came from where.",
            "type": "number"
          },
          "maxHealthFactor": {
            "description": "Keep only lending positions at or below this health factor, as a plain RATIO and not a percent: 1 is already liquidatable, 1.25 is close. The risk screen. It also pages server-side over all matches, so it has no merged-window bound, and it excludes supply-only positions entirely, because those have no health factor at all rather than a high one. Combining it with `scope: vaults` is rejected by name.",
            "type": "number"
          },
          "offset": {
            "description": "Row offset for paging (default 0). Pair it with the reply's `nextOffset`. Without `maxHealthFactor`, offset + limit must stay within 50 rows: the market half is a merge of three reads and that merge is only provably complete inside one window.",
            "type": "number"
          },
          "scope": {
            "description": "Which half to read, one of: all (default), markets, vaults. Lending positions can be liquidated; curated vault deposits cannot. Anything else is rejected by name.",
            "enum": [
              "markets",
              "vaults",
              "all"
            ],
            "type": "string"
          },
          "walletAddress": {
            "description": "The ONE account address whose Morpho positions to read, 0x-prefixed and 40 hex. A list or a second comma-separated address is rejected by name; call the tool once per wallet.",
            "type": "string"
          }
        },
        "required": [
          "walletAddress"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "morpho",
      "publicName": "morpho__positions_get",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Read a wallet's Morpho positions",
      "toolId": "morpho.positions.get"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 2695,
      "destructiveHint": true,
      "inputSchema": {
        "properties": {
          "chain": {
            "description": "The chain whose rewards to claim. Chain slug/alias, or the numeric chain id `TokenFind` returns (e.g. `base` or `8453`). Required and singular: a claim is one transaction on one chain, and rewards on another chain need their own call. There is no wallet parameter - the claim is always for the session's own wallet, which is also the only wallet the distributor would pay.",
            "type": "string"
          },
          "dryRun": {
            "description": "Set true to get the FULL preview and sign nothing: the built and decoded transaction, the allowance plan including any approval still needed, the gas bound and the simulation verdict. It is a preview of this exact operation, not a promise, and nothing is broadcast, approved or recorded as executed.",
            "type": "boolean"
          },
          "morphoOnly": {
            "description": "Claim only the reward token rows carrying a resolved Morpho campaign (default false). This narrows ROWS, not the amounts inside them: a selected row still delivers every campaign it contains. Leaving it false claims everything the wallet can claim on that chain in one transaction, which is almost always what the user wants and always the cheaper way to get it.",
            "type": "boolean"
          }
        },
        "required": [
          "chain"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "morpho",
      "publicName": "morpho__rewards_claim",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Claim earned Morpho rewards",
      "toolId": "morpho.rewards.claim"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 3189,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "chainIds": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Comma-separated chains, an array of the same, or 'all'. Slugs or numeric chain ids; discovery ships the supported slugs on this tool's `chains` metadata and an unsupported entry is rejected with the full set. This is a FAN-OUT and not a filter: the distributor answers one chain per request, so at most 4 chains are read per call and a longer list is rejected by name rather than trimmed. Omit it only when the user genuinely does not know where they earned."
          },
          "morphoOnly": {
            "description": "Keep only reward rows carrying a resolved Morpho campaign (default false). Leave it false when reporting what a claim would deliver, because one claim takes the whole token row whatever produced it. Set it true only when the question is specifically about Morpho's own incentives.",
            "type": "boolean"
          },
          "walletAddress": {
            "description": "The ONE account address whose rewards to read, 0x-prefixed and 40 hex. A second address is rejected by name; call the tool once per wallet.",
            "type": "string"
          }
        },
        "required": [
          "walletAddress"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "morpho",
      "publicName": "morpho__rewards_get",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Read claimable Morpho rewards",
      "toolId": "morpho.rewards.get"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 3288,
      "destructiveHint": true,
      "inputSchema": {
        "properties": {
          "chain": {
            "description": "The chain the vault lives on. Chain slug/alias, or the numeric chain id `TokenFind` returns (e.g. `base` or `8453`). Required because a vault address is chain-scoped: the same address on the wrong chain is a different contract entirely.",
            "type": "string"
          },
          "depositAmountRaw": {
            "description": "How much of the vault's ASSET to deposit, in that asset's RAW base units as a whole-number string. THE SCALE IS THE VAULT ASSET'S OWN: read `asset.decimals` from `morpho__vault_get` for this vault, not the vault's SHARE decimals, which are a different number reported beside it. A human amount is refused, not rounded. It must equal the amount the quote priced.",
            "type": "string"
          },
          "dryRun": {
            "description": "Set true to get the FULL preview and sign nothing: the built and decoded transaction, the allowance plan including any approval still needed, the gas bound and the simulation verdict. It is a preview of this exact operation, not a promise, and nothing is broadcast, approved or recorded as executed.",
            "type": "boolean"
          },
          "slippageBps": {
            "description": "Price protection in basis points (1 bps = 0.01%). Default 100 = 1%. Vex caps it at 1000 (10%) and REJECTS a higher value rather than clamping it.",
            "type": "number"
          },
          "vaultAddress": {
            "description": "The vault's 0x-prefixed 40-hex contract address, from `morpho__vaults_discover` or `morpho__vault_get`. A 64-hex value is rejected by name because that is a MARKET id, not a vault. Both vault generations are detected automatically.",
            "type": "string"
          }
        },
        "required": [
          "vaultAddress",
          "chain",
          "depositAmountRaw"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "morpho",
      "publicName": "morpho__vault_deposit",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Deposit into a Morpho vault",
      "toolId": "morpho.vault.deposit"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 3338,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "chain": {
            "description": "The chain the vault lives on. Chain slug/alias, or the numeric chain id `TokenFind` returns (e.g. `base` or `8453`). Required because a vault address is chain-scoped, so the same address on the wrong chain resolves to nothing. Discovery ships the supported slugs on this tool's `chains` metadata, and an unsupported chain is rejected with the full set spelled out.",
            "type": "string"
          },
          "includeAllocations": {
            "description": "Include the per-market allocation table with caps and each market's own APY (default TRUE). It is on by default because a vault's real risk is the risk of the markets underneath it, not of the vault contract. Set false only when you already know the allocations and want a smaller reply.",
            "type": "boolean"
          },
          "includeHistory": {
            "description": "Add Morpho's trailing average APY beside the live rate (default false). Morpho returns a single average and does NOT name the window it covers, so it is labelled 'recent average' rather than a fixed period; there is no `lookback` here because the vault schema exposes no named averaging windows the way markets do.",
            "type": "boolean"
          },
          "vaultAddress": {
            "description": "The vault's 0x-prefixed 40-hex contract address. Read one from `morpho__vaults_discover`. A 64-hex value is rejected by name because that is a MARKET id and belongs to `morpho__market_get`. You do not need to know whether the vault is V1 or V2; both are tried.",
            "type": "string"
          }
        },
        "required": [
          "vaultAddress",
          "chain"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "morpho",
      "publicName": "morpho__vault_get",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Read one Morpho vault",
      "toolId": "morpho.vault.get"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 4758,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "chain": {
            "description": "The chain the vault lives on. Chain slug/alias, or the numeric chain id `TokenFind` returns (e.g. `base` or `8453`). Required because a vault address is chain-scoped: the same address on the wrong chain is a different contract entirely.",
            "type": "string"
          },
          "depositAmountRaw": {
            "description": "How much of the vault's ASSET to price going IN, in that asset's RAW base units as a whole-number string. Send it with `direction: deposit`, never alongside `withdrawAmountRaw`. THE SCALE IS THE VAULT ASSET'S OWN: read `asset.decimals` from `morpho__vault_get` for this vault, not the vault's SHARE decimals, which are a different number reported beside it. A human amount is refused, not rounded.",
            "type": "string"
          },
          "direction": {
            "description": "Which way the money would move: `deposit` puts the vault's asset in and mints shares, `withdraw` takes the asset out and burns shares. It must AGREE with the amount parameter you send; a call whose direction and amount key disagree is rejected by name rather than resolved either way.",
            "enum": [
              "deposit",
              "withdraw"
            ],
            "type": "string"
          },
          "slippageBps": {
            "description": "Price protection in basis points (1 bps = 0.01%). Default 100 = 1%. Vex caps this at 1000 (10%) and REJECTS a higher value rather than clamping it. On a DEPOSIT it raises the maximum share price the transaction accepts on chain. A WITHDRAWAL has no share-price leg, so the value binds on nothing there and the reply says so.",
            "type": "number"
          },
          "vaultAddress": {
            "description": "The vault's 0x-prefixed 40-hex contract address, from `morpho__vaults_discover` or `morpho__vault_get`. A 64-hex value is rejected by name because that is a MARKET id, not a vault. Both generations are detected automatically.",
            "type": "string"
          },
          "walletAddress": {
            "description": "The 0x-prefixed 40-hex account the preview is for. Supply it to have `requirements` reflect that wallet's CURRENT allowance and permit state. Nothing is signed with it and no key is needed. Omit it entirely rather than inventing a placeholder of your own.",
            "type": "string"
          },
          "withdrawAmountRaw": {
            "description": "How much of the vault's ASSET to price coming OUT, in that asset's RAW base units as a whole-number string. Send it with `direction: withdraw`, never alongside `depositAmountRaw`. THE SCALE IS THE VAULT ASSET'S OWN: read `asset.decimals` from `morpho__vault_get` for this vault, not the SHARE decimals and not the share count you want to burn, which is a different quantity in a different unit.",
            "type": "string"
          }
        },
        "required": [
          "vaultAddress",
          "chain",
          "direction"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "morpho",
      "publicName": "morpho__vault_quote",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Quote a Morpho vault deposit or withdrawal",
      "toolId": "morpho.vault.quote"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 2844,
      "destructiveHint": true,
      "inputSchema": {
        "properties": {
          "chain": {
            "description": "The chain the vault lives on. Chain slug/alias, or the numeric chain id `TokenFind` returns (e.g. `base` or `8453`). Required because a vault address is chain-scoped: the same address on the wrong chain is a different contract entirely.",
            "type": "string"
          },
          "dryRun": {
            "description": "Set true to get the FULL preview and sign nothing: the built and decoded transaction, the allowance plan including any approval still needed, the gas bound and the simulation verdict. It is a preview of this exact operation, not a promise, and nothing is broadcast, approved or recorded as executed.",
            "type": "boolean"
          },
          "slippageBps": {
            "description": "Price protection in basis points (1 bps = 0.01%). Default 100 = 1%. Vex caps it at 1000 (10%) and REJECTS a higher value rather than clamping it.",
            "type": "number"
          },
          "vaultAddress": {
            "description": "The vault's 0x-prefixed 40-hex contract address, from `morpho__vaults_discover` or `morpho__vault_get`. A 64-hex value is rejected by name because that is a MARKET id, not a vault. Both vault generations are detected automatically.",
            "type": "string"
          },
          "withdrawAmountRaw": {
            "description": "How much of the vault's ASSET to take out, in that asset's RAW base units as a whole-number string. THE SCALE IS THE VAULT ASSET'S OWN: read `asset.decimals` from `morpho__vault_get` for this vault, not the SHARE decimals and not the share count you want to burn, which is a different quantity in a different unit. A human amount is refused, not rounded. It must equal the amount the quote priced.",
            "type": "string"
          }
        },
        "required": [
          "vaultAddress",
          "chain",
          "withdrawAmountRaw"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "morpho",
      "publicName": "morpho__vault_withdraw",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Withdraw from a Morpho vault",
      "toolId": "morpho.vault.withdraw"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 6470,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "assetSymbol": {
            "description": "Comma-separated symbols of the vault's asset (for example 'USDC,USDT'). V1-ONLY: Morpho's V2 filter input has no symbol predicate, so this is rejected by name unless `version` is 'v1'. Prefer `assetTokenAddress`, which both generations serve and which cannot match the wrong token.",
            "type": "string"
          },
          "assetTags": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Comma list or array of Morpho's own asset-class TAGS the vault's asset must carry, to screen a whole class of vault rather than one named token. The accepted set is not enumerated here, for the reason recorded on `morpho__markets_discover`'s own `loanAssetTags`; an unknown tag is rejected by name with the full set spelled out, rather than sent as a predicate that matches nothing. V1-ONLY: Morpho's V2 filter input declares no tag predicate, so this is rejected by name unless `version` is 'v1'."
          },
          "assetTokenAddress": {
            "description": "Comma-separated contract addresses of the asset the vault holds and pays out in - the token a depositor puts in. Up to 20 entries. Works on BOTH generations, which is why it is the right way to ask for 'USDC vaults'. Addresses only; a symbol is rejected by name.",
            "type": "string"
          },
          "chainIds": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Comma-separated chains to scope to, an array of the same, or 'all'. Each entry is a chain slug or a numeric chain id; discovery ships the supported slugs on this tool's `chains` metadata, and an unsupported entry is rejected with the full set spelled out. Omit for every supported chain."
          },
          "curatorAddress": {
            "description": "Comma-separated addresses of the curator, the party that decides where the vault's money goes. Up to 20 entries. Use it to find every vault run by a manager the user already trusts.",
            "type": "string"
          },
          "fields": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Row field groups to keep: a comma list, an array of the same, or 'all' (default). Groups: identity, asset, apy, size, fees, governance, gating. Keeps a large result small; address, version and chain are always present, the gated flags survive dropping the gating group, and an unknown group is rejected by name."
          },
          "limit": {
            "description": "Max vaults to return (default 20, maximum 50). A larger value is REJECTED by name rather than clamped, so what you asked for is always what was applied; `hasMore` and `nextOffset` report what is left.",
            "type": "number"
          },
          "listedOnly": {
            "description": "Keep only vaults Morpho itself curates (default true). Set false to include uncurated deployments - expect deploy tests holding a few dollars, and check every row's warnings, TVL and curator.",
            "type": "boolean"
          },
          "maxCuratorCutPercent": {
            "description": "Maximum curator fee as a PERCENT, 0-100 (20 means 20%). On V1 this bounds the vault's single fee; on V2 it bounds the PERFORMANCE fee only, because V2's management fee is a separate field Morpho does not offer a combined filter for - the management fee is still reported on every row.",
            "type": "number"
          },
          "maxTvlUsd": {
            "description": "Maximum total deposits in USD. USD is Morpho's oracle estimate, not a traded price.",
            "type": "number"
          },
          "minNetApyPercent": {
            "description": "Minimum NET APY as a PERCENT (5 means 5%), meaning after the curator's fee and with incentives included. Not comparable with a market's `supplyApyPercent`, which is gross of any vault fee. Pair it with minTvlUsd or the top hits will be near-empty vaults.",
            "type": "number"
          },
          "minTvlUsd": {
            "description": "Minimum total deposits in USD - the main way to screen out empty and test vaults, whose headline rates are meaningless. USD is Morpho's oracle estimate, not a traded price.",
            "type": "number"
          },
          "offset": {
            "description": "Row offset for paging (default 0). Pair it with the `nextOffset` the reply returns. At `version: both`, offset + limit must stay within 50 rows so the merged ranking stays provably exact.",
            "type": "number"
          },
          "order": {
            "description": "Ranking direction, one of: desc (default), asc. Anything else is rejected by name.",
            "enum": [
              "asc",
              "desc"
            ],
            "type": "string"
          },
          "query": {
            "description": "Free-text substring match on the vault's name and symbol (for example 'steakhouse', 'usdc'). It is a FILTER, not a ranker: it narrows the set and `sort` decides the order. V1-ONLY: Morpho's V2 filter input has no free-text predicate, so this is rejected by name unless `version` is 'v1'.",
            "type": "string"
          },
          "route": {
            "description": "Which option SETS to return, one of: curated (default), direct, both. `curated` returns the vault rows only, unchanged from before this key existed. `direct` returns only the Blue markets that lend the same asset, each with its supply APY, utilization, liquidity, collateral asset, LLTV and warnings, and queries no vault. `both` returns both sets AND a merged `options` list ranked on net APY, where every entry carries its net APY after fees, the fee, its diversification, its gating status, the tool to act on it with, and its distance in PERCENTAGE POINTS from the best direct option. `direct` and `both` require `assetTokenAddress` to name exactly one asset. Anything else is rejected by name.",
            "enum": [
              "curated",
              "direct",
              "both"
            ],
            "type": "string"
          },
          "sort": {
            "description": "Ranking key, one of: tvlUsd, netApy, apy, avgApy, avgNetApy, dailyApy, dailyNetApy, name, curator, fee, liquidityUsd, idleAssetsUsd, realAssetsUsd (default tvlUsd). Only tvlUsd, apy and netApy are served by BOTH generations. `name`, `curator`, `fee` and the four trailing-average keys (avgApy, avgNetApy, dailyApy, dailyNetApy) are V1-ONLY; `liquidityUsd`, `idleAssetsUsd` and `realAssetsUsd` are V2-ONLY. A key the selected generation cannot serve is rejected by name, naming the `version` that can, rather than being swapped for a key that exists. Applied server-side over ALL matches within each generation.",
            "enum": [
              "tvlUsd",
              "netApy",
              "apy",
              "avgApy",
              "avgNetApy",
              "dailyApy",
              "dailyNetApy",
              "name",
              "curator",
              "fee",
              "liquidityUsd",
              "idleAssetsUsd",
              "realAssetsUsd"
            ],
            "type": "string"
          },
          "suppliesMarketIds": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Comma list or array of up to 20 64-hex MARKET ids; keeps only vaults that supply at least one listed market. This is the 'which vaults are exposed to this market' question - use it to find the curated routes into a market you already picked, or to size who else is exposed when a market looks unsafe. V1-ONLY: Morpho's V2 filter input has no equivalent, so it is rejected by name unless `version` is 'v1'."
          },
          "version": {
            "description": "Which vault generation to read, one of: both (default), v1, v2. V1 is MetaMorpho with a single global timelock and no gating; V2 adds per-function timelocks, a management fee and transfer gates. At 'both' the two are queried separately and merged into one exact ranking, which bounds paging to a single window. Anything else is rejected by name.",
            "enum": [
              "v1",
              "v2",
              "both"
            ],
            "type": "string"
          }
        },
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "morpho",
      "publicName": "morpho__vaults_discover",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Screen Morpho vaults",
      "toolId": "morpho.vaults.discover"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 3149,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "chain": {
            "description": "The chain to read on. Chain slug/alias, or the numeric chain id `TokenFind` returns (e.g. `base` or `8453`). Required and single, because a balance and an approval exist per chain: the same token symbol on two chains is two different contracts with two different allowances, so a guessed chain answers confidently about the wrong one.",
            "type": "string"
          },
          "tokenAddress": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Comma-separated ERC-20 CONTRACT addresses, or an array of the same. Symbols are rejected: several tokens on one chain answer to the same symbol, and an allowance read against the wrong contract is a false safety signal. At most 12 per call, rejected by name rather than trimmed, because each token costs a balance read plus one read per Morpho spender. The native sentinel address is accepted and folded into the native balance. Omit it entirely for the native balance alone."
          },
          "walletAddress": {
            "description": "The ONE account address to read, 0x-prefixed and 40 hex. Any address works; no key is used and nothing is unlocked.",
            "type": "string"
          }
        },
        "required": [
          "walletAddress",
          "chain"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "morpho",
      "publicName": "morpho__wallet_balance_get",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Read on-chain balances for Morpho tokens",
      "toolId": "morpho.wallet.balance"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1251,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "chain": {
            "description": "Chain slug or numeric id (e.g. 'ethereum', 'arbitrum', 42161). One call reads ONE chain; an unsupported chain is rejected by name.",
            "type": "string"
          },
          "ids": {
            "description": "Comma-separated asset ids to price — bare 0x addresses or 'chainId-0xaddress' composites, at most 50 per call. An id belonging to a different chain than `chain` is rejected by name. Omit to page through the chain's assets instead.",
            "type": "string"
          },
          "limit": {
            "description": "Rows per page when no ids are given: whole number 1-100 (default 50). The response reports total, hasMore and nextOffset.",
            "type": "number"
          },
          "offset": {
            "description": "Row offset for paging, whole number ≥ 0 (default 0). Pair it with the returned nextOffset.",
            "type": "number"
          },
          "type": {
            "description": "Optional asset-class filter: PENDLE_LP, SY, PT, YT (comma-separated for several). Anything else is rejected by name. Most useful when paging without `ids`.",
            "type": "string"
          }
        },
        "required": [
          "chain"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "pendle",
      "publicName": "pendle__asset_prices_get",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Read Pendle asset prices",
      "toolId": "pendle.prices.assets"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1755,
      "destructiveHint": true,
      "inputSchema": {
        "properties": {
          "amountIn": {
            "description": "Amount of the payment token in human-readable units.",
            "type": "string"
          },
          "chain": {
            "description": "Chain slug or id — one of Pendle's 11 chains (e.g. 'ethereum', 'arbitrum', 'base', 'bsc').",
            "type": "string"
          },
          "dryRun": {
            "description": "Preview without executing.",
            "type": "boolean"
          },
          "market": {
            "description": "The Pendle MARKET (LP) CONTRACT ADDRESS — the LP anchor liquidity is added to or removed from.",
            "type": "string"
          },
          "slippageBps": {
            "description": "Slippage tolerance in basis points (default 100 = 1%).",
            "type": "number"
          },
          "tokenIn": {
            "description": "The payment token CONTRACT ADDRESS to deposit (ERC-20; use WETH for ETH).",
            "type": "string"
          }
        },
        "required": [
          "chain",
          "market",
          "tokenIn",
          "amountIn"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "pendle",
      "publicName": "pendle__lp_add",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Add single-token Pendle liquidity",
      "toolId": "pendle.lp.add"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1271,
      "destructiveHint": true,
      "inputSchema": {
        "properties": {
          "amountIn": {
            "description": "Amount of the payment token to deposit, in human-readable units.",
            "type": "string"
          },
          "chain": {
            "description": "Chain slug or id — one of Pendle's 11 chains (e.g. 'ethereum', 'arbitrum', 'base', 'bsc').",
            "type": "string"
          },
          "dryRun": {
            "description": "true = quote only. Prices both output legs, runs every fund-safety check including a separate price floor per leg, records the authorization this tool needs, and broadcasts NOTHING. Required before the real call.",
            "type": "boolean"
          },
          "market": {
            "description": "The Pendle MARKET (LP) CONTRACT ADDRESS - the market IS its LP token. Find it with pendle__markets_discover or read it from pendle__market_get.",
            "type": "string"
          },
          "slippageBps": {
            "description": "Slippage tolerance in whole basis points (default 100 = 1%; maximum 1000 = 10%). A fractional, negative or larger value is REJECTED, never clamped. It bounds BOTH output legs. The dry run and the execute must pass the SAME value (or omit it on both).",
            "type": "number"
          },
          "tokenIn": {
            "description": "The payment token CONTRACT ADDRESS to deposit (ERC-20; pass the chain's wrapped-native token for native exposure).",
            "type": "string"
          }
        },
        "required": [
          "chain",
          "market",
          "tokenIn",
          "amountIn"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "pendle",
      "publicName": "pendle__lp_add_keep_yt",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Add Pendle liquidity and keep the YT",
      "toolId": "pendle.lp.addKeepYt"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 971,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "amountIn": {
            "description": "Human-readable amount — add: the payment token amount; remove: the LP token amount to burn.",
            "type": "string"
          },
          "chain": {
            "description": "Chain slug or id — one of Pendle's 11 chains (e.g. 'ethereum', 'arbitrum', 'base', 'bsc').",
            "type": "string"
          },
          "direction": {
            "description": "'add' (token → LP) or 'remove' (LP → token).",
            "type": "string"
          },
          "market": {
            "description": "The Pendle MARKET (LP) CONTRACT ADDRESS — the LP anchor liquidity is added to or removed from.",
            "type": "string"
          },
          "slippageBps": {
            "description": "Slippage tolerance in basis points (default 100 = 1%).",
            "type": "number"
          },
          "tokenIn": {
            "description": "ADD only: the payment token CONTRACT ADDRESS to deposit (ERC-20; use WETH for ETH).",
            "type": "string"
          },
          "tokenOut": {
            "description": "REMOVE only: the output token CONTRACT ADDRESS. Defaults to the market's underlying asset.",
            "type": "string"
          }
        },
        "required": [
          "chain",
          "direction",
          "market",
          "amountIn"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "pendle",
      "publicName": "pendle__lp_quote",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Quote a Pendle liquidity add or remove",
      "toolId": "pendle.lp.quote"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1747,
      "destructiveHint": true,
      "inputSchema": {
        "properties": {
          "amountIn": {
            "description": "Amount of the LP token to remove in human-readable units.",
            "type": "string"
          },
          "chain": {
            "description": "Chain slug or id — one of Pendle's 11 chains (e.g. 'ethereum', 'arbitrum', 'base', 'bsc').",
            "type": "string"
          },
          "dryRun": {
            "description": "Preview without executing.",
            "type": "boolean"
          },
          "market": {
            "description": "The Pendle MARKET (LP) CONTRACT ADDRESS — the LP anchor liquidity is added to or removed from.",
            "type": "string"
          },
          "slippageBps": {
            "description": "Slippage tolerance in basis points (default 100 = 1%).",
            "type": "number"
          },
          "tokenOut": {
            "description": "The output token CONTRACT ADDRESS. Defaults to the market's underlying asset.",
            "type": "string"
          }
        },
        "required": [
          "chain",
          "market",
          "amountIn"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "pendle",
      "publicName": "pendle__lp_remove",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Remove Pendle liquidity to one token",
      "toolId": "pendle.lp.remove"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1304,
      "destructiveHint": true,
      "inputSchema": {
        "properties": {
          "amountIn": {
            "description": "Amount of the LP (market) token to burn, in human-readable units.",
            "type": "string"
          },
          "chain": {
            "description": "Chain slug or id — one of Pendle's 11 chains (e.g. 'ethereum', 'arbitrum', 'base', 'bsc').",
            "type": "string"
          },
          "dryRun": {
            "description": "true = quote only. Prices both output legs, runs every fund-safety check including a separate price floor per leg, records the authorization this tool needs, and broadcasts NOTHING. Required before the real call.",
            "type": "boolean"
          },
          "market": {
            "description": "The Pendle MARKET (LP) CONTRACT ADDRESS - the market IS its LP token. Find it with pendle__markets_discover or read it from pendle__market_get.",
            "type": "string"
          },
          "slippageBps": {
            "description": "Slippage tolerance in whole basis points (default 100 = 1%; maximum 1000 = 10%). A fractional, negative or larger value is REJECTED, never clamped. It bounds BOTH output legs. The dry run and the execute must pass the SAME value (or omit it on both).",
            "type": "number"
          },
          "tokenOut": {
            "description": "The TOKEN output leg's CONTRACT ADDRESS (ERC-20; pass wrapped native, never native). Defaults to the market's underlying asset. The other leg is always the market's own PT and is not a parameter.",
            "type": "string"
          }
        },
        "required": [
          "chain",
          "market",
          "amountIn"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "pendle",
      "publicName": "pendle__lp_remove_dual",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Remove Pendle liquidity to two tokens",
      "toolId": "pendle.lp.removeDual"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1927,
      "destructiveHint": true,
      "inputSchema": {
        "properties": {
          "amountIn": {
            "description": "Amount of the SOURCE token in human-readable units (e.g. '1.5', not raw base units).",
            "type": "string"
          },
          "chain": {
            "description": "Chain slug or id — one of Pendle's 11 chains (e.g. 'ethereum', 'arbitrum', 'base', 'bsc'). Both legs are on ONE chain; none of these tools bridges.",
            "type": "string"
          },
          "dryRun": {
            "description": "true = quote only. Prices the move, runs every fund-safety check including the per-leg price floor, records the authorization this tool needs, and broadcasts NOTHING. Required before the real call.",
            "type": "boolean"
          },
          "market": {
            "description": "The Pendle MARKET ADDRESS whose LP you hold and whose PT you want (the market IS the LP token).",
            "type": "string"
          },
          "pt": {
            "description": "OPTIONAL check: the PT contract address you expect to receive. Must be this market's own PT — a PT from any other market is refused by name. Omit it and Vex uses the market's PT.",
            "type": "string"
          },
          "slippageBps": {
            "description": "Slippage tolerance in whole basis points (default 100 = 1%; maximum 1000 = 10%). A fractional, negative or larger value is REJECTED, never clamped. The dry run and the execute must pass the SAME value (or omit it on both).",
            "type": "number"
          }
        },
        "required": [
          "chain",
          "market",
          "amountIn"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "pendle",
      "publicName": "pendle__lp_to_pt",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Convert Pendle liquidity into PT",
      "toolId": "pendle.lp.toPt"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1314,
      "destructiveHint": true,
      "inputSchema": {
        "properties": {
          "amountIn": {
            "description": "Amount of the SOURCE token in human-readable units (e.g. '1.5', not raw base units).",
            "type": "string"
          },
          "chain": {
            "description": "Chain slug or id — one of Pendle's 11 chains (e.g. 'ethereum', 'arbitrum', 'base', 'bsc'). Both legs are on ONE chain; none of these tools bridges.",
            "type": "string"
          },
          "dryRun": {
            "description": "true = quote only. Prices the move, runs every fund-safety check including the per-leg price floor, records the authorization this tool needs, and broadcasts NOTHING. Required before the real call.",
            "type": "boolean"
          },
          "fromMarket": {
            "description": "The Pendle MARKET ADDRESS whose LP you hold (the market IS the LP token). May be MATURED.",
            "type": "string"
          },
          "slippageBps": {
            "description": "Slippage tolerance in whole basis points (default 100 = 1%; maximum 1000 = 10%). A fractional, negative or larger value is REJECTED, never clamped. The dry run and the execute must pass the SAME value (or omit it on both).",
            "type": "number"
          },
          "toMarket": {
            "description": "The Pendle MARKET ADDRESS to move the liquidity into. Must be ACTIVE - find one with pendle__markets_discover.",
            "type": "string"
          }
        },
        "required": [
          "chain",
          "fromMarket",
          "toMarket",
          "amountIn"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "pendle",
      "publicName": "pendle__lp_transfer",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Move Pendle liquidity between markets",
      "toolId": "pendle.lp.transfer"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1083,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "asset": {
            "description": "PT, YT or LP CONTRACT ADDRESS, 0x-prefixed 40-hex. This endpoint covers those three only - it does not price SY or ordinary tokens. pendle__market_get lists a market's legs.",
            "type": "string"
          },
          "chain": {
            "description": "Chain slug or numeric id (e.g. 'ethereum', 'base', 8453). An unsupported chain is rejected by name.",
            "type": "string"
          },
          "from": {
            "description": "Window start as an ISO-8601 instant (e.g. '2026-07-20T00:00:00Z'). Omit to let Pendle choose the window. Combined with timeFrame it must cover at most 1440 candles.",
            "type": "string"
          },
          "timeFrame": {
            "description": "Candle size: 'hour', 'day' (default) or 'week'. Anything else is rejected by name.",
            "type": "string"
          },
          "to": {
            "description": "Window end as an ISO-8601 instant. Must be later than `from`. Omit for 'up to now'.",
            "type": "string"
          }
        },
        "required": [
          "chain",
          "asset"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "pendle",
      "publicName": "pendle__market_candles_get",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Read Pendle asset price candles",
      "toolId": "pendle.market.candles"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1063,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "chain": {
            "description": "Chain slug or numeric id — one of Pendle's supported chains (e.g. 'ethereum', 'arbitrum', 'base', 'bsc', 1, 42161). An unsupported chain is rejected by name. The same address on another chain is a different asset.",
            "type": "string"
          },
          "market": {
            "description": "Market (LP) CONTRACT ADDRESS, 0x-prefixed 40-hex. Pass exactly ONE of market, pt or yt — passing two is rejected by name rather than resolved.",
            "type": "string"
          },
          "pt": {
            "description": "Principal-token CONTRACT ADDRESS, 0x-prefixed 40-hex. Use this when you hold or are pricing a PT and do not know its market.",
            "type": "string"
          },
          "yt": {
            "description": "Yield-token CONTRACT ADDRESS, 0x-prefixed 40-hex. Use this when you hold or are pricing a YT and do not know its market.",
            "type": "string"
          }
        },
        "required": [
          "chain"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "pendle",
      "publicName": "pendle__market_get",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Read one Pendle market",
      "toolId": "pendle.market.get"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 739,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "chain": {
            "description": "Chain slug or numeric id (e.g. 'ethereum', 'arbitrum', 42161). An unsupported chain is rejected by name.",
            "type": "string"
          },
          "fields": {
            "description": "Comma-separated series fields. Default: impliedApy,underlyingApy,tvl. Selectable: impliedApy, underlyingApy, maxApy, baseApy, tvl, ptPrice, ytPrice, lpPrice, tradingVolume, pendleApy, aggregatedApy, swapFeeApy, ytFloatingApy, totalPt, totalSy, totalSupply, totalActiveSupply. An unknown field is rejected by name — Pendle serves only these. Ask for fewer fields to spend fewer compute units.",
            "type": "string"
          },
          "from": {
            "description": "Window start as an ISO-8601 instant (e.g. '2026-07-20T00:00:00Z'). Omit to let Pendle choose the window. Combined with timeFrame it must cover at most 1440 points.",
            "type": "string"
          },
          "market": {
            "description": "Market (LP) CONTRACT ADDRESS, 0x-prefixed 40-hex. Not the PT or YT - resolve those to their market with pendle__market_get first.",
            "type": "string"
          },
          "timeFrame": {
            "description": "Resolution: 'hour', 'day' (default) or 'week'. Anything else is rejected by name.",
            "type": "string"
          },
          "to": {
            "description": "Window end as an ISO-8601 instant. Must be later than `from`. Omit for 'up to now'.",
            "type": "string"
          }
        },
        "required": [
          "chain",
          "market"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "pendle",
      "publicName": "pendle__market_history_get",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Read a Pendle market's history",
      "toolId": "pendle.market.history"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1734,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "chain": {
            "description": "Chain slug or numeric id (e.g. 'ethereum', 'arbitrum', 42161). An unsupported chain is rejected by name.",
            "type": "string"
          },
          "limit": {
            "description": "Levels to show per side: whole number 1-50 (default 10); above that it is rejected by name. The response always reports how many levels exist, so nothing is dropped silently.",
            "type": "number"
          },
          "market": {
            "description": "Market (LP) CONTRACT ADDRESS, 0x-prefixed 40-hex. Not the PT or YT - pendle__market_get resolves either to its market.",
            "type": "string"
          },
          "precision": {
            "description": "Decimal places the implied-APY axis is rounded to: whole number 0-3 (default 2). Provider-required; anything outside the range is rejected by name. Coarser precision merges nearby levels into fewer, larger ones.",
            "type": "number"
          }
        },
        "required": [
          "chain",
          "market"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "pendle",
      "publicName": "pendle__market_orderbook_get",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Read Pendle limit-order depth",
      "toolId": "pendle.orderbook"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 2097,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "categories": {
            "description": "Comma-separated Pendle category ids to KEEP (e.g. 'stables,points,ethena').",
            "type": "string"
          },
          "chainIds": {
            "description": "Comma-separated chains to scope to (e.g. 'ethereum,arbitrum,base'), or 'all'. Each entry is a chain slug/alias or a numeric chain id. Omit for every Pendle chain. An unsupported chain is rejected by name.",
            "type": "string"
          },
          "excludeCategories": {
            "description": "Comma-separated Pendle category ids to EXCLUDE (e.g. 'points' to skip points-farming markets).",
            "type": "string"
          },
          "expiryAfter": {
            "description": "Only markets expiring after this ISO-8601 date.",
            "type": "string"
          },
          "expiryBefore": {
            "description": "Only markets expiring before this ISO-8601 date (e.g. '2027-01-01').",
            "type": "string"
          },
          "fields": {
            "description": "Comma-separated row field groups to keep: identity, apy, liquidity, expiry, legs, points, protocols — or 'all' (default). Use it to keep large result sets small.",
            "type": "string"
          },
          "includeMatured": {
            "description": "Include EXPIRED/matured markets (default false). Matured markets can only be redeemed or removed, never bought.",
            "type": "boolean"
          },
          "isNew": {
            "description": "Only markets Pendle flags as new (true), or only established ones (false).",
            "type": "boolean"
          },
          "isPrime": {
            "description": "Only markets Pendle flags as prime (true), or only non-prime (false).",
            "type": "boolean"
          },
          "limit": {
            "description": "Max markets to return (default 20). There is NO hidden ceiling — hasMore and nextOffset report what is left.",
            "type": "number"
          },
          "maxDaysToExpiry": {
            "description": "Maximum whole days until maturity — find the nearest maturities.",
            "type": "number"
          },
          "maxImpliedApyPercent": {
            "description": "Maximum implied APY as a PERCENT (25 = 25%). Useful to exclude implausible headline rates.",
            "type": "number"
          },
          "maxLiquidityUsd": {
            "description": "Maximum market liquidity in USD.",
            "type": "number"
          },
          "minDaysToExpiry": {
            "description": "Minimum whole days until maturity. Negative values match already-matured markets.",
            "type": "number"
          },
          "minImpliedApyPercent": {
            "description": "Minimum implied APY as a PERCENT (5 = 5%), not a fraction.",
            "type": "number"
          },
          "minLiquidityUsd": {
            "description": "Minimum market liquidity in USD — screen out thin markets you could not exit.",
            "type": "number"
          },
          "offset": {
            "description": "Row offset for paging (default 0). Pair with the returned nextOffset.",
            "type": "number"
          },
          "order": {
            "description": "'desc' (default) or 'asc'.",
            "type": "string"
          },
          "sort": {
            "description": "Ranking key: liquidity (default), impliedApy, aggregatedApy, underlyingApy, maxBoostedApy, tvl, volume, expiry, name. Anything else is rejected by name.",
            "type": "string"
          },
          "underlyingSymbol": {
            "description": "Substring match on the underlying asset symbol or market name (e.g. 'usde', 'weth', 'susd').",
            "type": "string"
          }
        },
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "pendle",
      "publicName": "pendle__markets_discover",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Screen Pendle yield markets",
      "toolId": "pendle.yields"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1668,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "chain": {
            "description": "Optional chain slug or numeric id (e.g. 'ethereum', 42161) to narrow the list to ONE chain. Omit it to see every chain's rewards in one call — there is no chainIds list here, and no 'all' literal to pass. An unsupported chain is rejected by name.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "pendle",
      "publicName": "pendle__merkle_rewards_list",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "List claimable Pendle merkle rewards",
      "toolId": "pendle.rewards.merkle"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1675,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "chainIds": {
            "description": "Comma-separated chains to scope to, or 'all'. Each entry is a chain slug/alias or a numeric chain id. Omit for every chain the dashboard returns.",
            "type": "string"
          },
          "fields": {
            "description": "Comma-separated leg field groups to keep: identity, balance, value, expiry, accrued — or 'all' (default).",
            "type": "string"
          },
          "includeAccrued": {
            "description": "Include accrued unclaimed interest and rewards per leg (default true). Pendle caches these up to 24h.",
            "type": "boolean"
          },
          "kinds": {
            "description": "Comma-separated position kinds to keep: pt, yt, lp, sy. Omit for all four. Cross-chain PT legs are always reported separately.",
            "type": "string"
          },
          "minValueUsd": {
            "description": "Server-side minimum USD value per position; smaller ones are not returned at all. Use it to drop dust. Default: unfiltered.",
            "type": "number"
          },
          "redeemableOnly": {
            "description": "Only legs in state matured_redeemable — the matured PTs ready to redeem (default false).",
            "type": "boolean"
          },
          "sort": {
            "description": "'value' (default), 'expiry' or 'chain'. Anything else is rejected by name.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "pendle",
      "publicName": "pendle__positions_get",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Value this wallet's Pendle positions",
      "toolId": "pendle.position.value"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1677,
      "destructiveHint": true,
      "inputSchema": {
        "properties": {
          "amountIn": {
            "description": "Amount of tokenIn in human-readable units.",
            "type": "string"
          },
          "chain": {
            "description": "Chain slug or id — one of Pendle's 11 chains (e.g. 'ethereum', 'arbitrum', 'base', 'bsc').",
            "type": "string"
          },
          "dryRun": {
            "description": "Preview without executing.",
            "type": "boolean"
          },
          "slippageBps": {
            "description": "Slippage tolerance in basis points (default 100 = 1%).",
            "type": "number"
          },
          "tokenIn": {
            "description": "Input token CONTRACT ADDRESS (ERC-20; use WETH for ETH). Buy: the payment token. Sell: the PT address.",
            "type": "string"
          },
          "tokenOut": {
            "description": "Output token CONTRACT ADDRESS. Buy: the PT address. Sell: the payment token.",
            "type": "string"
          }
        },
        "required": [
          "chain",
          "tokenIn",
          "tokenOut",
          "amountIn"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "pendle",
      "publicName": "pendle__pt_buy",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Buy a Pendle principal token",
      "toolId": "pendle.pt.buy"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1011,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "amountIn": {
            "description": "Amount of tokenIn in human-readable units.",
            "type": "string"
          },
          "chain": {
            "description": "Chain slug or id — one of Pendle's 11 chains (e.g. 'ethereum', 'arbitrum', 'base', 'bsc').",
            "type": "string"
          },
          "slippageBps": {
            "description": "Slippage tolerance in basis points (default 100 = 1%).",
            "type": "number"
          },
          "tokenIn": {
            "description": "Input token address (payment token for a buy; PT address for a sell/redeem).",
            "type": "string"
          },
          "tokenOut": {
            "description": "Output token address (PT for a buy; payment/underlying for a sell/redeem).",
            "type": "string"
          }
        },
        "required": [
          "chain",
          "tokenIn",
          "tokenOut",
          "amountIn"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "pendle",
      "publicName": "pendle__pt_quote",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Quote a Pendle PT trade",
      "toolId": "pendle.pt.quote"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1969,
      "destructiveHint": true,
      "inputSchema": {
        "properties": {
          "amountIn": {
            "description": "Amount of PT to redeem in human-readable units.",
            "type": "string"
          },
          "chain": {
            "description": "Chain slug or id — one of Pendle's 11 chains (e.g. 'ethereum', 'arbitrum', 'base', 'bsc').",
            "type": "string"
          },
          "dryRun": {
            "description": "Preview without executing.",
            "type": "boolean"
          },
          "slippageBps": {
            "description": "Slippage tolerance in basis points (default 100 = 1%).",
            "type": "number"
          },
          "tokenIn": {
            "description": "The matured PT CONTRACT ADDRESS to redeem.",
            "type": "string"
          }
        },
        "required": [
          "chain",
          "tokenIn",
          "amountIn"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "pendle",
      "publicName": "pendle__pt_redeem",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Redeem a matured Pendle principal token",
      "toolId": "pendle.pt.redeem"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1306,
      "destructiveHint": true,
      "inputSchema": {
        "properties": {
          "amountIn": {
            "description": "Amount of the SOURCE token in human-readable units (e.g. '1.5', not raw base units).",
            "type": "string"
          },
          "chain": {
            "description": "Chain slug or id — one of Pendle's 11 chains (e.g. 'ethereum', 'arbitrum', 'base', 'bsc'). Both legs are on ONE chain; none of these tools bridges.",
            "type": "string"
          },
          "dryRun": {
            "description": "true = quote only. Prices the move, runs every fund-safety check including the per-leg price floor, records the authorization this tool needs, and broadcasts NOTHING. Required before the real call.",
            "type": "boolean"
          },
          "fromPt": {
            "description": "The PT CONTRACT ADDRESS you currently hold (the source maturity). May belong to a MATURED market. Read it from pendle__market_get as the market's `pt` field.",
            "type": "string"
          },
          "slippageBps": {
            "description": "Slippage tolerance in whole basis points (default 100 = 1%; maximum 1000 = 10%). A fractional, negative or larger value is REJECTED, never clamped. The dry run and the execute must pass the SAME value (or omit it on both).",
            "type": "number"
          },
          "toPt": {
            "description": "The PT CONTRACT ADDRESS to roll INTO (the destination maturity). Must belong to an ACTIVE market - find one with pendle__markets_discover.",
            "type": "string"
          }
        },
        "required": [
          "chain",
          "fromPt",
          "toPt",
          "amountIn"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "pendle",
      "publicName": "pendle__pt_rollover",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Roll a Pendle PT into a later expiry",
      "toolId": "pendle.pt.rollover"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1729,
      "destructiveHint": true,
      "inputSchema": {
        "properties": {
          "amountIn": {
            "description": "Amount of tokenIn in human-readable units.",
            "type": "string"
          },
          "chain": {
            "description": "Chain slug or id — one of Pendle's 11 chains (e.g. 'ethereum', 'arbitrum', 'base', 'bsc').",
            "type": "string"
          },
          "dryRun": {
            "description": "Preview without executing.",
            "type": "boolean"
          },
          "slippageBps": {
            "description": "Slippage tolerance in basis points (default 100 = 1%).",
            "type": "number"
          },
          "tokenIn": {
            "description": "Input token CONTRACT ADDRESS (ERC-20; use WETH for ETH). Buy: the payment token. Sell: the PT address.",
            "type": "string"
          },
          "tokenOut": {
            "description": "Output token CONTRACT ADDRESS. Buy: the PT address. Sell: the payment token.",
            "type": "string"
          }
        },
        "required": [
          "chain",
          "tokenIn",
          "tokenOut",
          "amountIn"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "pendle",
      "publicName": "pendle__pt_sell",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Sell a Pendle principal token",
      "toolId": "pendle.pt.sell"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1824,
      "destructiveHint": true,
      "inputSchema": {
        "properties": {
          "amountIn": {
            "description": "Amount of the payment token in human-readable units.",
            "type": "string"
          },
          "chain": {
            "description": "Chain slug or id — one of Pendle's 11 chains (e.g. 'ethereum', 'arbitrum', 'base', 'bsc').",
            "type": "string"
          },
          "dryRun": {
            "description": "Preview without executing.",
            "type": "boolean"
          },
          "pt": {
            "description": "The market's PT (principal token) CONTRACT ADDRESS — the anchor that resolves the market and its YT.",
            "type": "string"
          },
          "slippageBps": {
            "description": "Slippage tolerance in basis points (default 100 = 1%).",
            "type": "number"
          },
          "tokenIn": {
            "description": "The payment token CONTRACT ADDRESS to spend (ERC-20; use WETH for ETH).",
            "type": "string"
          }
        },
        "required": [
          "chain",
          "pt",
          "tokenIn",
          "amountIn"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "pendle",
      "publicName": "pendle__py_mint",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Mint a Pendle PT and YT pair",
      "toolId": "pendle.py.mint"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 992,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "amountIn": {
            "description": "Human-readable amount — mint: the payment token amount; redeem: the PT+YT pair amount to burn.",
            "type": "string"
          },
          "chain": {
            "description": "Chain slug or id — one of Pendle's 11 chains (e.g. 'ethereum', 'arbitrum', 'base', 'bsc').",
            "type": "string"
          },
          "direction": {
            "description": "'mint' (token → PT+YT) or 'redeem' (pre-expiry PT+YT → token).",
            "type": "string"
          },
          "pt": {
            "description": "The market's PT (principal token) CONTRACT ADDRESS — the anchor that resolves the market and its YT.",
            "type": "string"
          },
          "slippageBps": {
            "description": "Slippage tolerance in basis points (default 100 = 1%).",
            "type": "number"
          },
          "tokenIn": {
            "description": "MINT only: the payment token CONTRACT ADDRESS to spend (ERC-20; use WETH for ETH).",
            "type": "string"
          },
          "tokenOut": {
            "description": "REDEEM only: the output token CONTRACT ADDRESS. Defaults to the market's underlying asset.",
            "type": "string"
          }
        },
        "required": [
          "chain",
          "direction",
          "pt",
          "amountIn"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "pendle",
      "publicName": "pendle__py_quote",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Quote a Pendle PT/YT mint or redeem",
      "toolId": "pendle.py.quote"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1706,
      "destructiveHint": true,
      "inputSchema": {
        "properties": {
          "amountIn": {
            "description": "Amount of the PT+YT pair to burn in human-readable units (equal PT and YT).",
            "type": "string"
          },
          "chain": {
            "description": "Chain slug or id — one of Pendle's 11 chains (e.g. 'ethereum', 'arbitrum', 'base', 'bsc').",
            "type": "string"
          },
          "dryRun": {
            "description": "Preview without executing.",
            "type": "boolean"
          },
          "pt": {
            "description": "The market's PT (principal token) CONTRACT ADDRESS — the anchor that resolves the market and its YT.",
            "type": "string"
          },
          "slippageBps": {
            "description": "Slippage tolerance in basis points (default 100 = 1%).",
            "type": "number"
          },
          "tokenOut": {
            "description": "The output token CONTRACT ADDRESS. Defaults to the market's underlying asset.",
            "type": "string"
          }
        },
        "required": [
          "chain",
          "pt",
          "amountIn"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "pendle",
      "publicName": "pendle__py_redeem",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Redeem a Pendle PT and YT pair early",
      "toolId": "pendle.py.redeem"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 2296,
      "destructiveHint": true,
      "inputSchema": {
        "properties": {
          "chain": {
            "description": "Chain slug or id — one of Pendle's 11 chains (e.g. 'ethereum', 'arbitrum', 'base', 'bsc').",
            "type": "string"
          },
          "dryRun": {
            "description": "Preview the positions that would be claimed — including eligibleMarkets and skippedMarkets — without executing.",
            "type": "boolean"
          },
          "market": {
            "description": "Optional MARKET CONTRACT ADDRESS to scope the claim to one market — the way to reach a market the unscoped sweep reported in skippedMarkets (no cap applies to a scoped claim). Omit to sweep up to 10 held markets on the chain.",
            "type": "string"
          }
        },
        "required": [
          "chain"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "pendle",
      "publicName": "pendle__rewards_claim",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Claim accrued Pendle rewards",
      "toolId": "pendle.claim"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1003,
      "destructiveHint": true,
      "inputSchema": {
        "properties": {
          "amountIn": {
            "description": "Amount of the payment token in human-readable units.",
            "type": "string"
          },
          "chain": {
            "description": "Chain slug or id — one of Pendle's 11 chains (e.g. 'ethereum', 'arbitrum', 'base', 'bsc').",
            "type": "string"
          },
          "dryRun": {
            "description": "true = quote only. Prices the wrap, runs every fund-safety check including the price floor, records the authorization this tool needs, and broadcasts NOTHING. Required before the real call.",
            "type": "boolean"
          },
          "slippageBps": {
            "description": "Slippage tolerance in whole basis points (default 100 = 1%; maximum 1000 = 10%). A fractional, negative or larger value is REJECTED, never clamped. The dry run and the execute must pass the SAME value (or omit it on both).",
            "type": "number"
          },
          "sy": {
            "description": "The SY (Standardised Yield) CONTRACT ADDRESS. Read it from pendle__market_get as the market's `sy` field, or from the `deliveredAsset` a pendle__pt_redeem fallback reported.",
            "type": "string"
          },
          "tokenIn": {
            "description": "The payment token CONTRACT ADDRESS to wrap — the INPUT leg (ERC-20; pass the chain's wrapped-native token for native exposure).",
            "type": "string"
          }
        },
        "required": [
          "chain",
          "sy",
          "tokenIn",
          "amountIn"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "pendle",
      "publicName": "pendle__sy_mint",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Wrap a token into Pendle SY",
      "toolId": "pendle.sy.mint"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1063,
      "destructiveHint": true,
      "inputSchema": {
        "properties": {
          "amountIn": {
            "description": "Amount of SY to unwrap, in human-readable units.",
            "type": "string"
          },
          "chain": {
            "description": "Chain slug or id — one of Pendle's 11 chains (e.g. 'ethereum', 'arbitrum', 'base', 'bsc').",
            "type": "string"
          },
          "dryRun": {
            "description": "true = quote only. Prices the wrap, runs every fund-safety check including the price floor, records the authorization this tool needs, and broadcasts NOTHING. Required before the real call.",
            "type": "boolean"
          },
          "slippageBps": {
            "description": "Slippage tolerance in whole basis points (default 100 = 1%; maximum 1000 = 10%). A fractional, negative or larger value is REJECTED, never clamped. The dry run and the execute must pass the SAME value (or omit it on both).",
            "type": "number"
          },
          "sy": {
            "description": "The SY (Standardised Yield) CONTRACT ADDRESS. Read it from pendle__market_get as the market's `sy` field, or from the `deliveredAsset` a pendle__pt_redeem fallback reported.",
            "type": "string"
          },
          "tokenOut": {
            "description": "The token CONTRACT ADDRESS to receive — the OUTPUT leg (ERC-20; pass the chain's wrapped-native token for native exposure).",
            "type": "string"
          }
        },
        "required": [
          "chain",
          "sy",
          "tokenOut",
          "amountIn"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "pendle",
      "publicName": "pendle__sy_redeem",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Unwrap Pendle SY back to a token",
      "toolId": "pendle.sy.redeem"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1857,
      "destructiveHint": true,
      "inputSchema": {
        "properties": {
          "amountIn": {
            "description": "Amount of tokenIn in human-readable units.",
            "type": "string"
          },
          "chain": {
            "description": "Chain slug or id — one of Pendle's 11 chains (e.g. 'ethereum', 'arbitrum', 'base', 'bsc').",
            "type": "string"
          },
          "dryRun": {
            "description": "Preview without executing.",
            "type": "boolean"
          },
          "slippageBps": {
            "description": "Slippage tolerance in basis points (default 100 = 1%).",
            "type": "number"
          },
          "tokenIn": {
            "description": "Input token CONTRACT ADDRESS (ERC-20; use WETH for ETH). Buy: the payment token. Sell: the YT address.",
            "type": "string"
          },
          "tokenOut": {
            "description": "Output token CONTRACT ADDRESS. Buy: the YT address. Sell: the payment token.",
            "type": "string"
          }
        },
        "required": [
          "chain",
          "tokenIn",
          "tokenOut",
          "amountIn"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "pendle",
      "publicName": "pendle__yt_buy",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Buy a Pendle yield token",
      "toolId": "pendle.yt.buy"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1083,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "amountIn": {
            "description": "Amount of tokenIn in human-readable units.",
            "type": "string"
          },
          "chain": {
            "description": "Chain slug or id — one of Pendle's 11 chains (e.g. 'ethereum', 'arbitrum', 'base', 'bsc').",
            "type": "string"
          },
          "slippageBps": {
            "description": "Slippage tolerance in basis points (default 100 = 1%).",
            "type": "number"
          },
          "tokenIn": {
            "description": "Input token address (payment token for a buy; YT address for a sell).",
            "type": "string"
          },
          "tokenOut": {
            "description": "Output token address (YT for a buy; payment token for a sell).",
            "type": "string"
          }
        },
        "required": [
          "chain",
          "tokenIn",
          "tokenOut",
          "amountIn"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "pendle",
      "publicName": "pendle__yt_quote",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Quote a Pendle YT trade",
      "toolId": "pendle.yt.quote"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1784,
      "destructiveHint": true,
      "inputSchema": {
        "properties": {
          "amountIn": {
            "description": "Amount of tokenIn in human-readable units.",
            "type": "string"
          },
          "chain": {
            "description": "Chain slug or id — one of Pendle's 11 chains (e.g. 'ethereum', 'arbitrum', 'base', 'bsc').",
            "type": "string"
          },
          "dryRun": {
            "description": "Preview without executing.",
            "type": "boolean"
          },
          "slippageBps": {
            "description": "Slippage tolerance in basis points (default 100 = 1%).",
            "type": "number"
          },
          "tokenIn": {
            "description": "Input token CONTRACT ADDRESS (ERC-20; use WETH for ETH). Buy: the payment token. Sell: the YT address.",
            "type": "string"
          },
          "tokenOut": {
            "description": "Output token CONTRACT ADDRESS. Buy: the YT address. Sell: the payment token.",
            "type": "string"
          }
        },
        "required": [
          "chain",
          "tokenIn",
          "tokenOut",
          "amountIn"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "pendle",
      "publicName": "pendle__yt_sell",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Sell a Pendle yield token",
      "toolId": "pendle.yt.sell"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1619,
      "destructiveHint": true,
      "inputSchema": {
        "properties": {
          "dryRun": {
            "description": "true simulates the claim and reports what it would pay without signing anything. Omit it to actually claim, which spends gas and is irreversible.",
            "type": "boolean"
          },
          "token": {
            "description": "The pools.fun token's contract ADDRESS. Symbols are not unique on this launchpad and copycats are routinely live, so the address is the only identity that means one token.",
            "type": "string"
          }
        },
        "required": [
          "token"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "pools",
      "publicName": "pools__fees_claim",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Claim pools.fun creator fees",
      "toolId": "pools.claim_fees"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 2801,
      "destructiveHint": true,
      "inputSchema": {
        "properties": {
          "imageId": {
            "description": "REQUIRED on this tool: the identifier of a picture the user already staged in the app's image locker, listed by trench__images_list (the locker is shared by both launchpads). The agent can never create one, only name one the locker already holds; without it this tool refuses and nothing is launched. A token launched with no image renders blank on pools.fun forever and that cannot be undone.",
            "type": "string"
          },
          "name": {
            "description": "The token's full display name, 1 to 64 characters, exactly as it should appear on the launchpad and in the pinned metadata.",
            "type": "string"
          },
          "pairedAsset": {
            "description": "Which asset the new pool trades against: weth (default) or usdg. These are the only two the factory allows today - tokenised stocks exist in the provider's vocabulary but the on-chain allowlist refuses them, so asking for one would deploy nothing.",
            "enum": [
              "weth",
              "usdg"
            ],
            "type": "string"
          },
          "prebuy": {
            "description": "Optional same-transaction first buy, in HUMAN decimal ETH (for example \"0.01\") - not wei. Autonomous launches support an ETH prebuy only; a USDG prebuy needs an approval leg and is available through the desktop form instead.",
            "type": "string"
          },
          "symbol": {
            "description": "The ticker, 1 to 16 characters. Symbols are NOT unique on pools.fun and copycats are routinely live, so this is a label rather than an identity.",
            "type": "string"
          }
        },
        "required": [
          "name",
          "symbol",
          "imageId"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "pools",
      "publicName": "pools__launch_execute",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Launch a token on pools.fun",
      "toolId": "pools.launch_execute"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 884,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "imageId": {
            "description": "Identifier of a picture the user already staged in the app's image locker, listed by trench__images_list (the locker is shared by both launchpads). The agent can never create one, only name one the locker already holds. Optional here; pools__launch_execute REQUIRES it.",
            "type": "string"
          },
          "name": {
            "description": "The token's full display name, 1 to 64 characters, exactly as it should appear on the launchpad and in the pinned metadata.",
            "type": "string"
          },
          "pairedAsset": {
            "description": "Which asset the new pool trades against: weth (default) or usdg. These are the only two the factory allows today - tokenised stocks exist in the provider's vocabulary but the on-chain allowlist refuses them, so asking for one would deploy nothing.",
            "enum": [
              "weth",
              "usdg"
            ],
            "type": "string"
          },
          "prebuy": {
            "description": "Optional same-transaction first buy, in HUMAN decimal ETH (for example \"0.01\") - not wei. Autonomous launches support an ETH prebuy only; a USDG prebuy needs an approval leg and is available through the desktop form instead.",
            "type": "string"
          },
          "symbol": {
            "description": "The ticker, 1 to 16 characters. Symbols are NOT unique on pools.fun and copycats are routinely live, so this is a label rather than an identity.",
            "type": "string"
          }
        },
        "required": [
          "name",
          "symbol"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "pools",
      "publicName": "pools__launch_preview",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Price a pools.fun launch",
      "toolId": "pools.launch_preview"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 688,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "imageId": {
            "description": "Identifier of a picture the user already staged in the app's image locker, listed by trench__images_list (the locker is shared by both launchpads). The agent can never create one, only name one the locker already holds. Optional here; pools__launch_execute REQUIRES it.",
            "type": "string"
          },
          "name": {
            "description": "The token's full display name, 1 to 64 characters, exactly as it should appear on the launchpad and in the pinned metadata.",
            "type": "string"
          },
          "pairedAsset": {
            "description": "Which asset the new pool trades against: weth (default) or usdg. These are the only two the factory allows today - tokenised stocks exist in the provider's vocabulary but the on-chain allowlist refuses them, so asking for one would deploy nothing.",
            "enum": [
              "weth",
              "usdg"
            ],
            "type": "string"
          },
          "prebuy": {
            "description": "Optional same-transaction first buy, in HUMAN decimal ETH (for example \"0.01\") - not wei. Autonomous launches support an ETH prebuy only; a USDG prebuy needs an approval leg and is available through the desktop form instead.",
            "type": "string"
          },
          "symbol": {
            "description": "The ticker, 1 to 16 characters. Symbols are NOT unique on pools.fun and copycats are routinely live, so this is a label rather than an identity.",
            "type": "string"
          }
        },
        "required": [
          "name",
          "symbol"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "pools",
      "publicName": "pools__launch_request_form",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Ask the user to confirm a pools.fun launch",
      "toolId": "pools.launch_request_form"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1371,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "cursor": {
            "description": "Opaque pagination token: pass back the nextCursor from a previous reply to fetch the following page. Keep the other arguments identical between pages, or the cursor walks a different result set.",
            "type": "string"
          },
          "includeClaimable": {
            "description": "true also SIMULATES a fee claim for the first few rows and reports what it would pay, in two separate assets with their decimals. Costs a couple of chain reads per row, so it is off by default; a row that was not simulated reports claimable: null, which means NOT MEASURED rather than nothing to claim.",
            "type": "boolean"
          },
          "limit": {
            "description": "Maximum launches returned, 1-100. Defaults to 25, newest first.",
            "type": "number"
          },
          "platform": {
            "description": "Which launcher to look under: all (default), poolsfun, or sushi. The default covers both so a launch is not missed because it went through the other launcher.",
            "enum": [
              "poolsfun",
              "sushi",
              "all"
            ],
            "type": "string"
          }
        },
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "pools",
      "publicName": "pools__my_launches_list",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "List this wallet's pools.fun launches",
      "toolId": "pools.my_launches"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1006,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "aggregate": {
            "description": "How many timeframe units make one candle, 1-24 (default 1). Combine with timeframe: minute plus 15 is a quarter-hour candle, hour plus 4 is a four-hour candle.",
            "type": "number"
          },
          "limit": {
            "description": "How many candles to return, 1-1000 (default 30). Ask for the window the analysis needs rather than the maximum.",
            "type": "number"
          },
          "timeframe": {
            "description": "Base unit of one candle: minute, hour (default) or day. The real candle span is this multiplied by aggregate.",
            "enum": [
              "minute",
              "hour",
              "day"
            ],
            "type": "string"
          },
          "tokenAddress": {
            "description": "Contract address of the pools.fun token whose price history to read. Resolve it with pools__tokens_search first - a symbol is not an identity on this launchpad.",
            "type": "string"
          }
        },
        "required": [
          "tokenAddress"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "pools",
      "publicName": "pools__token_candles_list",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Read pools.fun token candles",
      "toolId": "pools.candles"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1075,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "tokenAddress": {
            "description": "Contract address of the token to inspect. Resolve a name to an address with pools__tokens_search first, since symbols repeat across this launchpad.",
            "type": "string"
          }
        },
        "required": [
          "tokenAddress"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "pools",
      "publicName": "pools__token_get",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Read one pools.fun token",
      "toolId": "pools.token"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1549,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "cursor": {
            "description": "Opaque pagination token: pass back the nextCursor from a previous reply to fetch the following page. Keep every other filter identical between pages or the cursor walks a different result set.",
            "type": "string"
          },
          "deployerAddress": {
            "description": "Keep only tokens deployed by this wallet address, matched server-side. Use it to see everything one launcher has created.",
            "type": "string"
          },
          "feeRecipientAddress": {
            "description": "Keep only tokens whose creator fee stream is directed at this wallet address, matched server-side. On pools.fun the fee recipient can differ from the deployer, so this answers who EARNS from a token rather than who deployed it.",
            "type": "string"
          },
          "limit": {
            "description": "Maximum rows returned, 1-100 (the provider's own cap). Omitting it leaves the page size to the provider. Use the returned cursor to walk further rather than asking for more than the cap.",
            "type": "number"
          },
          "live": {
            "description": "When true, restrict the list to the provider's live feed of recently active tokens instead of the whole market. Default false, which lists everything matching the other filters.",
            "type": "boolean"
          },
          "maxAgeHours": {
            "description": "Keep only tokens launched within this many hours. This is the fresh-launch filter on pools.fun: there is no bonding curve or graduation stage to filter on, so age is what separates a brand new token from an established one.",
            "type": "number"
          },
          "maxMarketCapUsd": {
            "description": "Keep only tokens whose market capitalisation in US dollars is at most this. Pair with minMarketCapUsd to bracket a size band; the figure is display-grade.",
            "type": "number"
          },
          "minMarketCapUsd": {
            "description": "Keep only tokens whose market capitalisation in US dollars is at least this. The figure is the provider's own display-grade estimate, not an executable valuation.",
            "type": "number"
          },
          "minTxCount24h": {
            "description": "Keep only tokens with at least this many trades in the last 24 hours. A whole number; useful for separating tokens with real flow from ones whose market cap moved on a single fill.",
            "type": "number"
          },
          "minVolUsd": {
            "description": "Keep only tokens whose traded volume in US dollars over volTimeframe is at least this. It REQUIRES volTimeframe - a volume floor without a window has no meaning and is rejected by name rather than silently applied to some default window.",
            "type": "number"
          },
          "order": {
            "description": "Ranking direction: desc (default, biggest or newest first) or asc. Pair it with sortBy - asc on deployedAt walks the oldest launches, desc gives the freshest.",
            "enum": [
              "desc",
              "asc"
            ],
            "type": "string"
          },
          "platform": {
            "description": "Which launcher to list, poolsfun (default, the pools.fun PartyFactory), sushi (the older SushiLaunchpad on the same chain), or all. These are two different launchers whose tokens sit side by side on Robinhood Chain; the row echoes which one produced it.",
            "enum": [
              "poolsfun",
              "sushi",
              "all"
            ],
            "type": "string"
          },
          "query": {
            "description": "Free-text match against token names and symbols, applied by the launchpad alongside every other filter here. Reach for it when a name has to be combined with filters - a name plus an age or volume floor, say. When the name is all you have, the dedicated pools name-lookup tool is the shorter path.",
            "type": "string"
          },
          "sortBy": {
            "description": "Server-side ranking key. Accepts marketCapUsd (default), vol1m, vol5m, vol1h, vol6h, vol24h, txCount24h, priceChange1m, priceChange5m, priceChange1h, priceChange6h, priceChange24h, lastTradeAt, deployedAt. Sort by deployedAt for the newest launches and by vol1h or txCount24h for what is actually being traded right now.",
            "enum": [
              "marketCapUsd",
              "vol1m",
              "vol5m",
              "vol1h",
              "vol6h",
              "vol24h",
              "txCount24h",
              "priceChange1m",
              "priceChange5m",
              "priceChange1h",
              "priceChange6h",
              "priceChange24h",
              "lastTradeAt",
              "deployedAt"
            ],
            "type": "string"
          },
          "volTimeframe": {
            "description": "The window the minVolUsd floor is measured over: 1m, 5m, 1h, 6h or 24h. Supply it together with minVolUsd; on its own it filters nothing and is rejected.",
            "enum": [
              "1m",
              "5m",
              "1h",
              "6h",
              "24h"
            ],
            "type": "string"
          }
        },
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "pools",
      "publicName": "pools__tokens_discover",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Screen pools.fun tokens",
      "toolId": "pools.tokens"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 928,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "cursor": {
            "description": "Opaque pagination token: pass back the nextCursor from a previous reply to fetch the following page. Keep the other arguments identical between pages, or the cursor walks a different result set.",
            "type": "string"
          },
          "limit": {
            "description": "Maximum matches returned, 1-100. Defaults to 10, which is enough to expose copycat symbols sharing the name.",
            "type": "number"
          },
          "platform": {
            "description": "Which launcher to search: all (default here, so a name lookup cannot miss the other launcher), poolsfun, or sushi. Narrow it only when the user has already said which launcher they mean.",
            "enum": [
              "poolsfun",
              "sushi",
              "all"
            ],
            "type": "string"
          },
          "query": {
            "description": "The name or symbol fragment to look for, 1 to 64 characters. Matching is done by the launchpad across both token names and symbols.",
            "type": "string"
          }
        },
        "required": [
          "query"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "pools",
      "publicName": "pools__tokens_search",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Search pools.fun tokens",
      "toolId": "pools.search"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1782,
      "destructiveHint": true,
      "inputSchema": {
        "properties": {
          "amountRaw": {
            "description": "Amount to bridge, in raw atomic units of fromToken (e.g. USDC has 6 decimals, so 1 USDC = \"1000000\"). Raw base units as an integer string (not human decimals) — read the token's decimals from `TokenFind` first.",
            "type": "string"
          },
          "dryRun": {
            "description": "If true, fetch the route without signing.",
            "type": "boolean"
          },
          "fromChain": {
            "description": "Source chain. Chain slug/alias, or the numeric chain id `TokenFind` returns (e.g. `base` or `8453`). Robinhood Chain is `robinhood` / `4663`.",
            "type": "string"
          },
          "fromToken": {
            "description": "Source token address, or native ETH/native.",
            "type": "string"
          },
          "recipient": {
            "description": "Destination recipient (default: your wallet).",
            "type": "string"
          },
          "slippageBps": {
            "description": "Basis points (1 bps = 0.01%). Default 100 = 1%. Vex caps this at 1000 (10%) and REJECTS a higher value rather than clamping it.",
            "type": "number"
          },
          "toChain": {
            "description": "Destination chain. Chain slug/alias, or the numeric chain id `TokenFind` returns (e.g. `base` or `8453`). Robinhood Chain is `robinhood` / `4663`.",
            "type": "string"
          },
          "toToken": {
            "description": "Destination token address, or native ETH/native.",
            "type": "string"
          }
        },
        "required": [
          "fromChain",
          "fromToken",
          "toChain",
          "toToken",
          "amountRaw"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "relay",
      "publicName": "relay__bridge_execute",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Execute a Relay cross-chain bridge",
      "toolId": "relay.bridge"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1477,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "amountRaw": {
            "description": "Amount to bridge, in raw atomic units of fromToken (e.g. USDC has 6 decimals, so 1 USDC = \"1000000\"). Raw base units as an integer string (not human decimals) — read the token's decimals from `TokenFind` first.",
            "type": "string"
          },
          "fromChain": {
            "description": "Source chain. Chain slug/alias, or the numeric chain id `TokenFind` returns (e.g. `base` or `8453`). Robinhood Chain is `robinhood` / `4663`.",
            "type": "string"
          },
          "fromToken": {
            "description": "Source token address, or native ETH/native.",
            "type": "string"
          },
          "recipient": {
            "description": "Destination recipient (default: your wallet).",
            "type": "string"
          },
          "slippageBps": {
            "description": "Basis points (1 bps = 0.01%). Default 100 = 1%. Vex caps this at 1000 (10%) and REJECTS a higher value rather than clamping it.",
            "type": "number"
          },
          "toChain": {
            "description": "Destination chain. Chain slug/alias, or the numeric chain id `TokenFind` returns (e.g. `base` or `8453`). Robinhood Chain is `robinhood` / `4663`.",
            "type": "string"
          },
          "toToken": {
            "description": "Destination token address, or native ETH/native.",
            "type": "string"
          }
        },
        "required": [
          "fromChain",
          "fromToken",
          "toChain",
          "toToken",
          "amountRaw"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "relay",
      "publicName": "relay__bridge_quote_get",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Quote a Relay cross-chain bridge",
      "toolId": "relay.quote.get"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1980,
      "destructiveHint": true,
      "inputSchema": {
        "properties": {
          "borrowAmountRaw": {
            "description": "Borrow this many raw atomic units of the vault's debt token (scale = the vault's borrowTokenDecimals). Mutually exclusive with repayAmountRaw/repayAll.",
            "type": "string"
          },
          "depositAmountRaw": {
            "description": "Add this many raw atomic units of the vault's collateral token (scale = the vault's supplyTokenDecimals). Mutually exclusive with withdrawAmountRaw/withdrawAll.",
            "type": "string"
          },
          "market": {
            "description": "Which Borrow market: \"main\" or \"ethena\" (default \"main\"). Must match the vault's own market.",
            "type": "string"
          },
          "positionId": {
            "description": "Existing position id to adjust - from solana__lend_borrow_positions_list. Omit (or 0) to create a NEW position.",
            "type": "number"
          },
          "repayAll": {
            "description": "Repay ALL debt on this position, including accrued-interest dust. Mutually exclusive with borrowAmountRaw/repayAmountRaw.",
            "type": "boolean"
          },
          "repayAmountRaw": {
            "description": "Repay this many raw atomic units of debt (scale = the vault's borrowTokenDecimals). Mutually exclusive with borrowAmountRaw/repayAll.",
            "type": "string"
          },
          "vaultId": {
            "description": "Vault to operate on - from solana__lend_borrow_vaults_list, which also gives the two legs' decimals every amount param below is expressed in.",
            "type": "number"
          },
          "withdrawAll": {
            "description": "Withdraw ALL collateral from this position. Mutually exclusive with depositAmountRaw/withdrawAmountRaw.",
            "type": "boolean"
          },
          "withdrawAmountRaw": {
            "description": "Remove this many raw atomic units of collateral (scale = the vault's supplyTokenDecimals). Mutually exclusive with depositAmountRaw/withdrawAll.",
            "type": "string"
          }
        },
        "required": [
          "vaultId"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "solana",
      "publicName": "solana__lend_borrow_operate",
      "readOnlyHint": false,
      "requiresEnv": "JUPITER_API_KEY",
      "title": "Adjust a Jupiter Lend borrow position",
      "toolId": "solana.lend.borrowOperate"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1107,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "market": {
            "description": "Which Borrow market: \"main\" or \"ethena\" (default \"main\"). An unrecognized value is rejected.",
            "type": "string"
          },
          "vaultIds": {
            "description": "Comma-separated vault-id allow-list to filter positions to. Omit for all positions.",
            "type": "string"
          },
          "walletAddress": {
            "description": "The wallet account to read, as a Solana address. Defaults to the session's selected Solana wallet.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "solana",
      "publicName": "solana__lend_borrow_positions_list",
      "readOnlyHint": true,
      "requiresEnv": "JUPITER_API_KEY",
      "title": "List Jupiter Lend borrow positions",
      "toolId": "solana.lend.borrowPositions"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1325,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "market": {
            "description": "Which Borrow market: \"main\" or \"ethena\" (default \"main\"). Vault ids are scoped per market — the same vaultId in a different market is a different vault. An unrecognized value is rejected.",
            "type": "string"
          },
          "vaultIds": {
            "description": "Comma-separated vault-id allow-list (matches the provider's own numeric vault id). Omit for all vaults.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "solana",
      "publicName": "solana__lend_borrow_vaults_list",
      "readOnlyHint": true,
      "requiresEnv": "JUPITER_API_KEY",
      "title": "Screen Jupiter Lend borrow vaults",
      "toolId": "solana.lend.borrowVaults"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 955,
      "destructiveHint": true,
      "inputSchema": {
        "properties": {
          "amountRaw": {
            "description": "Amount to deposit, in RAW atomic units of the asset as an integer string, never human decimals (e.g. USDC has 6 decimals, so 1 USDC = \"1000000\") - read the asset's decimals from solana__lend_earn_rates_list's assetDecimals, or from TokenFind, before building it.",
            "type": "string"
          },
          "asset": {
            "description": "Token address to deposit.",
            "type": "string"
          }
        },
        "required": [
          "asset",
          "amountRaw"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "solana",
      "publicName": "solana__lend_earn_deposit",
      "readOnlyHint": false,
      "requiresEnv": "JUPITER_API_KEY",
      "title": "Deposit into a Jupiter Lend earn vault",
      "toolId": "solana.lend.deposit"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 702,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "walletAddress": {
            "description": "The wallet ACCOUNT to read, as a Solana address (not a token mint or a position key). Defaults to the session's selected Solana wallet — omit it to read your own. Under session scope a DIFFERENT address is rejected.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "solana",
      "publicName": "solana__lend_earn_positions_list",
      "readOnlyHint": true,
      "requiresEnv": "JUPITER_API_KEY",
      "title": "List Jupiter Lend earn positions",
      "toolId": "solana.lend.positions"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 952,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "assets": {
            "description": "Comma-separated list of asset identifiers to filter to — matches the underlying asset mint address, Earn share-token (jlToken) mint, symbol, or provider lending id. Case-insensitive for symbol. Omit for all markets.",
            "type": "string"
          },
          "minSupplyRate": {
            "description": "Only return markets whose base supply APY percent is at or above this value, e.g. 3 for 3%.",
            "type": "number"
          },
          "minTotalRate": {
            "description": "Only return markets whose combined (supply + rewards) APY percent is at or above this value, e.g. 5 for 5%.",
            "type": "number"
          }
        },
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "solana",
      "publicName": "solana__lend_earn_rates_list",
      "readOnlyHint": true,
      "requiresEnv": "JUPITER_API_KEY",
      "title": "Screen Jupiter Lend earn markets",
      "toolId": "solana.lend.rates"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1336,
      "destructiveHint": true,
      "inputSchema": {
        "properties": {
          "amountRaw": {
            "description": "Amount to withdraw, in RAW atomic units of the asset as an integer string, never human decimals (e.g. USDC has 6 decimals, so 1 USDC = \"1000000\") - see solana__lend_earn_positions_list for your current balance and the asset's decimals. Mutually exclusive with withdrawAll - provide exactly one, never both and never neither.",
            "type": "string"
          },
          "asset": {
            "description": "Token address to withdraw.",
            "type": "string"
          },
          "withdrawAll": {
            "description": "Exit the ENTIRE Earn position for this asset, dust-free (redeems the position's full share balance rather than an underlying amount). Mutually exclusive with amountRaw — provide exactly one, never both. Never pass withdrawAll: false; omit it and pass amountRaw instead.",
            "type": "boolean"
          }
        },
        "required": [
          "asset"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "solana",
      "publicName": "solana__lend_earn_withdraw",
      "readOnlyHint": false,
      "requiresEnv": "JUPITER_API_KEY",
      "title": "Withdraw from a Jupiter Lend earn vault",
      "toolId": "solana.lend.withdraw"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 976,
      "destructiveHint": true,
      "inputSchema": {
        "properties": {
          "amountUsdc": {
            "description": "Amount to spend, in whole/fractional USDC dollars (human-readable, e.g. 10 for $10) — not raw atomic units.",
            "type": "number"
          },
          "marketId": {
            "description": "Market ID (from solana__predict_events_discover or solana__predict_events_search).",
            "type": "string"
          },
          "side": {
            "description": "Side: yes or no.",
            "type": "string"
          }
        },
        "required": [
          "marketId",
          "side",
          "amountUsdc"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "solana",
      "publicName": "solana__predict_buy",
      "readOnlyHint": false,
      "requiresEnv": "JUPITER_API_KEY",
      "title": "Buy Jupiter prediction shares",
      "toolId": "solana.predict.buy"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 936,
      "destructiveHint": true,
      "inputSchema": {
        "properties": {
          "positionPubkey": {
            "description": "Position public key (from solana__predict_positions_list).",
            "type": "string"
          }
        },
        "required": [
          "positionPubkey"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "solana",
      "publicName": "solana__predict_claim",
      "readOnlyHint": false,
      "requiresEnv": "JUPITER_API_KEY",
      "title": "Claim a resolved Jupiter prediction payout",
      "toolId": "solana.predict.claim"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 870,
      "destructiveHint": true,
      "inputSchema": {
        "properties": {
          "minSellPriceSlippageBps": {
            "description": "Slippage tolerance applied to EACH sell in this batch, in basis points (0-1000; e.g. 100 = 1%). Vex caps slippage at 1000 bps (10%) and rejects anything above rather than clamping. Required by the provider — there is no default, so pick a value you accept across every position closed.",
            "type": "number"
          }
        },
        "required": [
          "minSellPriceSlippageBps"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "solana",
      "publicName": "solana__predict_close_all",
      "readOnlyHint": false,
      "requiresEnv": "JUPITER_API_KEY",
      "title": "Close every Jupiter prediction position",
      "toolId": "solana.predict.closeAll"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 684,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "eventId": {
            "description": "Event ID (from solana__predict_events_discover or solana__predict_events_search).",
            "type": "string"
          },
          "includeMarkets": {
            "description": "Include the event's nested markets array (default false — lean).",
            "type": "boolean"
          }
        },
        "required": [
          "eventId"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "solana",
      "publicName": "solana__predict_event_get",
      "readOnlyHint": true,
      "requiresEnv": "JUPITER_API_KEY",
      "title": "Read one Jupiter prediction event",
      "toolId": "solana.predict.event"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1104,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "category": {
            "description": "Category filter: all, crypto, sports, politics, esports, culture, economics, or tech. Omit for all categories.",
            "type": "string"
          },
          "filter": {
            "description": "Status filter: trending, live, new, or upcoming (not yet open for trading).",
            "type": "string"
          },
          "includeMarkets": {
            "description": "Include each event's nested markets array (default false - lean; use solana__predict_event_get for one event's markets).",
            "type": "boolean"
          },
          "limit": {
            "description": "Max results, 1-100 (default 20).",
            "type": "number"
          },
          "offset": {
            "description": "Skip first N results for pagination.",
            "type": "number"
          },
          "provider": {
            "description": "Provider: kalshi, polymarket, or bisonfi (Jupiter Forecast). Default polymarket.",
            "type": "string"
          },
          "sortBy": {
            "description": "Sort by: volume or beginAt.",
            "type": "string"
          },
          "sortDirection": {
            "description": "Sort direction: asc or desc.",
            "type": "string"
          },
          "subcategory": {
            "description": "Comma-separated subcategory filter.",
            "type": "string"
          },
          "tags": {
            "description": "Free-text tag filter, e.g. soccer.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "solana",
      "publicName": "solana__predict_events_discover",
      "readOnlyHint": true,
      "requiresEnv": "JUPITER_API_KEY",
      "title": "Browse Jupiter prediction events",
      "toolId": "solana.predict.events"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1152,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "includeMarkets": {
            "description": "Include each event's nested markets array (default false - lean; use solana__predict_event_get for one event's markets).",
            "type": "boolean"
          },
          "limit": {
            "description": "Max results, 1-20 (default 20). The provider ignores this param upstream — Vex enforces the requested count locally.",
            "type": "number"
          },
          "provider": {
            "description": "Provider: kalshi, polymarket, or bisonfi (Jupiter Forecast). Default polymarket.",
            "type": "string"
          },
          "query": {
            "description": "Search keyword or phrase, e.g. \"bitcoin\" or \"super bowl\".",
            "type": "string"
          }
        },
        "required": [
          "query"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "solana",
      "publicName": "solana__predict_events_search",
      "readOnlyHint": true,
      "requiresEnv": "JUPITER_API_KEY",
      "title": "Search Jupiter prediction events",
      "toolId": "solana.predict.search"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 885,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "limit": {
            "description": "Max results, 1-100.",
            "type": "number"
          },
          "metric": {
            "description": "Ranking metric: pnl, volume, or win_rate.",
            "type": "string"
          },
          "period": {
            "description": "Period: all_time, weekly, or monthly.",
            "type": "string"
          }
        },
        "required": [
          "period",
          "metric",
          "limit"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "solana",
      "publicName": "solana__predict_leaderboard_list",
      "readOnlyHint": true,
      "requiresEnv": "JUPITER_API_KEY",
      "title": "Rank Jupiter prediction traders",
      "toolId": "solana.predict.leaderboards"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 685,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "marketId": {
            "description": "Market ID (from solana__predict_events_discover, solana__predict_events_search, or an event's nested markets).",
            "type": "string"
          }
        },
        "required": [
          "marketId"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "solana",
      "publicName": "solana__predict_market_get",
      "readOnlyHint": true,
      "requiresEnv": "JUPITER_API_KEY",
      "title": "Read one Jupiter prediction market",
      "toolId": "solana.predict.market"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 627,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "orderPubkey": {
            "description": "Order public key (from solana__predict_orders_list).",
            "type": "string"
          }
        },
        "required": [
          "orderPubkey"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "solana",
      "publicName": "solana__predict_order_get",
      "readOnlyHint": true,
      "requiresEnv": "JUPITER_API_KEY",
      "title": "Read one live Jupiter prediction order",
      "toolId": "solana.predict.order"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 505,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "orderPubkey": {
            "description": "Order public key (from solana__predict_orders_list).",
            "type": "string"
          }
        },
        "required": [
          "orderPubkey"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "solana",
      "publicName": "solana__predict_order_status_get",
      "readOnlyHint": true,
      "requiresEnv": "JUPITER_API_KEY",
      "title": "Read a Jupiter prediction order's fill status",
      "toolId": "solana.predict.orderStatus"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 708,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "marketId": {
            "description": "Market ID.",
            "type": "string"
          }
        },
        "required": [
          "marketId"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "solana",
      "publicName": "solana__predict_orderbook_get",
      "readOnlyHint": true,
      "requiresEnv": "JUPITER_API_KEY",
      "title": "Read Jupiter prediction order-book depth",
      "toolId": "solana.predict.orderbook"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1041,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "limit": {
            "description": "Max results, 1-100 (default 20).",
            "type": "number"
          },
          "offset": {
            "description": "Skip first N results for pagination.",
            "type": "number"
          },
          "walletAddress": {
            "description": "The wallet ACCOUNT to read, as a Solana address (not a token mint or a position key). Defaults to the session's selected Solana wallet — omit it to read your own. Under session scope a DIFFERENT address is rejected.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "solana",
      "publicName": "solana__predict_orders_list",
      "readOnlyHint": true,
      "requiresEnv": "JUPITER_API_KEY",
      "title": "List Jupiter prediction orders",
      "toolId": "solana.predict.orders"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 883,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "count": {
            "description": "Number of data points to return, 1-100 (Vex owner-wide cap; provider default 10 when omitted).",
            "type": "number"
          },
          "interval": {
            "description": "Interval: 24h, 1w, or 1m.",
            "type": "string"
          },
          "walletAddress": {
            "description": "The wallet ACCOUNT to read, as a Solana address (not a token mint or a position key). Defaults to the session's selected Solana wallet — omit it to read your own. Under session scope a DIFFERENT address is rejected.",
            "type": "string"
          }
        },
        "required": [
          "interval"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "solana",
      "publicName": "solana__predict_pnl_history_get",
      "readOnlyHint": true,
      "requiresEnv": "JUPITER_API_KEY",
      "title": "Read Jupiter prediction realized-PnL history",
      "toolId": "solana.predict.pnlHistory"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 625,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "positionPubkey": {
            "description": "Position public key (from solana__predict_positions_list).",
            "type": "string"
          }
        },
        "required": [
          "positionPubkey"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "solana",
      "publicName": "solana__predict_position_get",
      "readOnlyHint": true,
      "requiresEnv": "JUPITER_API_KEY",
      "title": "Read one Jupiter prediction position",
      "toolId": "solana.predict.position"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1018,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "isYes": {
            "description": "Filter by side — true for YES, false for NO.",
            "type": "boolean"
          },
          "limit": {
            "description": "Max results, 1-100 (default 20).",
            "type": "number"
          },
          "marketId": {
            "description": "Filter by external market ID.",
            "type": "string"
          },
          "marketPubkey": {
            "description": "Filter by on-chain market account.",
            "type": "string"
          },
          "offset": {
            "description": "Skip first N results for pagination.",
            "type": "number"
          },
          "walletAddress": {
            "description": "The wallet ACCOUNT to read, as a Solana address (not a token mint or a position key). Defaults to the session's selected Solana wallet — omit it to read your own. Under session scope a DIFFERENT address is rejected.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "solana",
      "publicName": "solana__predict_positions_list",
      "readOnlyHint": true,
      "requiresEnv": "JUPITER_API_KEY",
      "title": "List open Jupiter prediction positions",
      "toolId": "solana.predict.positions"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 708,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "walletAddress": {
            "description": "The wallet ACCOUNT to read, as a Solana address (not a token mint or a position key). Defaults to the session's selected Solana wallet — omit it to read your own. Under session scope a DIFFERENT address is rejected.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "solana",
      "publicName": "solana__predict_profile_get",
      "readOnlyHint": true,
      "requiresEnv": "JUPITER_API_KEY",
      "title": "Read a Jupiter prediction trader profile",
      "toolId": "solana.predict.profile"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1112,
      "destructiveHint": true,
      "inputSchema": {
        "properties": {
          "positionPubkey": {
            "description": "Position public key (from solana__predict_positions_list).",
            "type": "string"
          }
        },
        "required": [
          "positionPubkey"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "solana",
      "publicName": "solana__predict_sell",
      "readOnlyHint": false,
      "requiresEnv": "JUPITER_API_KEY",
      "title": "Sell a Jupiter prediction position",
      "toolId": "solana.predict.sell"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 723,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "includeMarkets": {
            "description": "Include each recommended event's nested markets array (default false — lean).",
            "type": "boolean"
          },
          "provider": {
            "description": "Provider: kalshi, polymarket, or bisonfi (Jupiter Forecast). Default polymarket.",
            "type": "string"
          },
          "walletAddress": {
            "description": "The wallet ACCOUNT to base recommendations on, as a Solana address — any wallet, not only your own.",
            "type": "string"
          }
        },
        "required": [
          "walletAddress"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "solana",
      "publicName": "solana__predict_suggested_events_list",
      "readOnlyHint": true,
      "requiresEnv": "JUPITER_API_KEY",
      "title": "Suggest Jupiter prediction events for a wallet",
      "toolId": "solana.predict.suggestedEvents"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1009,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "id": {
            "description": "Filter to a single history event ID.",
            "type": "number"
          },
          "limit": {
            "description": "Max results, 1-100 (default 20).",
            "type": "number"
          },
          "offset": {
            "description": "Skip first N results for pagination.",
            "type": "number"
          },
          "positionPubkey": {
            "description": "Filter to one position's history.",
            "type": "string"
          },
          "walletAddress": {
            "description": "The wallet ACCOUNT to read, as a Solana address (not a token mint or a position key). Defaults to the session's selected Solana wallet — omit it to read your own. Under session scope a DIFFERENT address is rejected.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "solana",
      "publicName": "solana__predict_trade_history_list",
      "readOnlyHint": true,
      "requiresEnv": "JUPITER_API_KEY",
      "title": "Read Jupiter prediction trade history",
      "toolId": "solana.predict.history"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 906,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "limit": {
            "description": "Max results, 1-100. Required — this feed has no upstream owner/market scope, so an explicit window is required to avoid an unbounded response.",
            "type": "number"
          },
          "offset": {
            "description": "Skip first N results for pagination.",
            "type": "number"
          }
        },
        "required": [
          "limit"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "solana",
      "publicName": "solana__predict_trades_list",
      "readOnlyHint": true,
      "requiresEnv": "JUPITER_API_KEY",
      "title": "Browse the global Jupiter prediction trade feed",
      "toolId": "solana.predict.trades"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 604,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {},
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "solana",
      "publicName": "solana__predict_trading_status_get",
      "readOnlyHint": true,
      "requiresEnv": "JUPITER_API_KEY",
      "title": "Check Jupiter prediction trading status",
      "toolId": "solana.predict.tradingStatus"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 578,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {},
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "solana",
      "publicName": "solana__predict_vault_get",
      "readOnlyHint": true,
      "requiresEnv": "JUPITER_API_KEY",
      "title": "Read the Jupiter prediction protocol vault",
      "toolId": "solana.predict.vaultInfo"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 526,
      "destructiveHint": true,
      "inputSchema": {
        "properties": {
          "amountIn": {
            "description": "Amount to swap. In HUMAN decimal units (e.g. \"1.5\") — not wei, lamports, or any other base unit. Converted to base units exactly, with no floating-point step: a value carrying more precision than the input side holds is REJECTED, never silently rounded.",
            "type": "string"
          },
          "computeUnitPricePercentile": {
            "description": "Priority-fee strategy: medium (25th percentile), high (default, 50th), or veryHigh (75th). This is the LEVER for priority-fee cost — Jupiter picks the actual price per compute unit at the percentile you name, and Vex refuses to sign a build whose resulting priority fee exceeds 10,000,000 lamports (0.01 SOL). If a swap is refused for that reason, lower this and re-quote. Priority prices are congestion-driven and swing by an order of magnitude within minutes, so a fresh quote at the SAME setting often fits too — the refusal is not a property of the pair.",
            "type": "string"
          },
          "dexes": {
            "description": "Comma-separated DEX allow-list. Mutually exclusive with excludeDexes.",
            "type": "string"
          },
          "excludeDexes": {
            "description": "Comma-separated DEX deny-list. Mutually exclusive with dexes.",
            "type": "string"
          },
          "forJitoBundle": {
            "description": "Exclude DEXes incompatible with Jito bundles (default false).",
            "type": "boolean"
          },
          "maxAccounts": {
            "description": "Max account count in the route (1-64, default 64) — trims transaction size at the cost of route choice.",
            "type": "number"
          },
          "slippageBps": {
            "description": "Slippage tolerance in basis points (1 bps = 0.01%); default 100 = 1%, which fits deep, liquid pairs. It sets otherAmountThreshold, the minimum output the swap enforces once it executes, and is the ONLY price protection on the trade — the execute does NOT re-check the price against the quote. Pass the identical value to quote and execute (a mismatch blocks the execute). On a thin or volatile pair (new mints, memecoins, small pools) 100 bps often fails. Which failure you get depends on tipLamports. On the DEFAULT tipped lane (tipLamports omitted = 1,000,000) Jupiter forwards the transaction without simulating it, so a slippage failure ALWAYS lands on-chain FAILED: the network fee is spent, nothing is swapped, and the reason appears on the activity row rather than in this call's result — this call returns \"broadcast, confirmation pending\". Only tipLamports: 0 takes the RPC lane, which simulates first and can refuse for free with status \"rejected_before_broadcast\". Either way the remedy is the same: re-quote with a higher slippageBps and execute with the identical value. Read a slippage failure as \"this pair needs more tolerance\", not as \"this pair is untradeable\". Vex caps it at 1000 (10%) and REJECTS anything above rather than clamping. Raise it in steps — no tolerance is known to fit a given pair in advance, and every increase widens the worst-case price you accept.",
            "type": "number"
          },
          "tipLamports": {
            "description": "SOL tip in lamports for /tx/v1/submit landing (default 1,000,000 = 0.001 SOL; max 10,000,000 = 0.01 SOL).",
            "type": "number"
          },
          "tokenIn": {
            "description": "Input token symbol or mint — the side you sell.",
            "type": "string"
          },
          "tokenOut": {
            "description": "Output token symbol or mint — the side you buy.",
            "type": "string"
          },
          "wrapAndUnwrapSol": {
            "description": "Auto wrap/unwrap native SOL (default true).",
            "type": "boolean"
          }
        },
        "required": [
          "tokenIn",
          "tokenOut",
          "amountIn"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "solana",
      "publicName": "solana__swap_execute",
      "readOnlyHint": false,
      "requiresEnv": "JUPITER_API_KEY",
      "title": "Execute a Jupiter swap",
      "toolId": "solana.swap.execute"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1189,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "amountIn": {
            "description": "Amount to swap. In HUMAN decimal units (e.g. \"1.5\") — not wei, lamports, or any other base unit. Converted to base units exactly, with no floating-point step: a value carrying more precision than the input side holds is REJECTED, never silently rounded.",
            "type": "string"
          },
          "computeUnitPricePercentile": {
            "description": "Priority-fee strategy: medium (25th percentile), high (default, 50th), or veryHigh (75th). This is the LEVER for priority-fee cost — Jupiter picks the actual price per compute unit at the percentile you name, and Vex refuses to sign a build whose resulting priority fee exceeds 10,000,000 lamports (0.01 SOL). If a swap is refused for that reason, lower this and re-quote. Priority prices are congestion-driven and swing by an order of magnitude within minutes, so a fresh quote at the SAME setting often fits too — the refusal is not a property of the pair.",
            "type": "string"
          },
          "dexes": {
            "description": "Comma-separated DEX allow-list. Mutually exclusive with excludeDexes.",
            "type": "string"
          },
          "excludeDexes": {
            "description": "Comma-separated DEX deny-list. Mutually exclusive with dexes.",
            "type": "string"
          },
          "forJitoBundle": {
            "description": "Exclude DEXes incompatible with Jito bundles (default false).",
            "type": "boolean"
          },
          "maxAccounts": {
            "description": "Max account count in the route (1-64, default 64) — trims transaction size at the cost of route choice.",
            "type": "number"
          },
          "slippageBps": {
            "description": "Slippage tolerance in basis points (1 bps = 0.01%); default 100 = 1%, which fits deep, liquid pairs. It sets otherAmountThreshold, the minimum output the swap enforces once it executes, and is the ONLY price protection on the trade — the execute does NOT re-check the price against the quote. Pass the identical value to quote and execute (a mismatch blocks the execute). On a thin or volatile pair (new mints, memecoins, small pools) 100 bps often fails. Which failure you get depends on tipLamports. On the DEFAULT tipped lane (tipLamports omitted = 1,000,000) Jupiter forwards the transaction without simulating it, so a slippage failure ALWAYS lands on-chain FAILED: the network fee is spent, nothing is swapped, and the reason appears on the activity row rather than in this call's result — this call returns \"broadcast, confirmation pending\". Only tipLamports: 0 takes the RPC lane, which simulates first and can refuse for free with status \"rejected_before_broadcast\". Either way the remedy is the same: re-quote with a higher slippageBps and execute with the identical value. Read a slippage failure as \"this pair needs more tolerance\", not as \"this pair is untradeable\". Vex caps it at 1000 (10%) and REJECTS anything above rather than clamping. Raise it in steps — no tolerance is known to fit a given pair in advance, and every increase widens the worst-case price you accept.",
            "type": "number"
          },
          "tipLamports": {
            "description": "SOL tip in lamports for /tx/v1/submit landing (default 1,000,000 = 0.001 SOL; max 10,000,000 = 0.01 SOL).",
            "type": "number"
          },
          "tokenIn": {
            "description": "Input token symbol or mint — the side you sell.",
            "type": "string"
          },
          "tokenOut": {
            "description": "Output token symbol or mint — the side you buy.",
            "type": "string"
          },
          "wrapAndUnwrapSol": {
            "description": "Auto wrap/unwrap native SOL (default true).",
            "type": "boolean"
          }
        },
        "required": [
          "tokenIn",
          "tokenOut",
          "amountIn"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "solana",
      "publicName": "solana__swap_quote",
      "readOnlyHint": true,
      "requiresEnv": "JUPITER_API_KEY",
      "title": "Quote a Jupiter swap",
      "toolId": "solana.swap.quote"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 637,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "mints": {
            "description": "Comma-separated mint addresses. Provide this OR queries, not both.",
            "type": "string"
          },
          "queries": {
            "description": "Comma-separated token symbols, names, or mint addresses to resolve and price (e.g. \"SOL,BONK\"). Provide this OR mints, not both.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "solana",
      "publicName": "solana__token_prices_get",
      "readOnlyHint": true,
      "requiresEnv": "JUPITER_API_KEY",
      "title": "Read Solana token prices",
      "toolId": "solana.prices"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1123,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "category": {
            "description": "Category: recent (freshly launched tokens, newest first — measured live: ages 10-175 s since mint, createdAt on every row; use for brand-new/fresh tokens), toptrending (most price movement), toptraded (highest volume), toporganicscore (highest real/organic activity), verified (Jupiter-verified), lst (liquid staking), stocks (tokenized equities, e.g. Ondo, Remora).",
            "enum": [
              "toptrending",
              "toptraded",
              "toporganicscore",
              "recent",
              "lst",
              "verified",
              "stocks"
            ],
            "type": "string"
          },
          "interval": {
            "description": "Time interval: 5m, 1h, 6h, 24h.",
            "enum": [
              "5m",
              "1h",
              "6h",
              "24h"
            ],
            "type": "string"
          },
          "limit": {
            "description": "Max rows returned (default 20). Server-side for the top* categories; applied Vex-side for recent/lst/verified/stocks with returned/totalMatched/hasMore accounting — nothing is silently dropped.",
            "type": "number"
          },
          "minLiquidity": {
            "description": "Only include tokens with liquidity (USD) >= this value. Rejected if negative.",
            "type": "number"
          },
          "minOrganicScore": {
            "description": "Only include tokens with organicScore >= this value (0-100). Rejected if out of range.",
            "type": "number"
          },
          "statsInterval": {
            "description": "Which stats window to include per token: 5m, 1h, 6h, or 24h, or all for every window (default: same as interval, or 1h).",
            "enum": [
              "5m",
              "1h",
              "6h",
              "24h",
              "all"
            ],
            "type": "string"
          },
          "verifiedOnly": {
            "description": "Only include Jupiter-verified tokens.",
            "type": "boolean"
          }
        },
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "solana",
      "publicName": "solana__tokens_discover",
      "readOnlyHint": true,
      "requiresEnv": "JUPITER_API_KEY",
      "title": "Discover new and trending Solana tokens",
      "toolId": "solana.tokens.trending"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 835,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "minLiquidity": {
            "description": "Only include tokens with liquidity (USD) >= this value. Rejected if negative.",
            "type": "number"
          },
          "minOrganicScore": {
            "description": "Only include tokens with organicScore >= this value (0-100). Rejected if out of range.",
            "type": "number"
          },
          "query": {
            "description": "Token name, symbol, or mint address. Accepts a comma-separated list of mint addresses (up to 100) for batch lookup.",
            "type": "string"
          },
          "statsInterval": {
            "description": "Which stats window to include per token: 5m, 1h, 6h, or 24h, or all for every window (default: 1h).",
            "enum": [
              "5m",
              "1h",
              "6h",
              "24h",
              "all"
            ],
            "type": "string"
          },
          "verifiedOnly": {
            "description": "Only include Jupiter-verified tokens.",
            "type": "boolean"
          }
        },
        "required": [
          "query"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "solana",
      "publicName": "solana__tokens_search",
      "readOnlyHint": true,
      "requiresEnv": "JUPITER_API_KEY",
      "title": "Search Solana tokens by name",
      "toolId": "solana.tokens.search"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1034,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "limit": {
            "description": "Optional maximum number of images to return, most recently uploaded first (1-50, default 20).",
            "type": "number"
          }
        },
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "trench",
      "publicName": "trench__images_list",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "List Trench image-locker images",
      "toolId": "trench.images"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 2406,
      "destructiveHint": true,
      "inputSchema": {
        "properties": {
          "description": {
            "description": "Optional token description (max 512 chars).",
            "type": "string"
          },
          "imageId": {
            "description": "REQUIRED. The id of an image already uploaded to the Trench Photos locker (list them with trench__images_list). A token's image is written on-chain at creation and can never be added later, so a launch without one is refused.",
            "type": "string"
          },
          "links": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Optional 0-4 social links, each an https URL (comma-separated or array)."
          },
          "name": {
            "description": "Token name (1-18 chars) — the chain reverts a longer one.",
            "type": "string"
          },
          "prebuy": {
            "description": "Optional amount of ETH to spend buying the new token on its own curve in the SAME transaction, as a plain decimal (for example \"0.0003\"). Omit or \"0\" for no prebuy.",
            "type": "string"
          },
          "symbol": {
            "description": "Token symbol/ticker (1-16 chars).",
            "type": "string"
          }
        },
        "required": [
          "name",
          "symbol",
          "imageId"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "trench",
      "publicName": "trench__launch_execute",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Launch a token on Trench Express",
      "toolId": "trench.launch_execute"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1612,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "description": {
            "description": "Optional token description (max 512 chars).",
            "type": "string"
          },
          "imageByteLength": {
            "description": "Optional byte length of the image you intend to upload (0-20000; larger reverts on-chain). VALIDATED but NOT priced: pass imageId instead to price the real bytes. On its own the dry-run simulates an EMPTY image, so the gas estimate EXCLUDES image bytes and the real launch costs more, scaling with image size (measured: ~7.2M gas at 6.9 KB, ~15.3M at 17.4 KB). Passing both is REJECTED when they disagree.",
            "type": "number"
          },
          "imageId": {
            "description": "Id of an image already in the Trench Photos locker. WITH it the dry-run resolves those bytes and simulates the SAME create() calldata the launch would sign, so gas is priced for the real image and balanceVerdict can answer sufficient/insufficient. An unknown id or a digest mismatch degrades to the empty-image sim with imagePricedFallbackReason saying which; if the image store is not mounted the call REFUSES rather than returning an unpriced estimate.",
            "type": "string"
          },
          "links": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Optional 0-4 social links, each an https URL (comma-separated or array)."
          },
          "name": {
            "description": "Token name (1-18 chars) — the chain reverts a longer one.",
            "type": "string"
          },
          "prebuy": {
            "description": "Optional prebuy in ETH, as a plain decimal string (for example \"0.0003\") — the ETH the launch spends buying its own token in the same transaction. Priced into msg.value, the simulation, the gas estimate, the Vex fee and every total. Omit it (or pass \"0\") for a launch that buys nothing. No sign, exponent or separators, and an implausible magnitude is refused rather than priced.",
            "type": "string"
          },
          "symbol": {
            "description": "Token symbol/ticker (1-16 chars).",
            "type": "string"
          }
        },
        "required": [
          "name",
          "symbol"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "trench",
      "publicName": "trench__launch_preview",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Dry-run a Trench Express launch",
      "toolId": "trench.launch_preview"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 963,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "description": {
            "description": "Optional token description (max 512 chars).",
            "type": "string"
          },
          "imageId": {
            "description": "REQUIRED. The id of an image already uploaded to the Trench Photos locker (list them with trench__images_list). A token's image is written on-chain at creation and can never be added later, so a launch without one is refused.",
            "type": "string"
          },
          "links": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Optional 0-4 social links, each an https URL (comma-separated or array)."
          },
          "name": {
            "description": "Token name (1-18 chars) — the chain reverts a longer one.",
            "type": "string"
          },
          "prebuy": {
            "description": "Optional amount of ETH to spend buying the new token on its own curve in the SAME transaction, as a plain decimal (for example \"0.0003\"). Omit or \"0\" for no prebuy.",
            "type": "string"
          },
          "symbol": {
            "description": "Token symbol/ticker (1-16 chars).",
            "type": "string"
          }
        },
        "required": [
          "name",
          "symbol",
          "imageId"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "trench",
      "publicName": "trench__launch_request_form",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Ask the user to confirm a Trench launch",
      "toolId": "trench.launch_request_form"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 884,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "limit": {
            "description": "Max launches returned, 1-100 (default 25), most recent first.",
            "type": "number"
          }
        },
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "trench",
      "publicName": "trench__my_launches_list",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "List this wallet's Trench launches",
      "toolId": "trench.my_launches"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 950,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "limit": {
            "description": "Max trades per page, 1-30 (provider-capped).",
            "type": "number"
          },
          "page": {
            "description": "0-based page index (REQUIRED — the provider returns an error without it).",
            "type": "number"
          },
          "token": {
            "description": "Token contract address whose tape to read.",
            "type": "string"
          }
        },
        "required": [
          "token",
          "page"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "trench",
      "publicName": "trench__token_trades_list",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Read a Trench Express token's trade tape",
      "toolId": "trench.trades"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 2293,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "creator": {
            "description": "Client-side filter: keep only tokens created by this wallet address (exact, case-insensitive).",
            "type": "string"
          },
          "excludeRuggedFlagged": {
            "description": "Client-side filter: drop tokens the launchpad flagged as a likely rug. Defaults to true; set false to include flagged tokens. The response echoes how many were filtered.",
            "type": "boolean"
          },
          "explainDrops": {
            "description": "When true, include a per-filter census of how many tokens each client-side filter removed. Default false.",
            "type": "boolean"
          },
          "includeCurveProgress": {
            "description": "When true, enrich each returned row with curveProgressPct (0-100) WITHOUT filtering. Default false. Computed on-chain at one pinned block (echoed as curveProgressBlock) from each token's own reserves and the Diamond's live graduation threshold, read in Multicall3 batches of at most 30 rows. Graduated rows are 100; rows whose on-chain read fails are dropped and counted. Display/hunting-grade only — a token can graduate between this read and any trade.",
            "type": "boolean"
          },
          "limit": {
            "description": "Max deduped tokens to return, 1-200 (no default — omitting returns the walked window). The provider caps each page at 30; higher limits page-walk and dedupe by address.",
            "type": "number"
          },
          "maxCurveProgressPct": {
            "description": "Client-side: keep only tokens no further than this toward graduating, 0-100 (pair with minCurveProgressPct for a band; min must not exceed max). Same pinned-block on-chain curve read as minCurveProgressPct, in Multicall3 batches of at most 30 rows. Graduated tokens count as 100 and are excluded by any max below 100. Rows whose on-chain read fails are DROPPED and counted.",
            "type": "number"
          },
          "minCurveProgressPct": {
            "description": "Client-side: keep only tokens at least this far toward graduating, 0-100, and not greater than maxCurveProgressPct (an inverted band is rejected by name). Curve progress is computed by Vex ON-CHAIN at ONE PINNED BLOCK, echoed as curveProgressBlock: each token's own ethReserve and fakeEth from fakepool_stats over the graduation reserve derived from the Diamond's live ethMcapThreshold — never a hard-coded threshold. Rows are read in Multicall3 batches of at most 30 (so a 600-row page walk costs at most 20 batched calls, not one unbounded call). Applies to bonding-curve tokens; a graduated token counts as 100. Rows whose on-chain read fails are DROPPED and counted; if the threshold itself cannot be read the filter is refused rather than guessed. Setting this (or maxCurveProgressPct/includeCurveProgress) turns the on-chain read on; leaving all three unset keeps the list a pure REST call.",
            "type": "number"
          },
          "page": {
            "description": "1-based single-page index for browsing (min 1). When set, returns just that one provider page (≤30 rows) instead of page-walking.",
            "type": "number"
          },
          "sort": {
            "description": "Sort order (server-side): time (orders by launchedAtMs, newest first, default), price, or bump (recent activity). Unknown values are rejected.",
            "type": "string"
          },
          "status": {
            "description": "Curve stage filter (server-side): curve (still on the bonding curve), launched (graduated into a pool), or all (default, no filter).",
            "type": "string"
          }
        },
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "trench",
      "publicName": "trench__tokens_discover",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Screen Trench Express tokens",
      "toolId": "trench.tokens"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 729,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "limit": {
            "description": "Max results to return, 1-30 (provider-capped). Default leaves it to the provider.",
            "type": "number"
          },
          "query": {
            "description": "Name or symbol fragment to search for.",
            "type": "string"
          }
        },
        "required": [
          "query"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "trench",
      "publicName": "trench__tokens_search",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Search Trench Express tokens",
      "toolId": "trench.search"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1593,
      "destructiveHint": true,
      "inputSchema": {
        "properties": {
          "amountIn": {
            "description": "Human amount of the input token (ETH for a buy, tokens for a sell), e.g. \"0.01\".",
            "type": "string"
          },
          "chain": {
            "description": "Chain — Robinhood Chain / \"robinhood\" (the only chain; defaults to robinhood).",
            "type": "string"
          },
          "slippageBps": {
            "description": "Slippage tolerance in basis points (1 bps = 0.01%) for ETH-curve trading on Robinhood Chain; default 100 = 1%, which fits this deterministic, fee-inclusive curve. Pass the SAME value on trench__trade_quote and trench__trade_execute, or omit it on both - a mismatch blocks the execute (the prequote match requires identical params). It is the ONLY price protection on the trade: Vex derives the raw minimum output from a fresh quote at this tolerance (never zero, never from you). A thin or volatile fresh curve token can move between quote and broadcast, so raise it there. A too-tight tolerance USUALLY fails for FREE - the pre-sign gas estimate is refused before anything is signed, NO gas is spent, and the execute returns status \"not_attempted\" with failureCode slippage; LESS OFTEN the curve moves after that estimate passes and the transaction REVERTS after broadcast, recording mined_revert with the gas SPENT. After either, re-quote with a higher slippageBps and pass the same value. Vex caps it at 1000 (10%) and REJECTS anything above rather than clamping; every increase widens the worst-case price you accept.",
            "type": "number"
          },
          "tokenIn": {
            "description": "Input token: \"ETH\" for a BUY, or the curve token address for a SELL.",
            "type": "string"
          },
          "tokenOut": {
            "description": "Output token: the curve token address for a BUY, or \"ETH\" for a SELL.",
            "type": "string"
          }
        },
        "required": [
          "tokenIn",
          "tokenOut",
          "amountIn"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "trench",
      "publicName": "trench__trade_execute",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Trade a Trench Express token",
      "toolId": "trench.trade_execute"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 659,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "amountIn": {
            "description": "Human amount of the input token (ETH for a buy, tokens for a sell), e.g. \"0.01\".",
            "type": "string"
          },
          "chain": {
            "description": "Chain — Robinhood Chain / \"robinhood\" (the only chain; defaults to robinhood).",
            "type": "string"
          },
          "slippageBps": {
            "description": "Slippage tolerance in basis points (1 bps = 0.01%) for ETH-curve trading on Robinhood Chain; default 100 = 1%, which fits this deterministic, fee-inclusive curve. Pass the SAME value on trench__trade_quote and trench__trade_execute, or omit it on both - a mismatch blocks the execute (the prequote match requires identical params). It is the ONLY price protection on the trade: Vex derives the raw minimum output from a fresh quote at this tolerance (never zero, never from you). A thin or volatile fresh curve token can move between quote and broadcast, so raise it there. A too-tight tolerance USUALLY fails for FREE - the pre-sign gas estimate is refused before anything is signed, NO gas is spent, and the execute returns status \"not_attempted\" with failureCode slippage; LESS OFTEN the curve moves after that estimate passes and the transaction REVERTS after broadcast, recording mined_revert with the gas SPENT. After either, re-quote with a higher slippageBps and pass the same value. Vex caps it at 1000 (10%) and REJECTS anything above rather than clamping; every increase widens the worst-case price you accept.",
            "type": "number"
          },
          "tokenIn": {
            "description": "Input token: \"ETH\" for a BUY, or the curve token address for a SELL.",
            "type": "string"
          },
          "tokenOut": {
            "description": "Output token: the curve token address for a BUY, or \"ETH\" for a SELL.",
            "type": "string"
          }
        },
        "required": [
          "tokenIn",
          "tokenOut",
          "amountIn"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "trench",
      "publicName": "trench__trade_quote",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Quote a Trench Express trade",
      "toolId": "trench.trade_quote"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1059,
      "destructiveHint": true,
      "inputSchema": {
        "properties": {
          "amountIn": {
            "description": "Amount in human-readable units. This is the TOTAL debited: the swap executes on this amount minus Vex's 25 bps fee.",
            "type": "string"
          },
          "chain": {
            "description": "The chain to swap on. Chain slug/alias, or the numeric chain id `TokenFind` returns (e.g. `base` or `8453`). Robinhood Chain is `robinhood` / `4663`.",
            "type": "string"
          },
          "slippageBps": {
            "description": "Slippage tolerance in basis points (1 bps = 0.01%); default 100 = 1%, which fits deep, liquid pairs. It sets the minimum output written into the swap calldata, and is the ONLY price protection on the trade. Must match the uniswap__swap_quote value (or be omitted on both) - a mismatch blocks the execute. On a thin or volatile pair (new listings, memecoins, small pools) 100 bps often fails, in one of two ways. USUALLY it fails for FREE: the router's \"Too little received\" / \"INSUFFICIENT_OUTPUT_AMOUNT\" comes back from the pre-sign gas estimate, so nothing is signed and NO gas is spent - this call returns status \"not_attempted\" with retryable true, and the activity row records failure code slippage. LESS OFTEN the pool moves after that estimate passes: the router REVERTS once mined, the row records mined_revert, and the gas IS spent. Either is the signal to retry with more tolerance, not to give up on the pair: re-quote with a higher slippageBps and pass the same value here. Vex caps it at 1000 (10%) and REJECTS anything above rather than clamping. Raise it in steps, starting tight since the usual failure costs nothing - no tolerance is known to fit a given pair in advance, and every increase widens the worst-case price you accept.",
            "type": "number"
          },
          "tokenIn": {
            "description": "Input token CONTRACT ADDRESS or native ETH/native. Uniswap has no symbol search.",
            "type": "string"
          },
          "tokenOut": {
            "description": "Output token CONTRACT ADDRESS or native ETH/native.",
            "type": "string"
          }
        },
        "required": [
          "chain",
          "tokenIn",
          "tokenOut",
          "amountIn"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "uniswap",
      "publicName": "uniswap__swap_execute",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Execute a Uniswap V2/V3 swap",
      "toolId": "uniswap.swap.execute"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 696,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "amountIn": {
            "description": "Amount in human-readable units. This is the TOTAL debited: the route is priced for this amount minus Vex's 25 bps fee.",
            "type": "string"
          },
          "chain": {
            "description": "The chain to quote on. Chain slug/alias, or the numeric chain id `TokenFind` returns (e.g. `base` or `8453`). Robinhood Chain is `robinhood` / `4663`.",
            "type": "string"
          },
          "slippageBps": {
            "description": "Slippage tolerance in basis points (1 bps = 0.01%); default 100 = 1%, which fits deep, liquid pairs. It pins the minimum output the resulting swap will enforce, and is the ONLY price protection that trade has. Pass the SAME value to uniswap__swap_execute, or omit it on both - a mismatch blocks the execute. On a thin or volatile pair (new listings, memecoins, small pools) 100 bps often fails at execute time, in one of two ways. USUALLY it fails for FREE: the router's \"Too little received\" / \"INSUFFICIENT_OUTPUT_AMOUNT\" comes back from the pre-sign gas estimate, so nothing is signed and NO gas is spent, and the activity row records failure code slippage. LESS OFTEN the pool moves after that estimate passes: the router REVERTS once mined, the row records mined_revert, and the gas IS spent. After either, re-quote with a higher slippageBps rather than abandoning the pair - and since the usual failure costs nothing, a tighter tolerance is the cheap thing to try first. Vex caps it at 1000 (10%) and REJECTS anything above rather than clamping; every increase widens the worst-case price you accept.",
            "type": "number"
          },
          "tokenIn": {
            "description": "Input token CONTRACT ADDRESS or native ETH/native. Uniswap has no symbol search — resolve a symbol to its address first.",
            "type": "string"
          },
          "tokenOut": {
            "description": "Output token CONTRACT ADDRESS or native ETH/native.",
            "type": "string"
          }
        },
        "required": [
          "chain",
          "tokenIn",
          "tokenOut",
          "amountIn"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "uniswap",
      "publicName": "uniswap__swap_quote",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Quote a Uniswap V2/V3 route",
      "toolId": "uniswap.swap.quote"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 556,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "id": {
            "description": "Numeric Virtuals agent id, exactly as virtuals__agents_discover returns it (e.g. 96200 for VEX). The string spelling \"96200\" is accepted too.",
            "type": "number"
          }
        },
        "required": [
          "id"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "virtuals",
      "publicName": "virtuals__agent_get",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Read one Virtuals agent token",
      "toolId": "virtuals.get"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1889,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "chain": {
            "description": "REQUIRED. The one chain to list — Virtuals indexes exactly four: base, solana, robinhood, ethereum. Chain slug/alias, or the numeric chain id `TokenFind` returns (e.g. `base` or `8453`).",
            "enum": [
              "base",
              "solana",
              "robinhood",
              "ethereum"
            ],
            "type": "string"
          },
          "limit": {
            "description": "Max agents to return after filtering (default 20, max 100). Paging stops once this many rows match. Out of range is rejected, not clamped.",
            "type": "number"
          },
          "page": {
            "description": "1-based FIRST provider page of the window (default 1). The reply's windowNote names the next page to continue from.",
            "type": "number"
          },
          "pageSize": {
            "description": "Rows fetched per provider page (default 100, max 200 — the provider's proven ceiling). Up to 5 pages are scanned per call, so pageSize x 5 bounds how deep one call reads.",
            "type": "number"
          },
          "sort": {
            "description": "Alias of sortBy, accepted for compatibility. Same values. Send ONE of the two, never both — a silently dropped spelling is indistinguishable from one that was honoured.",
            "enum": [
              "mcap",
              "volume",
              "newest",
              "recentGraduation"
            ],
            "type": "string"
          },
          "sortBy": {
            "description": "Sort order, always descending: mcap (default), volume, newest, or recentGraduation. It decides WHICH rows the window contains, so pair it with status — sortBy: newest is what surfaces UNDERGRAD agents. An unrecognised value is rejected, not folded to mcap.",
            "enum": [
              "mcap",
              "volume",
              "newest",
              "recentGraduation"
            ],
            "type": "string"
          },
          "status": {
            "description": "Client-side status filter applied to the fetched window: undergrad (bonding curve), graduated (AVAILABLE), or all (default). An unrecognised value is rejected, not ignored.",
            "enum": [
              "undergrad",
              "graduated",
              "all"
            ],
            "type": "string"
          }
        },
        "required": [
          "chain"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "virtuals",
      "publicName": "virtuals__agents_discover",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Screen Virtuals agent tokens",
      "toolId": "virtuals.list"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1655,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "limit": {
            "description": "Max geneses to return (default 20, max 100). Out of range is rejected, not clamped.",
            "type": "number"
          },
          "page": {
            "description": "1-based page for older genesis events (default 1).",
            "type": "number"
          },
          "pageSize": {
            "description": "Rows fetched from the provider for this page (default 100, max 200).",
            "type": "number"
          }
        },
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "virtuals",
      "publicName": "virtuals__genesis_launches_list",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Browse the Virtuals Genesis calendar",
      "toolId": "virtuals.geneses"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1247,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "chain": {
            "description": "REQUIRED. The one chain to list — Virtuals indexes exactly four: base, solana, robinhood, ethereum. Chain slug/alias, or the numeric chain id `TokenFind` returns (e.g. `base` or `8453`).",
            "enum": [
              "base",
              "solana",
              "robinhood",
              "ethereum"
            ],
            "type": "string"
          },
          "limit": {
            "description": "Max graduations to return (default 20, max 100). Out of range is rejected, not clamped.",
            "type": "number"
          },
          "page": {
            "description": "1-based FIRST provider page of the window (default 1). The reply's windowNote names the next page to continue from.",
            "type": "number"
          },
          "pageSize": {
            "description": "Rows fetched per provider page (default 100, max 200). Up to 5 pages are scanned per call.",
            "type": "number"
          }
        },
        "required": [
          "chain"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "virtuals",
      "publicName": "virtuals__graduations_list",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "List recent Virtuals graduations",
      "toolId": "virtuals.graduations"
    }
  ]
}
