{
  "toolCount": 170,
  "tools": [
    {
      "alwaysLoad": true,
      "descriptionBytes": 2046,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "cursor": {
            "description": "Opaque pagination cursor (transactions) - pass the prior response's nextCursor to fetch the next page",
            "type": "string"
          },
          "limit": {
            "description": "Max rows (default 20)",
            "type": "number"
          },
          "namespace": {
            "description": "Protocol filter (e.g. kyberswap, khalani)",
            "type": "string"
          },
          "productType": {
            "description": "Product filter (e.g. spot, perps, prediction, bridge, order, lend)",
            "type": "string"
          },
          "txHash": {
            "description": "Transaction hash anchor (transactions) - return rows matching this txHash",
            "type": "string"
          },
          "view": {
            "description": "What to inspect (see description for group breakdown)",
            "enum": [
              "transactions",
              "activity",
              "balances",
              "snapshots",
              "summary",
              "executions",
              "mission_baseline"
            ],
            "type": "string"
          }
        },
        "required": [
          "view"
        ],
        "type": "object"
      },
      "kind": "internal",
      "publicName": "AgentScan",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Review this wallet's recorded activity"
    },
    {
      "alwaysLoad": true,
      "descriptionBytes": 2015,
      "destructiveHint": true,
      "inputSchema": {
        "properties": {
          "amountRaw": {
            "description": "Amount in raw atomic units of fromToken (e.g. USDC has 6 decimals, so 1 USDC = \"1000000\"). Read decimals from a contract-verified TokenFind candidate on the exact source chain. A provider-list decimal is not signing authority, and a raw amount without contract decimals is unsafe. A raw amount next to a token whose decimals you have not read is a thousandfold error waiting to happen.",
            "type": "string"
          },
          "filler": {
            "description": "Restrict quotes to a specific filler.",
            "type": "string"
          },
          "fromAddress": {
            "description": "Source wallet address override.",
            "type": "string"
          },
          "fromChain": {
            "description": "Source chain ID or alias.",
            "type": "string"
          },
          "fromToken": {
            "description": "Source token address.",
            "type": "string"
          },
          "toChain": {
            "description": "Destination chain ID or alias.",
            "type": "string"
          },
          "toToken": {
            "description": "Destination token address.",
            "type": "string"
          },
          "tradeType": {
            "description": "EXACT_INPUT or EXACT_OUTPUT (default: EXACT_INPUT).",
            "type": "string"
          }
        },
        "required": [
          "fromChain",
          "fromToken",
          "toChain",
          "toToken",
          "amountRaw"
        ],
        "type": "object"
      },
      "kind": "internal",
      "publicName": "BridgeExecute",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Execute a cross-chain bridge"
    },
    {
      "alwaysLoad": true,
      "descriptionBytes": 1937,
      "destructiveHint": true,
      "inputSchema": {
        "properties": {
          "amountRaw": {
            "description": "Amount in raw atomic units of fromToken (e.g. USDC has 6 decimals, so 1 USDC = \"1000000\"). Read decimals from a contract-verified TokenFind candidate on the exact source chain. A provider-list decimal is not signing authority, and a raw amount without contract decimals is unsafe. A raw amount next to a token whose decimals you have not read is a thousandfold error waiting to happen.",
            "type": "string"
          },
          "fromChain": {
            "description": "Source chain ID or alias.",
            "type": "string"
          },
          "fromToken": {
            "description": "Source token address, or native ETH/native.",
            "type": "string"
          },
          "slippageBps": {
            "description": "Slippage tolerance in basis points for the destination-side fill (1 bps = 0.01%). Higher tolerance widens the worst-case received amount on the destination chain. When a bridge quote fails or fills keep expiring, re-quote with a higher value rather than retrying the same one. Vex caps it at 1000 (10%) and rejects anything above rather than clamping.",
            "type": "number"
          },
          "toChain": {
            "description": "Destination chain ID or alias.",
            "type": "string"
          },
          "toToken": {
            "description": "Destination token address, or native ETH/native.",
            "type": "string"
          },
          "tradeType": {
            "description": "EXACT_INPUT or EXACT_OUTPUT (default: EXACT_INPUT).",
            "type": "string"
          }
        },
        "required": [
          "fromChain",
          "fromToken",
          "toChain",
          "toToken",
          "amountRaw"
        ],
        "type": "object"
      },
      "kind": "internal",
      "publicName": "BridgeExecuteRelay",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Execute a Relay bridge"
    },
    {
      "alwaysLoad": true,
      "descriptionBytes": 1889,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "amountRaw": {
            "description": "Amount in raw atomic units of fromToken (e.g. USDC has 6 decimals, so 1 USDC = \"1000000\"). Read decimals from a contract-verified TokenFind candidate on the exact source chain. A provider-list decimal is not signing authority, and a raw amount without contract decimals is unsafe. A raw amount next to a token whose decimals you have not read is a thousandfold error waiting to happen.",
            "type": "string"
          },
          "filler": {
            "description": "Restrict quotes to a specific filler.",
            "type": "string"
          },
          "fromAddress": {
            "description": "Source wallet address override.",
            "type": "string"
          },
          "fromChain": {
            "description": "Source chain ID or alias.",
            "type": "string"
          },
          "fromToken": {
            "description": "Source token address.",
            "type": "string"
          },
          "toChain": {
            "description": "Destination chain ID or alias.",
            "type": "string"
          },
          "toToken": {
            "description": "Destination token address.",
            "type": "string"
          },
          "tradeType": {
            "description": "EXACT_INPUT or EXACT_OUTPUT (default: EXACT_INPUT).",
            "type": "string"
          }
        },
        "required": [
          "fromChain",
          "fromToken",
          "toChain",
          "toToken",
          "amountRaw"
        ],
        "type": "object"
      },
      "kind": "internal",
      "publicName": "BridgeQuote",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Quote a cross-chain bridge"
    },
    {
      "alwaysLoad": true,
      "descriptionBytes": 1110,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "amountRaw": {
            "description": "Amount in raw atomic units of fromToken (e.g. USDC has 6 decimals, so 1 USDC = \"1000000\"). Read decimals from a contract-verified TokenFind candidate on the exact source chain. A provider-list decimal is not signing authority, and a raw amount without contract decimals is unsafe. A raw amount next to a token whose decimals you have not read is a thousandfold error waiting to happen.",
            "type": "string"
          },
          "fromChain": {
            "description": "Source chain ID or alias.",
            "type": "string"
          },
          "fromToken": {
            "description": "Source token address, or native ETH/native.",
            "type": "string"
          },
          "slippageBps": {
            "description": "Slippage tolerance in basis points for the destination-side fill (1 bps = 0.01%). Higher tolerance widens the worst-case received amount on the destination chain. When a bridge quote fails or fills keep expiring, re-quote with a higher value rather than retrying the same one. Vex caps it at 1000 (10%) and rejects anything above rather than clamping.",
            "type": "number"
          },
          "toChain": {
            "description": "Destination chain ID or alias.",
            "type": "string"
          },
          "toToken": {
            "description": "Destination token address, or native ETH/native.",
            "type": "string"
          },
          "tradeType": {
            "description": "EXACT_INPUT or EXACT_OUTPUT (default: EXACT_INPUT).",
            "type": "string"
          }
        },
        "required": [
          "fromChain",
          "fromToken",
          "toChain",
          "toToken",
          "amountRaw"
        ],
        "type": "object"
      },
      "kind": "internal",
      "publicName": "BridgeQuoteRelay",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Quote a Relay bridge"
    },
    {
      "alwaysLoad": true,
      "descriptionBytes": 1548,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "cursor": {
            "description": "List mode: the provider's own opaque continuation value, echoed back as `cursor` on the previous reply. Send back exactly what that reply returned; a reply with no `cursor` is the end of the list.",
            "type": "number"
          },
          "fromChain": {
            "description": "List mode: source chain filter (ID or alias).",
            "type": "string"
          },
          "limit": {
            "description": "List mode: max results.",
            "type": "number"
          },
          "orderId": {
            "description": "Khalani order ID. Provide to fetch a single order; omit to list your orders.",
            "type": "string"
          },
          "orderIds": {
            "description": "List mode: comma-separated order IDs to filter.",
            "type": "string"
          },
          "toChain": {
            "description": "List mode: destination chain filter (ID or alias).",
            "type": "string"
          },
          "txHashSearch": {
            "description": "List mode: search by transaction hash.",
            "type": "string"
          },
          "walletAddress": {
            "description": "List mode: the ACCOUNT address whose orders to list (optional - omit to use your configured wallet).",
            "type": "string"
          },
          "walletFamily": {
            "description": "List mode: wallet FAMILY, never a chain - eip155 or solana.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "kind": "internal",
      "publicName": "BridgeStatus",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Check a bridge order's status"
    },
    {
      "alwaysLoad": true,
      "descriptionBytes": 1326,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "action": {
            "description": "What to read",
            "enum": [
              "tx_receipt",
              "erc721_mint",
              "erc20_balance"
            ],
            "type": "string"
          },
          "address": {
            "description": "Recipient address - optional mint filter for erc721_mint (only mints to this address are returned)",
            "type": "string"
          },
          "chain": {
            "description": "Chain slug/alias, or the numeric chain id `TokenFind` returns (e.g. `base` or `8453`). Local-only chains work too: 'robinhood' / '4663'.",
            "type": "string"
          },
          "owner": {
            "description": "Address to read the ERC-20 balance of (erc20_balance). Omit to read your own selected wallet.",
            "type": "string"
          },
          "tokenAddress": {
            "description": "ERC-20 contract address (REQUIRED for erc20_balance)",
            "type": "string"
          },
          "txHash": {
            "description": "Transaction hash (for tx_receipt, erc721_mint)",
            "type": "string"
          }
        },
        "required": [
          "action",
          "chain"
        ],
        "type": "object"
      },
      "kind": "internal",
      "publicName": "ChainRead",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Read raw EVM chain data"
    },
    {
      "alwaysLoad": true,
      "descriptionBytes": 2047,
      "destructiveHint": true,
      "inputSchema": {
        "properties": {
          "amountIn": {
            "description": "Amount of tokenIn to swap, in HUMAN decimal units (e.g. \"1.5\"). Not wei/lamports.",
            "type": "string"
          },
          "chain": {
            "description": "Chain to swap on. EVM slugs/aliases route to KyberSwap (ethereum, base, arbitrum, robinhood, …); the literal \"solana\" routes to Jupiter. Accepts a chain slug/alias or the numeric chain id TokenFind returns (e.g. base or 8453).",
            "type": "string"
          },
          "slippageBps": {
            "description": "Slippage tolerance in basis points (1 bps = 0.01%); default 100 = 1%, which fits deep, liquid pairs. It is the ONLY price protection on the trade. Pass the SAME value to the quote and the execute, or omit it on both - a mismatch blocks the execute. On a thin or volatile pair (new listings, memecoins, small pools) 100 bps often fails. When it fails, the message says so and names this parameter: re-quote with a higher slippageBps and pass the same value to the execute. Do not read a slippage failure as \"this pair is untradeable\" and do not switch venue for it - another venue at 100 bps fails the same way. Vex caps it at 1000 (10%) and REJECTS anything above rather than clamping; every increase widens the worst-case price you accept, so raise it in steps.",
            "type": "number"
          },
          "tokenIn": {
            "description": "Input token. EVM: the token CONTRACT ADDRESS or native ETH/native. For an EVM token, call TokenFind with exactly one target chain and continue only when mutationReady is true. Ambiguous, provider-capped, or metadata-unreadable results are not executable identities. If the swap pair is already known, prefer its exact base or quote token address and use TokenFind to validate that address on the target chain. TokenFind reads symbol and decimals from the contract, never provider list metadata. Solana: symbol or mint.",
            "type": "string"
          },
          "tokenOut": {
            "description": "Output token. EVM: the token CONTRACT ADDRESS or native ETH/native. For an EVM token, call TokenFind with exactly one target chain and continue only when mutationReady is true. Ambiguous, provider-capped, or metadata-unreadable results are not executable identities. If the swap pair is already known, prefer its exact base or quote token address and use TokenFind to validate that address on the target chain. TokenFind reads symbol and decimals from the contract, never provider list metadata. Solana: symbol or mint.",
            "type": "string"
          }
        },
        "required": [
          "chain",
          "tokenIn",
          "tokenOut",
          "amountIn"
        ],
        "type": "object"
      },
      "kind": "internal",
      "publicName": "SwapExecute",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Execute a token swap"
    },
    {
      "alwaysLoad": true,
      "descriptionBytes": 2046,
      "destructiveHint": true,
      "inputSchema": {
        "properties": {
          "amountIn": {
            "description": "Amount of tokenIn to swap, in HUMAN decimal units (e.g. \"1.5\"). Not wei.",
            "type": "string"
          },
          "chain": {
            "description": "EVM chain to swap on (ethereum, base, arbitrum, robinhood, …) - Uniswap ONLY, no venue routing. Solana is NOT supported by this tool.",
            "type": "string"
          },
          "slippageBps": {
            "description": "Slippage tolerance in basis points (1 bps = 0.01%); default 100 = 1%, which fits deep, liquid pairs. It is the ONLY price protection on the trade. Pass the SAME value to the quote and the execute, or omit it on both - a mismatch blocks the execute. On a thin or volatile pair (new listings, memecoins, small pools) 100 bps often fails. When it fails, the message says so and names this parameter: re-quote with a higher slippageBps and pass the same value to the execute. Do not read a slippage failure as \"this pair is untradeable\" and do not switch venue for it - another venue at 100 bps fails the same way. Vex caps it at 1000 (10%) and REJECTS anything above rather than clamping; every increase widens the worst-case price you accept, so raise it in steps.",
            "type": "number"
          },
          "tokenIn": {
            "description": "Input token: the token CONTRACT ADDRESS or native ETH/native. For an EVM token, call TokenFind with exactly one target chain and continue only when mutationReady is true. Ambiguous, provider-capped, or metadata-unreadable results are not executable identities. If the swap pair is already known, prefer its exact base or quote token address and use TokenFind to validate that address on the target chain. TokenFind reads symbol and decimals from the contract, never provider list metadata.",
            "type": "string"
          },
          "tokenOut": {
            "description": "Output token: the token CONTRACT ADDRESS or native ETH/native. For an EVM token, call TokenFind with exactly one target chain and continue only when mutationReady is true. Ambiguous, provider-capped, or metadata-unreadable results are not executable identities. If the swap pair is already known, prefer its exact base or quote token address and use TokenFind to validate that address on the target chain. TokenFind reads symbol and decimals from the contract, never provider list metadata.",
            "type": "string"
          }
        },
        "required": [
          "chain",
          "tokenIn",
          "tokenOut",
          "amountIn"
        ],
        "type": "object"
      },
      "kind": "internal",
      "publicName": "SwapExecuteUniswap",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Execute a Uniswap swap"
    },
    {
      "alwaysLoad": true,
      "descriptionBytes": 2047,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "amountIn": {
            "description": "Amount of tokenIn to swap, in HUMAN decimal units (e.g. \"1.5\"). Not wei/lamports.",
            "type": "string"
          },
          "chain": {
            "description": "Chain to swap on. EVM slugs/aliases route to KyberSwap (ethereum, base, arbitrum, robinhood, …); the literal \"solana\" routes to Jupiter. Accepts a chain slug/alias or the numeric chain id TokenFind returns (e.g. base or 8453).",
            "type": "string"
          },
          "slippageBps": {
            "description": "Slippage tolerance in basis points (1 bps = 0.01%); default 100 = 1%, which fits deep, liquid pairs. It is the ONLY price protection on the trade. Pass the SAME value to the quote and the execute, or omit it on both - a mismatch blocks the execute. On a thin or volatile pair (new listings, memecoins, small pools) 100 bps often fails. When it fails, the message says so and names this parameter: re-quote with a higher slippageBps and pass the same value to the execute. Do not read a slippage failure as \"this pair is untradeable\" and do not switch venue for it - another venue at 100 bps fails the same way. Vex caps it at 1000 (10%) and REJECTS anything above rather than clamping; every increase widens the worst-case price you accept, so raise it in steps.",
            "type": "number"
          },
          "tokenIn": {
            "description": "Input token. EVM: the token CONTRACT ADDRESS or native ETH/native. For an EVM token, call TokenFind with exactly one target chain and continue only when mutationReady is true. Ambiguous, provider-capped, or metadata-unreadable results are not executable identities. If the swap pair is already known, prefer its exact base or quote token address and use TokenFind to validate that address on the target chain. TokenFind reads symbol and decimals from the contract, never provider list metadata. Solana: symbol or mint.",
            "type": "string"
          },
          "tokenOut": {
            "description": "Output token. EVM: the token CONTRACT ADDRESS or native ETH/native. For an EVM token, call TokenFind with exactly one target chain and continue only when mutationReady is true. Ambiguous, provider-capped, or metadata-unreadable results are not executable identities. If the swap pair is already known, prefer its exact base or quote token address and use TokenFind to validate that address on the target chain. TokenFind reads symbol and decimals from the contract, never provider list metadata. Solana: symbol or mint.",
            "type": "string"
          }
        },
        "required": [
          "chain",
          "tokenIn",
          "tokenOut",
          "amountIn"
        ],
        "type": "object"
      },
      "kind": "internal",
      "publicName": "SwapQuote",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Quote a token swap"
    },
    {
      "alwaysLoad": true,
      "descriptionBytes": 2017,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "amountIn": {
            "description": "Amount of tokenIn to swap, in HUMAN decimal units (e.g. \"1.5\"). Not wei.",
            "type": "string"
          },
          "chain": {
            "description": "EVM chain to swap on (ethereum, base, arbitrum, robinhood, …) - Uniswap ONLY, no venue routing. Solana is NOT supported by this tool.",
            "type": "string"
          },
          "slippageBps": {
            "description": "Slippage tolerance in basis points (1 bps = 0.01%); default 100 = 1%, which fits deep, liquid pairs. It is the ONLY price protection on the trade. Pass the SAME value to the quote and the execute, or omit it on both - a mismatch blocks the execute. On a thin or volatile pair (new listings, memecoins, small pools) 100 bps often fails. When it fails, the message says so and names this parameter: re-quote with a higher slippageBps and pass the same value to the execute. Do not read a slippage failure as \"this pair is untradeable\" and do not switch venue for it - another venue at 100 bps fails the same way. Vex caps it at 1000 (10%) and REJECTS anything above rather than clamping; every increase widens the worst-case price you accept, so raise it in steps.",
            "type": "number"
          },
          "tokenIn": {
            "description": "Input token: the token CONTRACT ADDRESS or native ETH/native. For an EVM token, call TokenFind with exactly one target chain and continue only when mutationReady is true. Ambiguous, provider-capped, or metadata-unreadable results are not executable identities. If the swap pair is already known, prefer its exact base or quote token address and use TokenFind to validate that address on the target chain. TokenFind reads symbol and decimals from the contract, never provider list metadata.",
            "type": "string"
          },
          "tokenOut": {
            "description": "Output token: the token CONTRACT ADDRESS or native ETH/native. For an EVM token, call TokenFind with exactly one target chain and continue only when mutationReady is true. Ambiguous, provider-capped, or metadata-unreadable results are not executable identities. If the swap pair is already known, prefer its exact base or quote token address and use TokenFind to validate that address on the target chain. TokenFind reads symbol and decimals from the contract, never provider list metadata.",
            "type": "string"
          }
        },
        "required": [
          "chain",
          "tokenIn",
          "tokenOut",
          "amountIn"
        ],
        "type": "object"
      },
      "kind": "internal",
      "publicName": "SwapQuoteUniswap",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Quote a Uniswap swap"
    },
    {
      "alwaysLoad": true,
      "descriptionBytes": 953,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "chain": {
            "description": "EVM chain slug or alias (ethereum, base, arbitrum, …). Accepts a chain slug/alias or the numeric chain id TokenFind returns (e.g. base or 8453).",
            "type": "string"
          },
          "tokenAddress": {
            "description": "Token contract address to inspect (0x form on the named chain). A symbol is not accepted. For an EVM token, call TokenFind with exactly one target chain and continue only when mutationReady is true. Ambiguous, provider-capped, or metadata-unreadable results are not executable identities. If the swap pair is already known, prefer its exact base or quote token address and use TokenFind to validate that address on the target chain. TokenFind reads symbol and decimals from the contract, never provider list metadata. The former key `address` is retired and is rejected by name.",
            "type": "string"
          }
        },
        "required": [
          "chain",
          "tokenAddress"
        ],
        "type": "object"
      },
      "kind": "internal",
      "publicName": "TokenCheck",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Check an EVM token for honeypot and tax"
    },
    {
      "alwaysLoad": true,
      "descriptionBytes": 1956,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "chainIds": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Comma-separated chain IDs or aliases. Accepts either a comma-separated string (\"1,8453\") or an array of strings ([\"1\",\"8453\"]) - the two are equivalent."
          },
          "query": {
            "description": "Search phrase or token address.",
            "type": "string"
          }
        },
        "required": [
          "query"
        ],
        "type": "object"
      },
      "kind": "internal",
      "publicName": "TokenFind",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Find a token's address and decimals"
    },
    {
      "alwaysLoad": true,
      "descriptionBytes": 2039,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "action": {
            "description": "Read-only Twitter/X operation. tweet_search: search posts by cashtag/hashtag/words with filters - the momentum workhorse (max 20 per call); user_timeline: recent posts by one account, newest first (max 20); user_details: one account's profile: follower count, verification, bio - author credibility; tweet_details: one post by id, with its engagement counts; tweet_replies: replies under one post - sentiment around a call; tweet_likers: accounts that liked a post (max 100) - who is behind a signal; tweet_retweeters: accounts that reposted a post (max 100); user_search: find accounts by name/handle text (max 20); user_replies: one account's replies, i.e. what it says under other posts (max 20); user_followers: an account's followers (max 100); user_following: who an account follows (max 100); space_details: one Space's metadata and participant counts; account_status: the configured research account itself - use it to check the session works.",
            "enum": [
              "tweet_search",
              "user_timeline",
              "user_details",
              "tweet_details",
              "tweet_replies",
              "tweet_likers",
              "tweet_retweeters",
              "user_search",
              "user_replies",
              "user_followers",
              "user_following",
              "space_details",
              "account_status"
            ],
            "type": "string"
          },
          "count": {
            "description": "Rows per call. Max 20 for search/timelines, max 100 for follower/liker lists. Budget: ~650 bytes per post row measured, so 20 post rows is ~12 KB in one response. `count` and `cursor` are what bound it.",
            "type": "number"
          },
          "cursor": {
            "description": "Cursor returned by a previous call (`next`).",
            "type": "string"
          },
          "filter": {
            "additionalProperties": false,
            "description": "tweet_search filter. Provide query or at least one filter field. For a momentum question the useful trio is cashtags + minLikes + withinHours.",
            "properties": {
              "cashtags": {
                "description": "Tickers to search as X cashtags, with or without the $ (e.g. [\"WIF\", \"$BONK\"]). Normalized to $TICKER and merged into includeWords. This is THE param for \"who is talking about this token\".",
                "items": {
                  "type": "string"
                },
                "type": "array"
              },
              "endDate": {
                "description": "Inclusive ISO datetime end of the window.",
                "type": "string"
              },
              "excludeWords": {
                "description": "Words that must not appear - useful against airdrop/giveaway spam.",
                "items": {
                  "type": "string"
                },
                "type": "array"
              },
              "fromUsers": {
                "description": "Only posts by these accounts (with or without @).",
                "items": {
                  "type": "string"
                },
                "type": "array"
              },
              "hashtags": {
                "description": "Hashtags, with or without #.",
                "items": {
                  "type": "string"
                },
                "type": "array"
              },
              "includePhrase": {
                "description": "Exact phrase to search.",
                "type": "string"
              },
              "includeWords": {
                "description": "Words that must ALL appear. Passed to X verbatim, so operators and $TICKERS work here directly.",
                "items": {
                  "type": "string"
                },
                "type": "array"
              },
              "language": {
                "description": "Language code such as en or pl. Honoured by X.",
                "type": "string"
              },
              "list": {
                "description": "Twitter/X list id to search within.",
                "type": "string"
              },
              "maxId": {
                "description": "Tweet id before which to search.",
                "type": "string"
              },
              "mentions": {
                "description": "Only posts mentioning these accounts.",
                "items": {
                  "type": "string"
                },
                "type": "array"
              },
              "minLikes": {
                "description": "Minimum likes, 1 or more. The single most useful filter for momentum: an unfiltered cashtag sweep measured 15 of 19 rows at zero engagement. 10 is a useful floor. 0 is rejected - X's query builder drops a 0 floor instead of applying it.",
                "type": "number"
              },
              "minReplies": {
                "description": "Minimum replies, 1 or more (0 is rejected - it would be silently dropped).",
                "type": "number"
              },
              "minRetweets": {
                "description": "Minimum reposts, 1 or more (0 is rejected - it would be silently dropped).",
                "type": "number"
              },
              "onlyLinks": {
                "description": "Only posts with links/media.",
                "type": "boolean"
              },
              "onlyOriginal": {
                "description": "Exclude replies. It does NOT exclude quote-tweets - measured, 7 of 19 \"originals\" were quote-tweets. Check each row's `quoted` field.",
                "type": "boolean"
              },
              "onlyReplies": {
                "description": "Only replies.",
                "type": "boolean"
              },
              "onlyText": {
                "description": "Only text-only posts.",
                "type": "boolean"
              },
              "optionalWords": {
                "description": "Words where any may appear.",
                "items": {
                  "type": "string"
                },
                "type": "array"
              },
              "quoted": {
                "description": "Only posts quoting this tweet id.",
                "type": "string"
              },
              "sinceId": {
                "description": "Tweet id after which to search.",
                "type": "string"
              },
              "startDate": {
                "description": "Inclusive ISO datetime start of the window. Honoured by X. Use withinHours instead for a relative window.",
                "type": "string"
              },
              "toUsers": {
                "description": "Only posts addressed to these accounts.",
                "items": {
                  "type": "string"
                },
                "type": "array"
              },
              "top": {
                "description": "Select from X's TOP (relevance) result set instead of LATEST. It changes WHICH posts match, NOT their order - the client re-sorts every result newest-first regardless.",
                "type": "boolean"
              },
              "withinHours": {
                "description": "Only posts from the last N hours (1-720). Derives startDate for you and echoes the resolved UTC instant in filtersApplied. Rejected together with an explicit startDate - pass one or the other.",
                "type": "number"
              }
            },
            "type": "object"
          },
          "query": {
            "description": "Required for user_search. For tweet_search, shortcut text split on spaces into filter.includeWords (each word must appear).",
            "type": "string"
          },
          "sortBy": {
            "description": "Sort order for tweet_replies.",
            "enum": [
              "LATEST",
              "LIKES",
              "RELEVANCE"
            ],
            "type": "string"
          },
          "spaceId": {
            "description": "Twitter/X Space id.",
            "type": "string"
          },
          "tweetId": {
            "description": "Numeric tweet id.",
            "type": "string"
          },
          "userId": {
            "description": "Numeric Twitter/X user id.",
            "type": "string"
          },
          "username": {
            "description": "Twitter/X username, with or without @.",
            "type": "string"
          },
          "withListeners": {
            "description": "space_details only: include listener information.",
            "type": "boolean"
          },
          "withReplays": {
            "description": "space_details only: include replay information.",
            "type": "boolean"
          }
        },
        "required": [
          "action"
        ],
        "type": "object"
      },
      "kind": "internal",
      "publicName": "TwitterAccount",
      "readOnlyHint": true,
      "requiresEnv": "RETTIWT_API_KEY",
      "title": "Read Twitter accounts and posts"
    },
    {
      "alwaysLoad": true,
      "descriptionBytes": 1393,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "amountRaw": {
            "description": "apply_bps / token_amount_to_usd. The amount in BASE units as a digits-only string, read together with `decimals`.",
            "type": "string"
          },
          "bps": {
            "description": "apply_bps only. The rate in basis points, 0 to 10000 (1 bps = 0.01%, so 25 bps = 0.25% and 10000 bps = 100%).",
            "type": "number"
          },
          "decimals": {
            "description": "The token's decimals, 0 to 36 (get them from TokenFind - 6 for USDC, 18 for most ERC-20s). Required for apply_bps, usd_to_token_amount and token_amount_to_usd, and for unit_convert whenever `from` or `to` is 'raw' or 'human'.",
            "type": "number"
          },
          "from": {
            "description": "unit_convert only. The unit `value` is currently in. Must be in the same family as `to`: {wei,gwei,eth}, {lamports,sol}, or {raw,human}.",
            "enum": [
              "wei",
              "gwei",
              "eth",
              "lamports",
              "sol",
              "raw",
              "human"
            ],
            "type": "string"
          },
          "gasPriceWei": {
            "description": "gas_cost only. The gas PRICE in wei as a digits-only string (e.g. '22518000', which is 0.022518 gwei).",
            "type": "string"
          },
          "gasUnits": {
            "description": "gas_cost only. The gas UNIT count as a digits-only string (a unitless count, e.g. '1634838') - never a gas price.",
            "type": "string"
          },
          "op": {
            "description": "Which calculation to run. 'unit_convert' - restate one amount in another unit of the SAME family ({wei,gwei,eth} or {lamports,sol} or {raw,human}); exact, never floors, so it returns no remainder. 'gas_cost' - gasUnits x gasPriceWei, answered in wei, gwei and eth at once; exact, no remainder. 'apply_bps' - split an amount into fee and net at a basis-point rate; fee.remainder counts TEN-THOUSANDTHS of one base unit that the floor dropped (numerator over denominator 10000). 'usd_to_token_amount' - how many base units a USD figure buys; remainder is the leftover numerator over the divisor (priceUsd scaled to an integer), always smaller than the cost of one base unit. 'token_amount_to_usd' - what a base-unit amount is worth; usd is floored to 6 decimals (usdPrecision) and remainder is the leftover numerator over 10^(priceUsd decimals + token decimals), i.e. the value below one micro-dollar.",
            "enum": [
              "unit_convert",
              "gas_cost",
              "apply_bps",
              "usd_to_token_amount",
              "token_amount_to_usd"
            ],
            "type": "string"
          },
          "priceUsd": {
            "description": "usd_to_token_amount / token_amount_to_usd. The price of ONE whole token in USD, as a decimal string greater than zero, e.g. '0.00031415'.",
            "type": "string"
          },
          "to": {
            "description": "unit_convert only. The unit to restate the amount in. Same family as `from` - there is no fixed rate between wei and lamports, so a cross-family request is refused.",
            "enum": [
              "wei",
              "gwei",
              "eth",
              "lamports",
              "sol",
              "raw",
              "human"
            ],
            "type": "string"
          },
          "usd": {
            "description": "usd_to_token_amount only. The USD figure as a decimal string, e.g. '250.75'. No currency symbol and no thousands separators.",
            "type": "string"
          },
          "value": {
            "description": "unit_convert only. The amount to restate, as a string. Digits only when `from` is an atomic unit (wei/lamports/raw); a decimal like '0.0225' is allowed for gwei/eth/sol/human, but never finer than one atomic unit.",
            "type": "string"
          }
        },
        "required": [
          "op"
        ],
        "type": "object"
      },
      "kind": "internal",
      "publicName": "UnitsConvert",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Convert token amounts and units"
    },
    {
      "alwaysLoad": true,
      "descriptionBytes": 2032,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "chainIds": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Optional. Omit (or pass empty) to scan all supported chains (Khalani + local). To restrict, pass chain IDs/aliases either comma-separated ('ethereum,base,solana') or as an array (['ethereum','base'])."
          },
          "limit": {
            "description": "Optional. Caps the PRICED non-native rows per wallet snapshot, top-N by held USD value. The structural native SOL row and held tokens with no usable price are appended outside this limit; unpriced held tokens are marked `priceUnavailable` and capped at 20 with the surplus counted in `unpricedOmitted`. Only applied when response_format='concise'; ignored under the default 'detailed'. `tokenCount`/`totalUsd` always reflect the full scan.",
            "type": "number"
          },
          "response_format": {
            "description": "Response shape, 'concise' or 'detailed'. Default 'detailed'. 'detailed' returns every token per wallet; 'concise' is what enables the `limit` trim (top rows by held USD value) and sets `truncated` when rows were left out.",
            "enum": [
              "concise",
              "detailed"
            ],
            "type": "string"
          },
          "walletFamily": {
            "description": "Which wallet FAMILY to read - 'eip155', 'solana', or 'all'. Default 'all' aggregates your EVM + Solana wallets. This is a wallet family, not a chain: chains go in `chainIds`.",
            "enum": [
              "eip155",
              "solana",
              "all"
            ],
            "type": "string"
          }
        },
        "type": "object"
      },
      "kind": "internal",
      "publicName": "WalletBalances",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Read wallet balances across chains"
    },
    {
      "alwaysLoad": true,
      "descriptionBytes": 2030,
      "destructiveHint": true,
      "inputSchema": {
        "properties": {
          "intentId": {
            "description": "The intent id that `WalletEvmTransactionPrepare` returned. It is scoped to this session and to the exact proposal that was approved.",
            "type": "string"
          }
        },
        "required": [
          "intentId"
        ],
        "type": "object"
      },
      "kind": "internal",
      "publicName": "WalletEvmTransactionConfirm",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Broadcast a prepared EVM transaction"
    },
    {
      "alwaysLoad": true,
      "descriptionBytes": 1945,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "chain": {
            "description": "The EVM chain the transaction executes on. Chain slug/alias, or the numeric chain id `TokenFind` returns (e.g. `base` or `8453`).",
            "type": "string"
          },
          "data": {
            "description": "Transaction calldata as 0x-prefixed hex. Pass `0x` (the default) for a plain native transfer, which is accepted only when `to` has no contract code.",
            "type": "string"
          },
          "gasLimit": {
            "description": "Maximum gas UNITS, as a decimal integer string. The second call passes this plus EITHER the 1559 pair (`maxFeePerGasWei` with `maxPriorityFeePerGasWei`, required TOGETHER) OR the legacy `gasPriceWei` - never both pricing modes. REQUIRED IN EFFECT, optional in the schema: Vex never derives a spending limit from a network estimate, so a call without the caps is refused by name and returns the current estimate as a labelled hint you choose from. That refusal is the first call of the two-call workflow.",
            "type": "string"
          },
          "gasPriceWei": {
            "description": "LEGACY cap on the price per gas unit, in RAW wei as a decimal integer string, for chains without EIP-1559. Mutually exclusive with the 1559 pair. REQUIRED IN EFFECT, optional in the schema: Vex never derives a spending limit from a network estimate, so a call without the caps is refused by name and returns the current estimate as a labelled hint you choose from. That refusal is the first call of the two-call workflow.",
            "type": "string"
          },
          "maxFeePerGasWei": {
            "description": "EIP-1559 cap on the total price per gas unit, in RAW wei as a decimal integer string. Pass this with maxPriorityFeePerGasWei, or pass gasPriceWei instead, never both. REQUIRED IN EFFECT, optional in the schema: Vex never derives a spending limit from a network estimate, so a call without the caps is refused by name and returns the current estimate as a labelled hint you choose from. That refusal is the first call of the two-call workflow.",
            "type": "string"
          },
          "maxPriorityFeePerGasWei": {
            "description": "EIP-1559 cap on the validator tip per gas unit, in RAW wei as a decimal integer string. Cannot exceed maxFeePerGasWei. REQUIRED IN EFFECT, optional in the schema: Vex never derives a spending limit from a network estimate, so a call without the caps is refused by name and returns the current estimate as a labelled hint you choose from. That refusal is the first call of the two-call workflow.",
            "type": "string"
          },
          "to": {
            "description": "Destination address (0x, 20 bytes): the contract being called, or the recipient for a plain native transfer.",
            "type": "string"
          },
          "valueWei": {
            "description": "Native coin to send with the call, in RAW wei as a decimal integer STRING (never a human decimal such as \"0.1\", never a JSON number). Defaults to \"0\". Must be \"0\" for a contract call: every function in the v1 decode set is non-payable.",
            "type": "string"
          }
        },
        "required": [
          "chain",
          "to"
        ],
        "type": "object"
      },
      "kind": "internal",
      "publicName": "WalletEvmTransactionPrepare",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Prepare an EVM transaction"
    },
    {
      "alwaysLoad": true,
      "descriptionBytes": 1900,
      "destructiveHint": true,
      "inputSchema": {
        "properties": {
          "intentId": {
            "description": "Prepared intent ID",
            "type": "string"
          },
          "walletFamily": {
            "description": "Which wallet FAMILY to act on - 'eip155' for your EVM wallet or 'solana' for your Solana wallet. This is a wallet family, not a chain: the EVM chain goes in `chain`.",
            "enum": [
              "eip155",
              "solana"
            ],
            "type": "string"
          }
        },
        "required": [
          "walletFamily",
          "intentId"
        ],
        "type": "object"
      },
      "kind": "internal",
      "publicName": "WalletSendConfirm",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Broadcast a prepared wallet transfer"
    },
    {
      "alwaysLoad": true,
      "descriptionBytes": 1680,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "amountIn": {
            "description": "Amount of the token to send (for native/ERC-20), or \"1\" for an ERC-721. In HUMAN decimal units (e.g. \"1.5\") - not wei, lamports, or any other base unit.",
            "type": "string"
          },
          "chain": {
            "description": "Required for eip155. EVM chain ID or alias (e.g. 'polygon', '137', 'base'). Ignored for solana.",
            "type": "string"
          },
          "to": {
            "description": "Recipient address",
            "type": "string"
          },
          "token": {
            "description": "Token: 'native' for chain native, contract address for ERC-20, 'nft:{contract}:{tokenId}' for ERC-721. Solana: symbol or mint (SOL + SPL only, NFT not supported).",
            "type": "string"
          },
          "walletFamily": {
            "description": "Which wallet FAMILY to act on - 'eip155' for your EVM wallet or 'solana' for your Solana wallet. This is a wallet family, not a chain: the EVM chain goes in `chain`.",
            "enum": [
              "eip155",
              "solana"
            ],
            "type": "string"
          }
        },
        "required": [
          "walletFamily",
          "to",
          "amountIn"
        ],
        "type": "object"
      },
      "kind": "internal",
      "publicName": "WalletSendPrepare",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Prepare a wallet transfer"
    },
    {
      "alwaysLoad": true,
      "descriptionBytes": 1815,
      "destructiveHint": true,
      "inputSchema": {
        "properties": {
          "intentId": {
            "description": "The intent id that `WalletSolanaTransactionPrepare` returned. It is scoped to this session and to the exact canonical message that was approved.",
            "type": "string"
          }
        },
        "required": [
          "intentId"
        ],
        "type": "object"
      },
      "kind": "internal",
      "publicName": "WalletSolanaTransactionConfirm",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Broadcast a prepared Solana transaction"
    },
    {
      "alwaysLoad": true,
      "descriptionBytes": 1898,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "computeUnitLimit": {
            "description": "Cap on the requested COMPUTE UNITS, as a decimal integer string. REQUIRED IN EFFECT, optional in the schema, and required TOGETHER with computeUnitPriceMicroLamports: a call without the caps is refused by name and returns the current network estimate as a labelled hint, which is the first call of the two-call workflow. The priority fee is charged on the requested limit rather than on units actually used, so this is part of what you authorize.",
            "type": "string"
          },
          "computeUnitPriceMicroLamports": {
            "description": "Cap on the priority price per compute unit, in RAW micro-lamports as a decimal integer string. REQUIRED IN EFFECT, optional in the schema, and required TOGETHER with computeUnitLimit; the two fix the maximum priority fee in lamports, which is echoed back in approvedFeeBounds.",
            "type": "string"
          },
          "transactionBase64": {
            "description": "The UNSIGNED Solana transaction or transaction message, base64 encoded. A proposal that already carries a signature is refused, because preparing installs a fresh blockhash and would invalidate it.",
            "type": "string"
          }
        },
        "required": [
          "transactionBase64"
        ],
        "type": "object"
      },
      "kind": "internal",
      "publicName": "WalletSolanaTransactionPrepare",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Prepare a Solana transaction"
    },
    {
      "alwaysLoad": true,
      "descriptionBytes": 1044,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "action": {
            "description": "pin adds a token to tracking, unpin removes it, list shows the chain's seed + pinned set.",
            "enum": [
              "pin",
              "unpin",
              "list"
            ],
            "type": "string"
          },
          "chain": {
            "description": "Local chain alias or id (e.g. 'robinhood', '4663').",
            "type": "string"
          },
          "token": {
            "description": "ERC-20 contract ADDRESS (0x…). Required for pin/unpin; ignored for list. Resolve a symbol to its address first.",
            "type": "string"
          }
        },
        "required": [
          "action",
          "chain"
        ],
        "type": "object"
      },
      "kind": "internal",
      "publicName": "WalletTrackToken",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Track a token in the local wallet view"
    },
    {
      "alwaysLoad": true,
      "descriptionBytes": 2023,
      "destructiveHint": true,
      "inputSchema": {
        "properties": {
          "intentId": {
            "description": "The intent id that `WalletWrapPrepare` returned. It is scoped to this session and to the exact conversion that was approved.",
            "type": "string"
          }
        },
        "required": [
          "intentId"
        ],
        "type": "object"
      },
      "kind": "internal",
      "publicName": "WalletWrapConfirm",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Broadcast a prepared wrap or unwrap"
    },
    {
      "alwaysLoad": true,
      "descriptionBytes": 2022,
      "destructiveHint": false,
      "inputSchema": {
        "properties": {
          "amountRaw": {
            "description": "How much to convert, in the token's SMALLEST BASE UNITS, as decimal digits only (18 decimals on every verified chain today, so \"1000000000000000000\" is one whole unit). Never a human decimal such as \"1.5\", never a JSON number, never a unit suffix. Must be greater than zero.",
            "type": "string"
          },
          "chain": {
            "description": "The EVM chain the conversion executes on. Only chains with a verified wrapped-native contract are accepted; today those are ethereum, optimism, bsc, polygon, robinhood, base, arbitrum and avalanche. Chain slug/alias, or the numeric chain id `TokenFind` returns (e.g. `base` or `8453`).",
            "type": "string"
          },
          "direction": {
            "description": "`wrap` turns native currency into its wrapped ERC-20 form and spends native; `unwrap` turns the wrapped ERC-20 back into native and spends the token. Nothing else is accepted, and the value is refused by name rather than guessed.",
            "enum": [
              "wrap",
              "unwrap"
            ],
            "type": "string"
          },
          "gasLimit": {
            "description": "Maximum gas UNITS, as a decimal integer string. The second call passes this plus EITHER the 1559 pair (`maxFeePerGasWei` with `maxPriorityFeePerGasWei`, required TOGETHER) OR the legacy `gasPriceWei` - never both pricing modes. REQUIRED IN EFFECT, optional in the schema: Vex never derives a spending limit from a network estimate, so a call without the caps is refused by name and returns the current estimate as a labelled hint. That refusal is the first call of the two-call workflow.",
            "type": "string"
          },
          "gasPriceWei": {
            "description": "LEGACY cap on the price per gas unit, in RAW wei as a decimal integer string, for chains without EIP-1559. Mutually exclusive with the 1559 pair. REQUIRED IN EFFECT, optional in the schema: Vex never derives a spending limit from a network estimate, so a call without the caps is refused by name and returns the current estimate as a labelled hint. That refusal is the first call of the two-call workflow.",
            "type": "string"
          },
          "maxFeePerGasWei": {
            "description": "EIP-1559 cap on the total price per gas unit, in RAW wei as a decimal integer string. Pass this with maxPriorityFeePerGasWei, or pass gasPriceWei instead, never both. REQUIRED IN EFFECT, optional in the schema: Vex never derives a spending limit from a network estimate, so a call without the caps is refused by name and returns the current estimate as a labelled hint. That refusal is the first call of the two-call workflow.",
            "type": "string"
          },
          "maxPriorityFeePerGasWei": {
            "description": "EIP-1559 cap on the validator tip per gas unit, in RAW wei as a decimal integer string. Cannot exceed maxFeePerGasWei. REQUIRED IN EFFECT, optional in the schema: Vex never derives a spending limit from a network estimate, so a call without the caps is refused by name and returns the current estimate as a labelled hint. That refusal is the first call of the two-call workflow.",
            "type": "string"
          }
        },
        "required": [
          "chain",
          "direction",
          "amountRaw"
        ],
        "type": "object"
      },
      "kind": "internal",
      "publicName": "WalletWrapPrepare",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Prepare a native / wrapped-native conversion"
    },
    {
      "alwaysLoad": true,
      "descriptionBytes": 1812,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "name": {
            "description": "The ONE tool name to describe: a hot-set name, a protocol publicName, or the `mcp__vex__<publicName>` form your client shows. The prefix is stripped; anything else is answered with the nearest names in the catalogue.",
            "type": "string"
          }
        },
        "required": [
          "name"
        ],
        "type": "object"
      },
      "kind": "internal",
      "publicName": "vex_ToolDescribe",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Read one tool's whole contract"
    },
    {
      "alwaysLoad": true,
      "descriptionBytes": 1500,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "limit": {
            "description": "Maximum rows to return, 1 to 20. Applies to both modes: default 5 with `query`, and the whole namespace with `namespace` when omitted. `totalCount` and `hasMore` report what a limit left out; there is no cursor, so reach the rest by raising this limit or by narrowing with `namespace` alone or a tighter query. Out-of-range values are refused, not clamped.",
            "type": "number"
          },
          "namespace": {
            "description": "A protocol namespace to list in full, for example \"pendle\". Mutually exclusive with `query`; exactly one of the two is required.",
            "type": "string"
          },
          "query": {
            "description": "An intent phrase to search for. Mutually exclusive with `namespace`; exactly one of the two is required.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "kind": "internal",
      "publicName": "vex_ToolSearch",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Search the protocol tool catalog"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 5291,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "beforeBlock": {
            "description": "Continue a truncated answer: pass back its nextBeforeBlock value unchanged. It is the provider's own EXCLUSIVE block anchor, so the continued walk starts at the bar below the oldest one the previous answer returned and repeats nothing. It is the only continuation this tool has, and it is refused together with endAtMs because both decide where the walk starts and honouring one silently would answer a different window than the one asked for.",
            "type": "number"
          },
          "chain": {
            "anyOf": [
              {
                "minLength": 1,
                "type": "string"
              },
              {
                "minimum": 1,
                "type": "integer"
              }
            ],
            "description": "Chain slug the pool lives on, for example solana, ethereum, bsc, base or robinhood. Chain slug/alias, or the numeric chain id `TokenFind` returns (e.g. `base` or `8453`). A pair or token address is only unique within one chain, so this is required; an unknown slug is refused by name with the nearest catalog matches rather than answered with zero rows."
          },
          "deadlineMs": {
            "description": "Wall-clock budget for the whole walk, in milliseconds. Defaults to 25000, ceiling 120000. Hitting it is reported exactly like the page budget: covered range plus a cursor, never a short answer presented as a complete one.",
            "type": "number"
          },
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "endAtMs": {
            "description": "Newest instant to cover, in epoch MILLISECONDS. Omit it for the latest bars. This is resolved internally to a block through the nearest PRIOR trade, so an arbitrary historical window costs two requests rather than a page walk (measured: a 90-day-old window in 980 ms and 33 KB against 1,582 ms and 436 KB walking). The anchor is APPROXIMATE by contract because trades are not evenly spaced: anchorResolvedAtMs and anchorDistanceMs report where it actually landed (measured 393 seconds early on one target), and an anchor that is missing or too far away falls back to walking backward from now with anchorFallback set.",
            "type": "number"
          },
          "fields": {
            "description": "Comma-separated column GROUPS, not individual field names. Supported groups: ohlc, volume, blockRange. Defaults to ohlc. volume adds the USD volume column; blockRange adds each bar's anchor block, which is what an exact continuation is built from. It emits blockMin and blockMax and THEY ARE THE SAME NUMBER: measured equal on 383 of 383 bars across both chains, three resolutions and both transports, while consecutive daily bars sat 7,173 blocks apart. Read blockMin as the bar's first block and ignore blockMax; neither is the bar's last block and the pair does not delimit a range. Base and quote token volumes and any volume-weighted average price are deliberately NOT offered: the provider's token volumes are raw fixed-point strings and it publishes no token decimals here, so all three would be wrong by a power of ten.",
            "type": "string"
          },
          "inverted": {
            "description": "Quote per base instead of base per quote. Defaults to false. The inversion is applied against the pool's OWN quote token, which the tool resolves: there is no quote parameter because sending a wrong one returns a valid-looking, silently inverted series with no marker of any kind (measured).",
            "type": "boolean"
          },
          "limit": {
            "description": "How many bars to return, 1 to 999. Defaults to 100. 999 is the PROVIDER's page size, not a Vex cap. It bounds rows RETURNED; a wide startAtMs range is what triggers an internal page walk, and the walk is reported separately in providerWindow. The two numbers measure different things. 999 hourly bars decode to about 271 KB of provider bytes, which is why the default is modest and the rows are column-oriented. When it holds back bars that were inside the requested range, the answer reports truncated true with barsWithheldByLimit and a nextBeforeBlock that continues from the OLDEST BAR RETURNED, never from the oldest bar fetched: a cursor below the fetched floor would make the withheld bars unreachable.",
            "type": "number"
          },
          "maxPages": {
            "description": "How many provider pages of up to 999 bars the backward walk may fetch. Defaults to 10; raise it for a deep historical range. There is no upper ceiling on it: deadlineMs is the real bound on a walk. Hitting either is reported as truncated with the covered range and a nextBeforeBlock cursor, so every bar not returned is reachable by asking again.",
            "type": "number"
          },
          "pairAddress": {
            "description": "The pool (pair) address. Give this when you already know the exact pool, from dexscreener__pairs_search, a screening board, or dexscreener__pair_get. Either this or tokenAddress is required. When both are given, pairAddress wins and the response says so rather than silently ignoring the other.",
            "type": "string"
          },
          "priceBasis": {
            "description": "Which price columns to return: usd, native (the quote token), or both. Defaults to usd. native is the pool's own arithmetic and has no exchange-rate dependence; usd is the one most questions mean. Every value is a DECIMAL STRING, never a floating-point number.",
            "enum": [
              "usd",
              "native",
              "both"
            ],
            "type": "string"
          },
          "resolution": {
            "description": "Bar size, one of the 18 the provider serves: 1s, 5s, 15s, 30s, 1m, 3m, 5m, 15m, 30m, 1h, 2h, 4h, 8h, 12h, 1d, 3d, 1w, 1mo. Defaults to 1h. Second-scale series are SPARSE by nature and not a data gap: 5s bars were measured with a median 50-second and maximum 1,600-second spacing, so summary.gapCount reports the bars the provider did not emit inside the requested range rather than letting a sparse series read as a continuous one. Which transport serves a resolution is a provider fact the tool hides: daily and above, and 5s, exist only on the WebSocket channel.",
            "enum": [
              "1s",
              "5s",
              "15s",
              "30s",
              "1m",
              "3m",
              "5m",
              "15m",
              "30m",
              "1h",
              "2h",
              "4h",
              "8h",
              "12h",
              "1d",
              "3d",
              "1w",
              "1mo"
            ],
            "type": "string"
          },
          "series": {
            "description": "price for the price series, marketCap for a market-capitalisation series. Defaults to price. Market-cap bars need NO supply argument: the provider computes them, and no circulating supply is sent or assumed.",
            "enum": [
              "price",
              "marketCap"
            ],
            "type": "string"
          },
          "startAtMs": {
            "description": "Oldest instant to cover, in epoch MILLISECONDS. The provider is paged backward until this is covered or a bound is hit. When given, it takes precedence over limit: with no explicit limit the row bound becomes the provider's full 999-bar page instead of the modest default, so a requested range is never quietly reduced to the newest few bars. An explicit limit is still honoured, and every in-range bar it holds back sets truncated true and moves nextBeforeBlock to the oldest bar returned, so the rest is reached by asking again. coveredRange against requestedRange states exactly how much of what you asked for arrived.",
            "type": "number"
          },
          "tokenAddress": {
            "description": "A token contract address, resolved to the deepest pool of the provider's bounded 30-row search window on this chain. Use it when you have a token but not a pool. resolutionBasis echoes that a resolution happened and resolutionNote states the bound: \"deepest\" means deepest among at most 30 pools the provider chose to return, never a claim about every pool the token trades in. For the full pool list call dexscreener__token_pairs_list.",
            "type": "string"
          }
        },
        "required": [
          "chain"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "dexscreener",
      "publicName": "dexscreener__candles_list",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Read a pair's OHLCV candles",
      "toolId": "dexscreener.candles"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 2946,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "chain": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "minimum": 1,
                "type": "integer"
              }
            ],
            "description": "Narrow the catalog to one chain. Omit it to list every supported chain. Narrowing does not add detail: every row carries that chain's complete DEX list either way, so this only changes how many rows come back. Chain slug/alias, or the numeric chain id `TokenFind` returns (e.g. `base` or `8453`)."
          },
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          }
        },
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "dexscreener",
      "publicName": "dexscreener__chains_list",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "List DEX Screener chains",
      "toolId": "dexscreener.chains"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 2711,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "baseTokenSuffixes": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Base-token mint-address suffixes to keep, the vanity-suffix convention some launchpads use. Measured on solana: pump matched 20,852 pairs and bonk matched 1,595. Accepts either a comma-separated string (\"a,b\") or an array of strings ([\"a\",\"b\"]); the two are equivalent."
          },
          "chainIds": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "DexScreener chain slugs to screen, for example solana, base, ethereum, bsc. Omit to screen every indexed chain at once. Accepts either a comma-separated string (\"a,b\") or an array of strings ([\"a\",\"b\"]); the two are equivalent. Chain and dex values come from dexscreener__chains_list; an unknown chain is refused by name with the nearest catalog matches, because the provider answers one with zero rows and HTTP 200, which is indistinguishable from a real empty result."
          },
          "dexIds": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "DEX identifiers to keep, for example raydium, uniswap, pumpswap. Multiple values are ORed: a pair on any listed dex is kept. The accepted set is open and per-chain, so read it from dexscreener__chains_list rather than guessing. Accepts either a comma-separated string (\"a,b\") or an array of strings ([\"a\",\"b\"]); the two are equivalent."
          },
          "disableQualityFloor": {
            "description": "Drop EVERY default quality floor this tool applies, in one flag. Defaults to false. Your own explicit thresholds still apply, so disableQualityFloor with minLiquidityUsd 5000 means \"none of the tool's defaults, but keep mine\". Each dropped default is named in floorAccounting and qualityFloorApplied becomes false. Expect the unfloored result to contain arithmetic artifacts: on the price-change boards the top rows without a floor are billion-percent moves on pairs nobody can trade. IT DOES NOT READMIT BONDING-CURVE PAIRS, and that is the half agents miss: dropping the floors is necessary but not sufficient, because the provider excludes bonding-curve VENUES by default and only includeLaunchpadPairs lifts that. Measured on solana: a launchpad filter without the lift returned 0 on-curve pairs, and with the lift 71,334; adding a 25,000 USD liquidity floor to those 71,334 returned 0 again, because on-curve rows carry no liquidity field at all. For on-curve pairs, send includeLaunchpadPairs AND keep every liquidity threshold off.",
            "type": "boolean"
          },
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "excludeDexIds": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "DEX identifiers to drop. DANGEROUS BY CONSTRUCTION: the provider applies a hidden default that already excludes bonding-curve venues, and sending this key AT ALL replaces that default, so excluding one dex can make the result set BIGGER. Measured on solana: 53,094 rows normally, 84,058 when only PumpSwap was excluded. Use includeLaunchpadPairs for the common case; exclusionDefaultReplaced reports whenever this happened. Accepts either a comma-separated string (\"a,b\") or an array of strings ([\"a\",\"b\"]); the two are equivalent."
          },
          "fields": {
            "description": "Comma-separated row field GROUPS to return, not individual field names. Supported groups: core, flow, reserves, allWindows, profile, launchpad, identity. Defaults to core, which carries the chain, dex, pair address, base-token address and symbols, the selected window's price, volume, liquidity, market cap, age, counts and every derived ratio. The two heaviest are profile (unbounded issuer-authored prose and links) and allWindows (four times the per-row metric payload). The `identity` group is NOT part of core and is what adds ammId, quoteTokenAddress, both tokens' decimals, priceNative and pairCreatedAtMs - the fields that let you tell a real pool from a mispriced one, so ask for it when the dollar columns are what you are judging. An unknown group name is refused with the full list rather than ignored.",
            "type": "string"
          },
          "includeInactive": {
            "description": "Keep pairs that had no activity inside the selected window. Defaults to false. Measured on solana at m5: 2,513 rows becomes 53,088, so this widens the set enormously on short windows and barely at all on h24.",
            "type": "boolean"
          },
          "includeLaunchpadPairs": {
            "description": "Lift the provider's hidden exclusion of bonding-curve pairs, the safe alternative to excludeDexIds. Defaults to false. Measured on solana: 53,094 rows becomes 102,676. Named for the effect rather than the mechanism, because the mechanism is an empty exclusion list.",
            "type": "boolean"
          },
          "labels": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Pool label values to keep, for example v2, v3, v4, CLMM, DLMM, CPMM. Matching is case-insensitive on the provider. Accepts either a comma-separated string (\"a,b\") or an array of strings ([\"a\",\"b\"]); the two are equivalent."
          },
          "launchpadIds": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Launchpad identifiers to keep, for example pumpfun, launchlab, meteoradbc, bags, fourmeme. MEASURED: the provider attaches a launchpad id to a pair only AFTER it graduates, so this filter matches graduated pairs and nothing else. While a curve is still running the launchpad is the pair's DEX, so use dexIds with the same value there (dexIds: pumpfun). On dexscreener__launchpad_pairs_list, pairing this with stage: \"bonding\" is refused by name rather than answered with an empty board: it returned 0 rows of a 0-row population while dexIds returned 53,478. KNOWN DEPTH GAP: no tool on this surface enumerates the launchpad vocabulary - dexscreener__chains_list carries dex ids only - so the five spellings above are the list, measured live, and an id outside them is not refused by name the way an unknown chain slug is. It returns an empty board instead, so check your spelling against those five before reading a zero as an absence of launches. Accepts either a comma-separated string (\"a,b\") or an array of strings ([\"a\",\"b\"]); the two are equivalent."
          },
          "limit": {
            "description": "How many rows to return, from 1 to 100. Defaults to 20. 100 is the provider's own page size, so asking for more than that is refused rather than silently served short; use offset to walk further into the ranking.",
            "type": "number"
          },
          "maxBoostCount": {
            "description": "Ceiling on the number of paid boosts ACTIVE on the pair, as a COUNT. It bounds WITHIN the boosted population and does not select against it. The presence rule below is the whole story here: an active-boost count exists only on a boosted pair, so maxBoostCount 3 means \"is boosted AND has at most 3 boosts\", never \"is not heavily advertised\". Measured 2026-08-24 on solana: min 1 gave 105 rows (the boosted population), max 10 gave 51, max 1000000 gave 105 again rather than the 64,420-row baseline, and max 0 gave 0. THIS IS NOT A WAY TO EXCLUDE ADS: an agent setting it to avoid ad-pumped pairs receives only ad-carrying pairs. To judge advertising, leave it unset and read boostsActive per row, which is on every row by default. PRESENCE RULE, measured, and it applies to every ceiling on this surface: a maximum matches only rows that CARRY the field. A row the provider reported no value for is excluded, not kept as if it were below the bound. Measured on a 64,420-row solana population: a liquidity ceiling removed 1,687 rows that carry no liquidity figure, an FDV ceiling removed 3,286, and a launchpad-progress ceiling reduced the set to launchpad pairs only. So a ceiling narrows on two axes at once, and the second one is silent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "maxBuyCount": {
            "description": "Ceiling on the number of BUY trades, as a COUNT. Measured over thresholdWindow, which defaults to window. One window per threshold family is reachable here: the provider itself accepts several windows of one family at once and ANDs them (a 1,000,000 USD h24 volume floor combined with a 1,000 USD m5 volume floor was measured returning 104 rows), but this surface carries a single thresholdWindow, so a two-window condition on one metric has to be checked against the returned rows instead of filtered for. PRESENCE RULE, measured, and it applies to every ceiling on this surface: a maximum matches only rows that CARRY the field. A row the provider reported no value for is excluded, not kept as if it were below the bound. Measured on a 64,420-row solana population: a liquidity ceiling removed 1,687 rows that carry no liquidity figure, an FDV ceiling removed 3,286, and a launchpad-progress ceiling reduced the set to launchpad pairs only. So a ceiling narrows on two axes at once, and the second one is silent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "maxFdvUsd": {
            "description": "Ceiling on fully diluted valuation, in US dollars. FDV values the total supply rather than the circulating supply, so it is the larger number. PRESENCE RULE, measured, and it applies to every ceiling on this surface: a maximum matches only rows that CARRY the field. A row the provider reported no value for is excluded, not kept as if it were below the bound. Measured on a 64,420-row solana population: a liquidity ceiling removed 1,687 rows that carry no liquidity figure, an FDV ceiling removed 3,286, and a launchpad-progress ceiling reduced the set to launchpad pairs only. So a ceiling narrows on two axes at once, and the second one is silent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "maxLaunchpadProgressPct": {
            "description": "Ceiling on bonding-curve completion, as a PERCENT of the curve. Below 100 keeps tokens still on the curve. Only launchpad pairs carry it. PRESENCE RULE, measured, and it applies to every ceiling on this surface: a maximum matches only rows that CARRY the field. A row the provider reported no value for is excluded, not kept as if it were below the bound. Measured on a 64,420-row solana population: a liquidity ceiling removed 1,687 rows that carry no liquidity figure, an FDV ceiling removed 3,286, and a launchpad-progress ceiling reduced the set to launchpad pairs only. So a ceiling narrows on two axes at once, and the second one is silent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "maxLiquidityUsd": {
            "description": "Ceiling on pool liquidity, in US dollars. Useful for finding small pools deliberately; liquidity has no window. PRESENCE RULE, measured, and it applies to every ceiling on this surface: a maximum matches only rows that CARRY the field. A row the provider reported no value for is excluded, not kept as if it were below the bound. Measured on a 64,420-row solana population: a liquidity ceiling removed 1,687 rows that carry no liquidity figure, an FDV ceiling removed 3,286, and a launchpad-progress ceiling reduced the set to launchpad pairs only. So a ceiling narrows on two axes at once, and the second one is silent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "maxMarketCapUsd": {
            "description": "Ceiling on market capitalisation, in US dollars. The provider's own circulating-supply valuation, with no window. PRESENCE RULE, measured, and it applies to every ceiling on this surface: a maximum matches only rows that CARRY the field. A row the provider reported no value for is excluded, not kept as if it were below the bound. Measured on a 64,420-row solana population: a liquidity ceiling removed 1,687 rows that carry no liquidity figure, an FDV ceiling removed 3,286, and a launchpad-progress ceiling reduced the set to launchpad pairs only. So a ceiling narrows on two axes at once, and the second one is silent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "maxPairAgeSeconds": {
            "description": "Ceiling on pair age, in SECONDS since the pair was created. 86400 is one day. The provider filters in hours, so sub-hour precision comes from the returned pairAgeSeconds rather than from this filter. PRESENCE RULE, measured, and it applies to every ceiling on this surface: a maximum matches only rows that CARRY the field. A row the provider reported no value for is excluded, not kept as if it were below the bound. Measured on a 64,420-row solana population: a liquidity ceiling removed 1,687 rows that carry no liquidity figure, an FDV ceiling removed 3,286, and a launchpad-progress ceiling reduced the set to launchpad pairs only. So a ceiling narrows on two axes at once, and the second one is silent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "maxPriceChangePct": {
            "description": "Ceiling on price change, as a PERCENT and not a fraction. Measured over thresholdWindow, which defaults to window. One window per threshold family is reachable here: the provider itself accepts several windows of one family at once and ANDs them (a 1,000,000 USD h24 volume floor combined with a 1,000 USD m5 volume floor was measured returning 104 rows), but this surface carries a single thresholdWindow, so a two-window condition on one metric has to be checked against the returned rows instead of filtered for. Negative values are legal, so -50 keeps only pairs down at least half. PRESENCE RULE, measured, and it applies to every ceiling on this surface: a maximum matches only rows that CARRY the field. A row the provider reported no value for is excluded, not kept as if it were below the bound. Measured on a 64,420-row solana population: a liquidity ceiling removed 1,687 rows that carry no liquidity figure, an FDV ceiling removed 3,286, and a launchpad-progress ceiling reduced the set to launchpad pairs only. So a ceiling narrows on two axes at once, and the second one is silent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "maxSellCount": {
            "description": "Ceiling on the number of SELL trades, as a COUNT. Measured over thresholdWindow, which defaults to window. One window per threshold family is reachable here: the provider itself accepts several windows of one family at once and ANDs them (a 1,000,000 USD h24 volume floor combined with a 1,000 USD m5 volume floor was measured returning 104 rows), but this surface carries a single thresholdWindow, so a two-window condition on one metric has to be checked against the returned rows instead of filtered for. PRESENCE RULE, measured, and it applies to every ceiling on this surface: a maximum matches only rows that CARRY the field. A row the provider reported no value for is excluded, not kept as if it were below the bound. Measured on a 64,420-row solana population: a liquidity ceiling removed 1,687 rows that carry no liquidity figure, an FDV ceiling removed 3,286, and a launchpad-progress ceiling reduced the set to launchpad pairs only. So a ceiling narrows on two axes at once, and the second one is silent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "maxTxnCount": {
            "description": "Ceiling on the number of trades, as a COUNT and not a volume. Measured over thresholdWindow, which defaults to window. One window per threshold family is reachable here: the provider itself accepts several windows of one family at once and ANDs them (a 1,000,000 USD h24 volume floor combined with a 1,000 USD m5 volume floor was measured returning 104 rows), but this surface carries a single thresholdWindow, so a two-window condition on one metric has to be checked against the returned rows instead of filtered for. PRESENCE RULE, measured, and it applies to every ceiling on this surface: a maximum matches only rows that CARRY the field. A row the provider reported no value for is excluded, not kept as if it were below the bound. Measured on a 64,420-row solana population: a liquidity ceiling removed 1,687 rows that carry no liquidity figure, an FDV ceiling removed 3,286, and a launchpad-progress ceiling reduced the set to launchpad pairs only. So a ceiling narrows on two axes at once, and the second one is silent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "maxVolumeUsd": {
            "description": "Ceiling on traded volume, in US dollars. Measured over thresholdWindow, which defaults to window. One window per threshold family is reachable here: the provider itself accepts several windows of one family at once and ANDs them (a 1,000,000 USD h24 volume floor combined with a 1,000 USD m5 volume floor was measured returning 104 rows), but this surface carries a single thresholdWindow, so a two-window condition on one metric has to be checked against the returned rows instead of filtered for. PRESENCE RULE, measured, and it applies to every ceiling on this surface: a maximum matches only rows that CARRY the field. A row the provider reported no value for is excluded, not kept as if it were below the bound. Measured on a 64,420-row solana population: a liquidity ceiling removed 1,687 rows that carry no liquidity figure, an FDV ceiling removed 3,286, and a launchpad-progress ceiling reduced the set to launchpad pairs only. So a ceiling narrows on two axes at once, and the second one is silent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "metaIds": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Narrative IDs to keep, taken from dexscreener__narratives_list. Send the ID and not the human-readable name: the provider accepts a name, matches nothing, and answers with zero rows, which reads as an empty market rather than as a wrong value. Accepts either a comma-separated string (\"a,b\") or an array of strings ([\"a\",\"b\"]); the two are equivalent."
          },
          "minBoostCount": {
            "description": "Floor on the number of paid boosts ACTIVE on the pair, as a COUNT. A boost is bought visibility, never demand or safety. This is more specific than onlyBoosted and wins over it when both are sent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "minBuyCount": {
            "description": "Floor on the number of BUY trades, as a COUNT. Measured over thresholdWindow, which defaults to window. One window per threshold family is reachable here: the provider itself accepts several windows of one family at once and ANDs them (a 1,000,000 USD h24 volume floor combined with a 1,000 USD m5 volume floor was measured returning 104 rows), but this surface carries a single thresholdWindow, so a two-window condition on one metric has to be checked against the returned rows instead of filtered for. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "minFdvUsd": {
            "description": "Floor on fully diluted valuation, in US dollars. FDV values the total supply rather than the circulating supply, so it is the larger number. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "minLaunchpadProgressPct": {
            "description": "Floor on bonding-curve completion, as a PERCENT of the curve. 100 means the token has graduated and migrated to a DEX. Only launchpad pairs carry it. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "minLiquidityUsd": {
            "description": "Floor on pool liquidity, in US dollars. Liquidity has no window: it is the pool's depth right now. Bonding-curve pairs carry no liquidity field at all, so any liquidity threshold excludes them by construction. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once. This board applies 250,000 USD by default.",
            "type": "number"
          },
          "minMarketCapUsd": {
            "description": "Floor on market capitalisation, in US dollars. The provider's own circulating-supply valuation, with no window. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "minPairAgeSeconds": {
            "description": "Floor on pair age, in SECONDS since the pair was created. The provider filters age in whole and fractional hours, so this is converted for you; the exact age comes back per row in pairAgeSeconds. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "minPriceChangePct": {
            "description": "Floor on price change, as a PERCENT and not a fraction. Measured over thresholdWindow, which defaults to window. One window per threshold family is reachable here: the provider itself accepts several windows of one family at once and ANDs them (a 1,000,000 USD h24 volume floor combined with a 1,000 USD m5 volume floor was measured returning 104 rows), but this surface carries a single thresholdWindow, so a two-window condition on one metric has to be checked against the returned rows instead of filtered for. 10 means ten percent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "minSellCount": {
            "description": "Floor on the number of SELL trades, as a COUNT. Measured over thresholdWindow, which defaults to window. One window per threshold family is reachable here: the provider itself accepts several windows of one family at once and ANDs them (a 1,000,000 USD h24 volume floor combined with a 1,000 USD m5 volume floor was measured returning 104 rows), but this surface carries a single thresholdWindow, so a two-window condition on one metric has to be checked against the returned rows instead of filtered for. A sell-side floor is what separates a real two-sided market from a pair nobody can exit. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once. This board applies 30 over h24 by default.",
            "type": "number"
          },
          "minTxnCount": {
            "description": "Floor on the number of trades, as a COUNT and not a volume. Measured over thresholdWindow, which defaults to window. One window per threshold family is reachable here: the provider itself accepts several windows of one family at once and ANDs them (a 1,000,000 USD h24 volume floor combined with a 1,000 USD m5 volume floor was measured returning 104 rows), but this surface carries a single thresholdWindow, so a two-window condition on one metric has to be checked against the returned rows instead of filtered for. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once. This board applies 300 over h24 by default.",
            "type": "number"
          },
          "minVolumeUsd": {
            "description": "Floor on traded volume, in US dollars. Measured over thresholdWindow, which defaults to window. One window per threshold family is reachable here: the provider itself accepts several windows of one family at once and ANDs them (a 1,000,000 USD h24 volume floor combined with a 1,000 USD m5 volume floor was measured returning 104 rows), but this surface carries a single thresholdWindow, so a two-window condition on one metric has to be checked against the returned rows instead of filtered for. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once. This board applies 100,000 USD over h24 by default.",
            "type": "number"
          },
          "offset": {
            "description": "How many ranked rows to skip before returning any, 0 or more. Defaults to 0. Mapped onto the provider's 100-row pages for you, and offset paging was measured reaching page 525 of 525, so the ranking is walkable to its live end. The ranking is live, so rows can repeat or be skipped between deep pages; hasMore and nextOffset carry the continuation.",
            "type": "number"
          },
          "onlyAds": {
            "description": "Keep only pairs with an ad placement running RIGHT NOW. Bought visibility, never demand or safety. Distinct from onlyRecentAds: a pair running a placement now may have bought it long ago.",
            "type": "boolean"
          },
          "onlyBoosted": {
            "description": "Keep only pairs carrying at least one ACTIVE paid boost. This is an advertising signal and not a quality signal: it says the project paid DexScreener for visibility. Use minBoostCount when you want a specific number rather than at least one.",
            "type": "boolean"
          },
          "onlyRecentAds": {
            "description": "Keep only pairs that bought an ad slot RECENTLY, whether or not it is currently running; this is the site's own ads badge. Bought visibility, never demand or safety. Combine with onlyAds to require both, since the provider ANDs them.",
            "type": "boolean"
          },
          "requireProfile": {
            "description": "Keep only pairs whose base token has a DexScreener profile. Measured on solana: 28,080 of 53,094 pairs qualify. Having a profile means the issuer filled a form in, not that the project is real. Sending false sends NO profile filter at all, because the provider has no not-profiled filter to send; on a board that applies this by default that removes the floor, and the floor accounting reports it as removed rather than as held. This board applies true by default.",
            "type": "boolean"
          },
          "thresholdWindow": {
            "description": "Which window the volume, transaction, buy, sell and price-change thresholds measure over: m5, h1, h6 or h24. Defaults to whatever window is set. Separating the two is how the site's own boards work, ranking by 5-minute price change while requiring 24-hour transaction and liquidity floors.",
            "enum": [
              "m5",
              "h1",
              "h6",
              "h24"
            ],
            "type": "string"
          },
          "window": {
            "description": "The stats window the ranking measures over: m5, h1, h6 or h24. Defaults to h24. It has a DUAL role and both halves matter: it selects which window's volume, transactions, buys, sells and price change are ranked and reported, AND it excludes pairs that had no activity in that window. Use includeInactive to keep the ranking and drop the gate.",
            "enum": [
              "m5",
              "h1",
              "h6",
              "h24"
            ],
            "type": "string"
          }
        },
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "dexscreener",
      "publicName": "dexscreener__gainers_list",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "List top-gaining DEX pairs",
      "toolId": "dexscreener.gainers"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 3122,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "baseTokenSuffixes": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Base-token mint-address suffixes to keep, the vanity-suffix convention some launchpads use. Measured on solana: pump matched 20,852 pairs and bonk matched 1,595. Accepts either a comma-separated string (\"a,b\") or an array of strings ([\"a\",\"b\"]); the two are equivalent."
          },
          "chainIds": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "DexScreener chain slugs to screen, for example solana, base, ethereum, bsc. Omit to screen every indexed chain at once. Accepts either a comma-separated string (\"a,b\") or an array of strings ([\"a\",\"b\"]); the two are equivalent. Chain and dex values come from dexscreener__chains_list; an unknown chain is refused by name with the nearest catalog matches, because the provider answers one with zero rows and HTTP 200, which is indistinguishable from a real empty result."
          },
          "dexIds": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "DEX identifiers to keep, for example raydium, uniswap, pumpswap. Multiple values are ORed: a pair on any listed dex is kept. The accepted set is open and per-chain, so read it from dexscreener__chains_list rather than guessing. Accepts either a comma-separated string (\"a,b\") or an array of strings ([\"a\",\"b\"]); the two are equivalent."
          },
          "disableQualityFloor": {
            "description": "Drop EVERY default quality floor this tool applies, in one flag. Defaults to false. Your own explicit thresholds still apply, so disableQualityFloor with minLiquidityUsd 5000 means \"none of the tool's defaults, but keep mine\". Each dropped default is named in floorAccounting and qualityFloorApplied becomes false. Expect the unfloored result to contain arithmetic artifacts: on the price-change boards the top rows without a floor are billion-percent moves on pairs nobody can trade. IT DOES NOT READMIT BONDING-CURVE PAIRS, and that is the half agents miss: dropping the floors is necessary but not sufficient, because the provider excludes bonding-curve VENUES by default and only includeLaunchpadPairs lifts that. Measured on solana: a launchpad filter without the lift returned 0 on-curve pairs, and with the lift 71,334; adding a 25,000 USD liquidity floor to those 71,334 returned 0 again, because on-curve rows carry no liquidity field at all. For on-curve pairs, send includeLaunchpadPairs AND keep every liquidity threshold off.",
            "type": "boolean"
          },
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "excludeDexIds": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "DEX identifiers to drop. DANGEROUS BY CONSTRUCTION: the provider applies a hidden default that already excludes bonding-curve venues, and sending this key AT ALL replaces that default, so excluding one dex can make the result set BIGGER. Measured on solana: 53,094 rows normally, 84,058 when only PumpSwap was excluded. Use includeLaunchpadPairs for the common case; exclusionDefaultReplaced reports whenever this happened. Accepts either a comma-separated string (\"a,b\") or an array of strings ([\"a\",\"b\"]); the two are equivalent."
          },
          "fields": {
            "description": "Comma-separated row field GROUPS to return, not individual field names. Supported groups: core, flow, reserves, allWindows, profile, launchpad, identity. Defaults to core, which carries the chain, dex, pair address, base-token address and symbols, the selected window's price, volume, liquidity, market cap, age, counts and every derived ratio. The two heaviest are profile (unbounded issuer-authored prose and links) and allWindows (four times the per-row metric payload). The `identity` group is NOT part of core and is what adds ammId, quoteTokenAddress, both tokens' decimals, priceNative and pairCreatedAtMs - the fields that let you tell a real pool from a mispriced one, so ask for it when the dollar columns are what you are judging. An unknown group name is refused with the full list rather than ignored.",
            "type": "string"
          },
          "includeInactive": {
            "description": "Keep pairs that had no activity inside the selected window. Defaults to false. Measured on solana at m5: 2,513 rows becomes 53,088, so this widens the set enormously on short windows and barely at all on h24.",
            "type": "boolean"
          },
          "includeLaunchpadPairs": {
            "description": "Lift the provider's hidden exclusion of bonding-curve pairs, the safe alternative to excludeDexIds. Defaults to false. Measured on solana: 53,094 rows becomes 102,676. Named for the effect rather than the mechanism, because the mechanism is an empty exclusion list.",
            "type": "boolean"
          },
          "labels": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Pool label values to keep, for example v2, v3, v4, CLMM, DLMM, CPMM. Matching is case-insensitive on the provider. Accepts either a comma-separated string (\"a,b\") or an array of strings ([\"a\",\"b\"]); the two are equivalent."
          },
          "launchpadIds": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Launchpad identifiers to keep, for example pumpfun, launchlab, meteoradbc, bags, fourmeme. MEASURED: the provider attaches a launchpad id to a pair only AFTER it graduates, so this filter matches graduated pairs and nothing else. While a curve is still running the launchpad is the pair's DEX, so use dexIds with the same value there (dexIds: pumpfun). On dexscreener__launchpad_pairs_list, pairing this with stage: \"bonding\" is refused by name rather than answered with an empty board: it returned 0 rows of a 0-row population while dexIds returned 53,478. KNOWN DEPTH GAP: no tool on this surface enumerates the launchpad vocabulary - dexscreener__chains_list carries dex ids only - so the five spellings above are the list, measured live, and an id outside them is not refused by name the way an unknown chain slug is. It returns an empty board instead, so check your spelling against those five before reading a zero as an absence of launches. Accepts either a comma-separated string (\"a,b\") or an array of strings ([\"a\",\"b\"]); the two are equivalent."
          },
          "limit": {
            "description": "How many rows to return, from 1 to 100. Defaults to 20. 100 is the provider's own page size, so asking for more than that is refused rather than silently served short; use offset to walk further into the ranking.",
            "type": "number"
          },
          "maxBoostCount": {
            "description": "Ceiling on the number of paid boosts ACTIVE on the pair, as a COUNT. It bounds WITHIN the boosted population and does not select against it. The presence rule below is the whole story here: an active-boost count exists only on a boosted pair, so maxBoostCount 3 means \"is boosted AND has at most 3 boosts\", never \"is not heavily advertised\". Measured 2026-08-24 on solana: min 1 gave 105 rows (the boosted population), max 10 gave 51, max 1000000 gave 105 again rather than the 64,420-row baseline, and max 0 gave 0. THIS IS NOT A WAY TO EXCLUDE ADS: an agent setting it to avoid ad-pumped pairs receives only ad-carrying pairs. To judge advertising, leave it unset and read boostsActive per row, which is on every row by default. PRESENCE RULE, measured, and it applies to every ceiling on this surface: a maximum matches only rows that CARRY the field. A row the provider reported no value for is excluded, not kept as if it were below the bound. Measured on a 64,420-row solana population: a liquidity ceiling removed 1,687 rows that carry no liquidity figure, an FDV ceiling removed 3,286, and a launchpad-progress ceiling reduced the set to launchpad pairs only. So a ceiling narrows on two axes at once, and the second one is silent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "maxBuyCount": {
            "description": "Ceiling on the number of BUY trades, as a COUNT. Measured over thresholdWindow, which defaults to window. One window per threshold family is reachable here: the provider itself accepts several windows of one family at once and ANDs them (a 1,000,000 USD h24 volume floor combined with a 1,000 USD m5 volume floor was measured returning 104 rows), but this surface carries a single thresholdWindow, so a two-window condition on one metric has to be checked against the returned rows instead of filtered for. PRESENCE RULE, measured, and it applies to every ceiling on this surface: a maximum matches only rows that CARRY the field. A row the provider reported no value for is excluded, not kept as if it were below the bound. Measured on a 64,420-row solana population: a liquidity ceiling removed 1,687 rows that carry no liquidity figure, an FDV ceiling removed 3,286, and a launchpad-progress ceiling reduced the set to launchpad pairs only. So a ceiling narrows on two axes at once, and the second one is silent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "maxFdvUsd": {
            "description": "Ceiling on fully diluted valuation, in US dollars. FDV values the total supply rather than the circulating supply, so it is the larger number. PRESENCE RULE, measured, and it applies to every ceiling on this surface: a maximum matches only rows that CARRY the field. A row the provider reported no value for is excluded, not kept as if it were below the bound. Measured on a 64,420-row solana population: a liquidity ceiling removed 1,687 rows that carry no liquidity figure, an FDV ceiling removed 3,286, and a launchpad-progress ceiling reduced the set to launchpad pairs only. So a ceiling narrows on two axes at once, and the second one is silent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "maxLaunchpadProgressPct": {
            "description": "Ceiling on bonding-curve completion, as a PERCENT of the curve. Below 100 keeps tokens still on the curve. Only launchpad pairs carry it. PRESENCE RULE, measured, and it applies to every ceiling on this surface: a maximum matches only rows that CARRY the field. A row the provider reported no value for is excluded, not kept as if it were below the bound. Measured on a 64,420-row solana population: a liquidity ceiling removed 1,687 rows that carry no liquidity figure, an FDV ceiling removed 3,286, and a launchpad-progress ceiling reduced the set to launchpad pairs only. So a ceiling narrows on two axes at once, and the second one is silent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "maxLiquidityUsd": {
            "description": "Ceiling on pool liquidity, in US dollars. Useful for finding small pools deliberately; liquidity has no window. PRESENCE RULE, measured, and it applies to every ceiling on this surface: a maximum matches only rows that CARRY the field. A row the provider reported no value for is excluded, not kept as if it were below the bound. Measured on a 64,420-row solana population: a liquidity ceiling removed 1,687 rows that carry no liquidity figure, an FDV ceiling removed 3,286, and a launchpad-progress ceiling reduced the set to launchpad pairs only. So a ceiling narrows on two axes at once, and the second one is silent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "maxMarketCapUsd": {
            "description": "Ceiling on market capitalisation, in US dollars. The provider's own circulating-supply valuation, with no window. PRESENCE RULE, measured, and it applies to every ceiling on this surface: a maximum matches only rows that CARRY the field. A row the provider reported no value for is excluded, not kept as if it were below the bound. Measured on a 64,420-row solana population: a liquidity ceiling removed 1,687 rows that carry no liquidity figure, an FDV ceiling removed 3,286, and a launchpad-progress ceiling reduced the set to launchpad pairs only. So a ceiling narrows on two axes at once, and the second one is silent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "maxPairAgeSeconds": {
            "description": "Ceiling on pair age, in SECONDS since the pair was created. 86400 is one day. The provider filters in hours, so sub-hour precision comes from the returned pairAgeSeconds rather than from this filter. PRESENCE RULE, measured, and it applies to every ceiling on this surface: a maximum matches only rows that CARRY the field. A row the provider reported no value for is excluded, not kept as if it were below the bound. Measured on a 64,420-row solana population: a liquidity ceiling removed 1,687 rows that carry no liquidity figure, an FDV ceiling removed 3,286, and a launchpad-progress ceiling reduced the set to launchpad pairs only. So a ceiling narrows on two axes at once, and the second one is silent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "maxPriceChangePct": {
            "description": "Ceiling on price change, as a PERCENT and not a fraction. Measured over thresholdWindow, which defaults to window. One window per threshold family is reachable here: the provider itself accepts several windows of one family at once and ANDs them (a 1,000,000 USD h24 volume floor combined with a 1,000 USD m5 volume floor was measured returning 104 rows), but this surface carries a single thresholdWindow, so a two-window condition on one metric has to be checked against the returned rows instead of filtered for. Negative values are legal, so -50 keeps only pairs down at least half. PRESENCE RULE, measured, and it applies to every ceiling on this surface: a maximum matches only rows that CARRY the field. A row the provider reported no value for is excluded, not kept as if it were below the bound. Measured on a 64,420-row solana population: a liquidity ceiling removed 1,687 rows that carry no liquidity figure, an FDV ceiling removed 3,286, and a launchpad-progress ceiling reduced the set to launchpad pairs only. So a ceiling narrows on two axes at once, and the second one is silent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "maxSellCount": {
            "description": "Ceiling on the number of SELL trades, as a COUNT. Measured over thresholdWindow, which defaults to window. One window per threshold family is reachable here: the provider itself accepts several windows of one family at once and ANDs them (a 1,000,000 USD h24 volume floor combined with a 1,000 USD m5 volume floor was measured returning 104 rows), but this surface carries a single thresholdWindow, so a two-window condition on one metric has to be checked against the returned rows instead of filtered for. PRESENCE RULE, measured, and it applies to every ceiling on this surface: a maximum matches only rows that CARRY the field. A row the provider reported no value for is excluded, not kept as if it were below the bound. Measured on a 64,420-row solana population: a liquidity ceiling removed 1,687 rows that carry no liquidity figure, an FDV ceiling removed 3,286, and a launchpad-progress ceiling reduced the set to launchpad pairs only. So a ceiling narrows on two axes at once, and the second one is silent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "maxTxnCount": {
            "description": "Ceiling on the number of trades, as a COUNT and not a volume. Measured over thresholdWindow, which defaults to window. One window per threshold family is reachable here: the provider itself accepts several windows of one family at once and ANDs them (a 1,000,000 USD h24 volume floor combined with a 1,000 USD m5 volume floor was measured returning 104 rows), but this surface carries a single thresholdWindow, so a two-window condition on one metric has to be checked against the returned rows instead of filtered for. PRESENCE RULE, measured, and it applies to every ceiling on this surface: a maximum matches only rows that CARRY the field. A row the provider reported no value for is excluded, not kept as if it were below the bound. Measured on a 64,420-row solana population: a liquidity ceiling removed 1,687 rows that carry no liquidity figure, an FDV ceiling removed 3,286, and a launchpad-progress ceiling reduced the set to launchpad pairs only. So a ceiling narrows on two axes at once, and the second one is silent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "maxVolumeUsd": {
            "description": "Ceiling on traded volume, in US dollars. Measured over thresholdWindow, which defaults to window. One window per threshold family is reachable here: the provider itself accepts several windows of one family at once and ANDs them (a 1,000,000 USD h24 volume floor combined with a 1,000 USD m5 volume floor was measured returning 104 rows), but this surface carries a single thresholdWindow, so a two-window condition on one metric has to be checked against the returned rows instead of filtered for. PRESENCE RULE, measured, and it applies to every ceiling on this surface: a maximum matches only rows that CARRY the field. A row the provider reported no value for is excluded, not kept as if it were below the bound. Measured on a 64,420-row solana population: a liquidity ceiling removed 1,687 rows that carry no liquidity figure, an FDV ceiling removed 3,286, and a launchpad-progress ceiling reduced the set to launchpad pairs only. So a ceiling narrows on two axes at once, and the second one is silent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "metaIds": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Narrative IDs to keep, taken from dexscreener__narratives_list. Send the ID and not the human-readable name: the provider accepts a name, matches nothing, and answers with zero rows, which reads as an empty market rather than as a wrong value. Accepts either a comma-separated string (\"a,b\") or an array of strings ([\"a\",\"b\"]); the two are equivalent."
          },
          "minBoostCount": {
            "description": "Floor on the number of paid boosts ACTIVE on the pair, as a COUNT. A boost is bought visibility, never demand or safety. This is more specific than onlyBoosted and wins over it when both are sent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "minBuyCount": {
            "description": "Floor on the number of BUY trades, as a COUNT. Measured over thresholdWindow, which defaults to window. One window per threshold family is reachable here: the provider itself accepts several windows of one family at once and ANDs them (a 1,000,000 USD h24 volume floor combined with a 1,000 USD m5 volume floor was measured returning 104 rows), but this surface carries a single thresholdWindow, so a two-window condition on one metric has to be checked against the returned rows instead of filtered for. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "minFdvUsd": {
            "description": "Floor on fully diluted valuation, in US dollars. FDV values the total supply rather than the circulating supply, so it is the larger number. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "minLaunchpadProgressPct": {
            "description": "Floor on bonding-curve completion, as a PERCENT of the curve. 100 means the token has graduated and migrated to a DEX. Only launchpad pairs carry it. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "minLiquidityUsd": {
            "description": "Floor on pool liquidity, in US dollars. Liquidity has no window: it is the pool's depth right now. Bonding-curve pairs carry no liquidity field at all, so any liquidity threshold excludes them by construction. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "minMarketCapUsd": {
            "description": "Floor on market capitalisation, in US dollars. The provider's own circulating-supply valuation, with no window. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "minPairAgeSeconds": {
            "description": "Floor on pair age, in SECONDS since the pair was created. The provider filters age in whole and fractional hours, so this is converted for you; the exact age comes back per row in pairAgeSeconds. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "minPriceChangePct": {
            "description": "Floor on price change, as a PERCENT and not a fraction. Measured over thresholdWindow, which defaults to window. One window per threshold family is reachable here: the provider itself accepts several windows of one family at once and ANDs them (a 1,000,000 USD h24 volume floor combined with a 1,000 USD m5 volume floor was measured returning 104 rows), but this surface carries a single thresholdWindow, so a two-window condition on one metric has to be checked against the returned rows instead of filtered for. 10 means ten percent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "minSellCount": {
            "description": "Floor on the number of SELL trades, as a COUNT. Measured over thresholdWindow, which defaults to window. One window per threshold family is reachable here: the provider itself accepts several windows of one family at once and ANDs them (a 1,000,000 USD h24 volume floor combined with a 1,000 USD m5 volume floor was measured returning 104 rows), but this surface carries a single thresholdWindow, so a two-window condition on one metric has to be checked against the returned rows instead of filtered for. A sell-side floor is what separates a real two-sided market from a pair nobody can exit. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "minTxnCount": {
            "description": "Floor on the number of trades, as a COUNT and not a volume. Measured over thresholdWindow, which defaults to window. One window per threshold family is reachable here: the provider itself accepts several windows of one family at once and ANDs them (a 1,000,000 USD h24 volume floor combined with a 1,000 USD m5 volume floor was measured returning 104 rows), but this surface carries a single thresholdWindow, so a two-window condition on one metric has to be checked against the returned rows instead of filtered for. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "minVolumeUsd": {
            "description": "Floor on traded volume, in US dollars. Measured over thresholdWindow, which defaults to window. One window per threshold family is reachable here: the provider itself accepts several windows of one family at once and ANDs them (a 1,000,000 USD h24 volume floor combined with a 1,000 USD m5 volume floor was measured returning 104 rows), but this surface carries a single thresholdWindow, so a two-window condition on one metric has to be checked against the returned rows instead of filtered for. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "offset": {
            "description": "How many ranked rows to skip before returning any, 0 or more. Defaults to 0. Mapped onto the provider's 100-row pages for you, and offset paging was measured reaching page 525 of 525, so the ranking is walkable to its live end. The ranking is live, so rows can repeat or be skipped between deep pages; hasMore and nextOffset carry the continuation.",
            "type": "number"
          },
          "onlyAds": {
            "description": "Keep only pairs with an ad placement running RIGHT NOW. Bought visibility, never demand or safety. Distinct from onlyRecentAds: a pair running a placement now may have bought it long ago.",
            "type": "boolean"
          },
          "onlyBoosted": {
            "description": "Keep only pairs carrying at least one ACTIVE paid boost. This is an advertising signal and not a quality signal: it says the project paid DexScreener for visibility. Use minBoostCount when you want a specific number rather than at least one.",
            "type": "boolean"
          },
          "onlyRecentAds": {
            "description": "Keep only pairs that bought an ad slot RECENTLY, whether or not it is currently running; this is the site's own ads badge. Bought visibility, never demand or safety. Combine with onlyAds to require both, since the provider ANDs them.",
            "type": "boolean"
          },
          "requireProfile": {
            "description": "Keep only pairs whose base token has a DexScreener profile. Measured on solana: 28,080 of 53,094 pairs qualify. Having a profile means the issuer filled a form in, not that the project is real. Sending false sends NO profile filter at all, because the provider has no not-profiled filter to send; on a board that applies this by default that removes the floor, and the floor accounting reports it as removed rather than as held.",
            "type": "boolean"
          },
          "sortBy": {
            "description": "Which metric ranks the board: launchpadProgress, trendingScore, priceChange, volume, txns, marketCap or pairAge. Defaults to launchpadProgress on the bonding stage, which is the near-graduation question, and to trendingScore on the graduated stage, where progress is 100 for every row and would rank nothing. This board takes sortDir like the other boards: every key defaults to descending EXCEPT pairAge, which defaults to ascending so that the newest bonding tokens come first, and sortDir overrides either way.",
            "enum": [
              "launchpadProgress",
              "trendingScore",
              "priceChange",
              "volume",
              "txns",
              "marketCap",
              "pairAge"
            ],
            "type": "string"
          },
          "sortDir": {
            "description": "Ranking direction: desc for the largest values first, asc for the smallest. Defaults to desc, which is the league-table reading. asc is how you find the thinnest pools that still pass your filters.",
            "enum": [
              "desc",
              "asc"
            ],
            "type": "string"
          },
          "stage": {
            "description": "Which side of graduation to list: bonding for tokens still on the curve, graduated for tokens that completed it and migrated to a DEX. Defaults to bonding. The stage sets the default progress floor, so bonding lists below 100 percent complete and graduated lists at 100 percent.",
            "enum": [
              "bonding",
              "graduated"
            ],
            "type": "string"
          },
          "thresholdWindow": {
            "description": "Which window the volume, transaction, buy, sell and price-change thresholds measure over: m5, h1, h6 or h24. Defaults to whatever window is set. Separating the two is how the site's own boards work, ranking by 5-minute price change while requiring 24-hour transaction and liquidity floors.",
            "enum": [
              "m5",
              "h1",
              "h6",
              "h24"
            ],
            "type": "string"
          },
          "window": {
            "description": "The stats window the ranking measures over: m5, h1, h6 or h24. Defaults to h24. It has a DUAL role and both halves matter: it selects which window's volume, transactions, buys, sells and price change are ranked and reported, AND it excludes pairs that had no activity in that window. Use includeInactive to keep the ranking and drop the gate.",
            "enum": [
              "m5",
              "h1",
              "h6",
              "h24"
            ],
            "type": "string"
          }
        },
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "dexscreener",
      "publicName": "dexscreener__launchpad_pairs_list",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "List launchpad pairs by stage",
      "toolId": "dexscreener.launchpad.pairs"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 2329,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "baseTokenSuffixes": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Base-token mint-address suffixes to keep, the vanity-suffix convention some launchpads use. Measured on solana: pump matched 20,852 pairs and bonk matched 1,595. Accepts either a comma-separated string (\"a,b\") or an array of strings ([\"a\",\"b\"]); the two are equivalent."
          },
          "chainIds": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "DexScreener chain slugs to screen, for example solana, base, ethereum, bsc. Omit to screen every indexed chain at once. Accepts either a comma-separated string (\"a,b\") or an array of strings ([\"a\",\"b\"]); the two are equivalent. Chain and dex values come from dexscreener__chains_list; an unknown chain is refused by name with the nearest catalog matches, because the provider answers one with zero rows and HTTP 200, which is indistinguishable from a real empty result."
          },
          "dexIds": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "DEX identifiers to keep, for example raydium, uniswap, pumpswap. Multiple values are ORed: a pair on any listed dex is kept. The accepted set is open and per-chain, so read it from dexscreener__chains_list rather than guessing. Accepts either a comma-separated string (\"a,b\") or an array of strings ([\"a\",\"b\"]); the two are equivalent."
          },
          "disableQualityFloor": {
            "description": "Drop EVERY default quality floor this tool applies, in one flag. Defaults to false. Your own explicit thresholds still apply, so disableQualityFloor with minLiquidityUsd 5000 means \"none of the tool's defaults, but keep mine\". Each dropped default is named in floorAccounting and qualityFloorApplied becomes false. Expect the unfloored result to contain arithmetic artifacts: on the price-change boards the top rows without a floor are billion-percent moves on pairs nobody can trade. IT DOES NOT READMIT BONDING-CURVE PAIRS, and that is the half agents miss: dropping the floors is necessary but not sufficient, because the provider excludes bonding-curve VENUES by default and only includeLaunchpadPairs lifts that. Measured on solana: a launchpad filter without the lift returned 0 on-curve pairs, and with the lift 71,334; adding a 25,000 USD liquidity floor to those 71,334 returned 0 again, because on-curve rows carry no liquidity field at all. For on-curve pairs, send includeLaunchpadPairs AND keep every liquidity threshold off.",
            "type": "boolean"
          },
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "excludeDexIds": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "DEX identifiers to drop. DANGEROUS BY CONSTRUCTION: the provider applies a hidden default that already excludes bonding-curve venues, and sending this key AT ALL replaces that default, so excluding one dex can make the result set BIGGER. Measured on solana: 53,094 rows normally, 84,058 when only PumpSwap was excluded. Use includeLaunchpadPairs for the common case; exclusionDefaultReplaced reports whenever this happened. Accepts either a comma-separated string (\"a,b\") or an array of strings ([\"a\",\"b\"]); the two are equivalent."
          },
          "fields": {
            "description": "Comma-separated row field GROUPS to return, not individual field names. Supported groups: core, flow, reserves, allWindows, profile, launchpad, identity. Defaults to core, which carries the chain, dex, pair address, base-token address and symbols, the selected window's price, volume, liquidity, market cap, age, counts and every derived ratio. The two heaviest are profile (unbounded issuer-authored prose and links) and allWindows (four times the per-row metric payload). The `identity` group is NOT part of core and is what adds ammId, quoteTokenAddress, both tokens' decimals, priceNative and pairCreatedAtMs - the fields that let you tell a real pool from a mispriced one, so ask for it when the dollar columns are what you are judging. An unknown group name is refused with the full list rather than ignored.",
            "type": "string"
          },
          "includeInactive": {
            "description": "Keep pairs that had no activity inside the selected window. Defaults to false. Measured on solana at m5: 2,513 rows becomes 53,088, so this widens the set enormously on short windows and barely at all on h24.",
            "type": "boolean"
          },
          "includeLaunchpadPairs": {
            "description": "Lift the provider's hidden exclusion of bonding-curve pairs, the safe alternative to excludeDexIds. Defaults to false. Measured on solana: 53,094 rows becomes 102,676. Named for the effect rather than the mechanism, because the mechanism is an empty exclusion list.",
            "type": "boolean"
          },
          "labels": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Pool label values to keep, for example v2, v3, v4, CLMM, DLMM, CPMM. Matching is case-insensitive on the provider. Accepts either a comma-separated string (\"a,b\") or an array of strings ([\"a\",\"b\"]); the two are equivalent."
          },
          "launchpadIds": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Launchpad identifiers to keep, for example pumpfun, launchlab, meteoradbc, bags, fourmeme. MEASURED: the provider attaches a launchpad id to a pair only AFTER it graduates, so this filter matches graduated pairs and nothing else. While a curve is still running the launchpad is the pair's DEX, so use dexIds with the same value there (dexIds: pumpfun). On dexscreener__launchpad_pairs_list, pairing this with stage: \"bonding\" is refused by name rather than answered with an empty board: it returned 0 rows of a 0-row population while dexIds returned 53,478. KNOWN DEPTH GAP: no tool on this surface enumerates the launchpad vocabulary - dexscreener__chains_list carries dex ids only - so the five spellings above are the list, measured live, and an id outside them is not refused by name the way an unknown chain slug is. It returns an empty board instead, so check your spelling against those five before reading a zero as an absence of launches. Accepts either a comma-separated string (\"a,b\") or an array of strings ([\"a\",\"b\"]); the two are equivalent."
          },
          "limit": {
            "description": "How many rows to return, from 1 to 100. Defaults to 20. 100 is the provider's own page size, so asking for more than that is refused rather than silently served short; use offset to walk further into the ranking.",
            "type": "number"
          },
          "maxBoostCount": {
            "description": "Ceiling on the number of paid boosts ACTIVE on the pair, as a COUNT. It bounds WITHIN the boosted population and does not select against it. The presence rule below is the whole story here: an active-boost count exists only on a boosted pair, so maxBoostCount 3 means \"is boosted AND has at most 3 boosts\", never \"is not heavily advertised\". Measured 2026-08-24 on solana: min 1 gave 105 rows (the boosted population), max 10 gave 51, max 1000000 gave 105 again rather than the 64,420-row baseline, and max 0 gave 0. THIS IS NOT A WAY TO EXCLUDE ADS: an agent setting it to avoid ad-pumped pairs receives only ad-carrying pairs. To judge advertising, leave it unset and read boostsActive per row, which is on every row by default. PRESENCE RULE, measured, and it applies to every ceiling on this surface: a maximum matches only rows that CARRY the field. A row the provider reported no value for is excluded, not kept as if it were below the bound. Measured on a 64,420-row solana population: a liquidity ceiling removed 1,687 rows that carry no liquidity figure, an FDV ceiling removed 3,286, and a launchpad-progress ceiling reduced the set to launchpad pairs only. So a ceiling narrows on two axes at once, and the second one is silent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "maxBuyCount": {
            "description": "Ceiling on the number of BUY trades, as a COUNT. Measured over thresholdWindow, which defaults to window. One window per threshold family is reachable here: the provider itself accepts several windows of one family at once and ANDs them (a 1,000,000 USD h24 volume floor combined with a 1,000 USD m5 volume floor was measured returning 104 rows), but this surface carries a single thresholdWindow, so a two-window condition on one metric has to be checked against the returned rows instead of filtered for. PRESENCE RULE, measured, and it applies to every ceiling on this surface: a maximum matches only rows that CARRY the field. A row the provider reported no value for is excluded, not kept as if it were below the bound. Measured on a 64,420-row solana population: a liquidity ceiling removed 1,687 rows that carry no liquidity figure, an FDV ceiling removed 3,286, and a launchpad-progress ceiling reduced the set to launchpad pairs only. So a ceiling narrows on two axes at once, and the second one is silent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "maxFdvUsd": {
            "description": "Ceiling on fully diluted valuation, in US dollars. FDV values the total supply rather than the circulating supply, so it is the larger number. PRESENCE RULE, measured, and it applies to every ceiling on this surface: a maximum matches only rows that CARRY the field. A row the provider reported no value for is excluded, not kept as if it were below the bound. Measured on a 64,420-row solana population: a liquidity ceiling removed 1,687 rows that carry no liquidity figure, an FDV ceiling removed 3,286, and a launchpad-progress ceiling reduced the set to launchpad pairs only. So a ceiling narrows on two axes at once, and the second one is silent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "maxLaunchpadProgressPct": {
            "description": "Ceiling on bonding-curve completion, as a PERCENT of the curve. Below 100 keeps tokens still on the curve. Only launchpad pairs carry it. PRESENCE RULE, measured, and it applies to every ceiling on this surface: a maximum matches only rows that CARRY the field. A row the provider reported no value for is excluded, not kept as if it were below the bound. Measured on a 64,420-row solana population: a liquidity ceiling removed 1,687 rows that carry no liquidity figure, an FDV ceiling removed 3,286, and a launchpad-progress ceiling reduced the set to launchpad pairs only. So a ceiling narrows on two axes at once, and the second one is silent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "maxLiquidityUsd": {
            "description": "Ceiling on pool liquidity, in US dollars. Useful for finding small pools deliberately; liquidity has no window. PRESENCE RULE, measured, and it applies to every ceiling on this surface: a maximum matches only rows that CARRY the field. A row the provider reported no value for is excluded, not kept as if it were below the bound. Measured on a 64,420-row solana population: a liquidity ceiling removed 1,687 rows that carry no liquidity figure, an FDV ceiling removed 3,286, and a launchpad-progress ceiling reduced the set to launchpad pairs only. So a ceiling narrows on two axes at once, and the second one is silent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "maxMarketCapUsd": {
            "description": "Ceiling on market capitalisation, in US dollars. The provider's own circulating-supply valuation, with no window. PRESENCE RULE, measured, and it applies to every ceiling on this surface: a maximum matches only rows that CARRY the field. A row the provider reported no value for is excluded, not kept as if it were below the bound. Measured on a 64,420-row solana population: a liquidity ceiling removed 1,687 rows that carry no liquidity figure, an FDV ceiling removed 3,286, and a launchpad-progress ceiling reduced the set to launchpad pairs only. So a ceiling narrows on two axes at once, and the second one is silent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "maxPairAgeSeconds": {
            "description": "Ceiling on pair age, in SECONDS since the pair was created. 86400 is one day. The provider filters in hours, so sub-hour precision comes from the returned pairAgeSeconds rather than from this filter. PRESENCE RULE, measured, and it applies to every ceiling on this surface: a maximum matches only rows that CARRY the field. A row the provider reported no value for is excluded, not kept as if it were below the bound. Measured on a 64,420-row solana population: a liquidity ceiling removed 1,687 rows that carry no liquidity figure, an FDV ceiling removed 3,286, and a launchpad-progress ceiling reduced the set to launchpad pairs only. So a ceiling narrows on two axes at once, and the second one is silent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "maxPriceChangePct": {
            "description": "Ceiling on price change, as a PERCENT and not a fraction. Measured over thresholdWindow, which defaults to window. One window per threshold family is reachable here: the provider itself accepts several windows of one family at once and ANDs them (a 1,000,000 USD h24 volume floor combined with a 1,000 USD m5 volume floor was measured returning 104 rows), but this surface carries a single thresholdWindow, so a two-window condition on one metric has to be checked against the returned rows instead of filtered for. Negative values are legal, so -50 keeps only pairs down at least half. PRESENCE RULE, measured, and it applies to every ceiling on this surface: a maximum matches only rows that CARRY the field. A row the provider reported no value for is excluded, not kept as if it were below the bound. Measured on a 64,420-row solana population: a liquidity ceiling removed 1,687 rows that carry no liquidity figure, an FDV ceiling removed 3,286, and a launchpad-progress ceiling reduced the set to launchpad pairs only. So a ceiling narrows on two axes at once, and the second one is silent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "maxSellCount": {
            "description": "Ceiling on the number of SELL trades, as a COUNT. Measured over thresholdWindow, which defaults to window. One window per threshold family is reachable here: the provider itself accepts several windows of one family at once and ANDs them (a 1,000,000 USD h24 volume floor combined with a 1,000 USD m5 volume floor was measured returning 104 rows), but this surface carries a single thresholdWindow, so a two-window condition on one metric has to be checked against the returned rows instead of filtered for. PRESENCE RULE, measured, and it applies to every ceiling on this surface: a maximum matches only rows that CARRY the field. A row the provider reported no value for is excluded, not kept as if it were below the bound. Measured on a 64,420-row solana population: a liquidity ceiling removed 1,687 rows that carry no liquidity figure, an FDV ceiling removed 3,286, and a launchpad-progress ceiling reduced the set to launchpad pairs only. So a ceiling narrows on two axes at once, and the second one is silent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "maxTxnCount": {
            "description": "Ceiling on the number of trades, as a COUNT and not a volume. Measured over thresholdWindow, which defaults to window. One window per threshold family is reachable here: the provider itself accepts several windows of one family at once and ANDs them (a 1,000,000 USD h24 volume floor combined with a 1,000 USD m5 volume floor was measured returning 104 rows), but this surface carries a single thresholdWindow, so a two-window condition on one metric has to be checked against the returned rows instead of filtered for. PRESENCE RULE, measured, and it applies to every ceiling on this surface: a maximum matches only rows that CARRY the field. A row the provider reported no value for is excluded, not kept as if it were below the bound. Measured on a 64,420-row solana population: a liquidity ceiling removed 1,687 rows that carry no liquidity figure, an FDV ceiling removed 3,286, and a launchpad-progress ceiling reduced the set to launchpad pairs only. So a ceiling narrows on two axes at once, and the second one is silent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "maxVolumeUsd": {
            "description": "Ceiling on traded volume, in US dollars. Measured over thresholdWindow, which defaults to window. One window per threshold family is reachable here: the provider itself accepts several windows of one family at once and ANDs them (a 1,000,000 USD h24 volume floor combined with a 1,000 USD m5 volume floor was measured returning 104 rows), but this surface carries a single thresholdWindow, so a two-window condition on one metric has to be checked against the returned rows instead of filtered for. PRESENCE RULE, measured, and it applies to every ceiling on this surface: a maximum matches only rows that CARRY the field. A row the provider reported no value for is excluded, not kept as if it were below the bound. Measured on a 64,420-row solana population: a liquidity ceiling removed 1,687 rows that carry no liquidity figure, an FDV ceiling removed 3,286, and a launchpad-progress ceiling reduced the set to launchpad pairs only. So a ceiling narrows on two axes at once, and the second one is silent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "metaIds": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Narrative IDs to keep, taken from dexscreener__narratives_list. Send the ID and not the human-readable name: the provider accepts a name, matches nothing, and answers with zero rows, which reads as an empty market rather than as a wrong value. Accepts either a comma-separated string (\"a,b\") or an array of strings ([\"a\",\"b\"]); the two are equivalent."
          },
          "minBoostCount": {
            "description": "Floor on the number of paid boosts ACTIVE on the pair, as a COUNT. A boost is bought visibility, never demand or safety. This is more specific than onlyBoosted and wins over it when both are sent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "minBuyCount": {
            "description": "Floor on the number of BUY trades, as a COUNT. Measured over thresholdWindow, which defaults to window. One window per threshold family is reachable here: the provider itself accepts several windows of one family at once and ANDs them (a 1,000,000 USD h24 volume floor combined with a 1,000 USD m5 volume floor was measured returning 104 rows), but this surface carries a single thresholdWindow, so a two-window condition on one metric has to be checked against the returned rows instead of filtered for. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "minFdvUsd": {
            "description": "Floor on fully diluted valuation, in US dollars. FDV values the total supply rather than the circulating supply, so it is the larger number. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "minLaunchpadProgressPct": {
            "description": "Floor on bonding-curve completion, as a PERCENT of the curve. 100 means the token has graduated and migrated to a DEX. Only launchpad pairs carry it. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "minLiquidityUsd": {
            "description": "Floor on pool liquidity, in US dollars. Liquidity has no window: it is the pool's depth right now. Bonding-curve pairs carry no liquidity field at all, so any liquidity threshold excludes them by construction. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once. This board applies 250,000 USD by default.",
            "type": "number"
          },
          "minMarketCapUsd": {
            "description": "Floor on market capitalisation, in US dollars. The provider's own circulating-supply valuation, with no window. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "minPairAgeSeconds": {
            "description": "Floor on pair age, in SECONDS since the pair was created. The provider filters age in whole and fractional hours, so this is converted for you; the exact age comes back per row in pairAgeSeconds. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "minPriceChangePct": {
            "description": "Floor on price change, as a PERCENT and not a fraction. Measured over thresholdWindow, which defaults to window. One window per threshold family is reachable here: the provider itself accepts several windows of one family at once and ANDs them (a 1,000,000 USD h24 volume floor combined with a 1,000 USD m5 volume floor was measured returning 104 rows), but this surface carries a single thresholdWindow, so a two-window condition on one metric has to be checked against the returned rows instead of filtered for. 10 means ten percent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "minSellCount": {
            "description": "Floor on the number of SELL trades, as a COUNT. Measured over thresholdWindow, which defaults to window. One window per threshold family is reachable here: the provider itself accepts several windows of one family at once and ANDs them (a 1,000,000 USD h24 volume floor combined with a 1,000 USD m5 volume floor was measured returning 104 rows), but this surface carries a single thresholdWindow, so a two-window condition on one metric has to be checked against the returned rows instead of filtered for. A sell-side floor is what separates a real two-sided market from a pair nobody can exit. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once. This board applies 30 over h24 by default.",
            "type": "number"
          },
          "minTxnCount": {
            "description": "Floor on the number of trades, as a COUNT and not a volume. Measured over thresholdWindow, which defaults to window. One window per threshold family is reachable here: the provider itself accepts several windows of one family at once and ANDs them (a 1,000,000 USD h24 volume floor combined with a 1,000 USD m5 volume floor was measured returning 104 rows), but this surface carries a single thresholdWindow, so a two-window condition on one metric has to be checked against the returned rows instead of filtered for. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once. This board applies 300 over h24 by default.",
            "type": "number"
          },
          "minVolumeUsd": {
            "description": "Floor on traded volume, in US dollars. Measured over thresholdWindow, which defaults to window. One window per threshold family is reachable here: the provider itself accepts several windows of one family at once and ANDs them (a 1,000,000 USD h24 volume floor combined with a 1,000 USD m5 volume floor was measured returning 104 rows), but this surface carries a single thresholdWindow, so a two-window condition on one metric has to be checked against the returned rows instead of filtered for. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once. This board applies 100,000 USD over h24 by default.",
            "type": "number"
          },
          "offset": {
            "description": "How many ranked rows to skip before returning any, 0 or more. Defaults to 0. Mapped onto the provider's 100-row pages for you, and offset paging was measured reaching page 525 of 525, so the ranking is walkable to its live end. The ranking is live, so rows can repeat or be skipped between deep pages; hasMore and nextOffset carry the continuation.",
            "type": "number"
          },
          "onlyAds": {
            "description": "Keep only pairs with an ad placement running RIGHT NOW. Bought visibility, never demand or safety. Distinct from onlyRecentAds: a pair running a placement now may have bought it long ago.",
            "type": "boolean"
          },
          "onlyBoosted": {
            "description": "Keep only pairs carrying at least one ACTIVE paid boost. This is an advertising signal and not a quality signal: it says the project paid DexScreener for visibility. Use minBoostCount when you want a specific number rather than at least one.",
            "type": "boolean"
          },
          "onlyRecentAds": {
            "description": "Keep only pairs that bought an ad slot RECENTLY, whether or not it is currently running; this is the site's own ads badge. Bought visibility, never demand or safety. Combine with onlyAds to require both, since the provider ANDs them.",
            "type": "boolean"
          },
          "requireProfile": {
            "description": "Keep only pairs whose base token has a DexScreener profile. Measured on solana: 28,080 of 53,094 pairs qualify. Having a profile means the issuer filled a form in, not that the project is real. Sending false sends NO profile filter at all, because the provider has no not-profiled filter to send; on a board that applies this by default that removes the floor, and the floor accounting reports it as removed rather than as held. This board applies true by default.",
            "type": "boolean"
          },
          "thresholdWindow": {
            "description": "Which window the volume, transaction, buy, sell and price-change thresholds measure over: m5, h1, h6 or h24. Defaults to whatever window is set. Separating the two is how the site's own boards work, ranking by 5-minute price change while requiring 24-hour transaction and liquidity floors.",
            "enum": [
              "m5",
              "h1",
              "h6",
              "h24"
            ],
            "type": "string"
          },
          "window": {
            "description": "The stats window the ranking measures over: m5, h1, h6 or h24. Defaults to h24. It has a DUAL role and both halves matter: it selects which window's volume, transactions, buys, sells and price change are ranked and reported, AND it excludes pairs that had no activity in that window. Use includeInactive to keep the ranking and drop the gate.",
            "enum": [
              "m5",
              "h1",
              "h6",
              "h24"
            ],
            "type": "string"
          }
        },
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "dexscreener",
      "publicName": "dexscreener__losers_list",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "List top-losing DEX pairs",
      "toolId": "dexscreener.losers"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 2231,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "chain": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "minimum": 1,
                "type": "integer"
              }
            ],
            "description": "Chain slug to aggregate the narratives for, for example solana, bsc, base or ethereum. Omit it for the cross-chain document, which is what the site's own narratives page shows. Chain slug/alias, or the numeric chain id `TokenFind` returns (e.g. `base` or `8453`). ANY chain in the catalog may be asked: aggregates were measured live on robinhood, ton and polygon, none of which the site gives a narratives page to, so the four surfaced chains are a visibility label and never a data gate. A chain with no narrative activity is a successful answer reading 0 of 18 active, not a refusal; only a slug that is not a chain at all is refused, with the nearest matches."
          },
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "fields": {
            "description": "Comma-separated row field GROUPS to return, not individual field names. Supported groups: core, windows, description. Defaults to core, which carries the id, name, slug, the aggregate market cap, liquidity, volume and token count, the selected window's change in percent and dollars, and the derived turnover ratio. windows adds all four change windows; description adds DexScreener's own blurb and the icon. An unknown group name is refused with the full list rather than ignored.",
            "type": "string"
          },
          "limit": {
            "description": "How many narratives to return, 1 or more. Defaults to 20. The provider serves the entire set in one small document with no pagination, so returned, activeNarratives, totalNarratives and hasMore together say exactly how many exist, how many are active on the requested scope and how many were shown; a limit above the population returns the population.",
            "type": "number"
          },
          "maxEnrichedNarratives": {
            "description": "How many narratives topTokens may enrich in one call, 1 or more. Defaults to 5. There is NO hard ceiling: raise it and the wider enrichment is issued, because the only real bound is this call's deadline. Each additional narrative is ONE additional WebSocket screener exchange against the site host, issued sequentially, so enriching twenty narratives is twenty exchanges and a timeout returns nothing rather than a partial board. It does nothing unless topTokens is set. topTokensCoverage.requestsIssued reports what was actually spent, and the cheaper route for a deep board is to call a screening tool directly with the narrative id as metaIds.",
            "type": "number"
          },
          "sortBy": {
            "description": "How to order the narratives: marketCapUsd, volumeUsd, liquidityUsd, tokenCount, marketCapChangePct, volumeToMarketCapRatio. Defaults to marketCapUsd. Ordering runs HERE, over the complete set the provider sent, which on this channel is the whole population rather than a page, so the ranking is exhaustive and not a sample. marketCapChangePct ranks on the selected window.",
            "enum": [
              "marketCapUsd",
              "volumeUsd",
              "liquidityUsd",
              "tokenCount",
              "marketCapChangePct",
              "volumeToMarketCapRatio"
            ],
            "type": "string"
          },
          "sortDir": {
            "description": "Ranking direction: desc for the largest values first, asc for the smallest. Defaults to desc. Combine asc with marketCapChangePct to surface the narratives that are bleeding.",
            "enum": [
              "desc",
              "asc"
            ],
            "type": "string"
          },
          "topTokens": {
            "description": "How many of each narrative's leading pairs to embed, 1 or more. Omit it or send 0 for none, which is the default and the cheap path. There is no upper bound to refuse against: the enriching call already fetched a whole screener page, so a value above that page returns the page. What it COSTS is the enrichment itself: each enriched narrative is one extra WebSocket exchange on the screener channel, issued sequentially inside this call's deadline. How many narratives are enriched is maxEnrichedNarratives, and topTokensCoverage reports exactly which were enriched and which were not, rather than leaving a row's empty pair list ambiguous between \"no pairs\" and \"not asked\".",
            "type": "number"
          },
          "window": {
            "description": "Which change window the flat marketCapChangePct and marketCapDeltaUsd fields report, and which one sortBy marketCapChangePct ranks on: m5, h1, h6 or h24. Defaults to h24. The provider always sends all four regardless of this value, so selecting one costs nothing and the windows field group returns every one of them; this is a projection choice, never a narrowing of the request.",
            "enum": [
              "m5",
              "h1",
              "h6",
              "h24"
            ],
            "type": "string"
          }
        },
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "dexscreener",
      "publicName": "dexscreener__narratives_list",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "List trending DEX Screener narratives",
      "toolId": "dexscreener.trending"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 4668,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "chain": {
            "anyOf": [
              {
                "minLength": 1,
                "type": "string"
              },
              {
                "minimum": 1,
                "type": "integer"
              }
            ],
            "description": "Chain slug the pool lives on, for example solana, ethereum, bsc, base or robinhood. Chain slug/alias, or the numeric chain id `TokenFind` returns (e.g. `base` or `8453`). A pair or token address is only unique within one chain, so this is required; an unknown slug is refused by name with the nearest catalog matches rather than answered with zero rows."
          },
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "fields": {
            "description": "Comma-separated row field GROUPS to return, not individual field names. Supported groups: security, holders, liquidityLocks, supply, profile, listings, venues, suspiciousFunctionSource. Defaults to security, holders, liquidityLocks, supply. security carries the per-provider audit blocks and their disagreements and is always included. venues adds every venue GoPlus saw the token trading on (measured 26 for PEPE), which answers \"where else does this trade\" without a second call. suspiciousFunctionSource adds QuickIntel's verbatim Solidity for the functions it flagged and is the heaviest group by far, which is why it is opt-in: it is the difference between \"canMint: true\" and seeing the mint function. An unknown group name is refused with the full list rather than ignored.",
            "type": "string"
          },
          "inverted": {
            "description": "Report on the QUOTE token instead of the base token. Defaults to false. Use it when the pool is quoted in the token you actually care about; subject.reportedToken always names which token the report is about, so orientation is never inferred from the pool name.",
            "type": "boolean"
          },
          "pairAddress": {
            "description": "The pool (pair) address. Give this when you already know the exact pool, from dexscreener__pairs_search, a screening board, or dexscreener__pair_get. Either this or tokenAddress is required. When both are given, pairAddress wins and the response says so rather than silently ignoring the other.",
            "type": "string"
          },
          "tokenAddress": {
            "description": "A token contract address, resolved to the deepest pool of the provider's bounded 30-row search window on this chain. Use it when you have a token but not a pool. resolutionBasis echoes that a resolution happened and resolutionNote states the bound: \"deepest\" means deepest among at most 30 pools the provider chose to return, never a claim about every pool the token trades in. For the full pool list call dexscreener__token_pairs_list. On this tool the choice also changes the CACHE the provider answers from: the pair-id route and the token-id route hold SEPARATE cache entries, and what they routinely differ in is FRESHNESS rather than answers. Re-measured twice back to back, the two entries carried the same holder counts and the same non-null block set while their underlying analyses were 32 minutes and 4 hours 45 minutes apart, and the set of blocks present has also been measured flapping between entries. The extreme is real but was seen once: 8,483 holders against 351 for six minutes. subject.route names which one served the report, so two reports that differ can be recognised as two different questions rather than a contradiction.",
            "type": "string"
          }
        },
        "required": [
          "chain"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "dexscreener",
      "publicName": "dexscreener__pair_details_get",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Read a pair's safety report",
      "toolId": "dexscreener.pair.details"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 3707,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "chain": {
            "anyOf": [
              {
                "minLength": 1,
                "type": "string"
              },
              {
                "minimum": 1,
                "type": "integer"
              }
            ],
            "description": "Chain slug the pair or token lives on, for example solana, ethereum, base or bsc. Chain slug/alias, or the numeric chain id `TokenFind` returns (e.g. `base` or `8453`). An unknown slug is refused by name with the nearest catalog matches rather than answered as if the pair did not exist."
          },
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "fields": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Comma-separated row field GROUPS to return, not individual field names, as a string or an array. Groups: core, flow, reserves, allWindows, profile, launchpad, identity, defaulting to core, which already carries identity, the selected window's price, volume, liquidity, market cap, age, counts and every derived ratio. profile (issuer links and description) and allWindows (four times the per-row metric payload) are the two heaviest and are NOT in the default projection. Every group here shapes data this call already fetched and costs no extra request. The side reads reactions and insight are NOT groups: they go in \"include\", and naming one here is refused by name with that remedy rather than silently performed, because it would cost a request the caller did not ask for. An unknown group name is refused with the full list rather than ignored."
          },
          "include": {
            "anyOf": [
              {
                "enum": [
                  "reactions",
                  "insight"
                ],
                "type": "string"
              },
              {
                "items": {
                  "enum": [
                    "reactions",
                    "insight"
                  ],
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Optional SIDE READS to perform, as a comma-separated string or an array. Each value costs one extra provider request, which is what separates this key from \"fields\": fields shapes rows already in hand and is free, include goes and fetches more. reactions returns the crowd emoji counters on this pair (a 142-byte call). insight returns a provider-generated text blurb about the token, labelled as provider-generated, sanitized like any untrusted text, and never evidence for an action. COVERAGE IS SOLANA-ONLY AND NARROW, measured from the provider's own catalog: 1,218 of 1,218 insights are on solana, no token on any other chain has one, and majors such as wrapped SOL have none either. On any non-Solana chain this request is a provider call that is certain to come back empty, so do not spend it there. An absent blurb is NORMAL and comes back carrying the provider's own reason code, never as an error, and the answer distinguishes \"the provider has written nothing\" from \"the provider faulted\". Omit this key and neither request is made. An unknown value is refused with the full list rather than ignored."
          },
          "pairAddress": {
            "description": "Address of the pool itself. The direct route: it names the pair exactly and needs no resolution step. Give this or tokenAddress; giving both uses pairAddress and says so. EVM checksum casing is accepted and the address you get back is the provider's own. MEASURED: the channel also accepts a TOKEN address here and answers with a pool of its own choosing, on a rule it does not publish. That is not refused, but it is not hidden either: resolvedPair is always the pool the answer describes, requestedPairAddress echoes what you sent, and when they differ resolutionBasis reads provider_resolved_from_token and says the pool was the provider's pick and not a deepest-pool claim. Pass a token as tokenAddress instead when you want the deepest pool chosen and reported.",
            "type": "string"
          },
          "tokenAddress": {
            "description": "Token contract address, when the pool address is not known. Resolved to the deepest pool of the provider's bounded search window, which is at most 30 pools and is NOT a global claim about every pool the token trades in; resolvedFrom and resolutionBasis echo what answered. Give this or pairAddress.",
            "type": "string"
          },
          "window": {
            "description": "Which stats window the single-window metrics report: m5, h1, h6 or h24. Defaults to h24. The provider measures all four regardless; this selects which one the flat fields carry, and the allWindows field group returns every one of them.",
            "enum": [
              "m5",
              "h1",
              "h6",
              "h24"
            ],
            "type": "string"
          }
        },
        "required": [
          "chain"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "dexscreener",
      "publicName": "dexscreener__pair_get",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Read one pair's live state",
      "toolId": "dexscreener.pair.get"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 2623,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "fields": {
            "description": "Comma-separated row field GROUPS to return, not individual field names. Supported groups: core, flow, reserves, allWindows, profile, launchpad, identity. Defaults to core, which carries the chain, dex, pair address, base-token address and symbols, the selected window's price, volume, liquidity, market cap, age, counts and every derived ratio. The two heaviest are profile (unbounded issuer-authored prose and links) and allWindows (four times the per-row metric payload). The `identity` group is NOT part of core and is what adds ammId, quoteTokenAddress, both tokens' decimals, priceNative and pairCreatedAtMs - the fields that let you tell a real pool from a mispriced one, so ask for it when the dollar columns are what you are judging. An unknown group name is refused with the full list rather than ignored.",
            "type": "string"
          },
          "maxBuyCount": {
            "description": "Ceiling on the number of BUY trades, as a COUNT. Measured over thresholdWindow, which defaults to window. One window per threshold family is reachable here: the provider itself accepts several windows of one family at once and ANDs them (a 1,000,000 USD h24 volume floor combined with a 1,000 USD m5 volume floor was measured returning 104 rows), but this surface carries a single thresholdWindow, so a two-window condition on one metric has to be checked against the returned rows instead of filtered for. PRESENCE RULE, measured, and it applies to every ceiling on this surface: a maximum matches only rows that CARRY the field. A row the provider reported no value for is excluded, not kept as if it were below the bound. Measured on a 64,420-row solana population: a liquidity ceiling removed 1,687 rows that carry no liquidity figure, an FDV ceiling removed 3,286, and a launchpad-progress ceiling reduced the set to launchpad pairs only. So a ceiling narrows on two axes at once, and the second one is silent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once. ON THIS TOOL the comparison runs HERE, over the rows already in hand, and NOT on the provider: this tool applies no default floor, so there is nothing to remove and no disableQualityFloor to send; there is no separate thresholdWindow, so a windowed threshold measures over window; and null is not a legal value here, so OMIT the parameter to keep every row. droppedByFilter counts every row removed and by which threshold. A row whose metric the provider did not report is NOT compared and NOT dropped: it is kept and counted in clientFiltering.notEvaluated, because a missing measurement is not a measurement of zero. The filtered set is the explicit list you passed, so the filtering is exhaustive over that list rather than a sample.",
            "type": "number"
          },
          "maxFdvUsd": {
            "description": "Ceiling on fully diluted valuation, in US dollars. FDV values the total supply rather than the circulating supply, so it is the larger number. PRESENCE RULE, measured, and it applies to every ceiling on this surface: a maximum matches only rows that CARRY the field. A row the provider reported no value for is excluded, not kept as if it were below the bound. Measured on a 64,420-row solana population: a liquidity ceiling removed 1,687 rows that carry no liquidity figure, an FDV ceiling removed 3,286, and a launchpad-progress ceiling reduced the set to launchpad pairs only. So a ceiling narrows on two axes at once, and the second one is silent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once. ON THIS TOOL the comparison runs HERE, over the rows already in hand, and NOT on the provider: this tool applies no default floor, so there is nothing to remove and no disableQualityFloor to send; there is no separate thresholdWindow, so a windowed threshold measures over window; and null is not a legal value here, so OMIT the parameter to keep every row. droppedByFilter counts every row removed and by which threshold. A row whose metric the provider did not report is NOT compared and NOT dropped: it is kept and counted in clientFiltering.notEvaluated, because a missing measurement is not a measurement of zero. The filtered set is the explicit list you passed, so the filtering is exhaustive over that list rather than a sample.",
            "type": "number"
          },
          "maxLaunchpadProgressPct": {
            "description": "Ceiling on bonding-curve completion, as a PERCENT of the curve. Below 100 keeps tokens still on the curve. Only launchpad pairs carry it. PRESENCE RULE, measured, and it applies to every ceiling on this surface: a maximum matches only rows that CARRY the field. A row the provider reported no value for is excluded, not kept as if it were below the bound. Measured on a 64,420-row solana population: a liquidity ceiling removed 1,687 rows that carry no liquidity figure, an FDV ceiling removed 3,286, and a launchpad-progress ceiling reduced the set to launchpad pairs only. So a ceiling narrows on two axes at once, and the second one is silent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once. ON THIS TOOL the comparison runs HERE, over the rows already in hand, and NOT on the provider: this tool applies no default floor, so there is nothing to remove and no disableQualityFloor to send; there is no separate thresholdWindow, so a windowed threshold measures over window; and null is not a legal value here, so OMIT the parameter to keep every row. droppedByFilter counts every row removed and by which threshold. A row whose metric the provider did not report is NOT compared and NOT dropped: it is kept and counted in clientFiltering.notEvaluated, because a missing measurement is not a measurement of zero. The filtered set is the explicit list you passed, so the filtering is exhaustive over that list rather than a sample.",
            "type": "number"
          },
          "maxLiquidityUsd": {
            "description": "Ceiling on pool liquidity, in US dollars. Useful for finding small pools deliberately; liquidity has no window. PRESENCE RULE, measured, and it applies to every ceiling on this surface: a maximum matches only rows that CARRY the field. A row the provider reported no value for is excluded, not kept as if it were below the bound. Measured on a 64,420-row solana population: a liquidity ceiling removed 1,687 rows that carry no liquidity figure, an FDV ceiling removed 3,286, and a launchpad-progress ceiling reduced the set to launchpad pairs only. So a ceiling narrows on two axes at once, and the second one is silent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once. ON THIS TOOL the comparison runs HERE, over the rows already in hand, and NOT on the provider: this tool applies no default floor, so there is nothing to remove and no disableQualityFloor to send; there is no separate thresholdWindow, so a windowed threshold measures over window; and null is not a legal value here, so OMIT the parameter to keep every row. droppedByFilter counts every row removed and by which threshold. A row whose metric the provider did not report is NOT compared and NOT dropped: it is kept and counted in clientFiltering.notEvaluated, because a missing measurement is not a measurement of zero. The filtered set is the explicit list you passed, so the filtering is exhaustive over that list rather than a sample.",
            "type": "number"
          },
          "maxMarketCapUsd": {
            "description": "Ceiling on market capitalisation, in US dollars. The provider's own circulating-supply valuation, with no window. PRESENCE RULE, measured, and it applies to every ceiling on this surface: a maximum matches only rows that CARRY the field. A row the provider reported no value for is excluded, not kept as if it were below the bound. Measured on a 64,420-row solana population: a liquidity ceiling removed 1,687 rows that carry no liquidity figure, an FDV ceiling removed 3,286, and a launchpad-progress ceiling reduced the set to launchpad pairs only. So a ceiling narrows on two axes at once, and the second one is silent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once. ON THIS TOOL the comparison runs HERE, over the rows already in hand, and NOT on the provider: this tool applies no default floor, so there is nothing to remove and no disableQualityFloor to send; there is no separate thresholdWindow, so a windowed threshold measures over window; and null is not a legal value here, so OMIT the parameter to keep every row. droppedByFilter counts every row removed and by which threshold. A row whose metric the provider did not report is NOT compared and NOT dropped: it is kept and counted in clientFiltering.notEvaluated, because a missing measurement is not a measurement of zero. The filtered set is the explicit list you passed, so the filtering is exhaustive over that list rather than a sample.",
            "type": "number"
          },
          "maxPairAgeSeconds": {
            "description": "Ceiling on pair age, in SECONDS since the pair was created. 86400 is one day. The provider filters in hours, so sub-hour precision comes from the returned pairAgeSeconds rather than from this filter. PRESENCE RULE, measured, and it applies to every ceiling on this surface: a maximum matches only rows that CARRY the field. A row the provider reported no value for is excluded, not kept as if it were below the bound. Measured on a 64,420-row solana population: a liquidity ceiling removed 1,687 rows that carry no liquidity figure, an FDV ceiling removed 3,286, and a launchpad-progress ceiling reduced the set to launchpad pairs only. So a ceiling narrows on two axes at once, and the second one is silent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once. ON THIS TOOL the comparison runs HERE, over the rows already in hand, and NOT on the provider: this tool applies no default floor, so there is nothing to remove and no disableQualityFloor to send; there is no separate thresholdWindow, so a windowed threshold measures over window; and null is not a legal value here, so OMIT the parameter to keep every row. droppedByFilter counts every row removed and by which threshold. A row whose metric the provider did not report is NOT compared and NOT dropped: it is kept and counted in clientFiltering.notEvaluated, because a missing measurement is not a measurement of zero. The filtered set is the explicit list you passed, so the filtering is exhaustive over that list rather than a sample.",
            "type": "number"
          },
          "maxPriceChangePct": {
            "description": "Ceiling on price change, as a PERCENT and not a fraction. Measured over thresholdWindow, which defaults to window. One window per threshold family is reachable here: the provider itself accepts several windows of one family at once and ANDs them (a 1,000,000 USD h24 volume floor combined with a 1,000 USD m5 volume floor was measured returning 104 rows), but this surface carries a single thresholdWindow, so a two-window condition on one metric has to be checked against the returned rows instead of filtered for. Negative values are legal, so -50 keeps only pairs down at least half. PRESENCE RULE, measured, and it applies to every ceiling on this surface: a maximum matches only rows that CARRY the field. A row the provider reported no value for is excluded, not kept as if it were below the bound. Measured on a 64,420-row solana population: a liquidity ceiling removed 1,687 rows that carry no liquidity figure, an FDV ceiling removed 3,286, and a launchpad-progress ceiling reduced the set to launchpad pairs only. So a ceiling narrows on two axes at once, and the second one is silent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once. ON THIS TOOL the comparison runs HERE, over the rows already in hand, and NOT on the provider: this tool applies no default floor, so there is nothing to remove and no disableQualityFloor to send; there is no separate thresholdWindow, so a windowed threshold measures over window; and null is not a legal value here, so OMIT the parameter to keep every row. droppedByFilter counts every row removed and by which threshold. A row whose metric the provider did not report is NOT compared and NOT dropped: it is kept and counted in clientFiltering.notEvaluated, because a missing measurement is not a measurement of zero. The filtered set is the explicit list you passed, so the filtering is exhaustive over that list rather than a sample.",
            "type": "number"
          },
          "maxSellCount": {
            "description": "Ceiling on the number of SELL trades, as a COUNT. Measured over thresholdWindow, which defaults to window. One window per threshold family is reachable here: the provider itself accepts several windows of one family at once and ANDs them (a 1,000,000 USD h24 volume floor combined with a 1,000 USD m5 volume floor was measured returning 104 rows), but this surface carries a single thresholdWindow, so a two-window condition on one metric has to be checked against the returned rows instead of filtered for. PRESENCE RULE, measured, and it applies to every ceiling on this surface: a maximum matches only rows that CARRY the field. A row the provider reported no value for is excluded, not kept as if it were below the bound. Measured on a 64,420-row solana population: a liquidity ceiling removed 1,687 rows that carry no liquidity figure, an FDV ceiling removed 3,286, and a launchpad-progress ceiling reduced the set to launchpad pairs only. So a ceiling narrows on two axes at once, and the second one is silent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once. ON THIS TOOL the comparison runs HERE, over the rows already in hand, and NOT on the provider: this tool applies no default floor, so there is nothing to remove and no disableQualityFloor to send; there is no separate thresholdWindow, so a windowed threshold measures over window; and null is not a legal value here, so OMIT the parameter to keep every row. droppedByFilter counts every row removed and by which threshold. A row whose metric the provider did not report is NOT compared and NOT dropped: it is kept and counted in clientFiltering.notEvaluated, because a missing measurement is not a measurement of zero. The filtered set is the explicit list you passed, so the filtering is exhaustive over that list rather than a sample.",
            "type": "number"
          },
          "maxTxnCount": {
            "description": "Ceiling on the number of trades, as a COUNT and not a volume. Measured over thresholdWindow, which defaults to window. One window per threshold family is reachable here: the provider itself accepts several windows of one family at once and ANDs them (a 1,000,000 USD h24 volume floor combined with a 1,000 USD m5 volume floor was measured returning 104 rows), but this surface carries a single thresholdWindow, so a two-window condition on one metric has to be checked against the returned rows instead of filtered for. PRESENCE RULE, measured, and it applies to every ceiling on this surface: a maximum matches only rows that CARRY the field. A row the provider reported no value for is excluded, not kept as if it were below the bound. Measured on a 64,420-row solana population: a liquidity ceiling removed 1,687 rows that carry no liquidity figure, an FDV ceiling removed 3,286, and a launchpad-progress ceiling reduced the set to launchpad pairs only. So a ceiling narrows on two axes at once, and the second one is silent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once. ON THIS TOOL the comparison runs HERE, over the rows already in hand, and NOT on the provider: this tool applies no default floor, so there is nothing to remove and no disableQualityFloor to send; there is no separate thresholdWindow, so a windowed threshold measures over window; and null is not a legal value here, so OMIT the parameter to keep every row. droppedByFilter counts every row removed and by which threshold. A row whose metric the provider did not report is NOT compared and NOT dropped: it is kept and counted in clientFiltering.notEvaluated, because a missing measurement is not a measurement of zero. The filtered set is the explicit list you passed, so the filtering is exhaustive over that list rather than a sample.",
            "type": "number"
          },
          "maxVolumeUsd": {
            "description": "Ceiling on traded volume, in US dollars. Measured over thresholdWindow, which defaults to window. One window per threshold family is reachable here: the provider itself accepts several windows of one family at once and ANDs them (a 1,000,000 USD h24 volume floor combined with a 1,000 USD m5 volume floor was measured returning 104 rows), but this surface carries a single thresholdWindow, so a two-window condition on one metric has to be checked against the returned rows instead of filtered for. PRESENCE RULE, measured, and it applies to every ceiling on this surface: a maximum matches only rows that CARRY the field. A row the provider reported no value for is excluded, not kept as if it were below the bound. Measured on a 64,420-row solana population: a liquidity ceiling removed 1,687 rows that carry no liquidity figure, an FDV ceiling removed 3,286, and a launchpad-progress ceiling reduced the set to launchpad pairs only. So a ceiling narrows on two axes at once, and the second one is silent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once. ON THIS TOOL the comparison runs HERE, over the rows already in hand, and NOT on the provider: this tool applies no default floor, so there is nothing to remove and no disableQualityFloor to send; there is no separate thresholdWindow, so a windowed threshold measures over window; and null is not a legal value here, so OMIT the parameter to keep every row. droppedByFilter counts every row removed and by which threshold. A row whose metric the provider did not report is NOT compared and NOT dropped: it is kept and counted in clientFiltering.notEvaluated, because a missing measurement is not a measurement of zero. The filtered set is the explicit list you passed, so the filtering is exhaustive over that list rather than a sample.",
            "type": "number"
          },
          "minBoostCount": {
            "description": "Floor on the number of paid boosts ACTIVE on the pair, as a COUNT. A boost is bought visibility, never demand or safety. This is more specific than onlyBoosted and wins over it when both are sent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once. ON THIS TOOL the comparison runs HERE, over the rows already in hand, and NOT on the provider: this tool applies no default floor, so there is nothing to remove and no disableQualityFloor to send; there is no separate thresholdWindow, so a windowed threshold measures over window; and null is not a legal value here, so OMIT the parameter to keep every row. droppedByFilter counts every row removed and by which threshold. A row whose metric the provider did not report is NOT compared and NOT dropped: it is kept and counted in clientFiltering.notEvaluated, because a missing measurement is not a measurement of zero. The filtered set is the explicit list you passed, so the filtering is exhaustive over that list rather than a sample.",
            "type": "number"
          },
          "minBuyCount": {
            "description": "Floor on the number of BUY trades, as a COUNT. Measured over thresholdWindow, which defaults to window. One window per threshold family is reachable here: the provider itself accepts several windows of one family at once and ANDs them (a 1,000,000 USD h24 volume floor combined with a 1,000 USD m5 volume floor was measured returning 104 rows), but this surface carries a single thresholdWindow, so a two-window condition on one metric has to be checked against the returned rows instead of filtered for. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once. ON THIS TOOL the comparison runs HERE, over the rows already in hand, and NOT on the provider: this tool applies no default floor, so there is nothing to remove and no disableQualityFloor to send; there is no separate thresholdWindow, so a windowed threshold measures over window; and null is not a legal value here, so OMIT the parameter to keep every row. droppedByFilter counts every row removed and by which threshold. A row whose metric the provider did not report is NOT compared and NOT dropped: it is kept and counted in clientFiltering.notEvaluated, because a missing measurement is not a measurement of zero. The filtered set is the explicit list you passed, so the filtering is exhaustive over that list rather than a sample.",
            "type": "number"
          },
          "minFdvUsd": {
            "description": "Floor on fully diluted valuation, in US dollars. FDV values the total supply rather than the circulating supply, so it is the larger number. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once. ON THIS TOOL the comparison runs HERE, over the rows already in hand, and NOT on the provider: this tool applies no default floor, so there is nothing to remove and no disableQualityFloor to send; there is no separate thresholdWindow, so a windowed threshold measures over window; and null is not a legal value here, so OMIT the parameter to keep every row. droppedByFilter counts every row removed and by which threshold. A row whose metric the provider did not report is NOT compared and NOT dropped: it is kept and counted in clientFiltering.notEvaluated, because a missing measurement is not a measurement of zero. The filtered set is the explicit list you passed, so the filtering is exhaustive over that list rather than a sample.",
            "type": "number"
          },
          "minLaunchpadProgressPct": {
            "description": "Floor on bonding-curve completion, as a PERCENT of the curve. 100 means the token has graduated and migrated to a DEX. Only launchpad pairs carry it. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once. ON THIS TOOL the comparison runs HERE, over the rows already in hand, and NOT on the provider: this tool applies no default floor, so there is nothing to remove and no disableQualityFloor to send; there is no separate thresholdWindow, so a windowed threshold measures over window; and null is not a legal value here, so OMIT the parameter to keep every row. droppedByFilter counts every row removed and by which threshold. A row whose metric the provider did not report is NOT compared and NOT dropped: it is kept and counted in clientFiltering.notEvaluated, because a missing measurement is not a measurement of zero. The filtered set is the explicit list you passed, so the filtering is exhaustive over that list rather than a sample.",
            "type": "number"
          },
          "minLiquidityUsd": {
            "description": "Floor on pool liquidity, in US dollars. Liquidity has no window: it is the pool's depth right now. Bonding-curve pairs carry no liquidity field at all, so any liquidity threshold excludes them by construction. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once. ON THIS TOOL the comparison runs HERE, over the rows already in hand, and NOT on the provider: this tool applies no default floor, so there is nothing to remove and no disableQualityFloor to send; there is no separate thresholdWindow, so a windowed threshold measures over window; and null is not a legal value here, so OMIT the parameter to keep every row. droppedByFilter counts every row removed and by which threshold. A row whose metric the provider did not report is NOT compared and NOT dropped: it is kept and counted in clientFiltering.notEvaluated, because a missing measurement is not a measurement of zero. The filtered set is the explicit list you passed, so the filtering is exhaustive over that list rather than a sample.",
            "type": "number"
          },
          "minMarketCapUsd": {
            "description": "Floor on market capitalisation, in US dollars. The provider's own circulating-supply valuation, with no window. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once. ON THIS TOOL the comparison runs HERE, over the rows already in hand, and NOT on the provider: this tool applies no default floor, so there is nothing to remove and no disableQualityFloor to send; there is no separate thresholdWindow, so a windowed threshold measures over window; and null is not a legal value here, so OMIT the parameter to keep every row. droppedByFilter counts every row removed and by which threshold. A row whose metric the provider did not report is NOT compared and NOT dropped: it is kept and counted in clientFiltering.notEvaluated, because a missing measurement is not a measurement of zero. The filtered set is the explicit list you passed, so the filtering is exhaustive over that list rather than a sample.",
            "type": "number"
          },
          "minPairAgeSeconds": {
            "description": "Floor on pair age, in SECONDS since the pair was created. The provider filters age in whole and fractional hours, so this is converted for you; the exact age comes back per row in pairAgeSeconds. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once. ON THIS TOOL the comparison runs HERE, over the rows already in hand, and NOT on the provider: this tool applies no default floor, so there is nothing to remove and no disableQualityFloor to send; there is no separate thresholdWindow, so a windowed threshold measures over window; and null is not a legal value here, so OMIT the parameter to keep every row. droppedByFilter counts every row removed and by which threshold. A row whose metric the provider did not report is NOT compared and NOT dropped: it is kept and counted in clientFiltering.notEvaluated, because a missing measurement is not a measurement of zero. The filtered set is the explicit list you passed, so the filtering is exhaustive over that list rather than a sample.",
            "type": "number"
          },
          "minPriceChangePct": {
            "description": "Floor on price change, as a PERCENT and not a fraction. Measured over thresholdWindow, which defaults to window. One window per threshold family is reachable here: the provider itself accepts several windows of one family at once and ANDs them (a 1,000,000 USD h24 volume floor combined with a 1,000 USD m5 volume floor was measured returning 104 rows), but this surface carries a single thresholdWindow, so a two-window condition on one metric has to be checked against the returned rows instead of filtered for. 10 means ten percent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once. ON THIS TOOL the comparison runs HERE, over the rows already in hand, and NOT on the provider: this tool applies no default floor, so there is nothing to remove and no disableQualityFloor to send; there is no separate thresholdWindow, so a windowed threshold measures over window; and null is not a legal value here, so OMIT the parameter to keep every row. droppedByFilter counts every row removed and by which threshold. A row whose metric the provider did not report is NOT compared and NOT dropped: it is kept and counted in clientFiltering.notEvaluated, because a missing measurement is not a measurement of zero. The filtered set is the explicit list you passed, so the filtering is exhaustive over that list rather than a sample.",
            "type": "number"
          },
          "minSellCount": {
            "description": "Floor on the number of SELL trades, as a COUNT. Measured over thresholdWindow, which defaults to window. One window per threshold family is reachable here: the provider itself accepts several windows of one family at once and ANDs them (a 1,000,000 USD h24 volume floor combined with a 1,000 USD m5 volume floor was measured returning 104 rows), but this surface carries a single thresholdWindow, so a two-window condition on one metric has to be checked against the returned rows instead of filtered for. A sell-side floor is what separates a real two-sided market from a pair nobody can exit. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once. ON THIS TOOL the comparison runs HERE, over the rows already in hand, and NOT on the provider: this tool applies no default floor, so there is nothing to remove and no disableQualityFloor to send; there is no separate thresholdWindow, so a windowed threshold measures over window; and null is not a legal value here, so OMIT the parameter to keep every row. droppedByFilter counts every row removed and by which threshold. A row whose metric the provider did not report is NOT compared and NOT dropped: it is kept and counted in clientFiltering.notEvaluated, because a missing measurement is not a measurement of zero. The filtered set is the explicit list you passed, so the filtering is exhaustive over that list rather than a sample.",
            "type": "number"
          },
          "minTxnCount": {
            "description": "Floor on the number of trades, as a COUNT and not a volume. Measured over thresholdWindow, which defaults to window. One window per threshold family is reachable here: the provider itself accepts several windows of one family at once and ANDs them (a 1,000,000 USD h24 volume floor combined with a 1,000 USD m5 volume floor was measured returning 104 rows), but this surface carries a single thresholdWindow, so a two-window condition on one metric has to be checked against the returned rows instead of filtered for. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once. ON THIS TOOL the comparison runs HERE, over the rows already in hand, and NOT on the provider: this tool applies no default floor, so there is nothing to remove and no disableQualityFloor to send; there is no separate thresholdWindow, so a windowed threshold measures over window; and null is not a legal value here, so OMIT the parameter to keep every row. droppedByFilter counts every row removed and by which threshold. A row whose metric the provider did not report is NOT compared and NOT dropped: it is kept and counted in clientFiltering.notEvaluated, because a missing measurement is not a measurement of zero. The filtered set is the explicit list you passed, so the filtering is exhaustive over that list rather than a sample.",
            "type": "number"
          },
          "minVolumeUsd": {
            "description": "Floor on traded volume, in US dollars. Measured over thresholdWindow, which defaults to window. One window per threshold family is reachable here: the provider itself accepts several windows of one family at once and ANDs them (a 1,000,000 USD h24 volume floor combined with a 1,000 USD m5 volume floor was measured returning 104 rows), but this surface carries a single thresholdWindow, so a two-window condition on one metric has to be checked against the returned rows instead of filtered for. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once. ON THIS TOOL the comparison runs HERE, over the rows already in hand, and NOT on the provider: this tool applies no default floor, so there is nothing to remove and no disableQualityFloor to send; there is no separate thresholdWindow, so a windowed threshold measures over window; and null is not a legal value here, so OMIT the parameter to keep every row. droppedByFilter counts every row removed and by which threshold. A row whose metric the provider did not report is NOT compared and NOT dropped: it is kept and counted in clientFiltering.notEvaluated, because a missing measurement is not a measurement of zero. The filtered set is the explicit list you passed, so the filtering is exhaustive over that list rather than a sample.",
            "type": "number"
          },
          "pairs": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Pool identities to refresh, spelled chain:pairAddress, comma-separated or as an array, across any mix of chains. There is no artificial ceiling on how many you may pass: a live 300-input probe completed, and longer lists are split into chunks of 500 internally with the split reported in providerWindow. Give pairs, tokens, or both."
          },
          "sortBy": {
            "description": "How the resolved rows are ordered: volume, txns, liquidity, marketCap, priceChange or pairAge. Defaults to volume. The ordering runs on the provider over your own list, so it ranks exactly the identities you passed and nothing else.",
            "enum": [
              "volume",
              "txns",
              "liquidity",
              "marketCap",
              "priceChange",
              "pairAge"
            ],
            "type": "string"
          },
          "sortDir": {
            "description": "Ranking direction: desc for the largest values first, asc for the smallest. Defaults to desc.",
            "enum": [
              "desc",
              "asc"
            ],
            "type": "string"
          },
          "tokens": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Token identities to refresh, spelled chain:tokenAddress, comma-separated or as an array. MEASURED CAVEAT: a token resolves to ONE provider-canonical pair and it is not necessarily the deepest (a WETH lookup answered with a 4.23M USD pool while a 117.31M USD pool existed), so resolutionBasis reports provider_canonical. Use dexscreener__pair_get when you want the deepest pool instead."
          },
          "window": {
            "description": "Which stats window the single-window metrics report: m5, h1, h6 or h24. Defaults to h24. The provider measures all four regardless; this selects which one the flat fields carry, and the allWindows field group returns every one of them.",
            "enum": [
              "m5",
              "h1",
              "h6",
              "h24"
            ],
            "type": "string"
          }
        },
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "dexscreener",
      "publicName": "dexscreener__pairs_batch_get",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Batch-read pairs or tokens",
      "toolId": "dexscreener.pairs.batch"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 2352,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "baseTokenSuffixes": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Base-token mint-address suffixes to keep, the vanity-suffix convention some launchpads use. Measured on solana: pump matched 20,852 pairs and bonk matched 1,595. Accepts either a comma-separated string (\"a,b\") or an array of strings ([\"a\",\"b\"]); the two are equivalent."
          },
          "chainIds": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "DexScreener chain slugs to screen, for example solana, base, ethereum, bsc. Omit to screen every indexed chain at once. Accepts either a comma-separated string (\"a,b\") or an array of strings ([\"a\",\"b\"]); the two are equivalent. Chain and dex values come from dexscreener__chains_list; an unknown chain is refused by name with the nearest catalog matches, because the provider answers one with zero rows and HTTP 200, which is indistinguishable from a real empty result."
          },
          "dexIds": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "DEX identifiers to keep, for example raydium, uniswap, pumpswap. Multiple values are ORed: a pair on any listed dex is kept. The accepted set is open and per-chain, so read it from dexscreener__chains_list rather than guessing. Accepts either a comma-separated string (\"a,b\") or an array of strings ([\"a\",\"b\"]); the two are equivalent."
          },
          "disableQualityFloor": {
            "description": "Drop EVERY default quality floor this tool applies, in one flag. Defaults to false. Your own explicit thresholds still apply, so disableQualityFloor with minLiquidityUsd 5000 means \"none of the tool's defaults, but keep mine\". Each dropped default is named in floorAccounting and qualityFloorApplied becomes false. Expect the unfloored result to contain arithmetic artifacts: on the price-change boards the top rows without a floor are billion-percent moves on pairs nobody can trade. IT DOES NOT READMIT BONDING-CURVE PAIRS, and that is the half agents miss: dropping the floors is necessary but not sufficient, because the provider excludes bonding-curve VENUES by default and only includeLaunchpadPairs lifts that. Measured on solana: a launchpad filter without the lift returned 0 on-curve pairs, and with the lift 71,334; adding a 25,000 USD liquidity floor to those 71,334 returned 0 again, because on-curve rows carry no liquidity field at all. For on-curve pairs, send includeLaunchpadPairs AND keep every liquidity threshold off.",
            "type": "boolean"
          },
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "excludeDexIds": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "DEX identifiers to drop. DANGEROUS BY CONSTRUCTION: the provider applies a hidden default that already excludes bonding-curve venues, and sending this key AT ALL replaces that default, so excluding one dex can make the result set BIGGER. Measured on solana: 53,094 rows normally, 84,058 when only PumpSwap was excluded. Use includeLaunchpadPairs for the common case; exclusionDefaultReplaced reports whenever this happened. Accepts either a comma-separated string (\"a,b\") or an array of strings ([\"a\",\"b\"]); the two are equivalent."
          },
          "fields": {
            "description": "Comma-separated row field GROUPS to return, not individual field names. Supported groups: core, flow, reserves, allWindows, profile, launchpad, identity. Defaults to core, which carries the chain, dex, pair address, base-token address and symbols, the selected window's price, volume, liquidity, market cap, age, counts and every derived ratio. The two heaviest are profile (unbounded issuer-authored prose and links) and allWindows (four times the per-row metric payload). The `identity` group is NOT part of core and is what adds ammId, quoteTokenAddress, both tokens' decimals, priceNative and pairCreatedAtMs - the fields that let you tell a real pool from a mispriced one, so ask for it when the dollar columns are what you are judging. An unknown group name is refused with the full list rather than ignored.",
            "type": "string"
          },
          "includeInactive": {
            "description": "Keep pairs that had no activity inside the selected window. Defaults to false. Measured on solana at m5: 2,513 rows becomes 53,088, so this widens the set enormously on short windows and barely at all on h24.",
            "type": "boolean"
          },
          "includeLaunchpadPairs": {
            "description": "Lift the provider's hidden exclusion of bonding-curve pairs, the safe alternative to excludeDexIds. Defaults to false. Measured on solana: 53,094 rows becomes 102,676. Named for the effect rather than the mechanism, because the mechanism is an empty exclusion list.",
            "type": "boolean"
          },
          "labels": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Pool label values to keep, for example v2, v3, v4, CLMM, DLMM, CPMM. Matching is case-insensitive on the provider. Accepts either a comma-separated string (\"a,b\") or an array of strings ([\"a\",\"b\"]); the two are equivalent."
          },
          "launchpadIds": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Launchpad identifiers to keep, for example pumpfun, launchlab, meteoradbc, bags, fourmeme. MEASURED: the provider attaches a launchpad id to a pair only AFTER it graduates, so this filter matches graduated pairs and nothing else. While a curve is still running the launchpad is the pair's DEX, so use dexIds with the same value there (dexIds: pumpfun). On dexscreener__launchpad_pairs_list, pairing this with stage: \"bonding\" is refused by name rather than answered with an empty board: it returned 0 rows of a 0-row population while dexIds returned 53,478. KNOWN DEPTH GAP: no tool on this surface enumerates the launchpad vocabulary - dexscreener__chains_list carries dex ids only - so the five spellings above are the list, measured live, and an id outside them is not refused by name the way an unknown chain slug is. It returns an empty board instead, so check your spelling against those five before reading a zero as an absence of launches. Accepts either a comma-separated string (\"a,b\") or an array of strings ([\"a\",\"b\"]); the two are equivalent."
          },
          "limit": {
            "description": "How many rows to return, from 1 to 100. Defaults to 20. 100 is the provider's own page size, so asking for more than that is refused rather than silently served short; use offset to walk further into the ranking.",
            "type": "number"
          },
          "maxBoostCount": {
            "description": "Ceiling on the number of paid boosts ACTIVE on the pair, as a COUNT. It bounds WITHIN the boosted population and does not select against it. The presence rule below is the whole story here: an active-boost count exists only on a boosted pair, so maxBoostCount 3 means \"is boosted AND has at most 3 boosts\", never \"is not heavily advertised\". Measured 2026-08-24 on solana: min 1 gave 105 rows (the boosted population), max 10 gave 51, max 1000000 gave 105 again rather than the 64,420-row baseline, and max 0 gave 0. THIS IS NOT A WAY TO EXCLUDE ADS: an agent setting it to avoid ad-pumped pairs receives only ad-carrying pairs. To judge advertising, leave it unset and read boostsActive per row, which is on every row by default. PRESENCE RULE, measured, and it applies to every ceiling on this surface: a maximum matches only rows that CARRY the field. A row the provider reported no value for is excluded, not kept as if it were below the bound. Measured on a 64,420-row solana population: a liquidity ceiling removed 1,687 rows that carry no liquidity figure, an FDV ceiling removed 3,286, and a launchpad-progress ceiling reduced the set to launchpad pairs only. So a ceiling narrows on two axes at once, and the second one is silent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "maxBuyCount": {
            "description": "Ceiling on the number of BUY trades, as a COUNT. Measured over thresholdWindow, which defaults to window. One window per threshold family is reachable here: the provider itself accepts several windows of one family at once and ANDs them (a 1,000,000 USD h24 volume floor combined with a 1,000 USD m5 volume floor was measured returning 104 rows), but this surface carries a single thresholdWindow, so a two-window condition on one metric has to be checked against the returned rows instead of filtered for. PRESENCE RULE, measured, and it applies to every ceiling on this surface: a maximum matches only rows that CARRY the field. A row the provider reported no value for is excluded, not kept as if it were below the bound. Measured on a 64,420-row solana population: a liquidity ceiling removed 1,687 rows that carry no liquidity figure, an FDV ceiling removed 3,286, and a launchpad-progress ceiling reduced the set to launchpad pairs only. So a ceiling narrows on two axes at once, and the second one is silent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "maxFdvUsd": {
            "description": "Ceiling on fully diluted valuation, in US dollars. FDV values the total supply rather than the circulating supply, so it is the larger number. PRESENCE RULE, measured, and it applies to every ceiling on this surface: a maximum matches only rows that CARRY the field. A row the provider reported no value for is excluded, not kept as if it were below the bound. Measured on a 64,420-row solana population: a liquidity ceiling removed 1,687 rows that carry no liquidity figure, an FDV ceiling removed 3,286, and a launchpad-progress ceiling reduced the set to launchpad pairs only. So a ceiling narrows on two axes at once, and the second one is silent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "maxLaunchpadProgressPct": {
            "description": "Ceiling on bonding-curve completion, as a PERCENT of the curve. Below 100 keeps tokens still on the curve. Only launchpad pairs carry it. PRESENCE RULE, measured, and it applies to every ceiling on this surface: a maximum matches only rows that CARRY the field. A row the provider reported no value for is excluded, not kept as if it were below the bound. Measured on a 64,420-row solana population: a liquidity ceiling removed 1,687 rows that carry no liquidity figure, an FDV ceiling removed 3,286, and a launchpad-progress ceiling reduced the set to launchpad pairs only. So a ceiling narrows on two axes at once, and the second one is silent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "maxLiquidityUsd": {
            "description": "Ceiling on pool liquidity, in US dollars. Useful for finding small pools deliberately; liquidity has no window. PRESENCE RULE, measured, and it applies to every ceiling on this surface: a maximum matches only rows that CARRY the field. A row the provider reported no value for is excluded, not kept as if it were below the bound. Measured on a 64,420-row solana population: a liquidity ceiling removed 1,687 rows that carry no liquidity figure, an FDV ceiling removed 3,286, and a launchpad-progress ceiling reduced the set to launchpad pairs only. So a ceiling narrows on two axes at once, and the second one is silent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "maxMarketCapUsd": {
            "description": "Ceiling on market capitalisation, in US dollars. The provider's own circulating-supply valuation, with no window. PRESENCE RULE, measured, and it applies to every ceiling on this surface: a maximum matches only rows that CARRY the field. A row the provider reported no value for is excluded, not kept as if it were below the bound. Measured on a 64,420-row solana population: a liquidity ceiling removed 1,687 rows that carry no liquidity figure, an FDV ceiling removed 3,286, and a launchpad-progress ceiling reduced the set to launchpad pairs only. So a ceiling narrows on two axes at once, and the second one is silent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "maxPairAgeSeconds": {
            "description": "Ceiling on pair age, in SECONDS since the pair was created. 86400 is one day. The provider filters in hours, so sub-hour precision comes from the returned pairAgeSeconds rather than from this filter. PRESENCE RULE, measured, and it applies to every ceiling on this surface: a maximum matches only rows that CARRY the field. A row the provider reported no value for is excluded, not kept as if it were below the bound. Measured on a 64,420-row solana population: a liquidity ceiling removed 1,687 rows that carry no liquidity figure, an FDV ceiling removed 3,286, and a launchpad-progress ceiling reduced the set to launchpad pairs only. So a ceiling narrows on two axes at once, and the second one is silent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once. This tool applies 86400 (one day) by default.",
            "type": "number"
          },
          "maxPriceChangePct": {
            "description": "Ceiling on price change, as a PERCENT and not a fraction. Measured over thresholdWindow, which defaults to window. One window per threshold family is reachable here: the provider itself accepts several windows of one family at once and ANDs them (a 1,000,000 USD h24 volume floor combined with a 1,000 USD m5 volume floor was measured returning 104 rows), but this surface carries a single thresholdWindow, so a two-window condition on one metric has to be checked against the returned rows instead of filtered for. Negative values are legal, so -50 keeps only pairs down at least half. PRESENCE RULE, measured, and it applies to every ceiling on this surface: a maximum matches only rows that CARRY the field. A row the provider reported no value for is excluded, not kept as if it were below the bound. Measured on a 64,420-row solana population: a liquidity ceiling removed 1,687 rows that carry no liquidity figure, an FDV ceiling removed 3,286, and a launchpad-progress ceiling reduced the set to launchpad pairs only. So a ceiling narrows on two axes at once, and the second one is silent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "maxSellCount": {
            "description": "Ceiling on the number of SELL trades, as a COUNT. Measured over thresholdWindow, which defaults to window. One window per threshold family is reachable here: the provider itself accepts several windows of one family at once and ANDs them (a 1,000,000 USD h24 volume floor combined with a 1,000 USD m5 volume floor was measured returning 104 rows), but this surface carries a single thresholdWindow, so a two-window condition on one metric has to be checked against the returned rows instead of filtered for. PRESENCE RULE, measured, and it applies to every ceiling on this surface: a maximum matches only rows that CARRY the field. A row the provider reported no value for is excluded, not kept as if it were below the bound. Measured on a 64,420-row solana population: a liquidity ceiling removed 1,687 rows that carry no liquidity figure, an FDV ceiling removed 3,286, and a launchpad-progress ceiling reduced the set to launchpad pairs only. So a ceiling narrows on two axes at once, and the second one is silent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "maxTxnCount": {
            "description": "Ceiling on the number of trades, as a COUNT and not a volume. Measured over thresholdWindow, which defaults to window. One window per threshold family is reachable here: the provider itself accepts several windows of one family at once and ANDs them (a 1,000,000 USD h24 volume floor combined with a 1,000 USD m5 volume floor was measured returning 104 rows), but this surface carries a single thresholdWindow, so a two-window condition on one metric has to be checked against the returned rows instead of filtered for. PRESENCE RULE, measured, and it applies to every ceiling on this surface: a maximum matches only rows that CARRY the field. A row the provider reported no value for is excluded, not kept as if it were below the bound. Measured on a 64,420-row solana population: a liquidity ceiling removed 1,687 rows that carry no liquidity figure, an FDV ceiling removed 3,286, and a launchpad-progress ceiling reduced the set to launchpad pairs only. So a ceiling narrows on two axes at once, and the second one is silent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "maxVolumeUsd": {
            "description": "Ceiling on traded volume, in US dollars. Measured over thresholdWindow, which defaults to window. One window per threshold family is reachable here: the provider itself accepts several windows of one family at once and ANDs them (a 1,000,000 USD h24 volume floor combined with a 1,000 USD m5 volume floor was measured returning 104 rows), but this surface carries a single thresholdWindow, so a two-window condition on one metric has to be checked against the returned rows instead of filtered for. PRESENCE RULE, measured, and it applies to every ceiling on this surface: a maximum matches only rows that CARRY the field. A row the provider reported no value for is excluded, not kept as if it were below the bound. Measured on a 64,420-row solana population: a liquidity ceiling removed 1,687 rows that carry no liquidity figure, an FDV ceiling removed 3,286, and a launchpad-progress ceiling reduced the set to launchpad pairs only. So a ceiling narrows on two axes at once, and the second one is silent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "metaIds": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Narrative IDs to keep, taken from dexscreener__narratives_list. Send the ID and not the human-readable name: the provider accepts a name, matches nothing, and answers with zero rows, which reads as an empty market rather than as a wrong value. Accepts either a comma-separated string (\"a,b\") or an array of strings ([\"a\",\"b\"]); the two are equivalent."
          },
          "minBoostCount": {
            "description": "Floor on the number of paid boosts ACTIVE on the pair, as a COUNT. A boost is bought visibility, never demand or safety. This is more specific than onlyBoosted and wins over it when both are sent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "minBuyCount": {
            "description": "Floor on the number of BUY trades, as a COUNT. Measured over thresholdWindow, which defaults to window. One window per threshold family is reachable here: the provider itself accepts several windows of one family at once and ANDs them (a 1,000,000 USD h24 volume floor combined with a 1,000 USD m5 volume floor was measured returning 104 rows), but this surface carries a single thresholdWindow, so a two-window condition on one metric has to be checked against the returned rows instead of filtered for. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "minFdvUsd": {
            "description": "Floor on fully diluted valuation, in US dollars. FDV values the total supply rather than the circulating supply, so it is the larger number. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "minLaunchpadProgressPct": {
            "description": "Floor on bonding-curve completion, as a PERCENT of the curve. 100 means the token has graduated and migrated to a DEX. Only launchpad pairs carry it. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "minLiquidityUsd": {
            "description": "Floor on pool liquidity, in US dollars. Liquidity has no window: it is the pool's depth right now. Bonding-curve pairs carry no liquidity field at all, so any liquidity threshold excludes them by construction. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once. This tool applies 1000 USD by default. Sending 0 does NOT remove the filter: 0 is still a floor, and the provider only matches a row that CARRIES a liquidity figure, so a liquidity threshold of any value excludes every bonding-curve pair (measured: 0 rows returned). Use disableQualityFloor to drop the key from the wire entirely. To reach new bonding-curve pairs on THIS board, send includeLaunchpadPairs: true together with disableQualityFloor: true: that combination WAS measured lifting the provider's hidden launchpad exclusion, returning 17 of 20 rows as Pump.fun bonding pairs with exclusionDefaultReplaced true. dexscreener__launchpad_pairs_list remains the tool that ranks by curve progress and carries the launchpad columns.",
            "type": "number"
          },
          "minMarketCapUsd": {
            "description": "Floor on market capitalisation, in US dollars. The provider's own circulating-supply valuation, with no window. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "minPairAgeSeconds": {
            "description": "Floor on pair age, in SECONDS since the pair was created. The provider filters age in whole and fractional hours, so this is converted for you; the exact age comes back per row in pairAgeSeconds. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "minPriceChangePct": {
            "description": "Floor on price change, as a PERCENT and not a fraction. Measured over thresholdWindow, which defaults to window. One window per threshold family is reachable here: the provider itself accepts several windows of one family at once and ANDs them (a 1,000,000 USD h24 volume floor combined with a 1,000 USD m5 volume floor was measured returning 104 rows), but this surface carries a single thresholdWindow, so a two-window condition on one metric has to be checked against the returned rows instead of filtered for. 10 means ten percent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "minSellCount": {
            "description": "Floor on the number of SELL trades, as a COUNT. Measured over thresholdWindow, which defaults to window. One window per threshold family is reachable here: the provider itself accepts several windows of one family at once and ANDs them (a 1,000,000 USD h24 volume floor combined with a 1,000 USD m5 volume floor was measured returning 104 rows), but this surface carries a single thresholdWindow, so a two-window condition on one metric has to be checked against the returned rows instead of filtered for. A sell-side floor is what separates a real two-sided market from a pair nobody can exit. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "minTxnCount": {
            "description": "Floor on the number of trades, as a COUNT and not a volume. Measured over thresholdWindow, which defaults to window. One window per threshold family is reachable here: the provider itself accepts several windows of one family at once and ANDs them (a 1,000,000 USD h24 volume floor combined with a 1,000 USD m5 volume floor was measured returning 104 rows), but this surface carries a single thresholdWindow, so a two-window condition on one metric has to be checked against the returned rows instead of filtered for. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "minVolumeUsd": {
            "description": "Floor on traded volume, in US dollars. Measured over thresholdWindow, which defaults to window. One window per threshold family is reachable here: the provider itself accepts several windows of one family at once and ANDs them (a 1,000,000 USD h24 volume floor combined with a 1,000 USD m5 volume floor was measured returning 104 rows), but this surface carries a single thresholdWindow, so a two-window condition on one metric has to be checked against the returned rows instead of filtered for. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "offset": {
            "description": "How many ranked rows to skip before returning any, 0 or more. Defaults to 0. Mapped onto the provider's 100-row pages for you, and offset paging was measured reaching page 525 of 525, so the ranking is walkable to its live end. The ranking is live, so rows can repeat or be skipped between deep pages; hasMore and nextOffset carry the continuation.",
            "type": "number"
          },
          "onlyAds": {
            "description": "Keep only pairs with an ad placement running RIGHT NOW. Bought visibility, never demand or safety. Distinct from onlyRecentAds: a pair running a placement now may have bought it long ago.",
            "type": "boolean"
          },
          "onlyBoosted": {
            "description": "Keep only pairs carrying at least one ACTIVE paid boost. This is an advertising signal and not a quality signal: it says the project paid DexScreener for visibility. Use minBoostCount when you want a specific number rather than at least one.",
            "type": "boolean"
          },
          "onlyRecentAds": {
            "description": "Keep only pairs that bought an ad slot RECENTLY, whether or not it is currently running; this is the site's own ads badge. Bought visibility, never demand or safety. Combine with onlyAds to require both, since the provider ANDs them.",
            "type": "boolean"
          },
          "requireProfile": {
            "description": "Keep only pairs whose base token has a DexScreener profile. Measured on solana: 28,080 of 53,094 pairs qualify. Having a profile means the issuer filled a form in, not that the project is real. Sending false sends NO profile filter at all, because the provider has no not-profiled filter to send; on a board that applies this by default that removes the floor, and the floor accounting reports it as removed rather than as held.",
            "type": "boolean"
          },
          "thresholdWindow": {
            "description": "Which window the volume, transaction, buy, sell and price-change thresholds measure over: m5, h1, h6 or h24. Defaults to whatever window is set. Separating the two is how the site's own boards work, ranking by 5-minute price change while requiring 24-hour transaction and liquidity floors.",
            "enum": [
              "m5",
              "h1",
              "h6",
              "h24"
            ],
            "type": "string"
          },
          "window": {
            "description": "The stats window the ranking measures over: m5, h1, h6 or h24. Defaults to h24. It has a DUAL role and both halves matter: it selects which window's volume, transactions, buys, sells and price change are ranked and reported, AND it excludes pairs that had no activity in that window. Use includeInactive to keep the ranking and drop the gate.",
            "enum": [
              "m5",
              "h1",
              "h6",
              "h24"
            ],
            "type": "string"
          }
        },
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "dexscreener",
      "publicName": "dexscreener__pairs_new_list",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "List newest DEX pairs",
      "toolId": "dexscreener.pairs.new"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 2417,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "chain": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "minimum": 1,
                "type": "integer"
              }
            ],
            "description": "One chain slug to scope the search to, for example solana, ethereum, base or bsc. This is the one narrowing the PROVIDER honours: it spends the whole window on that chain instead of on same-name tokens everywhere else. Chain slug/alias, or the numeric chain id `TokenFind` returns (e.g. `base` or `8453`). Omit it and one unscoped cross-chain request is issued, which is a legitimate query and not a missing parameter. Give chain or chainIds, not both."
          },
          "chainIds": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Several chain slugs to search. Each one costs its OWN bounded provider request, issued sequentially, because the 30-row window is applied per request and filtering a single global window locally cannot find rows the provider never sent. Every request is reported separately in perChain, so a chain that came back empty is distinguishable from one that filled its window. How many may be named at once is maxChains, which you may raise. Accepts either a comma-separated string (\"a,b\") or an array of strings ([\"a\",\"b\"]); the two are equivalent."
          },
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "fields": {
            "description": "Comma-separated row field GROUPS to return, not individual field names. Supported groups: core, flow, reserves, allWindows, profile, launchpad, identity. Defaults to core, which carries the chain, dex, pair address, base-token address and symbols, the selected window's price, volume, liquidity, market cap, age, counts and every derived ratio. The two heaviest are profile (unbounded issuer-authored prose and links) and allWindows (four times the per-row metric payload). The `identity` group is NOT part of core and is what adds ammId, quoteTokenAddress, both tokens' decimals, priceNative and pairCreatedAtMs - the fields that let you tell a real pool from a mispriced one, so ask for it when the dollar columns are what you are judging. An unknown group name is refused with the full list rather than ignored.",
            "type": "string"
          },
          "limit": {
            "description": "How many rows to return, 1 or more. Defaults to 20. The provider serves at most 30 rows PER chain requested and offers no continuation of any kind, so a limit above what arrived returns everything that arrived rather than reaching further. There is deliberately no offset parameter: this tool is bounded and non-pageable, and an offset would advertise a page that cannot exist.",
            "type": "number"
          },
          "maxChains": {
            "description": "How many chains this one call may fan out over, 1 or more. Defaults to 5. There is NO hard ceiling: raise it and the wider fan-out is issued, because the only real bound is the call's own deadline. What it costs is one sequential provider request per chain, so a fan-out of twenty is twenty requests inside one deadline and a timeout returns nothing rather than a partial board. requestsIssued and perChain report exactly what was spent.",
            "type": "number"
          },
          "minLiquidityUsd": {
            "description": "Drop returned rows whose pool liquidity is below this many US dollars. Applied HERE, to the bounded window the provider already returned, NOT on the provider: it can only remove rows from that window and can never reach a row the provider did not send, so a narrow threshold on a capped window is a sample and not a survey. droppedByFilter reports every row it removed and why. OMIT it to keep every row; null is not a legal value for any threshold on this surface (plan 14.6 item 1) and is refused by name rather than guessed at.",
            "type": "number"
          },
          "minMarketCapUsd": {
            "description": "Drop returned rows whose market cap is below this many US dollars. Applied HERE, to the bounded window the provider already returned, NOT on the provider: it can only remove rows from that window and can never reach a row the provider did not send, so a narrow threshold on a capped window is a sample and not a survey. droppedByFilter reports every row it removed and why. OMIT it to keep every row; null is not a legal value for any threshold on this surface (plan 14.6 item 1) and is refused by name rather than guessed at.",
            "type": "number"
          },
          "minPairAgeSeconds": {
            "description": "Drop returned rows younger than this many SECONDS, the usual way to push aside a freshly deployed copycat of an established ticker. Applied HERE, to the bounded window the provider already returned, NOT on the provider: it can only remove rows from that window and can never reach a row the provider did not send, so a narrow threshold on a capped window is a sample and not a survey. droppedByFilter reports every row it removed and why. OMIT it to keep every row; null is not a legal value for any threshold on this surface (plan 14.6 item 1) and is refused by name rather than guessed at.",
            "type": "number"
          },
          "minVolumeUsd": {
            "description": "Drop returned rows whose volume in the selected window is below this many US dollars. Applied HERE, to the bounded window the provider already returned, NOT on the provider: it can only remove rows from that window and can never reach a row the provider did not send, so a narrow threshold on a capped window is a sample and not a survey. droppedByFilter reports every row it removed and why. OMIT it to keep every row; null is not a legal value for any threshold on this surface (plan 14.6 item 1) and is refused by name rather than guessed at.",
            "type": "number"
          },
          "query": {
            "description": "What to search for: a token name, a ticker symbol, a token contract address, or a pool address. At least 2 characters. An exact address is matched as an address and returns that token's pools inside the same bounded window; anything else is matched as text and ranked by the provider's own relevance. TWO MEASURED GRAMMAR RULES, because neither is visible in the answer. A DOUBLE QUOTE KILLS THE QUERY: there is no quoted-phrase syntax, the quote is not stripped, and a quoted query comes back completely empty, which reads as \"no such token\" when it means \"that is not a query\". Never quote the term. MULTI-WORD IS OR, NOT AND: a three-word query returns rows matching ANY of its words, across chains, so it widens the search rather than narrowing it. Narrow with chain and the thresholds instead of with more words.",
            "minLength": 1,
            "type": "string"
          },
          "sortBy": {
            "description": "How to order the returned rows: relevance, liquidityUsd, volumeUsd, marketCapUsd, priceChangePct, pairAgeSeconds. Sorting runs HERE, over the rows the provider already sent, so it re-orders that window and never reaches past it. relevance keeps the provider's own ranking untouched, which is the only ordering that carries information about the text match itself.",
            "enum": [
              "relevance",
              "liquidityUsd",
              "volumeUsd",
              "marketCapUsd",
              "priceChangePct",
              "pairAgeSeconds"
            ],
            "type": "string"
          },
          "sortDir": {
            "description": "Ranking direction: desc for the largest values first, asc for the smallest. Defaults to desc, except under sortBy relevance, where the provider's order is kept as sent and this parameter is refused rather than silently reversing a ranking it does not own.",
            "enum": [
              "desc",
              "asc"
            ],
            "type": "string"
          },
          "window": {
            "description": "Which stats window the single-window metrics report: m5, h1, h6 or h24. Defaults to h24. The provider measures all four regardless; this selects which one the flat fields carry, and the allWindows field group returns every one of them.",
            "enum": [
              "m5",
              "h1",
              "h6",
              "h24"
            ],
            "type": "string"
          }
        },
        "required": [
          "query"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "dexscreener",
      "publicName": "dexscreener__pairs_search",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Search DEX pairs",
      "toolId": "dexscreener.search"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 2470,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "baseTokenSuffixes": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Base-token mint-address suffixes to keep, the vanity-suffix convention some launchpads use. Measured on solana: pump matched 20,852 pairs and bonk matched 1,595. Accepts either a comma-separated string (\"a,b\") or an array of strings ([\"a\",\"b\"]); the two are equivalent."
          },
          "chainIds": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "DexScreener chain slugs to screen, for example solana, base, ethereum, bsc. Omit to screen every indexed chain at once. Accepts either a comma-separated string (\"a,b\") or an array of strings ([\"a\",\"b\"]); the two are equivalent. Chain and dex values come from dexscreener__chains_list; an unknown chain is refused by name with the nearest catalog matches, because the provider answers one with zero rows and HTTP 200, which is indistinguishable from a real empty result."
          },
          "dexIds": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "DEX identifiers to keep, for example raydium, uniswap, pumpswap. Multiple values are ORed: a pair on any listed dex is kept. The accepted set is open and per-chain, so read it from dexscreener__chains_list rather than guessing. Accepts either a comma-separated string (\"a,b\") or an array of strings ([\"a\",\"b\"]); the two are equivalent."
          },
          "disableQualityFloor": {
            "description": "Drop EVERY default quality floor this tool applies, in one flag. Defaults to false. Your own explicit thresholds still apply, so disableQualityFloor with minLiquidityUsd 5000 means \"none of the tool's defaults, but keep mine\". Each dropped default is named in floorAccounting and qualityFloorApplied becomes false. Expect the unfloored result to contain arithmetic artifacts: on the price-change boards the top rows without a floor are billion-percent moves on pairs nobody can trade. IT DOES NOT READMIT BONDING-CURVE PAIRS, and that is the half agents miss: dropping the floors is necessary but not sufficient, because the provider excludes bonding-curve VENUES by default and only includeLaunchpadPairs lifts that. Measured on solana: a launchpad filter without the lift returned 0 on-curve pairs, and with the lift 71,334; adding a 25,000 USD liquidity floor to those 71,334 returned 0 again, because on-curve rows carry no liquidity field at all. For on-curve pairs, send includeLaunchpadPairs AND keep every liquidity threshold off.",
            "type": "boolean"
          },
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "excludeDexIds": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "DEX identifiers to drop. DANGEROUS BY CONSTRUCTION: the provider applies a hidden default that already excludes bonding-curve venues, and sending this key AT ALL replaces that default, so excluding one dex can make the result set BIGGER. Measured on solana: 53,094 rows normally, 84,058 when only PumpSwap was excluded. Use includeLaunchpadPairs for the common case; exclusionDefaultReplaced reports whenever this happened. Accepts either a comma-separated string (\"a,b\") or an array of strings ([\"a\",\"b\"]); the two are equivalent."
          },
          "fields": {
            "description": "Comma-separated row field GROUPS to return, not individual field names. Supported groups: core, flow, reserves, allWindows, profile, launchpad, identity. Defaults to core, which carries the chain, dex, pair address, base-token address and symbols, the selected window's price, volume, liquidity, market cap, age, counts and every derived ratio. The two heaviest are profile (unbounded issuer-authored prose and links) and allWindows (four times the per-row metric payload). The `identity` group is NOT part of core and is what adds ammId, quoteTokenAddress, both tokens' decimals, priceNative and pairCreatedAtMs - the fields that let you tell a real pool from a mispriced one, so ask for it when the dollar columns are what you are judging. An unknown group name is refused with the full list rather than ignored.",
            "type": "string"
          },
          "includeInactive": {
            "description": "Keep pairs that had no activity inside the selected window. Defaults to false. Measured on solana at m5: 2,513 rows becomes 53,088, so this widens the set enormously on short windows and barely at all on h24.",
            "type": "boolean"
          },
          "includeLaunchpadPairs": {
            "description": "Lift the provider's hidden exclusion of bonding-curve pairs, the safe alternative to excludeDexIds. Defaults to false. Measured on solana: 53,094 rows becomes 102,676. Named for the effect rather than the mechanism, because the mechanism is an empty exclusion list.",
            "type": "boolean"
          },
          "labels": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Pool label values to keep, for example v2, v3, v4, CLMM, DLMM, CPMM. Matching is case-insensitive on the provider. Accepts either a comma-separated string (\"a,b\") or an array of strings ([\"a\",\"b\"]); the two are equivalent."
          },
          "launchpadIds": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Launchpad identifiers to keep, for example pumpfun, launchlab, meteoradbc, bags, fourmeme. MEASURED: the provider attaches a launchpad id to a pair only AFTER it graduates, so this filter matches graduated pairs and nothing else. While a curve is still running the launchpad is the pair's DEX, so use dexIds with the same value there (dexIds: pumpfun). On dexscreener__launchpad_pairs_list, pairing this with stage: \"bonding\" is refused by name rather than answered with an empty board: it returned 0 rows of a 0-row population while dexIds returned 53,478. KNOWN DEPTH GAP: no tool on this surface enumerates the launchpad vocabulary - dexscreener__chains_list carries dex ids only - so the five spellings above are the list, measured live, and an id outside them is not refused by name the way an unknown chain slug is. It returns an empty board instead, so check your spelling against those five before reading a zero as an absence of launches. Accepts either a comma-separated string (\"a,b\") or an array of strings ([\"a\",\"b\"]); the two are equivalent."
          },
          "limit": {
            "description": "How many rows to return, from 1 to 100. Defaults to 20. 100 is the provider's own page size, so asking for more than that is refused rather than silently served short; use offset to walk further into the ranking.",
            "type": "number"
          },
          "maxBoostCount": {
            "description": "Ceiling on the number of paid boosts ACTIVE on the pair, as a COUNT. It bounds WITHIN the boosted population and does not select against it. The presence rule below is the whole story here: an active-boost count exists only on a boosted pair, so maxBoostCount 3 means \"is boosted AND has at most 3 boosts\", never \"is not heavily advertised\". Measured 2026-08-24 on solana: min 1 gave 105 rows (the boosted population), max 10 gave 51, max 1000000 gave 105 again rather than the 64,420-row baseline, and max 0 gave 0. THIS IS NOT A WAY TO EXCLUDE ADS: an agent setting it to avoid ad-pumped pairs receives only ad-carrying pairs. To judge advertising, leave it unset and read boostsActive per row, which is on every row by default. PRESENCE RULE, measured, and it applies to every ceiling on this surface: a maximum matches only rows that CARRY the field. A row the provider reported no value for is excluded, not kept as if it were below the bound. Measured on a 64,420-row solana population: a liquidity ceiling removed 1,687 rows that carry no liquidity figure, an FDV ceiling removed 3,286, and a launchpad-progress ceiling reduced the set to launchpad pairs only. So a ceiling narrows on two axes at once, and the second one is silent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "maxBuyCount": {
            "description": "Ceiling on the number of BUY trades, as a COUNT. Measured over thresholdWindow, which defaults to window. One window per threshold family is reachable here: the provider itself accepts several windows of one family at once and ANDs them (a 1,000,000 USD h24 volume floor combined with a 1,000 USD m5 volume floor was measured returning 104 rows), but this surface carries a single thresholdWindow, so a two-window condition on one metric has to be checked against the returned rows instead of filtered for. PRESENCE RULE, measured, and it applies to every ceiling on this surface: a maximum matches only rows that CARRY the field. A row the provider reported no value for is excluded, not kept as if it were below the bound. Measured on a 64,420-row solana population: a liquidity ceiling removed 1,687 rows that carry no liquidity figure, an FDV ceiling removed 3,286, and a launchpad-progress ceiling reduced the set to launchpad pairs only. So a ceiling narrows on two axes at once, and the second one is silent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "maxFdvUsd": {
            "description": "Ceiling on fully diluted valuation, in US dollars. FDV values the total supply rather than the circulating supply, so it is the larger number. PRESENCE RULE, measured, and it applies to every ceiling on this surface: a maximum matches only rows that CARRY the field. A row the provider reported no value for is excluded, not kept as if it were below the bound. Measured on a 64,420-row solana population: a liquidity ceiling removed 1,687 rows that carry no liquidity figure, an FDV ceiling removed 3,286, and a launchpad-progress ceiling reduced the set to launchpad pairs only. So a ceiling narrows on two axes at once, and the second one is silent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "maxLaunchpadProgressPct": {
            "description": "Ceiling on bonding-curve completion, as a PERCENT of the curve. Below 100 keeps tokens still on the curve. Only launchpad pairs carry it. PRESENCE RULE, measured, and it applies to every ceiling on this surface: a maximum matches only rows that CARRY the field. A row the provider reported no value for is excluded, not kept as if it were below the bound. Measured on a 64,420-row solana population: a liquidity ceiling removed 1,687 rows that carry no liquidity figure, an FDV ceiling removed 3,286, and a launchpad-progress ceiling reduced the set to launchpad pairs only. So a ceiling narrows on two axes at once, and the second one is silent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "maxLiquidityUsd": {
            "description": "Ceiling on pool liquidity, in US dollars. Useful for finding small pools deliberately; liquidity has no window. PRESENCE RULE, measured, and it applies to every ceiling on this surface: a maximum matches only rows that CARRY the field. A row the provider reported no value for is excluded, not kept as if it were below the bound. Measured on a 64,420-row solana population: a liquidity ceiling removed 1,687 rows that carry no liquidity figure, an FDV ceiling removed 3,286, and a launchpad-progress ceiling reduced the set to launchpad pairs only. So a ceiling narrows on two axes at once, and the second one is silent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "maxMarketCapUsd": {
            "description": "Ceiling on market capitalisation, in US dollars. The provider's own circulating-supply valuation, with no window. PRESENCE RULE, measured, and it applies to every ceiling on this surface: a maximum matches only rows that CARRY the field. A row the provider reported no value for is excluded, not kept as if it were below the bound. Measured on a 64,420-row solana population: a liquidity ceiling removed 1,687 rows that carry no liquidity figure, an FDV ceiling removed 3,286, and a launchpad-progress ceiling reduced the set to launchpad pairs only. So a ceiling narrows on two axes at once, and the second one is silent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "maxPairAgeSeconds": {
            "description": "Ceiling on pair age, in SECONDS since the pair was created. 86400 is one day. The provider filters in hours, so sub-hour precision comes from the returned pairAgeSeconds rather than from this filter. PRESENCE RULE, measured, and it applies to every ceiling on this surface: a maximum matches only rows that CARRY the field. A row the provider reported no value for is excluded, not kept as if it were below the bound. Measured on a 64,420-row solana population: a liquidity ceiling removed 1,687 rows that carry no liquidity figure, an FDV ceiling removed 3,286, and a launchpad-progress ceiling reduced the set to launchpad pairs only. So a ceiling narrows on two axes at once, and the second one is silent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "maxPriceChangePct": {
            "description": "Ceiling on price change, as a PERCENT and not a fraction. Measured over thresholdWindow, which defaults to window. One window per threshold family is reachable here: the provider itself accepts several windows of one family at once and ANDs them (a 1,000,000 USD h24 volume floor combined with a 1,000 USD m5 volume floor was measured returning 104 rows), but this surface carries a single thresholdWindow, so a two-window condition on one metric has to be checked against the returned rows instead of filtered for. Negative values are legal, so -50 keeps only pairs down at least half. PRESENCE RULE, measured, and it applies to every ceiling on this surface: a maximum matches only rows that CARRY the field. A row the provider reported no value for is excluded, not kept as if it were below the bound. Measured on a 64,420-row solana population: a liquidity ceiling removed 1,687 rows that carry no liquidity figure, an FDV ceiling removed 3,286, and a launchpad-progress ceiling reduced the set to launchpad pairs only. So a ceiling narrows on two axes at once, and the second one is silent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "maxSellCount": {
            "description": "Ceiling on the number of SELL trades, as a COUNT. Measured over thresholdWindow, which defaults to window. One window per threshold family is reachable here: the provider itself accepts several windows of one family at once and ANDs them (a 1,000,000 USD h24 volume floor combined with a 1,000 USD m5 volume floor was measured returning 104 rows), but this surface carries a single thresholdWindow, so a two-window condition on one metric has to be checked against the returned rows instead of filtered for. PRESENCE RULE, measured, and it applies to every ceiling on this surface: a maximum matches only rows that CARRY the field. A row the provider reported no value for is excluded, not kept as if it were below the bound. Measured on a 64,420-row solana population: a liquidity ceiling removed 1,687 rows that carry no liquidity figure, an FDV ceiling removed 3,286, and a launchpad-progress ceiling reduced the set to launchpad pairs only. So a ceiling narrows on two axes at once, and the second one is silent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "maxTxnCount": {
            "description": "Ceiling on the number of trades, as a COUNT and not a volume. Measured over thresholdWindow, which defaults to window. One window per threshold family is reachable here: the provider itself accepts several windows of one family at once and ANDs them (a 1,000,000 USD h24 volume floor combined with a 1,000 USD m5 volume floor was measured returning 104 rows), but this surface carries a single thresholdWindow, so a two-window condition on one metric has to be checked against the returned rows instead of filtered for. PRESENCE RULE, measured, and it applies to every ceiling on this surface: a maximum matches only rows that CARRY the field. A row the provider reported no value for is excluded, not kept as if it were below the bound. Measured on a 64,420-row solana population: a liquidity ceiling removed 1,687 rows that carry no liquidity figure, an FDV ceiling removed 3,286, and a launchpad-progress ceiling reduced the set to launchpad pairs only. So a ceiling narrows on two axes at once, and the second one is silent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "maxVolumeUsd": {
            "description": "Ceiling on traded volume, in US dollars. Measured over thresholdWindow, which defaults to window. One window per threshold family is reachable here: the provider itself accepts several windows of one family at once and ANDs them (a 1,000,000 USD h24 volume floor combined with a 1,000 USD m5 volume floor was measured returning 104 rows), but this surface carries a single thresholdWindow, so a two-window condition on one metric has to be checked against the returned rows instead of filtered for. PRESENCE RULE, measured, and it applies to every ceiling on this surface: a maximum matches only rows that CARRY the field. A row the provider reported no value for is excluded, not kept as if it were below the bound. Measured on a 64,420-row solana population: a liquidity ceiling removed 1,687 rows that carry no liquidity figure, an FDV ceiling removed 3,286, and a launchpad-progress ceiling reduced the set to launchpad pairs only. So a ceiling narrows on two axes at once, and the second one is silent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "metaIds": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Narrative IDs to keep, taken from dexscreener__narratives_list. Send the ID and not the human-readable name: the provider accepts a name, matches nothing, and answers with zero rows, which reads as an empty market rather than as a wrong value. Accepts either a comma-separated string (\"a,b\") or an array of strings ([\"a\",\"b\"]); the two are equivalent."
          },
          "minBoostCount": {
            "description": "Floor on the number of paid boosts ACTIVE on the pair, as a COUNT. A boost is bought visibility, never demand or safety. This is more specific than onlyBoosted and wins over it when both are sent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "minBuyCount": {
            "description": "Floor on the number of BUY trades, as a COUNT. Measured over thresholdWindow, which defaults to window. One window per threshold family is reachable here: the provider itself accepts several windows of one family at once and ANDs them (a 1,000,000 USD h24 volume floor combined with a 1,000 USD m5 volume floor was measured returning 104 rows), but this surface carries a single thresholdWindow, so a two-window condition on one metric has to be checked against the returned rows instead of filtered for. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "minFdvUsd": {
            "description": "Floor on fully diluted valuation, in US dollars. FDV values the total supply rather than the circulating supply, so it is the larger number. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "minLaunchpadProgressPct": {
            "description": "Floor on bonding-curve completion, as a PERCENT of the curve. 100 means the token has graduated and migrated to a DEX. Only launchpad pairs carry it. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "minLiquidityUsd": {
            "description": "Floor on pool liquidity, in US dollars. Liquidity has no window: it is the pool's depth right now. Bonding-curve pairs carry no liquidity field at all, so any liquidity threshold excludes them by construction. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "minMarketCapUsd": {
            "description": "Floor on market capitalisation, in US dollars. The provider's own circulating-supply valuation, with no window. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "minPairAgeSeconds": {
            "description": "Floor on pair age, in SECONDS since the pair was created. The provider filters age in whole and fractional hours, so this is converted for you; the exact age comes back per row in pairAgeSeconds. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "minPriceChangePct": {
            "description": "Floor on price change, as a PERCENT and not a fraction. Measured over thresholdWindow, which defaults to window. One window per threshold family is reachable here: the provider itself accepts several windows of one family at once and ANDs them (a 1,000,000 USD h24 volume floor combined with a 1,000 USD m5 volume floor was measured returning 104 rows), but this surface carries a single thresholdWindow, so a two-window condition on one metric has to be checked against the returned rows instead of filtered for. 10 means ten percent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "minSellCount": {
            "description": "Floor on the number of SELL trades, as a COUNT. Measured over thresholdWindow, which defaults to window. One window per threshold family is reachable here: the provider itself accepts several windows of one family at once and ANDs them (a 1,000,000 USD h24 volume floor combined with a 1,000 USD m5 volume floor was measured returning 104 rows), but this surface carries a single thresholdWindow, so a two-window condition on one metric has to be checked against the returned rows instead of filtered for. A sell-side floor is what separates a real two-sided market from a pair nobody can exit. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "minTxnCount": {
            "description": "Floor on the number of trades, as a COUNT and not a volume. Measured over thresholdWindow, which defaults to window. One window per threshold family is reachable here: the provider itself accepts several windows of one family at once and ANDs them (a 1,000,000 USD h24 volume floor combined with a 1,000 USD m5 volume floor was measured returning 104 rows), but this surface carries a single thresholdWindow, so a two-window condition on one metric has to be checked against the returned rows instead of filtered for. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "minVolumeUsd": {
            "description": "Floor on traded volume, in US dollars. Measured over thresholdWindow, which defaults to window. One window per threshold family is reachable here: the provider itself accepts several windows of one family at once and ANDs them (a 1,000,000 USD h24 volume floor combined with a 1,000 USD m5 volume floor was measured returning 104 rows), but this surface carries a single thresholdWindow, so a two-window condition on one metric has to be checked against the returned rows instead of filtered for. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "offset": {
            "description": "How many ranked rows to skip before returning any, 0 or more. Defaults to 0. Mapped onto the provider's 100-row pages for you, and offset paging was measured reaching page 525 of 525, so the ranking is walkable to its live end. The ranking is live, so rows can repeat or be skipped between deep pages; hasMore and nextOffset carry the continuation.",
            "type": "number"
          },
          "onlyAds": {
            "description": "Keep only pairs with an ad placement running RIGHT NOW. Bought visibility, never demand or safety. Distinct from onlyRecentAds: a pair running a placement now may have bought it long ago.",
            "type": "boolean"
          },
          "onlyBoosted": {
            "description": "Keep only pairs carrying at least one ACTIVE paid boost. This is an advertising signal and not a quality signal: it says the project paid DexScreener for visibility. Use minBoostCount when you want a specific number rather than at least one.",
            "type": "boolean"
          },
          "onlyRecentAds": {
            "description": "Keep only pairs that bought an ad slot RECENTLY, whether or not it is currently running; this is the site's own ads badge. Bought visibility, never demand or safety. Combine with onlyAds to require both, since the provider ANDs them.",
            "type": "boolean"
          },
          "requireProfile": {
            "description": "Keep only pairs whose base token has a DexScreener profile. Measured on solana: 28,080 of 53,094 pairs qualify. Having a profile means the issuer filled a form in, not that the project is real. Sending false sends NO profile filter at all, because the provider has no not-profiled filter to send; on a board that applies this by default that removes the floor, and the floor accounting reports it as removed rather than as held.",
            "type": "boolean"
          },
          "sortBy": {
            "description": "Which hard metric ranks the board: volume, txns, buys, sells, liquidity or boosts. Defaults to volume. volume, txns, buys and sells are measured over the selected window; liquidity is point-in-time and ignores it. boosts ranks by the count of ACTIVE paid boosts (measured live: 100 rows served from a 54,051 population) and is an advertising ranking, never a demand or safety one; pair it with maxBoostCount to bound the other end. Sorting by fdv is deliberately not offered: the provider returns the txns ordering for it, identical on 100 of 100 measured rows. Ranking by marketCap is deliberately NOT offered either, for the same reason it was withdrawn from dexscreener__tokens_screen: the provider accepts the key and answers with a degenerate board built out of mispriced pools (measured on this very tool, rank 1 carried a market cap of 263.09 trillion USD beside a fully diluted value of 332,916 on the SAME row, and all ten rows were the same class of junk). Filter with minMarketCapUsd or maxMarketCapUsd and rank by liquidity or volume instead; that keeps the market-cap question and drops the ordering that cannot answer it.",
            "enum": [
              "volume",
              "txns",
              "buys",
              "sells",
              "liquidity",
              "boosts"
            ],
            "type": "string"
          },
          "sortDir": {
            "description": "Ranking direction: desc for the largest values first, asc for the smallest. Defaults to desc, which is the league-table reading. asc is how you find the thinnest pools that still pass your filters.",
            "enum": [
              "desc",
              "asc"
            ],
            "type": "string"
          },
          "thresholdWindow": {
            "description": "Which window the volume, transaction, buy, sell and price-change thresholds measure over: m5, h1, h6 or h24. Defaults to whatever window is set. Separating the two is how the site's own boards work, ranking by 5-minute price change while requiring 24-hour transaction and liquidity floors.",
            "enum": [
              "m5",
              "h1",
              "h6",
              "h24"
            ],
            "type": "string"
          },
          "window": {
            "description": "The stats window the ranking measures over: m5, h1, h6 or h24. Defaults to h24. It has a DUAL role and both halves matter: it selects which window's volume, transactions, buys, sells and price change are ranked and reported, AND it excludes pairs that had no activity in that window. Use includeInactive to keep the ranking and drop the gate.",
            "enum": [
              "m5",
              "h1",
              "h6",
              "h24"
            ],
            "type": "string"
          }
        },
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "dexscreener",
      "publicName": "dexscreener__pairs_top_list",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Rank DEX pairs by a metric",
      "toolId": "dexscreener.pairs.top"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 2189,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "baseTokenSuffixes": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Base-token mint-address suffixes to keep, the vanity-suffix convention some launchpads use. Measured on solana: pump matched 20,852 pairs and bonk matched 1,595. Accepts either a comma-separated string (\"a,b\") or an array of strings ([\"a\",\"b\"]); the two are equivalent."
          },
          "chainIds": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "DexScreener chain slugs to screen, for example solana, base, ethereum, bsc. Omit to screen every indexed chain at once. Accepts either a comma-separated string (\"a,b\") or an array of strings ([\"a\",\"b\"]); the two are equivalent. Chain and dex values come from dexscreener__chains_list; an unknown chain is refused by name with the nearest catalog matches, because the provider answers one with zero rows and HTTP 200, which is indistinguishable from a real empty result."
          },
          "dexIds": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "DEX identifiers to keep, for example raydium, uniswap, pumpswap. Multiple values are ORed: a pair on any listed dex is kept. The accepted set is open and per-chain, so read it from dexscreener__chains_list rather than guessing. Accepts either a comma-separated string (\"a,b\") or an array of strings ([\"a\",\"b\"]); the two are equivalent."
          },
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "excludeDexIds": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "DEX identifiers to drop. DANGEROUS BY CONSTRUCTION: the provider applies a hidden default that already excludes bonding-curve venues, and sending this key AT ALL replaces that default, so excluding one dex can make the result set BIGGER. Measured on solana: 53,094 rows normally, 84,058 when only PumpSwap was excluded. Use includeLaunchpadPairs for the common case; exclusionDefaultReplaced reports whenever this happened. Accepts either a comma-separated string (\"a,b\") or an array of strings ([\"a\",\"b\"]); the two are equivalent."
          },
          "fields": {
            "description": "Comma-separated row field GROUPS to return, not individual field names. Supported groups: core, flow, reserves, allWindows, profile, launchpad, identity. Defaults to core, which carries the chain, dex, pair address, base-token address and symbols, the selected window's price, volume, liquidity, market cap, age, counts and every derived ratio. The two heaviest are profile (unbounded issuer-authored prose and links) and allWindows (four times the per-row metric payload). The `identity` group is NOT part of core and is what adds ammId, quoteTokenAddress, both tokens' decimals, priceNative and pairCreatedAtMs - the fields that let you tell a real pool from a mispriced one, so ask for it when the dollar columns are what you are judging. An unknown group name is refused with the full list rather than ignored.",
            "type": "string"
          },
          "includeInactive": {
            "description": "Keep pairs that had no activity inside the selected window. Defaults to false. Measured on solana at m5: 2,513 rows becomes 53,088, so this widens the set enormously on short windows and barely at all on h24.",
            "type": "boolean"
          },
          "includeLaunchpadPairs": {
            "description": "Lift the provider's hidden exclusion of bonding-curve pairs, the safe alternative to excludeDexIds. Defaults to false. Measured on solana: 53,094 rows becomes 102,676. Named for the effect rather than the mechanism, because the mechanism is an empty exclusion list.",
            "type": "boolean"
          },
          "labels": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Pool label values to keep, for example v2, v3, v4, CLMM, DLMM, CPMM. Matching is case-insensitive on the provider. Accepts either a comma-separated string (\"a,b\") or an array of strings ([\"a\",\"b\"]); the two are equivalent."
          },
          "launchpadIds": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Launchpad identifiers to keep, for example pumpfun, launchlab, meteoradbc, bags, fourmeme. MEASURED: the provider attaches a launchpad id to a pair only AFTER it graduates, so this filter matches graduated pairs and nothing else. While a curve is still running the launchpad is the pair's DEX, so use dexIds with the same value there (dexIds: pumpfun). On dexscreener__launchpad_pairs_list, pairing this with stage: \"bonding\" is refused by name rather than answered with an empty board: it returned 0 rows of a 0-row population while dexIds returned 53,478. KNOWN DEPTH GAP: no tool on this surface enumerates the launchpad vocabulary - dexscreener__chains_list carries dex ids only - so the five spellings above are the list, measured live, and an id outside them is not refused by name the way an unknown chain slug is. It returns an empty board instead, so check your spelling against those five before reading a zero as an absence of launches. Accepts either a comma-separated string (\"a,b\") or an array of strings ([\"a\",\"b\"]); the two are equivalent."
          },
          "limit": {
            "description": "How many rows to return, from 1 to 100. Defaults to 20. 100 is the provider's own page size, so asking for more than that is refused rather than silently served short; use offset to walk further into the ranking.",
            "type": "number"
          },
          "maxBoostCount": {
            "description": "Ceiling on the number of paid boosts ACTIVE on the pair, as a COUNT. It bounds WITHIN the boosted population and does not select against it. The presence rule below is the whole story here: an active-boost count exists only on a boosted pair, so maxBoostCount 3 means \"is boosted AND has at most 3 boosts\", never \"is not heavily advertised\". Measured 2026-08-24 on solana: min 1 gave 105 rows (the boosted population), max 10 gave 51, max 1000000 gave 105 again rather than the 64,420-row baseline, and max 0 gave 0. THIS IS NOT A WAY TO EXCLUDE ADS: an agent setting it to avoid ad-pumped pairs receives only ad-carrying pairs. To judge advertising, leave it unset and read boostsActive per row, which is on every row by default. PRESENCE RULE, measured, and it applies to every ceiling on this surface: a maximum matches only rows that CARRY the field. A row the provider reported no value for is excluded, not kept as if it were below the bound. Measured on a 64,420-row solana population: a liquidity ceiling removed 1,687 rows that carry no liquidity figure, an FDV ceiling removed 3,286, and a launchpad-progress ceiling reduced the set to launchpad pairs only. So a ceiling narrows on two axes at once, and the second one is silent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "maxBuyCount": {
            "description": "Ceiling on the number of BUY trades, as a COUNT. Measured over thresholdWindow, which defaults to window. One window per threshold family is reachable here: the provider itself accepts several windows of one family at once and ANDs them (a 1,000,000 USD h24 volume floor combined with a 1,000 USD m5 volume floor was measured returning 104 rows), but this surface carries a single thresholdWindow, so a two-window condition on one metric has to be checked against the returned rows instead of filtered for. PRESENCE RULE, measured, and it applies to every ceiling on this surface: a maximum matches only rows that CARRY the field. A row the provider reported no value for is excluded, not kept as if it were below the bound. Measured on a 64,420-row solana population: a liquidity ceiling removed 1,687 rows that carry no liquidity figure, an FDV ceiling removed 3,286, and a launchpad-progress ceiling reduced the set to launchpad pairs only. So a ceiling narrows on two axes at once, and the second one is silent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "maxFdvUsd": {
            "description": "Ceiling on fully diluted valuation, in US dollars. FDV values the total supply rather than the circulating supply, so it is the larger number. PRESENCE RULE, measured, and it applies to every ceiling on this surface: a maximum matches only rows that CARRY the field. A row the provider reported no value for is excluded, not kept as if it were below the bound. Measured on a 64,420-row solana population: a liquidity ceiling removed 1,687 rows that carry no liquidity figure, an FDV ceiling removed 3,286, and a launchpad-progress ceiling reduced the set to launchpad pairs only. So a ceiling narrows on two axes at once, and the second one is silent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "maxLaunchpadProgressPct": {
            "description": "Ceiling on bonding-curve completion, as a PERCENT of the curve. Below 100 keeps tokens still on the curve. Only launchpad pairs carry it. PRESENCE RULE, measured, and it applies to every ceiling on this surface: a maximum matches only rows that CARRY the field. A row the provider reported no value for is excluded, not kept as if it were below the bound. Measured on a 64,420-row solana population: a liquidity ceiling removed 1,687 rows that carry no liquidity figure, an FDV ceiling removed 3,286, and a launchpad-progress ceiling reduced the set to launchpad pairs only. So a ceiling narrows on two axes at once, and the second one is silent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "maxLiquidityUsd": {
            "description": "Ceiling on pool liquidity, in US dollars. Useful for finding small pools deliberately; liquidity has no window. PRESENCE RULE, measured, and it applies to every ceiling on this surface: a maximum matches only rows that CARRY the field. A row the provider reported no value for is excluded, not kept as if it were below the bound. Measured on a 64,420-row solana population: a liquidity ceiling removed 1,687 rows that carry no liquidity figure, an FDV ceiling removed 3,286, and a launchpad-progress ceiling reduced the set to launchpad pairs only. So a ceiling narrows on two axes at once, and the second one is silent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "maxMarketCapUsd": {
            "description": "Ceiling on market capitalisation, in US dollars. The provider's own circulating-supply valuation, with no window. PRESENCE RULE, measured, and it applies to every ceiling on this surface: a maximum matches only rows that CARRY the field. A row the provider reported no value for is excluded, not kept as if it were below the bound. Measured on a 64,420-row solana population: a liquidity ceiling removed 1,687 rows that carry no liquidity figure, an FDV ceiling removed 3,286, and a launchpad-progress ceiling reduced the set to launchpad pairs only. So a ceiling narrows on two axes at once, and the second one is silent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "maxPairAgeSeconds": {
            "description": "Ceiling on pair age, in SECONDS since the pair was created. 86400 is one day. The provider filters in hours, so sub-hour precision comes from the returned pairAgeSeconds rather than from this filter. PRESENCE RULE, measured, and it applies to every ceiling on this surface: a maximum matches only rows that CARRY the field. A row the provider reported no value for is excluded, not kept as if it were below the bound. Measured on a 64,420-row solana population: a liquidity ceiling removed 1,687 rows that carry no liquidity figure, an FDV ceiling removed 3,286, and a launchpad-progress ceiling reduced the set to launchpad pairs only. So a ceiling narrows on two axes at once, and the second one is silent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "maxPriceChangePct": {
            "description": "Ceiling on price change, as a PERCENT and not a fraction. Measured over thresholdWindow, which defaults to window. One window per threshold family is reachable here: the provider itself accepts several windows of one family at once and ANDs them (a 1,000,000 USD h24 volume floor combined with a 1,000 USD m5 volume floor was measured returning 104 rows), but this surface carries a single thresholdWindow, so a two-window condition on one metric has to be checked against the returned rows instead of filtered for. Negative values are legal, so -50 keeps only pairs down at least half. PRESENCE RULE, measured, and it applies to every ceiling on this surface: a maximum matches only rows that CARRY the field. A row the provider reported no value for is excluded, not kept as if it were below the bound. Measured on a 64,420-row solana population: a liquidity ceiling removed 1,687 rows that carry no liquidity figure, an FDV ceiling removed 3,286, and a launchpad-progress ceiling reduced the set to launchpad pairs only. So a ceiling narrows on two axes at once, and the second one is silent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "maxSellCount": {
            "description": "Ceiling on the number of SELL trades, as a COUNT. Measured over thresholdWindow, which defaults to window. One window per threshold family is reachable here: the provider itself accepts several windows of one family at once and ANDs them (a 1,000,000 USD h24 volume floor combined with a 1,000 USD m5 volume floor was measured returning 104 rows), but this surface carries a single thresholdWindow, so a two-window condition on one metric has to be checked against the returned rows instead of filtered for. PRESENCE RULE, measured, and it applies to every ceiling on this surface: a maximum matches only rows that CARRY the field. A row the provider reported no value for is excluded, not kept as if it were below the bound. Measured on a 64,420-row solana population: a liquidity ceiling removed 1,687 rows that carry no liquidity figure, an FDV ceiling removed 3,286, and a launchpad-progress ceiling reduced the set to launchpad pairs only. So a ceiling narrows on two axes at once, and the second one is silent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "maxTxnCount": {
            "description": "Ceiling on the number of trades, as a COUNT and not a volume. Measured over thresholdWindow, which defaults to window. One window per threshold family is reachable here: the provider itself accepts several windows of one family at once and ANDs them (a 1,000,000 USD h24 volume floor combined with a 1,000 USD m5 volume floor was measured returning 104 rows), but this surface carries a single thresholdWindow, so a two-window condition on one metric has to be checked against the returned rows instead of filtered for. PRESENCE RULE, measured, and it applies to every ceiling on this surface: a maximum matches only rows that CARRY the field. A row the provider reported no value for is excluded, not kept as if it were below the bound. Measured on a 64,420-row solana population: a liquidity ceiling removed 1,687 rows that carry no liquidity figure, an FDV ceiling removed 3,286, and a launchpad-progress ceiling reduced the set to launchpad pairs only. So a ceiling narrows on two axes at once, and the second one is silent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "maxVolumeUsd": {
            "description": "Ceiling on traded volume, in US dollars. Measured over thresholdWindow, which defaults to window. One window per threshold family is reachable here: the provider itself accepts several windows of one family at once and ANDs them (a 1,000,000 USD h24 volume floor combined with a 1,000 USD m5 volume floor was measured returning 104 rows), but this surface carries a single thresholdWindow, so a two-window condition on one metric has to be checked against the returned rows instead of filtered for. PRESENCE RULE, measured, and it applies to every ceiling on this surface: a maximum matches only rows that CARRY the field. A row the provider reported no value for is excluded, not kept as if it were below the bound. Measured on a 64,420-row solana population: a liquidity ceiling removed 1,687 rows that carry no liquidity figure, an FDV ceiling removed 3,286, and a launchpad-progress ceiling reduced the set to launchpad pairs only. So a ceiling narrows on two axes at once, and the second one is silent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "metaIds": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Narrative IDs to keep, taken from dexscreener__narratives_list. Send the ID and not the human-readable name: the provider accepts a name, matches nothing, and answers with zero rows, which reads as an empty market rather than as a wrong value. Accepts either a comma-separated string (\"a,b\") or an array of strings ([\"a\",\"b\"]); the two are equivalent."
          },
          "minBoostCount": {
            "description": "Floor on the number of paid boosts ACTIVE on the pair, as a COUNT. A boost is bought visibility, never demand or safety. This is more specific than onlyBoosted and wins over it when both are sent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "minBuyCount": {
            "description": "Floor on the number of BUY trades, as a COUNT. Measured over thresholdWindow, which defaults to window. One window per threshold family is reachable here: the provider itself accepts several windows of one family at once and ANDs them (a 1,000,000 USD h24 volume floor combined with a 1,000 USD m5 volume floor was measured returning 104 rows), but this surface carries a single thresholdWindow, so a two-window condition on one metric has to be checked against the returned rows instead of filtered for. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "minFdvUsd": {
            "description": "Floor on fully diluted valuation, in US dollars. FDV values the total supply rather than the circulating supply, so it is the larger number. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "minLaunchpadProgressPct": {
            "description": "Floor on bonding-curve completion, as a PERCENT of the curve. 100 means the token has graduated and migrated to a DEX. Only launchpad pairs carry it. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "minLiquidityUsd": {
            "description": "Floor on pool liquidity, in US dollars. Liquidity has no window: it is the pool's depth right now. Bonding-curve pairs carry no liquidity field at all, so any liquidity threshold excludes them by construction. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "minMarketCapUsd": {
            "description": "Floor on market capitalisation, in US dollars. The provider's own circulating-supply valuation, with no window. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "minPairAgeSeconds": {
            "description": "Floor on pair age, in SECONDS since the pair was created. The provider filters age in whole and fractional hours, so this is converted for you; the exact age comes back per row in pairAgeSeconds. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "minPriceChangePct": {
            "description": "Floor on price change, as a PERCENT and not a fraction. Measured over thresholdWindow, which defaults to window. One window per threshold family is reachable here: the provider itself accepts several windows of one family at once and ANDs them (a 1,000,000 USD h24 volume floor combined with a 1,000 USD m5 volume floor was measured returning 104 rows), but this surface carries a single thresholdWindow, so a two-window condition on one metric has to be checked against the returned rows instead of filtered for. 10 means ten percent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "minSellCount": {
            "description": "Floor on the number of SELL trades, as a COUNT. Measured over thresholdWindow, which defaults to window. One window per threshold family is reachable here: the provider itself accepts several windows of one family at once and ANDs them (a 1,000,000 USD h24 volume floor combined with a 1,000 USD m5 volume floor was measured returning 104 rows), but this surface carries a single thresholdWindow, so a two-window condition on one metric has to be checked against the returned rows instead of filtered for. A sell-side floor is what separates a real two-sided market from a pair nobody can exit. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "minTxnCount": {
            "description": "Floor on the number of trades, as a COUNT and not a volume. Measured over thresholdWindow, which defaults to window. One window per threshold family is reachable here: the provider itself accepts several windows of one family at once and ANDs them (a 1,000,000 USD h24 volume floor combined with a 1,000 USD m5 volume floor was measured returning 104 rows), but this surface carries a single thresholdWindow, so a two-window condition on one metric has to be checked against the returned rows instead of filtered for. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "minVolumeUsd": {
            "description": "Floor on traded volume, in US dollars. Measured over thresholdWindow, which defaults to window. One window per threshold family is reachable here: the provider itself accepts several windows of one family at once and ANDs them (a 1,000,000 USD h24 volume floor combined with a 1,000 USD m5 volume floor was measured returning 104 rows), but this surface carries a single thresholdWindow, so a two-window condition on one metric has to be checked against the returned rows instead of filtered for. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "offset": {
            "description": "How many ranked rows to skip before returning any, 0 or more. Defaults to 0. Mapped onto the provider's 100-row pages for you, and offset paging was measured reaching page 525 of 525, so the ranking is walkable to its live end. The ranking is live, so rows can repeat or be skipped between deep pages; hasMore and nextOffset carry the continuation.",
            "type": "number"
          },
          "onlyAds": {
            "description": "Keep only pairs with an ad placement running RIGHT NOW. Bought visibility, never demand or safety. Distinct from onlyRecentAds: a pair running a placement now may have bought it long ago.",
            "type": "boolean"
          },
          "onlyBoosted": {
            "description": "Keep only pairs carrying at least one ACTIVE paid boost. This is an advertising signal and not a quality signal: it says the project paid DexScreener for visibility. Use minBoostCount when you want a specific number rather than at least one.",
            "type": "boolean"
          },
          "onlyRecentAds": {
            "description": "Keep only pairs that bought an ad slot RECENTLY, whether or not it is currently running; this is the site's own ads badge. Bought visibility, never demand or safety. Combine with onlyAds to require both, since the provider ANDs them.",
            "type": "boolean"
          },
          "requireProfile": {
            "description": "Keep only pairs whose base token has a DexScreener profile. Measured on solana: 28,080 of 53,094 pairs qualify. Having a profile means the issuer filled a form in, not that the project is real. Sending false sends NO profile filter at all, because the provider has no not-profiled filter to send; on a board that applies this by default that removes the floor, and the floor accounting reports it as removed rather than as held.",
            "type": "boolean"
          },
          "thresholdWindow": {
            "description": "Which window the volume, transaction, buy, sell and price-change thresholds measure over: m5, h1, h6 or h24. Defaults to whatever window is set. Separating the two is how the site's own boards work, ranking by 5-minute price change while requiring 24-hour transaction and liquidity floors.",
            "enum": [
              "m5",
              "h1",
              "h6",
              "h24"
            ],
            "type": "string"
          },
          "window": {
            "description": "The stats window the ranking measures over: m5, h1, h6 or h24. Defaults to h24. It has a DUAL role and both halves matter: it selects which window's volume, transactions, buys, sells and price change are ranked and reported, AND it excludes pairs that had no activity in that window. Use includeInactive to keep the ranking and drop the gate.",
            "enum": [
              "m5",
              "h1",
              "h6",
              "h24"
            ],
            "type": "string"
          }
        },
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "dexscreener",
      "publicName": "dexscreener__pairs_trending_list",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "List trending DEX pairs",
      "toolId": "dexscreener.pairs.trending"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 2808,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "chainIds": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Chain slugs to keep, comma-separated or as an array. Filtering happens HERE, over the document the provider already sent, so narrowing cannot reveal rows beyond the feeds' fixed sizes; clientFiltering.droppedByChain reports what it removed. Chain slug/alias, or the numeric chain id `TokenFind` returns (e.g. `base` or `8453`)."
          },
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "feed": {
            "description": "Which feed to return: topBoosts (who has paid the most in total), recentBoosts (who just bought a boost), latestProfiles (the newest issuer-published token profiles), or all. Defaults to all. Every feed comes from the same single provider document, so asking for one instead of all saves context, not a request.",
            "enum": [
              "topBoosts",
              "recentBoosts",
              "latestProfiles",
              "all"
            ],
            "type": "string"
          },
          "fields": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Comma-separated row field GROUPS to return, not individual field names, as a string or an array. Supported groups: core, description, links, media. Defaults to core, which carries chain, token address, symbol, feed rank and the boost amounts. description adds the issuer-authored blurb and the provider's nsfw flag; links adds the issuer's claimed website and socials. Both exist only on latestProfiles rows, both are issuer-authored text kept out of the default projection for that reason, and both are claims to verify onward rather than facts. media adds the provider's own image references: tokenImageUrl on a boost row and iconId plus headerId on a profile row. Those are provider-hosted assets rather than issuer text, and they are out of the default projection because a text model cannot read an image; ask for them when something will render or fetch one. An unknown group name is refused with the full list rather than ignored."
          },
          "limit": {
            "description": "How many rows per feed to return, 1 or more. Defaults to 10. There is no upper bound to refuse against: the feeds measured 30, 30 and 36 rows, but that is a BOUND and not a promise (a recent-boosts feed was measured at 28), and there is no continuation past whatever a feed holds, so a larger limit returns everything that feed has rather than more. Rows a limit left unshown are counted in notShownByLimit, separately from the chain filter.",
            "type": "number"
          }
        },
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "dexscreener",
      "publicName": "dexscreener__spotlight_get",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Read the DEX Screener spotlight feeds",
      "toolId": "dexscreener.spotlight"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 3038,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "chain": {
            "anyOf": [
              {
                "minLength": 1,
                "type": "string"
              },
              {
                "minimum": 1,
                "type": "integer"
              }
            ],
            "description": "Chain slug the token lives on, for example solana, ethereum, base or bsc. Required, and honoured by the provider: the same contract address exists on forked chains, so an unscoped lookup would spend the window on the wrong chain's copies. Chain slug/alias, or the numeric chain id `TokenFind` returns (e.g. `base` or `8453`)."
          },
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "fields": {
            "description": "Comma-separated row field GROUPS to return, not individual field names. Supported groups: core, flow, reserves, allWindows, profile, launchpad, identity. Defaults to core, which carries the chain, dex, pair address, base-token address and symbols, the selected window's price, volume, liquidity, market cap, age, counts and every derived ratio. The two heaviest are profile (unbounded issuer-authored prose and links) and allWindows (four times the per-row metric payload). The `identity` group is NOT part of core and is what adds ammId, quoteTokenAddress, both tokens' decimals, priceNative and pairCreatedAtMs - the fields that let you tell a real pool from a mispriced one, so ask for it when the dollar columns are what you are judging. An unknown group name is refused with the full list rather than ignored.",
            "type": "string"
          },
          "limit": {
            "description": "How many pools to return, 1 or more. Defaults to 20. The provider serves at most 30 pools for one token with no continuation, so a token that trades in more pools than that is partially covered and providerCapped says so. There is no offset: this tool is bounded and non-pageable.",
            "type": "number"
          },
          "maxBuyCount": {
            "description": "Ceiling on the number of BUY trades, as a COUNT. Measured over thresholdWindow, which defaults to window. One window per threshold family is reachable here: the provider itself accepts several windows of one family at once and ANDs them (a 1,000,000 USD h24 volume floor combined with a 1,000 USD m5 volume floor was measured returning 104 rows), but this surface carries a single thresholdWindow, so a two-window condition on one metric has to be checked against the returned rows instead of filtered for. PRESENCE RULE, measured, and it applies to every ceiling on this surface: a maximum matches only rows that CARRY the field. A row the provider reported no value for is excluded, not kept as if it were below the bound. Measured on a 64,420-row solana population: a liquidity ceiling removed 1,687 rows that carry no liquidity figure, an FDV ceiling removed 3,286, and a launchpad-progress ceiling reduced the set to launchpad pairs only. So a ceiling narrows on two axes at once, and the second one is silent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once. ON THIS TOOL the comparison runs HERE, over the rows already in hand, and NOT on the provider: this tool applies no default floor, so there is nothing to remove and no disableQualityFloor to send; there is no separate thresholdWindow, so a windowed threshold measures over window; and null is not a legal value here, so OMIT the parameter to keep every row. droppedByFilter counts every row removed and by which threshold. A row whose metric the provider did not report is NOT compared and NOT dropped: it is kept and counted in clientFiltering.notEvaluated, because a missing measurement is not a measurement of zero. Applied HERE, to the bounded window the provider already returned, NOT on the provider: it can only remove rows from that window and can never reach a row the provider did not send, so a narrow threshold on a capped window is a sample and not a survey. droppedByFilter reports every row it removed and why. OMIT it to keep every row; null is not a legal value for any threshold on this surface (plan 14.6 item 1) and is refused by name rather than guessed at.",
            "type": "number"
          },
          "maxFdvUsd": {
            "description": "Ceiling on fully diluted valuation, in US dollars. FDV values the total supply rather than the circulating supply, so it is the larger number. PRESENCE RULE, measured, and it applies to every ceiling on this surface: a maximum matches only rows that CARRY the field. A row the provider reported no value for is excluded, not kept as if it were below the bound. Measured on a 64,420-row solana population: a liquidity ceiling removed 1,687 rows that carry no liquidity figure, an FDV ceiling removed 3,286, and a launchpad-progress ceiling reduced the set to launchpad pairs only. So a ceiling narrows on two axes at once, and the second one is silent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once. ON THIS TOOL the comparison runs HERE, over the rows already in hand, and NOT on the provider: this tool applies no default floor, so there is nothing to remove and no disableQualityFloor to send; there is no separate thresholdWindow, so a windowed threshold measures over window; and null is not a legal value here, so OMIT the parameter to keep every row. droppedByFilter counts every row removed and by which threshold. A row whose metric the provider did not report is NOT compared and NOT dropped: it is kept and counted in clientFiltering.notEvaluated, because a missing measurement is not a measurement of zero. Applied HERE, to the bounded window the provider already returned, NOT on the provider: it can only remove rows from that window and can never reach a row the provider did not send, so a narrow threshold on a capped window is a sample and not a survey. droppedByFilter reports every row it removed and why. OMIT it to keep every row; null is not a legal value for any threshold on this surface (plan 14.6 item 1) and is refused by name rather than guessed at.",
            "type": "number"
          },
          "maxLaunchpadProgressPct": {
            "description": "Ceiling on bonding-curve completion, as a PERCENT of the curve. Below 100 keeps tokens still on the curve. Only launchpad pairs carry it. PRESENCE RULE, measured, and it applies to every ceiling on this surface: a maximum matches only rows that CARRY the field. A row the provider reported no value for is excluded, not kept as if it were below the bound. Measured on a 64,420-row solana population: a liquidity ceiling removed 1,687 rows that carry no liquidity figure, an FDV ceiling removed 3,286, and a launchpad-progress ceiling reduced the set to launchpad pairs only. So a ceiling narrows on two axes at once, and the second one is silent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once. ON THIS TOOL the comparison runs HERE, over the rows already in hand, and NOT on the provider: this tool applies no default floor, so there is nothing to remove and no disableQualityFloor to send; there is no separate thresholdWindow, so a windowed threshold measures over window; and null is not a legal value here, so OMIT the parameter to keep every row. droppedByFilter counts every row removed and by which threshold. A row whose metric the provider did not report is NOT compared and NOT dropped: it is kept and counted in clientFiltering.notEvaluated, because a missing measurement is not a measurement of zero. Applied HERE, to the bounded window the provider already returned, NOT on the provider: it can only remove rows from that window and can never reach a row the provider did not send, so a narrow threshold on a capped window is a sample and not a survey. droppedByFilter reports every row it removed and why. OMIT it to keep every row; null is not a legal value for any threshold on this surface (plan 14.6 item 1) and is refused by name rather than guessed at.",
            "type": "number"
          },
          "maxLiquidityUsd": {
            "description": "Ceiling on pool liquidity, in US dollars. Useful for finding small pools deliberately; liquidity has no window. PRESENCE RULE, measured, and it applies to every ceiling on this surface: a maximum matches only rows that CARRY the field. A row the provider reported no value for is excluded, not kept as if it were below the bound. Measured on a 64,420-row solana population: a liquidity ceiling removed 1,687 rows that carry no liquidity figure, an FDV ceiling removed 3,286, and a launchpad-progress ceiling reduced the set to launchpad pairs only. So a ceiling narrows on two axes at once, and the second one is silent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once. ON THIS TOOL the comparison runs HERE, over the rows already in hand, and NOT on the provider: this tool applies no default floor, so there is nothing to remove and no disableQualityFloor to send; there is no separate thresholdWindow, so a windowed threshold measures over window; and null is not a legal value here, so OMIT the parameter to keep every row. droppedByFilter counts every row removed and by which threshold. A row whose metric the provider did not report is NOT compared and NOT dropped: it is kept and counted in clientFiltering.notEvaluated, because a missing measurement is not a measurement of zero. Applied HERE, to the bounded window the provider already returned, NOT on the provider: it can only remove rows from that window and can never reach a row the provider did not send, so a narrow threshold on a capped window is a sample and not a survey. droppedByFilter reports every row it removed and why. OMIT it to keep every row; null is not a legal value for any threshold on this surface (plan 14.6 item 1) and is refused by name rather than guessed at.",
            "type": "number"
          },
          "maxMarketCapUsd": {
            "description": "Ceiling on market capitalisation, in US dollars. The provider's own circulating-supply valuation, with no window. PRESENCE RULE, measured, and it applies to every ceiling on this surface: a maximum matches only rows that CARRY the field. A row the provider reported no value for is excluded, not kept as if it were below the bound. Measured on a 64,420-row solana population: a liquidity ceiling removed 1,687 rows that carry no liquidity figure, an FDV ceiling removed 3,286, and a launchpad-progress ceiling reduced the set to launchpad pairs only. So a ceiling narrows on two axes at once, and the second one is silent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once. ON THIS TOOL the comparison runs HERE, over the rows already in hand, and NOT on the provider: this tool applies no default floor, so there is nothing to remove and no disableQualityFloor to send; there is no separate thresholdWindow, so a windowed threshold measures over window; and null is not a legal value here, so OMIT the parameter to keep every row. droppedByFilter counts every row removed and by which threshold. A row whose metric the provider did not report is NOT compared and NOT dropped: it is kept and counted in clientFiltering.notEvaluated, because a missing measurement is not a measurement of zero. Applied HERE, to the bounded window the provider already returned, NOT on the provider: it can only remove rows from that window and can never reach a row the provider did not send, so a narrow threshold on a capped window is a sample and not a survey. droppedByFilter reports every row it removed and why. OMIT it to keep every row; null is not a legal value for any threshold on this surface (plan 14.6 item 1) and is refused by name rather than guessed at.",
            "type": "number"
          },
          "maxPairAgeSeconds": {
            "description": "Ceiling on pair age, in SECONDS since the pair was created. 86400 is one day. The provider filters in hours, so sub-hour precision comes from the returned pairAgeSeconds rather than from this filter. PRESENCE RULE, measured, and it applies to every ceiling on this surface: a maximum matches only rows that CARRY the field. A row the provider reported no value for is excluded, not kept as if it were below the bound. Measured on a 64,420-row solana population: a liquidity ceiling removed 1,687 rows that carry no liquidity figure, an FDV ceiling removed 3,286, and a launchpad-progress ceiling reduced the set to launchpad pairs only. So a ceiling narrows on two axes at once, and the second one is silent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once. ON THIS TOOL the comparison runs HERE, over the rows already in hand, and NOT on the provider: this tool applies no default floor, so there is nothing to remove and no disableQualityFloor to send; there is no separate thresholdWindow, so a windowed threshold measures over window; and null is not a legal value here, so OMIT the parameter to keep every row. droppedByFilter counts every row removed and by which threshold. A row whose metric the provider did not report is NOT compared and NOT dropped: it is kept and counted in clientFiltering.notEvaluated, because a missing measurement is not a measurement of zero. Applied HERE, to the bounded window the provider already returned, NOT on the provider: it can only remove rows from that window and can never reach a row the provider did not send, so a narrow threshold on a capped window is a sample and not a survey. droppedByFilter reports every row it removed and why. OMIT it to keep every row; null is not a legal value for any threshold on this surface (plan 14.6 item 1) and is refused by name rather than guessed at.",
            "type": "number"
          },
          "maxPriceChangePct": {
            "description": "Ceiling on price change, as a PERCENT and not a fraction. Measured over thresholdWindow, which defaults to window. One window per threshold family is reachable here: the provider itself accepts several windows of one family at once and ANDs them (a 1,000,000 USD h24 volume floor combined with a 1,000 USD m5 volume floor was measured returning 104 rows), but this surface carries a single thresholdWindow, so a two-window condition on one metric has to be checked against the returned rows instead of filtered for. Negative values are legal, so -50 keeps only pairs down at least half. PRESENCE RULE, measured, and it applies to every ceiling on this surface: a maximum matches only rows that CARRY the field. A row the provider reported no value for is excluded, not kept as if it were below the bound. Measured on a 64,420-row solana population: a liquidity ceiling removed 1,687 rows that carry no liquidity figure, an FDV ceiling removed 3,286, and a launchpad-progress ceiling reduced the set to launchpad pairs only. So a ceiling narrows on two axes at once, and the second one is silent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once. ON THIS TOOL the comparison runs HERE, over the rows already in hand, and NOT on the provider: this tool applies no default floor, so there is nothing to remove and no disableQualityFloor to send; there is no separate thresholdWindow, so a windowed threshold measures over window; and null is not a legal value here, so OMIT the parameter to keep every row. droppedByFilter counts every row removed and by which threshold. A row whose metric the provider did not report is NOT compared and NOT dropped: it is kept and counted in clientFiltering.notEvaluated, because a missing measurement is not a measurement of zero. Applied HERE, to the bounded window the provider already returned, NOT on the provider: it can only remove rows from that window and can never reach a row the provider did not send, so a narrow threshold on a capped window is a sample and not a survey. droppedByFilter reports every row it removed and why. OMIT it to keep every row; null is not a legal value for any threshold on this surface (plan 14.6 item 1) and is refused by name rather than guessed at.",
            "type": "number"
          },
          "maxSellCount": {
            "description": "Ceiling on the number of SELL trades, as a COUNT. Measured over thresholdWindow, which defaults to window. One window per threshold family is reachable here: the provider itself accepts several windows of one family at once and ANDs them (a 1,000,000 USD h24 volume floor combined with a 1,000 USD m5 volume floor was measured returning 104 rows), but this surface carries a single thresholdWindow, so a two-window condition on one metric has to be checked against the returned rows instead of filtered for. PRESENCE RULE, measured, and it applies to every ceiling on this surface: a maximum matches only rows that CARRY the field. A row the provider reported no value for is excluded, not kept as if it were below the bound. Measured on a 64,420-row solana population: a liquidity ceiling removed 1,687 rows that carry no liquidity figure, an FDV ceiling removed 3,286, and a launchpad-progress ceiling reduced the set to launchpad pairs only. So a ceiling narrows on two axes at once, and the second one is silent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once. ON THIS TOOL the comparison runs HERE, over the rows already in hand, and NOT on the provider: this tool applies no default floor, so there is nothing to remove and no disableQualityFloor to send; there is no separate thresholdWindow, so a windowed threshold measures over window; and null is not a legal value here, so OMIT the parameter to keep every row. droppedByFilter counts every row removed and by which threshold. A row whose metric the provider did not report is NOT compared and NOT dropped: it is kept and counted in clientFiltering.notEvaluated, because a missing measurement is not a measurement of zero. Applied HERE, to the bounded window the provider already returned, NOT on the provider: it can only remove rows from that window and can never reach a row the provider did not send, so a narrow threshold on a capped window is a sample and not a survey. droppedByFilter reports every row it removed and why. OMIT it to keep every row; null is not a legal value for any threshold on this surface (plan 14.6 item 1) and is refused by name rather than guessed at.",
            "type": "number"
          },
          "maxTxnCount": {
            "description": "Ceiling on the number of trades, as a COUNT and not a volume. Measured over thresholdWindow, which defaults to window. One window per threshold family is reachable here: the provider itself accepts several windows of one family at once and ANDs them (a 1,000,000 USD h24 volume floor combined with a 1,000 USD m5 volume floor was measured returning 104 rows), but this surface carries a single thresholdWindow, so a two-window condition on one metric has to be checked against the returned rows instead of filtered for. PRESENCE RULE, measured, and it applies to every ceiling on this surface: a maximum matches only rows that CARRY the field. A row the provider reported no value for is excluded, not kept as if it were below the bound. Measured on a 64,420-row solana population: a liquidity ceiling removed 1,687 rows that carry no liquidity figure, an FDV ceiling removed 3,286, and a launchpad-progress ceiling reduced the set to launchpad pairs only. So a ceiling narrows on two axes at once, and the second one is silent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once. ON THIS TOOL the comparison runs HERE, over the rows already in hand, and NOT on the provider: this tool applies no default floor, so there is nothing to remove and no disableQualityFloor to send; there is no separate thresholdWindow, so a windowed threshold measures over window; and null is not a legal value here, so OMIT the parameter to keep every row. droppedByFilter counts every row removed and by which threshold. A row whose metric the provider did not report is NOT compared and NOT dropped: it is kept and counted in clientFiltering.notEvaluated, because a missing measurement is not a measurement of zero. Applied HERE, to the bounded window the provider already returned, NOT on the provider: it can only remove rows from that window and can never reach a row the provider did not send, so a narrow threshold on a capped window is a sample and not a survey. droppedByFilter reports every row it removed and why. OMIT it to keep every row; null is not a legal value for any threshold on this surface (plan 14.6 item 1) and is refused by name rather than guessed at.",
            "type": "number"
          },
          "maxVolumeUsd": {
            "description": "Ceiling on traded volume, in US dollars. Measured over thresholdWindow, which defaults to window. One window per threshold family is reachable here: the provider itself accepts several windows of one family at once and ANDs them (a 1,000,000 USD h24 volume floor combined with a 1,000 USD m5 volume floor was measured returning 104 rows), but this surface carries a single thresholdWindow, so a two-window condition on one metric has to be checked against the returned rows instead of filtered for. PRESENCE RULE, measured, and it applies to every ceiling on this surface: a maximum matches only rows that CARRY the field. A row the provider reported no value for is excluded, not kept as if it were below the bound. Measured on a 64,420-row solana population: a liquidity ceiling removed 1,687 rows that carry no liquidity figure, an FDV ceiling removed 3,286, and a launchpad-progress ceiling reduced the set to launchpad pairs only. So a ceiling narrows on two axes at once, and the second one is silent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once. ON THIS TOOL the comparison runs HERE, over the rows already in hand, and NOT on the provider: this tool applies no default floor, so there is nothing to remove and no disableQualityFloor to send; there is no separate thresholdWindow, so a windowed threshold measures over window; and null is not a legal value here, so OMIT the parameter to keep every row. droppedByFilter counts every row removed and by which threshold. A row whose metric the provider did not report is NOT compared and NOT dropped: it is kept and counted in clientFiltering.notEvaluated, because a missing measurement is not a measurement of zero. Applied HERE, to the bounded window the provider already returned, NOT on the provider: it can only remove rows from that window and can never reach a row the provider did not send, so a narrow threshold on a capped window is a sample and not a survey. droppedByFilter reports every row it removed and why. OMIT it to keep every row; null is not a legal value for any threshold on this surface (plan 14.6 item 1) and is refused by name rather than guessed at.",
            "type": "number"
          },
          "minBoostCount": {
            "description": "Floor on the number of paid boosts ACTIVE on the pair, as a COUNT. A boost is bought visibility, never demand or safety. This is more specific than onlyBoosted and wins over it when both are sent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once. ON THIS TOOL the comparison runs HERE, over the rows already in hand, and NOT on the provider: this tool applies no default floor, so there is nothing to remove and no disableQualityFloor to send; there is no separate thresholdWindow, so a windowed threshold measures over window; and null is not a legal value here, so OMIT the parameter to keep every row. droppedByFilter counts every row removed and by which threshold. A row whose metric the provider did not report is NOT compared and NOT dropped: it is kept and counted in clientFiltering.notEvaluated, because a missing measurement is not a measurement of zero. Applied HERE, to the bounded window the provider already returned, NOT on the provider: it can only remove rows from that window and can never reach a row the provider did not send, so a narrow threshold on a capped window is a sample and not a survey. droppedByFilter reports every row it removed and why. OMIT it to keep every row; null is not a legal value for any threshold on this surface (plan 14.6 item 1) and is refused by name rather than guessed at.",
            "type": "number"
          },
          "minBuyCount": {
            "description": "Floor on the number of BUY trades, as a COUNT. Measured over thresholdWindow, which defaults to window. One window per threshold family is reachable here: the provider itself accepts several windows of one family at once and ANDs them (a 1,000,000 USD h24 volume floor combined with a 1,000 USD m5 volume floor was measured returning 104 rows), but this surface carries a single thresholdWindow, so a two-window condition on one metric has to be checked against the returned rows instead of filtered for. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once. ON THIS TOOL the comparison runs HERE, over the rows already in hand, and NOT on the provider: this tool applies no default floor, so there is nothing to remove and no disableQualityFloor to send; there is no separate thresholdWindow, so a windowed threshold measures over window; and null is not a legal value here, so OMIT the parameter to keep every row. droppedByFilter counts every row removed and by which threshold. A row whose metric the provider did not report is NOT compared and NOT dropped: it is kept and counted in clientFiltering.notEvaluated, because a missing measurement is not a measurement of zero. Applied HERE, to the bounded window the provider already returned, NOT on the provider: it can only remove rows from that window and can never reach a row the provider did not send, so a narrow threshold on a capped window is a sample and not a survey. droppedByFilter reports every row it removed and why. OMIT it to keep every row; null is not a legal value for any threshold on this surface (plan 14.6 item 1) and is refused by name rather than guessed at.",
            "type": "number"
          },
          "minFdvUsd": {
            "description": "Floor on fully diluted valuation, in US dollars. FDV values the total supply rather than the circulating supply, so it is the larger number. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once. ON THIS TOOL the comparison runs HERE, over the rows already in hand, and NOT on the provider: this tool applies no default floor, so there is nothing to remove and no disableQualityFloor to send; there is no separate thresholdWindow, so a windowed threshold measures over window; and null is not a legal value here, so OMIT the parameter to keep every row. droppedByFilter counts every row removed and by which threshold. A row whose metric the provider did not report is NOT compared and NOT dropped: it is kept and counted in clientFiltering.notEvaluated, because a missing measurement is not a measurement of zero. Applied HERE, to the bounded window the provider already returned, NOT on the provider: it can only remove rows from that window and can never reach a row the provider did not send, so a narrow threshold on a capped window is a sample and not a survey. droppedByFilter reports every row it removed and why. OMIT it to keep every row; null is not a legal value for any threshold on this surface (plan 14.6 item 1) and is refused by name rather than guessed at.",
            "type": "number"
          },
          "minLaunchpadProgressPct": {
            "description": "Floor on bonding-curve completion, as a PERCENT of the curve. 100 means the token has graduated and migrated to a DEX. Only launchpad pairs carry it. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once. ON THIS TOOL the comparison runs HERE, over the rows already in hand, and NOT on the provider: this tool applies no default floor, so there is nothing to remove and no disableQualityFloor to send; there is no separate thresholdWindow, so a windowed threshold measures over window; and null is not a legal value here, so OMIT the parameter to keep every row. droppedByFilter counts every row removed and by which threshold. A row whose metric the provider did not report is NOT compared and NOT dropped: it is kept and counted in clientFiltering.notEvaluated, because a missing measurement is not a measurement of zero. Applied HERE, to the bounded window the provider already returned, NOT on the provider: it can only remove rows from that window and can never reach a row the provider did not send, so a narrow threshold on a capped window is a sample and not a survey. droppedByFilter reports every row it removed and why. OMIT it to keep every row; null is not a legal value for any threshold on this surface (plan 14.6 item 1) and is refused by name rather than guessed at.",
            "type": "number"
          },
          "minLiquidityUsd": {
            "description": "Floor on pool liquidity, in US dollars. Liquidity has no window: it is the pool's depth right now. Bonding-curve pairs carry no liquidity field at all, so any liquidity threshold excludes them by construction. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once. ON THIS TOOL the comparison runs HERE, over the rows already in hand, and NOT on the provider: this tool applies no default floor, so there is nothing to remove and no disableQualityFloor to send; there is no separate thresholdWindow, so a windowed threshold measures over window; and null is not a legal value here, so OMIT the parameter to keep every row. droppedByFilter counts every row removed and by which threshold. A row whose metric the provider did not report is NOT compared and NOT dropped: it is kept and counted in clientFiltering.notEvaluated, because a missing measurement is not a measurement of zero. Applied HERE, to the bounded window the provider already returned, NOT on the provider: it can only remove rows from that window and can never reach a row the provider did not send, so a narrow threshold on a capped window is a sample and not a survey. droppedByFilter reports every row it removed and why. OMIT it to keep every row; null is not a legal value for any threshold on this surface (plan 14.6 item 1) and is refused by name rather than guessed at.",
            "type": "number"
          },
          "minMarketCapUsd": {
            "description": "Floor on market capitalisation, in US dollars. The provider's own circulating-supply valuation, with no window. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once. ON THIS TOOL the comparison runs HERE, over the rows already in hand, and NOT on the provider: this tool applies no default floor, so there is nothing to remove and no disableQualityFloor to send; there is no separate thresholdWindow, so a windowed threshold measures over window; and null is not a legal value here, so OMIT the parameter to keep every row. droppedByFilter counts every row removed and by which threshold. A row whose metric the provider did not report is NOT compared and NOT dropped: it is kept and counted in clientFiltering.notEvaluated, because a missing measurement is not a measurement of zero. Applied HERE, to the bounded window the provider already returned, NOT on the provider: it can only remove rows from that window and can never reach a row the provider did not send, so a narrow threshold on a capped window is a sample and not a survey. droppedByFilter reports every row it removed and why. OMIT it to keep every row; null is not a legal value for any threshold on this surface (plan 14.6 item 1) and is refused by name rather than guessed at.",
            "type": "number"
          },
          "minPairAgeSeconds": {
            "description": "Floor on pair age, in SECONDS since the pair was created. The provider filters age in whole and fractional hours, so this is converted for you; the exact age comes back per row in pairAgeSeconds. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once. ON THIS TOOL the comparison runs HERE, over the rows already in hand, and NOT on the provider: this tool applies no default floor, so there is nothing to remove and no disableQualityFloor to send; there is no separate thresholdWindow, so a windowed threshold measures over window; and null is not a legal value here, so OMIT the parameter to keep every row. droppedByFilter counts every row removed and by which threshold. A row whose metric the provider did not report is NOT compared and NOT dropped: it is kept and counted in clientFiltering.notEvaluated, because a missing measurement is not a measurement of zero. Applied HERE, to the bounded window the provider already returned, NOT on the provider: it can only remove rows from that window and can never reach a row the provider did not send, so a narrow threshold on a capped window is a sample and not a survey. droppedByFilter reports every row it removed and why. OMIT it to keep every row; null is not a legal value for any threshold on this surface (plan 14.6 item 1) and is refused by name rather than guessed at.",
            "type": "number"
          },
          "minPriceChangePct": {
            "description": "Floor on price change, as a PERCENT and not a fraction. Measured over thresholdWindow, which defaults to window. One window per threshold family is reachable here: the provider itself accepts several windows of one family at once and ANDs them (a 1,000,000 USD h24 volume floor combined with a 1,000 USD m5 volume floor was measured returning 104 rows), but this surface carries a single thresholdWindow, so a two-window condition on one metric has to be checked against the returned rows instead of filtered for. 10 means ten percent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once. ON THIS TOOL the comparison runs HERE, over the rows already in hand, and NOT on the provider: this tool applies no default floor, so there is nothing to remove and no disableQualityFloor to send; there is no separate thresholdWindow, so a windowed threshold measures over window; and null is not a legal value here, so OMIT the parameter to keep every row. droppedByFilter counts every row removed and by which threshold. A row whose metric the provider did not report is NOT compared and NOT dropped: it is kept and counted in clientFiltering.notEvaluated, because a missing measurement is not a measurement of zero. Applied HERE, to the bounded window the provider already returned, NOT on the provider: it can only remove rows from that window and can never reach a row the provider did not send, so a narrow threshold on a capped window is a sample and not a survey. droppedByFilter reports every row it removed and why. OMIT it to keep every row; null is not a legal value for any threshold on this surface (plan 14.6 item 1) and is refused by name rather than guessed at.",
            "type": "number"
          },
          "minSellCount": {
            "description": "Floor on the number of SELL trades, as a COUNT. Measured over thresholdWindow, which defaults to window. One window per threshold family is reachable here: the provider itself accepts several windows of one family at once and ANDs them (a 1,000,000 USD h24 volume floor combined with a 1,000 USD m5 volume floor was measured returning 104 rows), but this surface carries a single thresholdWindow, so a two-window condition on one metric has to be checked against the returned rows instead of filtered for. A sell-side floor is what separates a real two-sided market from a pair nobody can exit. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once. ON THIS TOOL the comparison runs HERE, over the rows already in hand, and NOT on the provider: this tool applies no default floor, so there is nothing to remove and no disableQualityFloor to send; there is no separate thresholdWindow, so a windowed threshold measures over window; and null is not a legal value here, so OMIT the parameter to keep every row. droppedByFilter counts every row removed and by which threshold. A row whose metric the provider did not report is NOT compared and NOT dropped: it is kept and counted in clientFiltering.notEvaluated, because a missing measurement is not a measurement of zero. Applied HERE, to the bounded window the provider already returned, NOT on the provider: it can only remove rows from that window and can never reach a row the provider did not send, so a narrow threshold on a capped window is a sample and not a survey. droppedByFilter reports every row it removed and why. OMIT it to keep every row; null is not a legal value for any threshold on this surface (plan 14.6 item 1) and is refused by name rather than guessed at.",
            "type": "number"
          },
          "minTxnCount": {
            "description": "Floor on the number of trades, as a COUNT and not a volume. Measured over thresholdWindow, which defaults to window. One window per threshold family is reachable here: the provider itself accepts several windows of one family at once and ANDs them (a 1,000,000 USD h24 volume floor combined with a 1,000 USD m5 volume floor was measured returning 104 rows), but this surface carries a single thresholdWindow, so a two-window condition on one metric has to be checked against the returned rows instead of filtered for. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once. ON THIS TOOL the comparison runs HERE, over the rows already in hand, and NOT on the provider: this tool applies no default floor, so there is nothing to remove and no disableQualityFloor to send; there is no separate thresholdWindow, so a windowed threshold measures over window; and null is not a legal value here, so OMIT the parameter to keep every row. droppedByFilter counts every row removed and by which threshold. A row whose metric the provider did not report is NOT compared and NOT dropped: it is kept and counted in clientFiltering.notEvaluated, because a missing measurement is not a measurement of zero. Applied HERE, to the bounded window the provider already returned, NOT on the provider: it can only remove rows from that window and can never reach a row the provider did not send, so a narrow threshold on a capped window is a sample and not a survey. droppedByFilter reports every row it removed and why. OMIT it to keep every row; null is not a legal value for any threshold on this surface (plan 14.6 item 1) and is refused by name rather than guessed at.",
            "type": "number"
          },
          "minVolumeUsd": {
            "description": "Floor on traded volume, in US dollars. Measured over thresholdWindow, which defaults to window. One window per threshold family is reachable here: the provider itself accepts several windows of one family at once and ANDs them (a 1,000,000 USD h24 volume floor combined with a 1,000 USD m5 volume floor was measured returning 104 rows), but this surface carries a single thresholdWindow, so a two-window condition on one metric has to be checked against the returned rows instead of filtered for. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once. ON THIS TOOL the comparison runs HERE, over the rows already in hand, and NOT on the provider: this tool applies no default floor, so there is nothing to remove and no disableQualityFloor to send; there is no separate thresholdWindow, so a windowed threshold measures over window; and null is not a legal value here, so OMIT the parameter to keep every row. droppedByFilter counts every row removed and by which threshold. A row whose metric the provider did not report is NOT compared and NOT dropped: it is kept and counted in clientFiltering.notEvaluated, because a missing measurement is not a measurement of zero. Applied HERE, to the bounded window the provider already returned, NOT on the provider: it can only remove rows from that window and can never reach a row the provider did not send, so a narrow threshold on a capped window is a sample and not a survey. droppedByFilter reports every row it removed and why. OMIT it to keep every row; null is not a legal value for any threshold on this surface (plan 14.6 item 1) and is refused by name rather than guessed at.",
            "type": "number"
          },
          "sortBy": {
            "description": "How to order the returned rows: relevance, liquidityUsd, volumeUsd, marketCapUsd, priceChangePct, pairAgeSeconds. Sorting runs HERE, over the rows the provider already sent, so it re-orders that window and never reaches past it. relevance keeps the provider's own ranking untouched, which is the only ordering that carries information about the text match itself.",
            "enum": [
              "relevance",
              "liquidityUsd",
              "volumeUsd",
              "marketCapUsd",
              "priceChangePct",
              "pairAgeSeconds"
            ],
            "type": "string"
          },
          "sortDir": {
            "description": "Ranking direction: desc for the largest values first, asc for the smallest. Defaults to desc, except under sortBy relevance, where the provider's order is kept as sent and this parameter is refused rather than silently reversing a ranking it does not own.",
            "enum": [
              "desc",
              "asc"
            ],
            "type": "string"
          },
          "tokenAddress": {
            "description": "The token's contract address. This tool takes an ADDRESS, never a ticker: a ticker is not identity and resolving one here would silently pick a copycat's pools. Use dexscreener__pairs_search first when only the name is known.",
            "minLength": 1,
            "type": "string"
          },
          "window": {
            "description": "Which stats window the single-window metrics report: m5, h1, h6 or h24. Defaults to h24. The provider measures all four regardless; this selects which one the flat fields carry, and the allWindows field group returns every one of them.",
            "enum": [
              "m5",
              "h1",
              "h6",
              "h24"
            ],
            "type": "string"
          }
        },
        "required": [
          "chain",
          "tokenAddress"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "dexscreener",
      "publicName": "dexscreener__token_pairs_list",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "List a token's DEX pools",
      "toolId": "dexscreener.tokenPairs"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1994,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "baseTokenSuffixes": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Base-token mint-address suffixes to keep, the vanity-suffix convention some launchpads use. Measured on solana: pump matched 20,852 pairs and bonk matched 1,595. Accepts either a comma-separated string (\"a,b\") or an array of strings ([\"a\",\"b\"]); the two are equivalent."
          },
          "chainIds": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "DexScreener chain slugs to screen, for example solana, base, ethereum, bsc. Omit to screen every indexed chain at once. Accepts either a comma-separated string (\"a,b\") or an array of strings ([\"a\",\"b\"]); the two are equivalent. Chain and dex values come from dexscreener__chains_list; an unknown chain is refused by name with the nearest catalog matches, because the provider answers one with zero rows and HTTP 200, which is indistinguishable from a real empty result."
          },
          "dexIds": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "DEX identifiers to keep, for example raydium, uniswap, pumpswap. Multiple values are ORed: a pair on any listed dex is kept. The accepted set is open and per-chain, so read it from dexscreener__chains_list rather than guessing. Accepts either a comma-separated string (\"a,b\") or an array of strings ([\"a\",\"b\"]); the two are equivalent."
          },
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "excludeDexIds": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "DEX identifiers to drop. DANGEROUS BY CONSTRUCTION: the provider applies a hidden default that already excludes bonding-curve venues, and sending this key AT ALL replaces that default, so excluding one dex can make the result set BIGGER. Measured on solana: 53,094 rows normally, 84,058 when only PumpSwap was excluded. Use includeLaunchpadPairs for the common case; exclusionDefaultReplaced reports whenever this happened. Accepts either a comma-separated string (\"a,b\") or an array of strings ([\"a\",\"b\"]); the two are equivalent."
          },
          "fields": {
            "description": "Comma-separated row field GROUPS to return, not individual field names. Supported groups: core, flow, reserves, allWindows, profile, launchpad, identity. Defaults to core, which carries the chain, dex, pair address, base-token address and symbols, the selected window's price, volume, liquidity, market cap, age, counts and every derived ratio. The two heaviest are profile (unbounded issuer-authored prose and links) and allWindows (four times the per-row metric payload). The `identity` group is NOT part of core and is what adds ammId, quoteTokenAddress, both tokens' decimals, priceNative and pairCreatedAtMs - the fields that let you tell a real pool from a mispriced one, so ask for it when the dollar columns are what you are judging. An unknown group name is refused with the full list rather than ignored.",
            "type": "string"
          },
          "includeInactive": {
            "description": "Keep pairs that had no activity inside the selected window. Defaults to false. Measured on solana at m5: 2,513 rows becomes 53,088, so this widens the set enormously on short windows and barely at all on h24.",
            "type": "boolean"
          },
          "includeLaunchpadPairs": {
            "description": "Lift the provider's hidden exclusion of bonding-curve pairs, the safe alternative to excludeDexIds. Defaults to false. Measured on solana: 53,094 rows becomes 102,676. Named for the effect rather than the mechanism, because the mechanism is an empty exclusion list.",
            "type": "boolean"
          },
          "labels": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Pool label values to keep, for example v2, v3, v4, CLMM, DLMM, CPMM. Matching is case-insensitive on the provider. Accepts either a comma-separated string (\"a,b\") or an array of strings ([\"a\",\"b\"]); the two are equivalent."
          },
          "launchpadIds": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Launchpad identifiers to keep, for example pumpfun, launchlab, meteoradbc, bags, fourmeme. MEASURED: the provider attaches a launchpad id to a pair only AFTER it graduates, so this filter matches graduated pairs and nothing else. While a curve is still running the launchpad is the pair's DEX, so use dexIds with the same value there (dexIds: pumpfun). On dexscreener__launchpad_pairs_list, pairing this with stage: \"bonding\" is refused by name rather than answered with an empty board: it returned 0 rows of a 0-row population while dexIds returned 53,478. KNOWN DEPTH GAP: no tool on this surface enumerates the launchpad vocabulary - dexscreener__chains_list carries dex ids only - so the five spellings above are the list, measured live, and an id outside them is not refused by name the way an unknown chain slug is. It returns an empty board instead, so check your spelling against those five before reading a zero as an absence of launches. Accepts either a comma-separated string (\"a,b\") or an array of strings ([\"a\",\"b\"]); the two are equivalent."
          },
          "limit": {
            "description": "How many rows to return, from 1 to 100. Defaults to 20. 100 is the provider's own page size, so asking for more than that is refused rather than silently served short; use offset to walk further into the ranking.",
            "type": "number"
          },
          "maxBoostCount": {
            "description": "Ceiling on the number of paid boosts ACTIVE on the pair, as a COUNT. It bounds WITHIN the boosted population and does not select against it. The presence rule below is the whole story here: an active-boost count exists only on a boosted pair, so maxBoostCount 3 means \"is boosted AND has at most 3 boosts\", never \"is not heavily advertised\". Measured 2026-08-24 on solana: min 1 gave 105 rows (the boosted population), max 10 gave 51, max 1000000 gave 105 again rather than the 64,420-row baseline, and max 0 gave 0. THIS IS NOT A WAY TO EXCLUDE ADS: an agent setting it to avoid ad-pumped pairs receives only ad-carrying pairs. To judge advertising, leave it unset and read boostsActive per row, which is on every row by default. PRESENCE RULE, measured, and it applies to every ceiling on this surface: a maximum matches only rows that CARRY the field. A row the provider reported no value for is excluded, not kept as if it were below the bound. Measured on a 64,420-row solana population: a liquidity ceiling removed 1,687 rows that carry no liquidity figure, an FDV ceiling removed 3,286, and a launchpad-progress ceiling reduced the set to launchpad pairs only. So a ceiling narrows on two axes at once, and the second one is silent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "maxBuyCount": {
            "description": "Ceiling on the number of BUY trades, as a COUNT. Measured over thresholdWindow, which defaults to window. One window per threshold family is reachable here: the provider itself accepts several windows of one family at once and ANDs them (a 1,000,000 USD h24 volume floor combined with a 1,000 USD m5 volume floor was measured returning 104 rows), but this surface carries a single thresholdWindow, so a two-window condition on one metric has to be checked against the returned rows instead of filtered for. PRESENCE RULE, measured, and it applies to every ceiling on this surface: a maximum matches only rows that CARRY the field. A row the provider reported no value for is excluded, not kept as if it were below the bound. Measured on a 64,420-row solana population: a liquidity ceiling removed 1,687 rows that carry no liquidity figure, an FDV ceiling removed 3,286, and a launchpad-progress ceiling reduced the set to launchpad pairs only. So a ceiling narrows on two axes at once, and the second one is silent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "maxFdvUsd": {
            "description": "Ceiling on fully diluted valuation, in US dollars. FDV values the total supply rather than the circulating supply, so it is the larger number. PRESENCE RULE, measured, and it applies to every ceiling on this surface: a maximum matches only rows that CARRY the field. A row the provider reported no value for is excluded, not kept as if it were below the bound. Measured on a 64,420-row solana population: a liquidity ceiling removed 1,687 rows that carry no liquidity figure, an FDV ceiling removed 3,286, and a launchpad-progress ceiling reduced the set to launchpad pairs only. So a ceiling narrows on two axes at once, and the second one is silent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "maxLaunchpadProgressPct": {
            "description": "Ceiling on bonding-curve completion, as a PERCENT of the curve. Below 100 keeps tokens still on the curve. Only launchpad pairs carry it. PRESENCE RULE, measured, and it applies to every ceiling on this surface: a maximum matches only rows that CARRY the field. A row the provider reported no value for is excluded, not kept as if it were below the bound. Measured on a 64,420-row solana population: a liquidity ceiling removed 1,687 rows that carry no liquidity figure, an FDV ceiling removed 3,286, and a launchpad-progress ceiling reduced the set to launchpad pairs only. So a ceiling narrows on two axes at once, and the second one is silent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "maxLiquidityUsd": {
            "description": "Ceiling on pool liquidity, in US dollars. Useful for finding small pools deliberately; liquidity has no window. PRESENCE RULE, measured, and it applies to every ceiling on this surface: a maximum matches only rows that CARRY the field. A row the provider reported no value for is excluded, not kept as if it were below the bound. Measured on a 64,420-row solana population: a liquidity ceiling removed 1,687 rows that carry no liquidity figure, an FDV ceiling removed 3,286, and a launchpad-progress ceiling reduced the set to launchpad pairs only. So a ceiling narrows on two axes at once, and the second one is silent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "maxMarketCapUsd": {
            "description": "Ceiling on market capitalisation, in US dollars. The provider's own circulating-supply valuation, with no window. PRESENCE RULE, measured, and it applies to every ceiling on this surface: a maximum matches only rows that CARRY the field. A row the provider reported no value for is excluded, not kept as if it were below the bound. Measured on a 64,420-row solana population: a liquidity ceiling removed 1,687 rows that carry no liquidity figure, an FDV ceiling removed 3,286, and a launchpad-progress ceiling reduced the set to launchpad pairs only. So a ceiling narrows on two axes at once, and the second one is silent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "maxPairAgeSeconds": {
            "description": "Ceiling on pair age, in SECONDS since the pair was created. 86400 is one day. The provider filters in hours, so sub-hour precision comes from the returned pairAgeSeconds rather than from this filter. PRESENCE RULE, measured, and it applies to every ceiling on this surface: a maximum matches only rows that CARRY the field. A row the provider reported no value for is excluded, not kept as if it were below the bound. Measured on a 64,420-row solana population: a liquidity ceiling removed 1,687 rows that carry no liquidity figure, an FDV ceiling removed 3,286, and a launchpad-progress ceiling reduced the set to launchpad pairs only. So a ceiling narrows on two axes at once, and the second one is silent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "maxPriceChangePct": {
            "description": "Ceiling on price change, as a PERCENT and not a fraction. Measured over thresholdWindow, which defaults to window. One window per threshold family is reachable here: the provider itself accepts several windows of one family at once and ANDs them (a 1,000,000 USD h24 volume floor combined with a 1,000 USD m5 volume floor was measured returning 104 rows), but this surface carries a single thresholdWindow, so a two-window condition on one metric has to be checked against the returned rows instead of filtered for. Negative values are legal, so -50 keeps only pairs down at least half. PRESENCE RULE, measured, and it applies to every ceiling on this surface: a maximum matches only rows that CARRY the field. A row the provider reported no value for is excluded, not kept as if it were below the bound. Measured on a 64,420-row solana population: a liquidity ceiling removed 1,687 rows that carry no liquidity figure, an FDV ceiling removed 3,286, and a launchpad-progress ceiling reduced the set to launchpad pairs only. So a ceiling narrows on two axes at once, and the second one is silent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "maxSellCount": {
            "description": "Ceiling on the number of SELL trades, as a COUNT. Measured over thresholdWindow, which defaults to window. One window per threshold family is reachable here: the provider itself accepts several windows of one family at once and ANDs them (a 1,000,000 USD h24 volume floor combined with a 1,000 USD m5 volume floor was measured returning 104 rows), but this surface carries a single thresholdWindow, so a two-window condition on one metric has to be checked against the returned rows instead of filtered for. PRESENCE RULE, measured, and it applies to every ceiling on this surface: a maximum matches only rows that CARRY the field. A row the provider reported no value for is excluded, not kept as if it were below the bound. Measured on a 64,420-row solana population: a liquidity ceiling removed 1,687 rows that carry no liquidity figure, an FDV ceiling removed 3,286, and a launchpad-progress ceiling reduced the set to launchpad pairs only. So a ceiling narrows on two axes at once, and the second one is silent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "maxTxnCount": {
            "description": "Ceiling on the number of trades, as a COUNT and not a volume. Measured over thresholdWindow, which defaults to window. One window per threshold family is reachable here: the provider itself accepts several windows of one family at once and ANDs them (a 1,000,000 USD h24 volume floor combined with a 1,000 USD m5 volume floor was measured returning 104 rows), but this surface carries a single thresholdWindow, so a two-window condition on one metric has to be checked against the returned rows instead of filtered for. PRESENCE RULE, measured, and it applies to every ceiling on this surface: a maximum matches only rows that CARRY the field. A row the provider reported no value for is excluded, not kept as if it were below the bound. Measured on a 64,420-row solana population: a liquidity ceiling removed 1,687 rows that carry no liquidity figure, an FDV ceiling removed 3,286, and a launchpad-progress ceiling reduced the set to launchpad pairs only. So a ceiling narrows on two axes at once, and the second one is silent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "maxVolumeUsd": {
            "description": "Ceiling on traded volume, in US dollars. Measured over thresholdWindow, which defaults to window. One window per threshold family is reachable here: the provider itself accepts several windows of one family at once and ANDs them (a 1,000,000 USD h24 volume floor combined with a 1,000 USD m5 volume floor was measured returning 104 rows), but this surface carries a single thresholdWindow, so a two-window condition on one metric has to be checked against the returned rows instead of filtered for. PRESENCE RULE, measured, and it applies to every ceiling on this surface: a maximum matches only rows that CARRY the field. A row the provider reported no value for is excluded, not kept as if it were below the bound. Measured on a 64,420-row solana population: a liquidity ceiling removed 1,687 rows that carry no liquidity figure, an FDV ceiling removed 3,286, and a launchpad-progress ceiling reduced the set to launchpad pairs only. So a ceiling narrows on two axes at once, and the second one is silent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "metaIds": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Narrative IDs to keep, taken from dexscreener__narratives_list. Send the ID and not the human-readable name: the provider accepts a name, matches nothing, and answers with zero rows, which reads as an empty market rather than as a wrong value. Accepts either a comma-separated string (\"a,b\") or an array of strings ([\"a\",\"b\"]); the two are equivalent."
          },
          "minBoostCount": {
            "description": "Floor on the number of paid boosts ACTIVE on the pair, as a COUNT. A boost is bought visibility, never demand or safety. This is more specific than onlyBoosted and wins over it when both are sent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "minBuyCount": {
            "description": "Floor on the number of BUY trades, as a COUNT. Measured over thresholdWindow, which defaults to window. One window per threshold family is reachable here: the provider itself accepts several windows of one family at once and ANDs them (a 1,000,000 USD h24 volume floor combined with a 1,000 USD m5 volume floor was measured returning 104 rows), but this surface carries a single thresholdWindow, so a two-window condition on one metric has to be checked against the returned rows instead of filtered for. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "minFdvUsd": {
            "description": "Floor on fully diluted valuation, in US dollars. FDV values the total supply rather than the circulating supply, so it is the larger number. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "minLaunchpadProgressPct": {
            "description": "Floor on bonding-curve completion, as a PERCENT of the curve. 100 means the token has graduated and migrated to a DEX. Only launchpad pairs carry it. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "minLiquidityUsd": {
            "description": "Floor on pool liquidity, in US dollars. Liquidity has no window: it is the pool's depth right now. Bonding-curve pairs carry no liquidity field at all, so any liquidity threshold excludes them by construction. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "minMarketCapUsd": {
            "description": "Floor on market capitalisation, in US dollars. The provider's own circulating-supply valuation, with no window. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "minPairAgeSeconds": {
            "description": "Floor on pair age, in SECONDS since the pair was created. The provider filters age in whole and fractional hours, so this is converted for you; the exact age comes back per row in pairAgeSeconds. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "minPriceChangePct": {
            "description": "Floor on price change, as a PERCENT and not a fraction. Measured over thresholdWindow, which defaults to window. One window per threshold family is reachable here: the provider itself accepts several windows of one family at once and ANDs them (a 1,000,000 USD h24 volume floor combined with a 1,000 USD m5 volume floor was measured returning 104 rows), but this surface carries a single thresholdWindow, so a two-window condition on one metric has to be checked against the returned rows instead of filtered for. 10 means ten percent. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "minSellCount": {
            "description": "Floor on the number of SELL trades, as a COUNT. Measured over thresholdWindow, which defaults to window. One window per threshold family is reachable here: the provider itself accepts several windows of one family at once and ANDs them (a 1,000,000 USD h24 volume floor combined with a 1,000 USD m5 volume floor was measured returning 104 rows), but this surface carries a single thresholdWindow, so a two-window condition on one metric has to be checked against the returned rows instead of filtered for. A sell-side floor is what separates a real two-sided market from a pair nobody can exit. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "minTxnCount": {
            "description": "Floor on the number of trades, as a COUNT and not a volume. Measured over thresholdWindow, which defaults to window. One window per threshold family is reachable here: the provider itself accepts several windows of one family at once and ANDs them (a 1,000,000 USD h24 volume floor combined with a 1,000 USD m5 volume floor was measured returning 104 rows), but this surface carries a single thresholdWindow, so a two-window condition on one metric has to be checked against the returned rows instead of filtered for. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "minVolumeUsd": {
            "description": "Floor on traded volume, in US dollars. Measured over thresholdWindow, which defaults to window. One window per threshold family is reachable here: the provider itself accepts several windows of one family at once and ANDs them (a 1,000,000 USD h24 volume floor combined with a 1,000 USD m5 volume floor was measured returning 104 rows), but this surface carries a single thresholdWindow, so a two-window condition on one metric has to be checked against the returned rows instead of filtered for. Omit it to leave the tool's default in place, or send a number to set your own; whichever happens is echoed in filtersApplied. null is not accepted here: send disableQualityFloor: true to drop every default floor at once.",
            "type": "number"
          },
          "offset": {
            "description": "How many ranked rows to skip before returning any, 0 or more. Defaults to 0. Mapped onto the provider's 100-row pages for you, and offset paging was measured reaching page 525 of 525, so the ranking is walkable to its live end. The ranking is live, so rows can repeat or be skipped between deep pages; hasMore and nextOffset carry the continuation.",
            "type": "number"
          },
          "onlyAds": {
            "description": "Keep only pairs with an ad placement running RIGHT NOW. Bought visibility, never demand or safety. Distinct from onlyRecentAds: a pair running a placement now may have bought it long ago.",
            "type": "boolean"
          },
          "onlyBoosted": {
            "description": "Keep only pairs carrying at least one ACTIVE paid boost. This is an advertising signal and not a quality signal: it says the project paid DexScreener for visibility. Use minBoostCount when you want a specific number rather than at least one.",
            "type": "boolean"
          },
          "onlyRecentAds": {
            "description": "Keep only pairs that bought an ad slot RECENTLY, whether or not it is currently running; this is the site's own ads badge. Bought visibility, never demand or safety. Combine with onlyAds to require both, since the provider ANDs them.",
            "type": "boolean"
          },
          "sortBy": {
            "description": "Which provider rank key orders the token rows: trendingScore, volume, txns, liquidity, priceChange or pairAge. Defaults to trendingScore. pairAge ranks by creation time and is the only way to ask this channel for the newest tokens; it defaults to ascending (newest first), and sortDir overrides that like any other key. Ranking by marketCap is deliberately NOT offered: the provider accepts it and answers with a degenerate board (measured 2026-08-25 on solana: 43 rows in total, 18 of 42 adjacent pairs out of order, JUP served at 3.68 trillion USD and PUMP at 23.98 trillion, quoted in junk pairs), so filter with minMarketCapUsd or maxMarketCapUsd and rank by liquidity or volume instead. HONEST LIMIT, measured: the served order is provider-opaque and does not reproduce from any visible metric (48 to 51 ordering violations of 99 adjacent pairs against every single-metric reconstruction tried), so treat the order as the provider's opinion and the per-row numbers as the facts.",
            "enum": [
              "trendingScore",
              "volume",
              "txns",
              "liquidity",
              "priceChange",
              "pairAge"
            ],
            "type": "string"
          },
          "sortDir": {
            "description": "Ranking direction: desc for the largest values first, asc for the smallest. Defaults to desc on every key except pairAge, which defaults to asc so that the newest tokens come first; rankApplied echoes the direction that actually ran. Measured honoured live on this channel 2026-08-25: pairAge desc served tokens created in 2022 and 2023, pairAge asc served tokens created the same day. Note that asc on a metric key surfaces the bottom of the population, where the metric is frequently absent rather than small.",
            "enum": [
              "desc",
              "asc"
            ],
            "type": "string"
          },
          "thresholdWindow": {
            "description": "Which window the volume, transaction, buy, sell and price-change thresholds measure over: m5, h1, h6 or h24. Defaults to whatever window is set. Separating the two is how the site's own boards work, ranking by 5-minute price change while requiring 24-hour transaction and liquidity floors.",
            "enum": [
              "m5",
              "h1",
              "h6",
              "h24"
            ],
            "type": "string"
          },
          "window": {
            "description": "The stats window the ranking measures over: m5, h1, h6 or h24. Defaults to h24. It has a DUAL role and both halves matter: it selects which window's volume, transactions, buys, sells and price change are ranked and reported, AND it excludes pairs that had no activity in that window. Use includeInactive to keep the ranking and drop the gate.",
            "enum": [
              "m5",
              "h1",
              "h6",
              "h24"
            ],
            "type": "string"
          }
        },
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "dexscreener",
      "publicName": "dexscreener__tokens_screen",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Screen tokens across chains",
      "toolId": "dexscreener.tokens.screen"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 6552,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "chain": {
            "anyOf": [
              {
                "minLength": 1,
                "type": "string"
              },
              {
                "minimum": 1,
                "type": "integer"
              }
            ],
            "description": "Chain slug the pool lives on, for example solana, ethereum, bsc, base or robinhood. Chain slug/alias, or the numeric chain id `TokenFind` returns (e.g. `base` or `8453`). A pair or token address is only unique within one chain, so this is required; an unknown slug is refused by name with the nearest catalog matches rather than answered with zero rows."
          },
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "limit": {
            "description": "How many wallets to return, 1 to 100. Defaults to 25. 100 is the provider's whole leaderboard. There is NO offset and no cursor: this surface serves one bounded leaderboard per sort and wallets beyond it are unreachable, which the envelope states rather than hiding behind a page parameter that does nothing.",
            "type": "number"
          },
          "lookbackDays": {
            "description": "Rank only wallets active within this many days (the provider's maxDaysAgo), from 1 to 30. MEASURED: 30 is the provider's own maximum AND its default. On a 2023-vintage pool with no lookback sent, the oldest first trade on every sort was 30.01 days old; mda=30 returned the same 100 wallets as omitting it; and mda=31, 60, 90 and 365 each answered HTTP 400 with an empty body. So THIS LEADERBOARD IS A 30-DAY LEADERBOARD and cannot rank a pair's whole history: a wallet that traded heavily more than 30 days ago does not appear at all. A value above 30 is refused by name here rather than sent, because the provider's 400 blames the route and would send you to debug an AMM id that is correct. The window in force is echoed in filtersApplied on every answer. Measured working: a 1-day lookback returned 100 rows all with a last trade inside 0.952 days. IT WINDOWS THE MONEY FIGURES, NOT ONLY WHO IS ELIGIBLE. Every per-wallet column - buys, sells, volumeUsdBuy, volumeUsdSell, amountBuy, amountSell, firstSwapAtMs, and therefore netCashFlowUsd and activeSpanSeconds - is RECOMPUTED over this window rather than being a lifetime total filtered by recency. Measured on the same wallet: at 30 days it reads buys 29 and volumeUsdBuy 246,836.60, at 1 day it reads buys 3 and 11,325.53, a 28x difference with no marker on the row. So \"how much has this wallet bought here\" is answered for the window in force and never for the pair's whole history.",
            "type": "number"
          },
          "onlyKol": {
            "description": "Restrict the leaderboard to wallets the provider labels as key opinion leaders. Defaults to false. MEASURED CAVEAT: this filter returned ZERO rows on the probed pair, so an empty result with it on is expected and is not evidence that no notable wallet traded the pair. The provider's labelling is its own opaque classification.",
            "type": "boolean"
          },
          "pairAddress": {
            "description": "The pool (pair) address. Give this when you already know the exact pool, from dexscreener__pairs_search, a screening board, or dexscreener__pair_get. Either this or tokenAddress is required. When both are given, pairAddress wins and the response says so rather than silently ignoring the other.",
            "type": "string"
          },
          "sortBy": {
            "description": "Which column ranks the leaderboard: boughtUsd, soldUsd, netCashFlowUsd, currentHoldingValueUsd. Defaults to boughtUsd. netCashFlowUsd maps to the provider's \"pnl\" rank and currentHoldingValueUsd to its \"unrealized\" rank; both provider names are wrong about what they measure, so the names here say what the column IS. Ranking runs on the provider over the whole pair, not locally over a sample, and asc is a true full-population reverse rank rather than a reversal of the top 100 (measured: zero maker overlap between the asc and desc pages of the same sort). The sort also decides whether `currentHoldingValueUsd` is derivable at all: the provider sends the balance it needs on some rankings and not others, and on boughtUsd asc and netCashFlowUsd desc it was measured absent on 100 of 100 rows. Ask for currentHoldingValueUsd when that column is the question; `balanceCoverage` on every answer reports what actually arrived. NAME MAPPING, because these two sort keys have no row field of the same spelling: `boughtUsd` ranks by the row column `volumeUsdBuy` and `soldUsd` by `volumeUsdSell`. Sorting by one and reading the other back is expected here, not a missing field.",
            "enum": [
              "boughtUsd",
              "soldUsd",
              "netCashFlowUsd",
              "currentHoldingValueUsd"
            ],
            "type": "string"
          },
          "sortDir": {
            "description": "desc for the largest values first, asc for the smallest. Defaults to desc. asc with netCashFlowUsd surfaces the wallets that have put in the most and taken out the least on this pair; that is a cash-flow statement and not a loss.",
            "enum": [
              "desc",
              "asc"
            ],
            "type": "string"
          },
          "tokenAddress": {
            "description": "A token contract address, resolved to the deepest pool of the provider's bounded 30-row search window on this chain. Use it when you have a token but not a pool. resolutionBasis echoes that a resolution happened and resolutionNote states the bound: \"deepest\" means deepest among at most 30 pools the provider chose to return, never a claim about every pool the token trades in. For the full pool list call dexscreener__token_pairs_list.",
            "type": "string"
          }
        },
        "required": [
          "chain"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "dexscreener",
      "publicName": "dexscreener__top_traders_list",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Rank a pair's top wallets",
      "toolId": "dexscreener.top.traders"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 4171,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "afterBlock": {
            "description": "Return only events STRICTLY ABOVE this BLOCK NUMBER. EXCLUSIVE, measured two-sided on a boundary block carrying three events: the block you pass is the last one you have ALREADY SEEN and none of its events come back, while passing block-minus-one returns all three of them. This is the incremental-poll primitive: take the highest blockNumber from a previous answer's rows and pass it back to ask what has traded since. MEASURED honoured server-side (a lower bound of 25830000 returned 100 rows spanning blocks 25830786 down to 25830275) and it AND-combines with startAtMs, endAtMs and every other filter. It is a BOUND, NOT A CURSOR: the rows inside the window still arrive newest-first, so it narrows the window rather than streaming forward from it. The window is served newest-first under the provider's 100-row page cap, so a busy pair returns only the newest slice of it: when hasMore is true, page DEEPER INTO THE WINDOW with the returned cursor, which this build expresses on both channels and which is the only way to reach the rows between afterBlock and the oldest block returned. Advancing afterBlock instead would skip them permanently. Use it instead of re-reading from the head of history: the provider's live trade push acknowledges a subscription and then sends nothing (re-measured), so polling with afterBlock and following hasMore is the gapless way to follow a pair.",
            "type": "number"
          },
          "chain": {
            "anyOf": [
              {
                "minLength": 1,
                "type": "string"
              },
              {
                "minimum": 1,
                "type": "integer"
              }
            ],
            "description": "Chain slug the pool lives on, for example solana, ethereum, bsc, base or robinhood. Chain slug/alias, or the numeric chain id `TokenFind` returns (e.g. `base` or `8453`). A pair or token address is only unique within one chain, so this is required; an unknown slug is refused by name with the nearest catalog matches rather than answered with zero rows."
          },
          "cursor": {
            "description": "Opaque continuation token from a previous call's nextCursor. It is built from the last row that answer RETURNED, never from the oldest row it fetched, so no row is skipped at any limit. It encodes the provider's exact block, transaction index and event index, so a continued page resumes INSIDE the boundary block rather than after it: a block-only cursor was measured skipping a real buy that shared the boundary block. It is bound to the pair, orientation and filters of the call that issued it and is refused if replayed against a different query.",
            "type": "string"
          },
          "deadlineMs": {
            "description": "Wall-clock budget for an aggregate walk, in milliseconds. Defaults to 25000, ceiling 120000. Reported exactly like maxPages when it is reached.",
            "type": "number"
          },
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "endAtMs": {
            "description": "Newest instant to include, epoch MILLISECONDS, second-precise like startAtMs. Together the two express \"what happened during that candle\", which is the usual follow-up to dexscreener__candles_list.",
            "type": "number"
          },
          "eventType": {
            "description": "Which events to return: all, swap, buy, sell, liquidity, add, remove. Defaults to all. swap is buys and sells together, liquidity is adds and removes together; both are the provider's own combined filters and both are served on the WebSocket, because the HTTP channel refuses every combined spelling with a structured 400 (measured). buy, sell, add and remove are each served on the cheaper HTTP read. The provider validates this value and answers a wrong one with a structured rejection rather than ignoring it, so an unsupported value is refused here by name instead of quietly widening the query. add and remove are LIQUIDITY events: they carry token amounts and no price or USD volume, so they are counted in the aggregate's liquidityAdds and liquidityRemoves and are never filed as zero-dollar trades.",
            "enum": [
              "all",
              "swap",
              "buy",
              "sell",
              "liquidity",
              "add",
              "remove"
            ],
            "type": "string"
          },
          "limit": {
            "description": "How many trade rows to return, 1 to 100. Defaults to 25. 100 is the provider's own page size, not a Vex cap. Deeper history is reached with cursor, which pages to any depth. A limit below the provider's page size withholds nothing: nextCursor is built from the LAST ROW THIS ANSWER RETURNED, so the next call resumes at the row immediately after it, and pagination reports rowsFetched, rowsReturned and rowsWithheldByLimit.",
            "type": "number"
          },
          "maker": {
            "description": "A wallet address. Returns that wallet's history ON THIS PAIR and nothing else: transfers, other pools and other venues are invisible to this channel, so an empty result is not evidence the wallet is inactive.",
            "type": "string"
          },
          "maxBaseAmountIn": {
            "description": "Upper bound on a trade's BASE-token amount, in HUMAN decimals, as a decimal string. Pair it with minBaseAmountIn for a size band in token terms rather than in dollars.",
            "type": "string"
          },
          "maxPages": {
            "description": "How many provider pages the AGGREGATE mode may walk to cover the requested range. Defaults to 10 with no upper ceiling, because deadlineMs is the real bound on a walk. Ignored in raw mode, which returns one page. When the bound is hit the summary is renamed pageAggregate and carries rangeFullyCovered false with the exact range it did cover, because a one-page summary presented as a range summary is the silent cut this avoids.",
            "type": "number"
          },
          "maxQuoteAmountIn": {
            "description": "Upper bound on a trade's QUOTE-token amount, in HUMAN decimals, as a decimal string. Pair it with minQuoteAmountIn for a size band measured in the quote token.",
            "type": "string"
          },
          "maxVolumeUsd": {
            "description": "Keep only trades of at most this many US dollars. Applied server-side. Combine with minVolumeUsd for a size band, which is how a whale filter and a retail filter are both expressed.",
            "type": "number"
          },
          "minBaseAmountIn": {
            "description": "Lower bound on a trade's BASE-token amount, in HUMAN decimals, as a decimal STRING exactly as the provider writes them (for example \"1000.5\"). A string and not a number because token amounts must never round-trip through binary floating point. The In suffix marks human decimals, which is this repository's amount grammar; it does NOT mean the buy side.",
            "type": "string"
          },
          "minQuoteAmountIn": {
            "description": "Lower bound on a trade's QUOTE-token amount, in HUMAN decimals, as a decimal string. The quote token is the pool's own and is named in the subject block.",
            "type": "string"
          },
          "minVolumeUsd": {
            "description": "Keep only trades of at least this many US dollars. Applied SERVER-side (measured exact: a 1,000 floor returned a minimum of 1,024). Omit for no floor.",
            "type": "number"
          },
          "mode": {
            "description": "raw for the trade rows, aggregate for one summary block, both for the summary plus the rows. Defaults to raw. All three come from the SAME fetch set, so aggregate costs no extra requests over raw and both costs nothing over aggregate.",
            "enum": [
              "raw",
              "aggregate",
              "both"
            ],
            "type": "string"
          },
          "pairAddress": {
            "description": "The pool (pair) address. Give this when you already know the exact pool, from dexscreener__pairs_search, a screening board, or dexscreener__pair_get. Either this or tokenAddress is required. When both are given, pairAddress wins and the response says so rather than silently ignoring the other.",
            "type": "string"
          },
          "startAtMs": {
            "description": "Oldest instant to include, epoch MILLISECONDS. The provider honours this to the SECOND (measured: a one-hour window returned 34 rows, every one inside it), so a sub-second value is rounded down to the second the provider can actually apply.",
            "type": "number"
          },
          "tokenAddress": {
            "description": "A token contract address, resolved to the deepest pool of the provider's bounded 30-row search window on this chain. Use it when you have a token but not a pool. resolutionBasis echoes that a resolution happened and resolutionNote states the bound: \"deepest\" means deepest among at most 30 pools the provider chose to return, never a claim about every pool the token trades in. For the full pool list call dexscreener__token_pairs_list.",
            "type": "string"
          },
          "traderProfile": {
            "description": "How much counterparty detail each row carries: compact, full, none. Defaults to compact (buys, sells, dollars in and out, retainedBoughtPct, newOnPair, first trade time). full adds `pairObservedRetainedAmount` (the amount this wallet retains of what it BOUGHT on THIS pool as the venue observed it, never an on-chain balance and never divisible by token supply, with that refusal carried as a field beside it) and base-token volumes. none drops the block entirely and is the cheapest shape when only the trades matter. Trader figures are venue-local and are NOT profit. netCashFlowUsd is dollars out minus dollars in on this pair; cost basis, transfers and every other venue are invisible here, so it cannot establish gain, loss, or whether a wallet exited. retainedBoughtPct is the share of what the wallet BOUGHT on this pair that it still holds, never a share of token supply. newOnPair means new on THIS pair, not a newly created wallet. An active trading span is the time between a wallet's first and last trade here, not a holding period. EVERY TRADER FIGURE IS COMPUTED OVER THE PROVIDER'S TRAILING 30-DAY WINDOW, not over the pair's whole life and not over the range you filtered to. firstSwapAtMs is CLAMPED to that window floor rather than reported as absent, so a wallet whose real first trade predates the window reads as exact UTC midnight thirty days ago with a zero millisecond remainder: read that value as \"no later than\", never as a first-trade time. This is also why dexscreener__top_traders_list, which ranks over its own declared window, can answer \"how much did this wallet buy here\" with a different number: two windows, not a contradiction.",
            "enum": [
              "compact",
              "full",
              "none"
            ],
            "type": "string"
          }
        },
        "required": [
          "chain"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "dexscreener",
      "publicName": "dexscreener__trades_list",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "List a pair's trades",
      "toolId": "dexscreener.trades"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 3471,
      "destructiveHint": true,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "amountRaw": {
            "description": "Amount to bridge, in raw atomic units of fromToken (e.g. USDC has 6 decimals, so 1 USDC = \"1000000\"). Raw base units as an integer string (not human decimals) - read the token's decimals from `TokenFind` first.",
            "minLength": 1,
            "type": "string"
          },
          "dryRun": {
            "description": "If true, build deposit plan without executing.",
            "type": "boolean"
          },
          "filler": {
            "description": "Restrict quotes to a specific filler.",
            "type": "string"
          },
          "fromAddress": {
            "description": "Source wallet address override.",
            "type": "string"
          },
          "fromChain": {
            "anyOf": [
              {
                "minLength": 1,
                "type": "string"
              },
              {
                "minimum": 1,
                "type": "integer"
              }
            ],
            "description": "Source chain ID or alias."
          },
          "fromToken": {
            "description": "Source token address.",
            "minLength": 1,
            "type": "string"
          },
          "toChain": {
            "anyOf": [
              {
                "minLength": 1,
                "type": "string"
              },
              {
                "minimum": 1,
                "type": "integer"
              }
            ],
            "description": "Destination chain ID or alias."
          },
          "toToken": {
            "description": "Destination token address.",
            "minLength": 1,
            "type": "string"
          },
          "tradeType": {
            "description": "EXACT_INPUT or EXACT_OUTPUT.",
            "type": "string"
          }
        },
        "required": [
          "fromChain",
          "fromToken",
          "toChain",
          "toToken",
          "amountRaw"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "khalani",
      "publicName": "khalani__bridge_execute",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Execute a Khalani bridge",
      "toolId": "khalani.bridge"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 2304,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "amountRaw": {
            "description": "Amount to bridge, in raw atomic units of fromToken (e.g. USDC has 6 decimals, so 1 USDC = \"1000000\"). Raw base units as an integer string (not human decimals) - read the token's decimals from `TokenFind` first.",
            "minLength": 1,
            "type": "string"
          },
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "filler": {
            "description": "Restrict quotes to a specific filler.",
            "type": "string"
          },
          "fromAddress": {
            "description": "Source wallet address override.",
            "type": "string"
          },
          "fromChain": {
            "anyOf": [
              {
                "minLength": 1,
                "type": "string"
              },
              {
                "minimum": 1,
                "type": "integer"
              }
            ],
            "description": "Source chain ID or alias."
          },
          "fromToken": {
            "description": "Source token address.",
            "minLength": 1,
            "type": "string"
          },
          "toChain": {
            "anyOf": [
              {
                "minLength": 1,
                "type": "string"
              },
              {
                "minimum": 1,
                "type": "integer"
              }
            ],
            "description": "Destination chain ID or alias."
          },
          "toToken": {
            "description": "Destination token address.",
            "minLength": 1,
            "type": "string"
          },
          "tradeType": {
            "description": "EXACT_INPUT or EXACT_OUTPUT (default: EXACT_INPUT).",
            "type": "string"
          }
        },
        "required": [
          "fromChain",
          "fromToken",
          "toChain",
          "toToken",
          "amountRaw"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "khalani",
      "publicName": "khalani__bridge_quote_get",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Quote a Khalani bridge",
      "toolId": "khalani.quote.get"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 850,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "refresh": {
            "description": "Force refresh chain cache.",
            "type": "boolean"
          }
        },
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "khalani",
      "publicName": "khalani__chains_list",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "List Khalani bridge chains",
      "toolId": "khalani.chains.list"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 990,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "orderId": {
            "description": "Khalani order ID.",
            "minLength": 1,
            "type": "string"
          }
        },
        "required": [
          "orderId"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "khalani",
      "publicName": "khalani__order_get",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Read one Khalani bridge order",
      "toolId": "khalani.orders.get"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1066,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "cursor": {
            "description": "The provider's own continuation value, echoed back as `cursor` on the previous reply. Pass back exactly what that reply returned; never compute one. Omit for the first page, and when a reply carries no `cursor` there is no next page.",
            "type": "number"
          },
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "fromChain": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "minimum": 1,
                "type": "integer"
              }
            ],
            "description": "Source chain filter (ID or alias)."
          },
          "limit": {
            "description": "Max results. Range 1-20 (provider maximum, default 10); anything else is rejected before the call.",
            "type": "number"
          },
          "orderIds": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Comma-separated order IDs to filter. Accepts either a comma-separated string (\"1,8453\") or an array of strings ([\"1\",\"8453\"]) - the two are equivalent."
          },
          "toChain": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "minimum": 1,
                "type": "integer"
              }
            ],
            "description": "Destination chain filter (ID or alias)."
          },
          "txHashSearch": {
            "description": "Search by transaction hash. At most 66 characters (0x + 64 hex, the provider maximum); a longer value is rejected before the call.",
            "type": "string"
          },
          "walletAddress": {
            "description": "Optional. The ACCOUNT address whose orders to list; omit to use your personal wallet. Under a session-scoped wallet it must equal the selected wallet.",
            "type": "string"
          },
          "walletFamily": {
            "description": "Wallet FAMILY, never a chain: eip155 or solana.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "khalani",
      "publicName": "khalani__orders_list",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "List Khalani bridge orders",
      "toolId": "khalani.orders.list"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 4836,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "chainIds": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Comma-separated chain IDs or aliases. Omit to scan all chains in the family. Accepts either a comma-separated string (\"1,8453\") or an array of strings ([\"1\",\"8453\"]) - the two are equivalent."
          },
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "walletAddress": {
            "description": "Optional. The ACCOUNT address whose balances to read; omit to use your personal wallet. Under a session-scoped wallet it must equal the selected wallet, or the call fails with WALLET_SCOPE_MISMATCH.",
            "type": "string"
          },
          "walletFamily": {
            "description": "Wallet FAMILY, never a chain: eip155 or solana (default: eip155).",
            "type": "string"
          }
        },
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "khalani",
      "publicName": "khalani__token_balances_get",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Read Khalani-chain wallet balances",
      "toolId": "khalani.tokens.balances"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 964,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "chainIds": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Comma-separated chain IDs or aliases. Accepts either a comma-separated string (\"1,8453\") or an array of strings ([\"1\",\"8453\"]) - the two are equivalent."
          },
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "keyword": {
            "description": "Autocomplete keyword.",
            "minLength": 1,
            "type": "string"
          },
          "limit": {
            "description": "Max results. Range 1-20 (provider maximum, default 10); anything else is rejected before the call.",
            "type": "number"
          }
        },
        "required": [
          "keyword"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "khalani",
      "publicName": "khalani__tokens_autocomplete",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Parse a token phrase into Khalani tokens",
      "toolId": "khalani.tokens.autocomplete"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1060,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "chainIds": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Comma-separated chain IDs or aliases. Accepts either a comma-separated string (\"1,8453\") or an array of strings ([\"1\",\"8453\"]) - the two are equivalent."
          },
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "query": {
            "description": "Search phrase or token address.",
            "minLength": 1,
            "type": "string"
          }
        },
        "required": [
          "query"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "khalani",
      "publicName": "khalani__tokens_search",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Search Khalani tokens",
      "toolId": "khalani.tokens.search"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 794,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "chainIds": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Comma-separated chain IDs or aliases (e.g. '1,solana'). Accepts either a comma-separated string (\"1,8453\") or an array of strings ([\"1\",\"8453\"]) - the two are equivalent."
          },
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          }
        },
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "khalani",
      "publicName": "khalani__tokens_top_list",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "List top bridged tokens on Khalani",
      "toolId": "khalani.tokens.top"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 776,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "liveStatus": {
            "description": "Join KyberSwap Common Service's live per-chain state onto the registry rows (default false, which makes no network call). A chain the live list does not carry, and every chain when the service is unavailable, comes back with state null and a stateReason naming which of the two happened - the call still returns the registry rows.",
            "type": "boolean"
          }
        },
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "kyberswap",
      "publicName": "kyberswap__chains_list",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "List KyberSwap chains",
      "toolId": "kyberswap.chains"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 3718,
      "destructiveHint": true,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "amountIn": {
            "description": "Amount in human-readable units.",
            "minLength": 1,
            "type": "string"
          },
          "chain": {
            "anyOf": [
              {
                "minLength": 1,
                "type": "string"
              },
              {
                "minimum": 1,
                "type": "integer"
              }
            ],
            "description": "Chain slug or alias."
          },
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "slippageBps": {
            "description": "Slippage tolerance in basis points (1 bps = 0.01%); default 100 = 1%, which fits deep, liquid pairs. Must match the kyberswap__swap_quote value exactly (or be omitted on both) - a mismatch blocks execution. It is how far BELOW THE QUOTED OUTPUT the fill may land: the execute does not re-price the trade, it builds from the exact route the quote returned, so the floor written into the calldata is the quoted amountOut minus this tolerance and nothing about the market between quote and execute can move it. That is what makes the number meaningful, and it is also why a stale quote is refused rather than silently re-priced. On a thin or volatile pair (new listings, memecoins, small pools) 100 bps often fails, in one of two ways. USUALLY it fails for FREE: the pre-sign gas estimate is refused before anything is signed, NO gas is spent, this call returns status \"not_attempted\" with retryable true and failureCode slippage, and the message names the price guard. LESS OFTEN the pool moves after that estimate passes: the transaction broadcasts, then REVERTS, this call returns status \"reverted\", the activity row records mined_revert, and the gas IS spent. Read either as \"the price moved further than this tolerance allows\", not as \"this pair is untradeable\": re-quote and pass the same higher slippageBps here. Vex caps it at 1000 (10%) and REJECTS anything above rather than clamping. Raise it in steps, starting tight since the usual failure costs nothing - no tolerance is known to fit a given pair in advance, and every increase widens the worst-case price you accept.",
            "minimum": 0,
            "type": "integer"
          },
          "tokenIn": {
            "description": "Input token CONTRACT ADDRESS (resolve a symbol with TokenFind first) or native ETH/native. Symbols are rejected here.",
            "minLength": 1,
            "type": "string"
          },
          "tokenOut": {
            "description": "Output token CONTRACT ADDRESS (resolve a symbol with TokenFind first) or native ETH/native. Symbols are rejected here.",
            "minLength": 1,
            "type": "string"
          }
        },
        "required": [
          "chain",
          "tokenIn",
          "tokenOut",
          "amountIn"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "kyberswap",
      "publicName": "kyberswap__swap_execute",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Execute a KyberSwap swap",
      "toolId": "kyberswap.swap.execute"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 3591,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "amountIn": {
            "description": "Amount in human-readable units.",
            "minLength": 1,
            "type": "string"
          },
          "chain": {
            "anyOf": [
              {
                "minLength": 1,
                "type": "string"
              },
              {
                "minimum": 1,
                "type": "integer"
              }
            ],
            "description": "Chain slug or alias."
          },
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "slippageBps": {
            "description": "Slippage tolerance in basis points (1 bps = 0.01%); default 100 = 1%, which fits deep, liquid pairs. Pass the SAME value on kyberswap__swap_execute, or omit it on both - a mismatch blocks the execute (the prequote match requires identical params). Not sent to the quote route; it pins how far below THIS quote's amountOut the eventual fill may land. The execute builds from the exact route this call returns rather than re-pricing the trade, so the approvedMinOut this call returns is the floor the calldata will carry, and the market moving between this quote and the execute cannot raise or lower it. On a thin or volatile pair (new listings, memecoins, small pools) 100 bps often fails at execute time, in one of two ways. USUALLY it fails for FREE: the pre-sign gas estimate is refused before anything is signed, NO gas is spent, and the execute returns status \"not_attempted\" with failureCode slippage. LESS OFTEN the pool moves after that estimate passes: the transaction REVERTS, the activity row records mined_revert, and the gas IS spent. After either, re-quote with a higher slippageBps rather than abandoning the pair - and since the usual failure costs nothing, a tighter tolerance is the cheap thing to try first. Vex caps it at 1000 (10%) and REJECTS anything above rather than clamping; every increase widens the worst-case price you accept.",
            "minimum": 0,
            "type": "integer"
          },
          "tokenIn": {
            "description": "Input token CONTRACT ADDRESS (resolve a symbol with TokenFind first) or native ETH/native. Symbols are not resolved here.",
            "minLength": 1,
            "type": "string"
          },
          "tokenOut": {
            "description": "Output token CONTRACT ADDRESS (resolve a symbol with TokenFind first) or native ETH/native. Symbols are not resolved here.",
            "minLength": 1,
            "type": "string"
          }
        },
        "required": [
          "chain",
          "tokenIn",
          "tokenOut",
          "amountIn"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "kyberswap",
      "publicName": "kyberswap__swap_quote",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Quote a KyberSwap swap",
      "toolId": "kyberswap.swap.quote"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1138,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "chain": {
            "anyOf": [
              {
                "minLength": 1,
                "type": "string"
              },
              {
                "minimum": 1,
                "type": "integer"
              }
            ],
            "description": "Chain slug or alias."
          },
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "tokenAddress": {
            "description": "Token contract address to inspect (0x… on the named chain). Resolve it with TokenFind first - a symbol is not accepted. The former key `address` is retired and is rejected by name.",
            "minLength": 1,
            "type": "string"
          }
        },
        "required": [
          "chain",
          "tokenAddress"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "kyberswap",
      "publicName": "kyberswap__token_safety_check",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Audit an EVM token with KyberSwap",
      "toolId": "kyberswap.tokens.check"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 2892,
      "destructiveHint": true,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "borrowAmountRaw": {
            "description": "How much to borrow, in the LOAN token's RAW base units as a whole-number string. THE SCALE IS THE LOAN TOKEN'S OWN, which is usually NOT the collateral token's: read `loanAsset.decimals` from `morpho__market_get`. A human decimal amount is refused, not rounded. Another operation's amount key is refused by name. Quote first to see where this leaves the health factor before committing to a size.",
            "minLength": 1,
            "type": "string"
          },
          "chain": {
            "anyOf": [
              {
                "minLength": 1,
                "type": "string"
              },
              {
                "minimum": 1,
                "type": "integer"
              }
            ],
            "description": "The chain the market lives on. Required: a market id is chain-scoped. Chain slug/alias, or the numeric chain id `TokenFind` returns (e.g. `base` or `8453`)."
          },
          "dryRun": {
            "description": "Set true to get the FULL preview and sign nothing: the vouching verdict, the health factor before and after, the free liquidity, the allowance plan, the decoded transaction, the gas bound and the simulation verdict.",
            "type": "boolean"
          },
          "marketId": {
            "description": "The market's 0x-prefixed 64-hex id, from `morpho__markets_discover` or `morpho__positions_get`. A 40-hex value is a CONTRACT ADDRESS and is rejected by name. Chain-scoped.",
            "minLength": 1,
            "type": "string"
          },
          "slippageBps": {
            "description": "Price protection in basis points (1 bps = 0.01%). Default 100, capped at 1000, and a higher value is REJECTED rather than clamped. It bounds a full-debt repayment's per-share price of the TWO operations that move shares: a market supply and a repayment. On each, Vex derives a `maxSharePrice` ceiling from it that the CHAIN enforces, so a worse price cannot mine; on a full-debt (shares-mode) repayment it also caps the approval. The other four - supply_collateral, withdraw, withdraw_collateral and borrow - are direct calls naming their own exact token amount, carry NO on-chain price guard, and are unaffected by this value.",
            "minimum": 0,
            "type": "integer"
          }
        },
        "required": [
          "marketId",
          "chain",
          "borrowAmountRaw"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "morpho",
      "publicName": "morpho__market_borrow",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Borrow from a Morpho market",
      "toolId": "morpho.market.borrow"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 2042,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "chain": {
            "anyOf": [
              {
                "minLength": 1,
                "type": "string"
              },
              {
                "minimum": 1,
                "type": "integer"
              }
            ],
            "description": "The chain the market lives on. Chain slug/alias, or the numeric chain id `TokenFind` returns (e.g. `base` or `8453`). Required because a Morpho market id is chain-scoped, so the same id on the wrong chain resolves to nothing. Discovery ships the supported slugs on this tool's `chains` metadata, and an unsupported chain is rejected with the full set spelled out."
          },
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "includeHistory": {
            "description": "Add the averaged APY window selected by `lookback` (default false). Use it to judge whether today's rate is high or low for this market - a rate is never high or low on its own.",
            "type": "boolean"
          },
          "includeSupplyingVaults": {
            "description": "Also list the curated Morpho vaults supplying this market, with each vault's NET APY (default false). Covers BOTH generations and tags every row with its `version`; a curator commonly runs a v1 and a v2 vault under the same name at different addresses, so identify a route by address. A vault APY is net of the vault fee and is a different basis from this market's supply APY.",
            "type": "boolean"
          },
          "lookback": {
            "description": "Which averaging window `includeHistory` returns, one of: one_day, seven_days (default), thirty_days, ninety_days, one_year, inception. Ignored unless includeHistory is true. Anything else is rejected by name.",
            "enum": [
              "one_day",
              "seven_days",
              "thirty_days",
              "ninety_days",
              "one_year",
              "inception"
            ],
            "type": "string"
          },
          "marketId": {
            "description": "The Morpho Blue market id: a 0x-prefixed 64-hex hash, NOT a 40-hex contract address. Read one from `morpho__markets_discover`. An address is rejected by name, because Morpho answers an address-shaped id with an empty result rather than an error.",
            "minLength": 1,
            "type": "string"
          }
        },
        "required": [
          "marketId",
          "chain"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "morpho",
      "publicName": "morpho__market_get",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Read one Morpho Blue market",
      "toolId": "morpho.market.get"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 2425,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "borrowAmountRaw": {
            "description": "Required when `direction` is `borrow`. RAW base units of the LOAN token, at that token's own decimals (`loanAsset.decimals` from `morpho__market_get`).",
            "type": "string"
          },
          "chain": {
            "anyOf": [
              {
                "minLength": 1,
                "type": "string"
              },
              {
                "minimum": 1,
                "type": "integer"
              }
            ],
            "description": "The chain the market lives on. Required: a market id is chain-scoped. Chain slug/alias, or the numeric chain id `TokenFind` returns (e.g. `base` or `8453`)."
          },
          "direction": {
            "description": "Which operation to price. `supplyCollateral` moves the collateral token onto the market (raises the health factor), `borrow` takes the loan token out against it (lowers it, and is the only one that creates liquidation risk), `repay` pays debt down (raises it), `withdrawCollateral` takes collateral back off (lowers it whenever debt remains). `supply` and `withdraw` are the LENDER'S side and move the LOAN token: `supply` lends into the market to earn its borrow rate and `withdraw` takes that back out. Neither moves any health factor, and neither is `supplyCollateral`/`withdrawCollateral`, which move the other token. The quote authorizes only the execute matching this direction.",
            "enum": [
              "supplyCollateral",
              "withdrawCollateral",
              "borrow",
              "repay",
              "supply",
              "withdraw"
            ],
            "type": "string"
          },
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "marketId": {
            "description": "The market's 0x-prefixed 64-hex id, from `morpho__markets_discover` or `morpho__positions_get`. A 40-hex value is a CONTRACT ADDRESS and is rejected by name. Chain-scoped.",
            "minLength": 1,
            "type": "string"
          },
          "repayAmountRaw": {
            "description": "Used when `direction` is `repay` and the repayment is PARTIAL. RAW base units of the LOAN token, at that token's own decimals: read `loanAsset.decimals` from `morpho__market_get`, not from TokenFind, because the market names its own loan asset. Omit it and send `repayFullDebt: true` to price closing the debt completely; sending both is refused.",
            "type": "string"
          },
          "repayFullDebt": {
            "description": "Set true with `direction: repay` to price closing the debt COMPLETELY. It reads the position's own borrow shares from the chain, which is the only denomination that reaches zero: an amount always leaves accruing dust behind. Only meaningful for a repayment.",
            "type": "boolean"
          },
          "slippageBps": {
            "description": "Price protection in basis points (1 bps = 0.01%). Default 100, capped at 1000, and a higher value is REJECTED rather than clamped. It bounds a full-debt repayment's per-share price of the TWO operations that move shares: a market supply and a repayment. On each, Vex derives a `maxSharePrice` ceiling from it that the CHAIN enforces, so a worse price cannot mine; on a full-debt (shares-mode) repayment it also caps the approval. The other four - supply_collateral, withdraw, withdraw_collateral and borrow - are direct calls naming their own exact token amount, carry NO on-chain price guard, and are unaffected by this value.",
            "minimum": 0,
            "type": "integer"
          },
          "supplyAmountRaw": {
            "description": "Required when `direction` is `supply`. RAW base units of the LOAN token being LENT into the market, at that token's own decimals (`loanAsset.decimals` from `morpho__market_get`). This is the lender's side; `supplyCollateralAmountRaw` is a different operation on a different token and is refused by name.",
            "type": "string"
          },
          "supplyCollateralAmountRaw": {
            "description": "Required when `direction` is `supplyCollateral`. RAW base units of the COLLATERAL token, whose decimals are usually NOT the loan token's: read `collateralAsset.decimals` from `morpho__market_get`. Another direction's amount key is refused by name rather than dropped.",
            "type": "string"
          },
          "walletAddress": {
            "description": "Whose position to price this against. A health factor has no meaning without one, so when this is omitted the quote uses the session's selected EVM wallet, and when there is none it uses a stand-in with NO position and says so. Read-only: it never selects a signer, and the executes refuse this parameter outright.",
            "type": "string"
          },
          "withdrawAmountRaw": {
            "description": "Required when `direction` is `withdraw`. RAW base units of the LOAN token being taken back out of the market, at that token's own decimals. Bounded by the wallet's own supplied position AND by the market's free liquidity, each refused by name rather than clamped.",
            "type": "string"
          },
          "withdrawCollateralAmountRaw": {
            "description": "Required when `direction` is `withdrawCollateral`. RAW base units of the COLLATERAL token, at that token's own decimals.",
            "type": "string"
          }
        },
        "required": [
          "direction",
          "marketId",
          "chain"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "morpho",
      "publicName": "morpho__market_quote",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Quote a Morpho Blue market operation",
      "toolId": "morpho.market.quote"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 3455,
      "destructiveHint": true,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "chain": {
            "anyOf": [
              {
                "minLength": 1,
                "type": "string"
              },
              {
                "minimum": 1,
                "type": "integer"
              }
            ],
            "description": "The chain the market lives on. Required: a market id is chain-scoped. Chain slug/alias, or the numeric chain id `TokenFind` returns (e.g. `base` or `8453`)."
          },
          "dryRun": {
            "description": "Set true to get the FULL preview and sign nothing: the vouching verdict, the health factor before and after, the free liquidity, the allowance plan, the decoded transaction, the gas bound and the simulation verdict.",
            "type": "boolean"
          },
          "marketId": {
            "description": "The market's 0x-prefixed 64-hex id, from `morpho__markets_discover` or `morpho__positions_get`. A 40-hex value is a CONTRACT ADDRESS and is rejected by name. Chain-scoped.",
            "minLength": 1,
            "type": "string"
          },
          "repayAmountRaw": {
            "description": "How much debt to repay, in the LOAN token's RAW base units as a whole-number string. Read `loanAsset.decimals` from `morpho__market_get` for the scale. Required UNLESS `repayFullDebt` is true, and supplying BOTH is refused because they disagree about how much debt to clear. A human decimal amount is refused, not rounded. An amount at or above the current debt is refused by name: use `repayFullDebt` instead, because an assets repayment cannot reach zero.",
            "type": "string"
          },
          "repayFullDebt": {
            "description": "Set true to close the debt COMPLETELY. It routes the repayment through borrow SHARES, reading the position's own share count from the chain and burning exactly that, which is the only denomination that lands at zero. Do not combine it with `repayAmountRaw`. The wallet must hold enough of the loan token to cover the ceiling those shares can cost; the excess is swept back in the same transaction.",
            "type": "boolean"
          },
          "slippageBps": {
            "description": "Price protection in basis points (1 bps = 0.01%). Default 100, capped at 1000, and a higher value is REJECTED rather than clamped. It bounds a full-debt repayment's per-share price of the TWO operations that move shares: a market supply and a repayment. On each, Vex derives a `maxSharePrice` ceiling from it that the CHAIN enforces, so a worse price cannot mine; on a full-debt (shares-mode) repayment it also caps the approval. The other four - supply_collateral, withdraw, withdraw_collateral and borrow - are direct calls naming their own exact token amount, carry NO on-chain price guard, and are unaffected by this value.",
            "minimum": 0,
            "type": "integer"
          }
        },
        "required": [
          "marketId",
          "chain"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "morpho",
      "publicName": "morpho__market_repay",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Repay debt on a Morpho market",
      "toolId": "morpho.market.repay"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 3959,
      "destructiveHint": true,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "chain": {
            "anyOf": [
              {
                "minLength": 1,
                "type": "string"
              },
              {
                "minimum": 1,
                "type": "integer"
              }
            ],
            "description": "The chain the market lives on. Required: a market id is chain-scoped. Chain slug/alias, or the numeric chain id `TokenFind` returns (e.g. `base` or `8453`)."
          },
          "dryRun": {
            "description": "Set true to get the FULL preview and sign nothing: the vouching verdict, the health factor before and after, the free liquidity, the allowance plan, the decoded transaction, the gas bound and the simulation verdict.",
            "type": "boolean"
          },
          "marketId": {
            "description": "The market's 0x-prefixed 64-hex id, from `morpho__markets_discover` or `morpho__positions_get`. A 40-hex value is a CONTRACT ADDRESS and is rejected by name. Chain-scoped.",
            "minLength": 1,
            "type": "string"
          },
          "slippageBps": {
            "description": "Price protection in basis points (1 bps = 0.01%). Default 100, capped at 1000, and a higher value is REJECTED rather than clamped. It bounds a full-debt repayment's per-share price of the TWO operations that move shares: a market supply and a repayment. On each, Vex derives a `maxSharePrice` ceiling from it that the CHAIN enforces, so a worse price cannot mine; on a full-debt (shares-mode) repayment it also caps the approval. The other four - supply_collateral, withdraw, withdraw_collateral and borrow - are direct calls naming their own exact token amount, carry NO on-chain price guard, and are unaffected by this value.",
            "minimum": 0,
            "type": "integer"
          },
          "supplyAmountRaw": {
            "description": "How much of the LOAN asset to lend, in the LOAN token's RAW base units as a whole-number string. THE SCALE IS THE LOAN TOKEN'S OWN, which is usually NOT the collateral token's: read `loanAsset.decimals` from `morpho__market_get`. A human decimal amount is refused, not rounded. `supplyCollateralAmountRaw` is a DIFFERENT operation on a DIFFERENT token and is refused by name rather than accepted here.",
            "minLength": 1,
            "type": "string"
          }
        },
        "required": [
          "marketId",
          "chain",
          "supplyAmountRaw"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "morpho",
      "publicName": "morpho__market_supply",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Supply the loan asset to a Morpho market",
      "toolId": "morpho.market.supply"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 2862,
      "destructiveHint": true,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "chain": {
            "anyOf": [
              {
                "minLength": 1,
                "type": "string"
              },
              {
                "minimum": 1,
                "type": "integer"
              }
            ],
            "description": "The chain the market lives on. Required: a market id is chain-scoped. Chain slug/alias, or the numeric chain id `TokenFind` returns (e.g. `base` or `8453`)."
          },
          "dryRun": {
            "description": "Set true to get the FULL preview and sign nothing: the vouching verdict, the health factor before and after, the free liquidity, the allowance plan, the decoded transaction, the gas bound and the simulation verdict.",
            "type": "boolean"
          },
          "marketId": {
            "description": "The market's 0x-prefixed 64-hex id, from `morpho__markets_discover` or `morpho__positions_get`. A 40-hex value is a CONTRACT ADDRESS and is rejected by name. Chain-scoped.",
            "minLength": 1,
            "type": "string"
          },
          "slippageBps": {
            "description": "Price protection in basis points (1 bps = 0.01%). Default 100, capped at 1000, and a higher value is REJECTED rather than clamped. It bounds a full-debt repayment's per-share price of the TWO operations that move shares: a market supply and a repayment. On each, Vex derives a `maxSharePrice` ceiling from it that the CHAIN enforces, so a worse price cannot mine; on a full-debt (shares-mode) repayment it also caps the approval. The other four - supply_collateral, withdraw, withdraw_collateral and borrow - are direct calls naming their own exact token amount, carry NO on-chain price guard, and are unaffected by this value.",
            "minimum": 0,
            "type": "integer"
          },
          "supplyCollateralAmountRaw": {
            "description": "How much COLLATERAL to supply, in the COLLATERAL token's RAW base units as a whole-number string. THE SCALE IS THE COLLATERAL TOKEN'S OWN, which is usually NOT the loan token's: read `collateralAsset.decimals` from `morpho__market_get`. A market pairing 8-decimal cbBTC against 6-decimal USDC will silently accept a loan-scaled number as a hundredfold wrong amount, so read the scale rather than assuming it. A human decimal amount is refused, not rounded. Another operation's amount key is refused by name.",
            "minLength": 1,
            "type": "string"
          }
        },
        "required": [
          "marketId",
          "chain",
          "supplyCollateralAmountRaw"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "morpho",
      "publicName": "morpho__market_supply_collateral",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Supply collateral to a Morpho market",
      "toolId": "morpho.market.supplyCollateral"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 3270,
      "destructiveHint": true,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "chain": {
            "anyOf": [
              {
                "minLength": 1,
                "type": "string"
              },
              {
                "minimum": 1,
                "type": "integer"
              }
            ],
            "description": "The chain the market lives on. Required: a market id is chain-scoped. Chain slug/alias, or the numeric chain id `TokenFind` returns (e.g. `base` or `8453`)."
          },
          "dryRun": {
            "description": "Set true to get the FULL preview and sign nothing: the vouching verdict, the health factor before and after, the free liquidity, the allowance plan, the decoded transaction, the gas bound and the simulation verdict.",
            "type": "boolean"
          },
          "marketId": {
            "description": "The market's 0x-prefixed 64-hex id, from `morpho__markets_discover` or `morpho__positions_get`. A 40-hex value is a CONTRACT ADDRESS and is rejected by name. Chain-scoped.",
            "minLength": 1,
            "type": "string"
          },
          "slippageBps": {
            "description": "Price protection in basis points (1 bps = 0.01%). Default 100, capped at 1000, and a higher value is REJECTED rather than clamped. It bounds a full-debt repayment's per-share price of the TWO operations that move shares: a market supply and a repayment. On each, Vex derives a `maxSharePrice` ceiling from it that the CHAIN enforces, so a worse price cannot mine; on a full-debt (shares-mode) repayment it also caps the approval. The other four - supply_collateral, withdraw, withdraw_collateral and borrow - are direct calls naming their own exact token amount, carry NO on-chain price guard, and are unaffected by this value.",
            "minimum": 0,
            "type": "integer"
          },
          "withdrawAmountRaw": {
            "description": "How much of the LOAN asset to withdraw, in the LOAN token's RAW base units as a whole-number string. THE SCALE IS THE LOAN TOKEN'S OWN: read `loanAsset.decimals` from `morpho__market_get`. A human decimal amount is refused, not rounded. `withdrawCollateralAmountRaw` is a DIFFERENT operation on a DIFFERENT token and is refused by name. An amount above the supplied position, or above the market's free liquidity, is refused by name rather than reduced to what would fit.",
            "minLength": 1,
            "type": "string"
          }
        },
        "required": [
          "marketId",
          "chain",
          "withdrawAmountRaw"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "morpho",
      "publicName": "morpho__market_withdraw",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Withdraw a supply from a Morpho market",
      "toolId": "morpho.market.withdraw"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 2894,
      "destructiveHint": true,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "chain": {
            "anyOf": [
              {
                "minLength": 1,
                "type": "string"
              },
              {
                "minimum": 1,
                "type": "integer"
              }
            ],
            "description": "The chain the market lives on. Required: a market id is chain-scoped. Chain slug/alias, or the numeric chain id `TokenFind` returns (e.g. `base` or `8453`)."
          },
          "dryRun": {
            "description": "Set true to get the FULL preview and sign nothing: the vouching verdict, the health factor before and after, the free liquidity, the allowance plan, the decoded transaction, the gas bound and the simulation verdict.",
            "type": "boolean"
          },
          "marketId": {
            "description": "The market's 0x-prefixed 64-hex id, from `morpho__markets_discover` or `morpho__positions_get`. A 40-hex value is a CONTRACT ADDRESS and is rejected by name. Chain-scoped.",
            "minLength": 1,
            "type": "string"
          },
          "slippageBps": {
            "description": "Price protection in basis points (1 bps = 0.01%). Default 100, capped at 1000, and a higher value is REJECTED rather than clamped. It bounds a full-debt repayment's per-share price of the TWO operations that move shares: a market supply and a repayment. On each, Vex derives a `maxSharePrice` ceiling from it that the CHAIN enforces, so a worse price cannot mine; on a full-debt (shares-mode) repayment it also caps the approval. The other four - supply_collateral, withdraw, withdraw_collateral and borrow - are direct calls naming their own exact token amount, carry NO on-chain price guard, and are unaffected by this value.",
            "minimum": 0,
            "type": "integer"
          },
          "withdrawCollateralAmountRaw": {
            "description": "How much COLLATERAL to withdraw, in the COLLATERAL token's RAW base units as a whole-number string. THE SCALE IS THE COLLATERAL TOKEN'S OWN, not the loan token's: read `collateralAsset.decimals` from `morpho__market_get`. A human decimal amount is refused, not rounded. Another operation's amount key is refused by name. Quote first: on a position with debt this lowers the health factor.",
            "minLength": 1,
            "type": "string"
          }
        },
        "required": [
          "marketId",
          "chain",
          "withdrawCollateralAmountRaw"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "morpho",
      "publicName": "morpho__market_withdraw_collateral",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Withdraw collateral from a Morpho market",
      "toolId": "morpho.market.withdrawCollateral"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 3263,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "chainIds": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Comma-separated chains, an array of the same, or 'all'. Slugs or numeric chain ids; discovery ships the supported slugs on this tool's `chains` metadata and an unsupported entry is rejected with the full set."
          },
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "limit": {
            "description": "Max transactions (default 25, maximum 100). A larger value is REJECTED by name rather than clamped; `hasMore` and `nextOffset` report what is left.",
            "type": "number"
          },
          "liquidatorAddress": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Comma list or array of LIQUIDATOR addresses, up to 20. This is the other side of `walletAddress`, which matches the borrower who was liquidated: use this one to profile who is doing the liquidating on a market. It only ever matches liquidation rows, so pair it with `types: 'liquidation'` for a clean page."
          },
          "marketIds": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Morpho Blue market ids as a comma list OR an array, up to 20. A market id is a 0x-prefixed 64-hex hash, NOT a contract address, and it is chain-scoped; read one from morpho__markets_discover. An address here is rejected by name and told what it actually is."
          },
          "minBadDebtAssetsRaw": {
            "description": "Keep only liquidations that left at least this much BAD DEBT, in the LOAN asset's RAW base units as a quoted string (\"1000000\" is 1 USDC at 6 decimals). Bad debt is loss socialised across everyone supplying the market, so this is the direct way to ask 'has this market ever lost lenders money'. Scope it to one market or one asset, because a raw floor means different money in different markets. A human decimal amount, or an unquoted number, is rejected by name rather than rounded.",
            "type": "string"
          },
          "minSeizedAssetsRaw": {
            "description": "Keep only liquidations that seized at least this much COLLATERAL, in the COLLATERAL asset's RAW base units as a quoted string. Note the different asset from `minBadDebtAssetsRaw`: on one live row the two legs sat at 18 and 6 decimals, so the same integer means wildly different money on each. Same by-name refusal of a human decimal.",
            "type": "string"
          },
          "offset": {
            "description": "Row offset for paging (default 0). Pair it with the `nextOffset` the reply returns.",
            "type": "number"
          },
          "order": {
            "description": "Ranking direction, one of: desc (default, newest or largest first), asc. Anything else is rejected by name.",
            "enum": [
              "asc",
              "desc"
            ],
            "type": "string"
          },
          "since": {
            "description": "Only events at or after this unix timestamp, in SECONDS (10 digits today), not milliseconds. A milliseconds value is rejected by name: it would select a window thousands of years out and return an empty history that reads as a dead market.",
            "type": "number"
          },
          "sort": {
            "description": "Ranking key, one of: timestamp, assets, repaidAssets, seizedAssets, badDebtAssets. Default timestamp. The last three rank liquidations by size, the fastest way to the worst ones; they order non-liquidation rows arbitrarily, so pair them with types 'liquidation'. Applied server-side over ALL matches.",
            "enum": [
              "timestamp",
              "assets",
              "repaidAssets",
              "seizedAssets",
              "badDebtAssets"
            ],
            "type": "string"
          },
          "txHash": {
            "description": "Look up ONE transaction by its 0x-prefixed 64-hex hash. Use it when the user pastes a hash and asks what it did on Morpho; a single transaction can produce several rows, so expect more than one. Morpho declares no list form of this filter, so it takes exactly one hash.",
            "type": "string"
          },
          "types": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Event types as a comma list or an array: supply, withdraw, borrow, repay, supplyCollateral, withdrawCollateral, liquidation. Omit for every type. 'liquidation' alone is the risk lens; 'borrow' and 'repay' show whether anyone actually uses the market. A value outside this set is rejected by name."
          },
          "until": {
            "description": "Only events at or before this unix timestamp, in SECONDS, not milliseconds. Earlier than `since` is rejected by name, because nothing could match.",
            "type": "number"
          },
          "walletAddress": {
            "description": "Scope the history to ONE actor, 0x-prefixed and 40 hex. On liquidation rows this matches the BORROWER whose position was taken, not the liquidator - which is what makes it the way to see who liquidated an address and when.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "morpho",
      "publicName": "morpho__markets_activity_list",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Read Morpho market activity",
      "toolId": "morpho.markets.activity"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 2961,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "chainIds": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Comma-separated chains to scope to, an array of the same, or 'all'. Each entry is a chain slug or a numeric chain id; discovery ships the supported slugs on this tool's `chains` metadata, and an unsupported entry is rejected with the full set spelled out. Omit for every supported chain. A chain Morpho serves but Vex does not operate on is rejected by name, so a coverage gap is never reported as an absence of markets."
          },
          "collateralAssetTags": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Comma list or array of Morpho's own asset-class TAGS the COLLATERAL asset must carry - the way to ask what can be borrowed against a whole class of collateral rather than one named token. Same accepted set as `loanAssetTags`, and the same reason it is not enumerated here; an unknown tag is rejected by name with the full set."
          },
          "collateralTokenAddress": {
            "description": "Comma-separated contract addresses of the COLLATERAL asset - what a borrower locks up. Up to 20 entries. Use it to answer 'where can I borrow against X'. Addresses only.",
            "type": "string"
          },
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "fields": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Row field groups to keep: a comma list, an array of the same, or 'all' (default). Groups: identity, assets, apy, size, risk. Keeps a large result small; marketId, chain and chainId are always present. An unknown group is rejected by name."
          },
          "irmAddress": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Comma list or array of INTEREST RATE MODEL contract addresses to scope to. Markets sharing an IRM share the shape of how their rate responds to utilization. Read an address from any row's `irmAddress`."
          },
          "isIdle": {
            "description": "True keeps ONLY idle markets, false excludes them. An idle market has no collateral asset and cannot be borrowed from: it is where a vault parks uninvested cash, so it earns nothing and is never a lending recommendation. Omit to include both kinds.",
            "type": "boolean"
          },
          "limit": {
            "description": "Max markets to return (default 20, maximum 50). A larger value is REJECTED by name rather than clamped, so what you asked for is always what was applied; `hasMore` and `nextOffset` report what is left.",
            "type": "number"
          },
          "listedOnly": {
            "description": "Keep only markets Morpho itself curates (default true). Set false to include permissionless deployments - expect unusable oracles and enormous headline rates on near-empty markets, and check every row's warnings.",
            "type": "boolean"
          },
          "loanAssetTags": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Comma list or array of Morpho's own asset-class TAGS the BORROWABLE asset must carry. The accepted set is Morpho's, not ours, and it is NOT enumerated here on purpose: discovery indexes this prose, and repeating a list of asset-class words in a screening tool's parameter made it outrank other namespaces on queries that merely NAMED an asset. Send a tag; a wrong one is rejected by name with the whole accepted set spelled out, which is where that list belongs."
          },
          "loanTokenAddress": {
            "description": "Comma-separated contract addresses of the BORROWABLE asset - the token a lender supplies and a borrower receives. Up to 20 entries. Addresses only; a symbol is rejected by name.",
            "type": "string"
          },
          "marketIds": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Comma list or array of up to 20 known 64-hex market ids, to re-read a specific set in ONE call instead of one morpho__market_get per market. Use it to refresh markets you already hold or already shortlisted. A 40-hex contract address here is rejected by name as an address."
          },
          "maxBorrowUsd": {
            "description": "Maximum total borrowed value in USD. USD is the market oracle's estimate.",
            "type": "number"
          },
          "maxLltvPercent": {
            "description": "Maximum liquidation loan-to-value threshold as a PERCENT, 0-100. Cap it for a wider safety margin before liquidation.",
            "type": "number"
          },
          "maxNetBorrowApyPercent": {
            "description": "Maximum NET borrow APY as a PERCENT (8 means 8%), incentives INCLUDED. The filter for 'cheapest place to borrow'. Not comparable with the incentive-free `borrowApyPercent`.",
            "type": "number"
          },
          "maxSupplyUsd": {
            "description": "Maximum total supplied value in USD. USD is the market oracle's estimate, not a traded price.",
            "type": "number"
          },
          "maxUtilizationPercent": {
            "description": "Maximum borrowed/supplied as a PERCENT, 0-100. Cap it to keep markets you could actually exit; a market at 100% has no withdrawable liquidity at all.",
            "type": "number"
          },
          "minBorrowUsd": {
            "description": "Minimum total borrowed value in USD - real borrowing demand, as opposed to a market that merely holds deposits. USD is the market oracle's estimate.",
            "type": "number"
          },
          "minLltvPercent": {
            "description": "Minimum liquidation loan-to-value threshold as a PERCENT, 0-100 (86 means 86%). Higher lets a borrower take more debt per unit of collateral and liquidates sooner in a drawdown.",
            "type": "number"
          },
          "minNetSupplyApyPercent": {
            "description": "Minimum NET supply APY as a PERCENT (5 means 5%), incentives INCLUDED. Not comparable with the incentive-free `supplyApyPercent` in the result; pair it with minSupplyUsd or the top hits will be dust.",
            "type": "number"
          },
          "minSupplyUsd": {
            "description": "Minimum total supplied value in USD - the main way to screen out dust markets whose headline rate is meaningless. USD is the market oracle's estimate, not a traded price.",
            "type": "number"
          },
          "minUtilizationPercent": {
            "description": "Minimum borrowed/supplied as a PERCENT, 0-100 (80 means 80%), not a fraction. High utilization means a high supply rate but little free liquidity to withdraw.",
            "type": "number"
          },
          "offset": {
            "description": "Row offset for paging (default 0). Pair it with the `nextOffset` the reply returns.",
            "type": "number"
          },
          "oracleAddress": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Comma list or array of ORACLE contract addresses to scope to. Every market names the oracle that prices its collateral, so this is how you find the other markets exposed to one price feed - the blast radius when an oracle is flagged unusable or is known to be stale. Read an address from any row's `oracle.address`."
          },
          "order": {
            "description": "Ranking direction, one of: desc (default), asc. Anything else is rejected by name.",
            "enum": [
              "asc",
              "desc"
            ],
            "type": "string"
          },
          "query": {
            "description": "Free-text substring match on the market's asset symbols and name (for example 'usdc', 'wsteth'). Use it when the user names an asset informally instead of by contract address. It is a FILTER, not a ranker: it narrows the set and `sort` decides the order.",
            "type": "string"
          },
          "sort": {
            "description": "Ranking key, one of: supplyUsd, borrowUsd, sizeUsd, liquidityUsd, supplyApy, netSupplyApy, borrowApy, netBorrowApy, avgBorrowApy, avgNetBorrowApy, dailyBorrowApy, dailyNetBorrowApy, apyAtTarget, utilization, lltv, fee, loanAssetSymbol, collateralAssetSymbol (default supplyUsd). `supplyApy` and `borrowApy` rank by the incentive-FREE rate while `netSupplyApy` and `netBorrowApy` include incentives; `avg*` and `daily*` rank by a trailing average instead of the spot rate, which is the way to avoid ranking on a one-block spike. `sizeUsd` is Morpho's own combined-size measure, `fee` is the market's protocol fee share, and the two symbol keys sort alphabetically for a readable listing. Applied server-side over ALL matches, not just the returned page. Anything else is rejected by name.",
            "enum": [
              "supplyUsd",
              "borrowUsd",
              "sizeUsd",
              "liquidityUsd",
              "supplyApy",
              "netSupplyApy",
              "borrowApy",
              "netBorrowApy",
              "avgBorrowApy",
              "avgNetBorrowApy",
              "dailyBorrowApy",
              "dailyNetBorrowApy",
              "apyAtTarget",
              "utilization",
              "lltv",
              "fee",
              "loanAssetSymbol",
              "collateralAssetSymbol"
            ],
            "type": "string"
          }
        },
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "morpho",
      "publicName": "morpho__markets_discover",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Screen Morpho Blue lending markets",
      "toolId": "morpho.markets.discover"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 4446,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "chainIds": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Comma-separated chains, an array of the same, or 'all'. Slugs or numeric chain ids; discovery ships the supported slugs on this tool's `chains` metadata and an unsupported entry is rejected with the full set. Omit for every chain, which is the right default when the user asks what they hold."
          },
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "includeVaultV2": {
            "description": "Sweep for VaultV2 positions as well as V1 (default true). Morpho publishes no per-user V2 list, so this costs one read to find candidate vaults from the wallet's V2 transaction history plus one per vault, and its coverage is reported in `vaultV2Coverage`. Set false to skip it; the reply then claims nothing about V2.",
            "type": "boolean"
          },
          "limit": {
            "description": "Max positions per section (default 20, maximum 50). A larger value is REJECTED by name rather than clamped. Market and vault positions page independently, each with its own `hasMore`. In the vault section it bounds the V1 page only: the V2 sweep adds its rows on top, so `returned` there can exceed it and `v1Returned` and `v2Returned` say which part came from where.",
            "type": "number"
          },
          "maxHealthFactor": {
            "description": "Keep only lending positions at or below this health factor, as a plain RATIO and not a percent: 1 is already liquidatable, 1.25 is close. The risk screen. It also pages server-side over all matches, so it has no merged-window bound, and it excludes supply-only positions entirely, because those have no health factor at all rather than a high one. Combining it with `scope: vaults` is rejected by name.",
            "type": "number"
          },
          "offset": {
            "description": "Row offset for paging (default 0). Pair it with the reply's `nextOffset`. Without `maxHealthFactor`, offset + limit must stay within 50 rows: the market half is a merge of three reads and that merge is only provably complete inside one window.",
            "type": "number"
          },
          "scope": {
            "description": "Which half to read, one of: all (default), markets, vaults. Lending positions can be liquidated; curated vault deposits cannot. Anything else is rejected by name.",
            "enum": [
              "markets",
              "vaults",
              "all"
            ],
            "type": "string"
          },
          "walletAddress": {
            "description": "The ONE account address whose Morpho positions to read, 0x-prefixed and 40 hex. A list or a second comma-separated address is rejected by name; call the tool once per wallet.",
            "minLength": 1,
            "type": "string"
          }
        },
        "required": [
          "walletAddress"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "morpho",
      "publicName": "morpho__positions_get",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Read a wallet's Morpho positions",
      "toolId": "morpho.positions.get"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 2695,
      "destructiveHint": true,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "chain": {
            "anyOf": [
              {
                "minLength": 1,
                "type": "string"
              },
              {
                "minimum": 1,
                "type": "integer"
              }
            ],
            "description": "The chain whose rewards to claim. Chain slug/alias, or the numeric chain id `TokenFind` returns (e.g. `base` or `8453`). Required and singular: a claim is one transaction on one chain, and rewards on another chain need their own call. There is no wallet parameter - the claim is always for the session's own wallet, which is also the only wallet the distributor would pay."
          },
          "dryRun": {
            "description": "Set true to get the FULL preview and sign nothing: the built and decoded transaction, the allowance plan including any approval still needed, the gas bound and the simulation verdict. It is a preview of this exact operation, not a promise, and nothing is broadcast, approved or recorded as executed.",
            "type": "boolean"
          },
          "morphoOnly": {
            "description": "Claim only the reward token rows carrying a resolved Morpho campaign (default false). This narrows ROWS, not the amounts inside them: a selected row still delivers every campaign it contains. Leaving it false claims everything the wallet can claim on that chain in one transaction, which is almost always what the user wants and always the cheaper way to get it.",
            "type": "boolean"
          }
        },
        "required": [
          "chain"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "morpho",
      "publicName": "morpho__rewards_claim",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Claim earned Morpho rewards",
      "toolId": "morpho.rewards.claim"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 3189,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "chainIds": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Comma-separated chains, an array of the same, or 'all'. Slugs or numeric chain ids; discovery ships the supported slugs on this tool's `chains` metadata and an unsupported entry is rejected with the full set. This is a FAN-OUT and not a filter: the distributor answers one chain per request, so at most 4 chains are read per call and a longer list is rejected by name rather than trimmed. Omit it only when the user genuinely does not know where they earned."
          },
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "morphoOnly": {
            "description": "Keep only reward rows carrying a resolved Morpho campaign (default false). Leave it false when reporting what a claim would deliver, because one claim takes the whole token row whatever produced it. Set it true only when the question is specifically about Morpho's own incentives.",
            "type": "boolean"
          },
          "walletAddress": {
            "description": "The ONE account address whose rewards to read, 0x-prefixed and 40 hex. A second address is rejected by name; call the tool once per wallet.",
            "minLength": 1,
            "type": "string"
          }
        },
        "required": [
          "walletAddress"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "morpho",
      "publicName": "morpho__rewards_get",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Read claimable Morpho rewards",
      "toolId": "morpho.rewards.get"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 3288,
      "destructiveHint": true,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "chain": {
            "anyOf": [
              {
                "minLength": 1,
                "type": "string"
              },
              {
                "minimum": 1,
                "type": "integer"
              }
            ],
            "description": "The chain the vault lives on. Chain slug/alias, or the numeric chain id `TokenFind` returns (e.g. `base` or `8453`). Required because a vault address is chain-scoped: the same address on the wrong chain is a different contract entirely."
          },
          "depositAmountRaw": {
            "description": "How much of the vault's ASSET to deposit, in that asset's RAW base units as a whole-number string. THE SCALE IS THE VAULT ASSET'S OWN: read `asset.decimals` from `morpho__vault_get` for this vault, not the vault's SHARE decimals, which are a different number reported beside it. A human amount is refused, not rounded. It must equal the amount the quote priced.",
            "minLength": 1,
            "type": "string"
          },
          "dryRun": {
            "description": "Set true to get the FULL preview and sign nothing: the built and decoded transaction, the allowance plan including any approval still needed, the gas bound and the simulation verdict. It is a preview of this exact operation, not a promise, and nothing is broadcast, approved or recorded as executed.",
            "type": "boolean"
          },
          "slippageBps": {
            "description": "Price protection in basis points (1 bps = 0.01%). Default 100 = 1%. Vex caps it at 1000 (10%) and REJECTS a higher value rather than clamping it.",
            "minimum": 0,
            "type": "integer"
          },
          "vaultAddress": {
            "description": "The vault's 0x-prefixed 40-hex contract address, from `morpho__vaults_discover` or `morpho__vault_get`. A 64-hex value is rejected by name because that is a MARKET id, not a vault. Both vault generations are detected automatically.",
            "minLength": 1,
            "type": "string"
          }
        },
        "required": [
          "vaultAddress",
          "chain",
          "depositAmountRaw"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "morpho",
      "publicName": "morpho__vault_deposit",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Deposit into a Morpho vault",
      "toolId": "morpho.vault.deposit"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 3338,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "chain": {
            "anyOf": [
              {
                "minLength": 1,
                "type": "string"
              },
              {
                "minimum": 1,
                "type": "integer"
              }
            ],
            "description": "The chain the vault lives on. Chain slug/alias, or the numeric chain id `TokenFind` returns (e.g. `base` or `8453`). Required because a vault address is chain-scoped, so the same address on the wrong chain resolves to nothing. Discovery ships the supported slugs on this tool's `chains` metadata, and an unsupported chain is rejected with the full set spelled out."
          },
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "includeAllocations": {
            "description": "Include the per-market allocation table with caps and each market's own APY (default TRUE). It is on by default because a vault's real risk is the risk of the markets underneath it, not of the vault contract. Set false only when you already know the allocations and want a smaller reply.",
            "type": "boolean"
          },
          "includeHistory": {
            "description": "Add Morpho's trailing average APY beside the live rate (default false). Morpho returns a single average and does NOT name the window it covers, so it is labelled 'recent average' rather than a fixed period; there is no `lookback` here because the vault schema exposes no named averaging windows the way markets do.",
            "type": "boolean"
          },
          "vaultAddress": {
            "description": "The vault's 0x-prefixed 40-hex contract address. Read one from `morpho__vaults_discover`. A 64-hex value is rejected by name because that is a MARKET id and belongs to `morpho__market_get`. You do not need to know whether the vault is V1 or V2; both are tried.",
            "minLength": 1,
            "type": "string"
          }
        },
        "required": [
          "vaultAddress",
          "chain"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "morpho",
      "publicName": "morpho__vault_get",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Read one Morpho vault",
      "toolId": "morpho.vault.get"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 4758,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "chain": {
            "anyOf": [
              {
                "minLength": 1,
                "type": "string"
              },
              {
                "minimum": 1,
                "type": "integer"
              }
            ],
            "description": "The chain the vault lives on. Chain slug/alias, or the numeric chain id `TokenFind` returns (e.g. `base` or `8453`). Required because a vault address is chain-scoped: the same address on the wrong chain is a different contract entirely."
          },
          "depositAmountRaw": {
            "description": "How much of the vault's ASSET to price going IN, in that asset's RAW base units as a whole-number string. Send it with `direction: deposit`, never alongside `withdrawAmountRaw`. THE SCALE IS THE VAULT ASSET'S OWN: read `asset.decimals` from `morpho__vault_get` for this vault, not the vault's SHARE decimals, which are a different number reported beside it. A human amount is refused, not rounded.",
            "type": "string"
          },
          "direction": {
            "description": "Which way the money would move: `deposit` puts the vault's asset in and mints shares, `withdraw` takes the asset out and burns shares. It must AGREE with the amount parameter you send; a call whose direction and amount key disagree is rejected by name rather than resolved either way.",
            "enum": [
              "deposit",
              "withdraw"
            ],
            "type": "string"
          },
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "slippageBps": {
            "description": "Price protection in basis points (1 bps = 0.01%). Default 100 = 1%. Vex caps this at 1000 (10%) and REJECTS a higher value rather than clamping it. On a DEPOSIT it raises the maximum share price the transaction accepts on chain. A WITHDRAWAL has no share-price leg, so the value binds on nothing there and the reply says so.",
            "minimum": 0,
            "type": "integer"
          },
          "vaultAddress": {
            "description": "The vault's 0x-prefixed 40-hex contract address, from `morpho__vaults_discover` or `morpho__vault_get`. A 64-hex value is rejected by name because that is a MARKET id, not a vault. Both generations are detected automatically.",
            "minLength": 1,
            "type": "string"
          },
          "walletAddress": {
            "description": "The 0x-prefixed 40-hex account the preview is for. Supply it to have `requirements` reflect that wallet's CURRENT allowance and permit state. Nothing is signed with it and no key is needed. Omit it entirely rather than inventing a placeholder of your own.",
            "type": "string"
          },
          "withdrawAmountRaw": {
            "description": "How much of the vault's ASSET to price coming OUT, in that asset's RAW base units as a whole-number string. Send it with `direction: withdraw`, never alongside `depositAmountRaw`. THE SCALE IS THE VAULT ASSET'S OWN: read `asset.decimals` from `morpho__vault_get` for this vault, not the SHARE decimals and not the share count you want to burn, which is a different quantity in a different unit.",
            "type": "string"
          }
        },
        "required": [
          "vaultAddress",
          "chain",
          "direction"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "morpho",
      "publicName": "morpho__vault_quote",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Quote a Morpho vault deposit or withdrawal",
      "toolId": "morpho.vault.quote"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 2844,
      "destructiveHint": true,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "chain": {
            "anyOf": [
              {
                "minLength": 1,
                "type": "string"
              },
              {
                "minimum": 1,
                "type": "integer"
              }
            ],
            "description": "The chain the vault lives on. Chain slug/alias, or the numeric chain id `TokenFind` returns (e.g. `base` or `8453`). Required because a vault address is chain-scoped: the same address on the wrong chain is a different contract entirely."
          },
          "dryRun": {
            "description": "Set true to get the FULL preview and sign nothing: the built and decoded transaction, the allowance plan including any approval still needed, the gas bound and the simulation verdict. It is a preview of this exact operation, not a promise, and nothing is broadcast, approved or recorded as executed.",
            "type": "boolean"
          },
          "slippageBps": {
            "description": "Price protection in basis points (1 bps = 0.01%). Default 100 = 1%. Vex caps it at 1000 (10%) and REJECTS a higher value rather than clamping it.",
            "minimum": 0,
            "type": "integer"
          },
          "vaultAddress": {
            "description": "The vault's 0x-prefixed 40-hex contract address, from `morpho__vaults_discover` or `morpho__vault_get`. A 64-hex value is rejected by name because that is a MARKET id, not a vault. Both vault generations are detected automatically.",
            "minLength": 1,
            "type": "string"
          },
          "withdrawAmountRaw": {
            "description": "How much of the vault's ASSET to take out, in that asset's RAW base units as a whole-number string. THE SCALE IS THE VAULT ASSET'S OWN: read `asset.decimals` from `morpho__vault_get` for this vault, not the SHARE decimals and not the share count you want to burn, which is a different quantity in a different unit. A human amount is refused, not rounded. It must equal the amount the quote priced.",
            "minLength": 1,
            "type": "string"
          }
        },
        "required": [
          "vaultAddress",
          "chain",
          "withdrawAmountRaw"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "morpho",
      "publicName": "morpho__vault_withdraw",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Withdraw from a Morpho vault",
      "toolId": "morpho.vault.withdraw"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 6470,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "assetSymbol": {
            "description": "Comma-separated symbols of the vault's asset (for example 'USDC,USDT'). V1-ONLY: Morpho's V2 filter input has no symbol predicate, so this is rejected by name unless `version` is 'v1'. Prefer `assetTokenAddress`, which both generations serve and which cannot match the wrong token.",
            "type": "string"
          },
          "assetTags": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Comma list or array of Morpho's own asset-class TAGS the vault's asset must carry, to screen a whole class of vault rather than one named token. The accepted set is not enumerated here, for the reason recorded on `morpho__markets_discover`'s own `loanAssetTags`; an unknown tag is rejected by name with the full set spelled out, rather than sent as a predicate that matches nothing. V1-ONLY: Morpho's V2 filter input declares no tag predicate, so this is rejected by name unless `version` is 'v1'."
          },
          "assetTokenAddress": {
            "description": "Comma-separated contract addresses of the asset the vault holds and pays out in - the token a depositor puts in. Up to 20 entries. Works on BOTH generations, which is why it is the right way to ask for 'USDC vaults'. Addresses only; a symbol is rejected by name.",
            "type": "string"
          },
          "chainIds": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Comma-separated chains to scope to, an array of the same, or 'all'. Each entry is a chain slug or a numeric chain id; discovery ships the supported slugs on this tool's `chains` metadata, and an unsupported entry is rejected with the full set spelled out. Omit for every supported chain."
          },
          "curatorAddress": {
            "description": "Comma-separated addresses of the curator, the party that decides where the vault's money goes. Up to 20 entries. Use it to find every vault run by a manager the user already trusts.",
            "type": "string"
          },
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "fields": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Row field groups to keep: a comma list, an array of the same, or 'all' (default). Groups: identity, asset, apy, size, fees, governance, gating. Keeps a large result small; address, version and chain are always present, the gated flags survive dropping the gating group, and an unknown group is rejected by name."
          },
          "limit": {
            "description": "Max vaults to return (default 20, maximum 50). A larger value is REJECTED by name rather than clamped, so what you asked for is always what was applied; `hasMore` and `nextOffset` report what is left.",
            "type": "number"
          },
          "listedOnly": {
            "description": "Keep only vaults Morpho itself curates (default true). Set false to include uncurated deployments - expect deploy tests holding a few dollars, and check every row's warnings, TVL and curator.",
            "type": "boolean"
          },
          "maxCuratorCutPercent": {
            "description": "Maximum curator fee as a PERCENT, 0-100 (20 means 20%). On V1 this bounds the vault's single fee; on V2 it bounds the PERFORMANCE fee only, because V2's management fee is a separate field Morpho does not offer a combined filter for - the management fee is still reported on every row.",
            "type": "number"
          },
          "maxTvlUsd": {
            "description": "Maximum total deposits in USD. USD is Morpho's oracle estimate, not a traded price.",
            "type": "number"
          },
          "minNetApyPercent": {
            "description": "Minimum NET APY as a PERCENT (5 means 5%), meaning after the curator's fee and with incentives included. Not comparable with a market's `supplyApyPercent`, which is gross of any vault fee. Pair it with minTvlUsd or the top hits will be near-empty vaults.",
            "type": "number"
          },
          "minTvlUsd": {
            "description": "Minimum total deposits in USD - the main way to screen out empty and test vaults, whose headline rates are meaningless. USD is Morpho's oracle estimate, not a traded price.",
            "type": "number"
          },
          "offset": {
            "description": "Row offset for paging (default 0). Pair it with the `nextOffset` the reply returns. At `version: both`, offset + limit must stay within 50 rows so the merged ranking stays provably exact.",
            "type": "number"
          },
          "order": {
            "description": "Ranking direction, one of: desc (default), asc. Anything else is rejected by name.",
            "enum": [
              "asc",
              "desc"
            ],
            "type": "string"
          },
          "query": {
            "description": "Free-text substring match on the vault's name and symbol (for example 'steakhouse', 'usdc'). It is a FILTER, not a ranker: it narrows the set and `sort` decides the order. V1-ONLY: Morpho's V2 filter input has no free-text predicate, so this is rejected by name unless `version` is 'v1'.",
            "type": "string"
          },
          "route": {
            "description": "Which option SETS to return, one of: curated (default), direct, both. `curated` returns the vault rows only, unchanged from before this key existed. `direct` returns only the Blue markets that lend the same asset, each with its supply APY, utilization, liquidity, collateral asset, LLTV and warnings, and queries no vault. `both` returns both sets AND a merged `options` list ranked on net APY, where every entry carries its net APY after fees, the fee, its diversification, its gating status, the tool to act on it with, and its distance in PERCENTAGE POINTS from the best direct option. `direct` and `both` require `assetTokenAddress` to name exactly one asset. Anything else is rejected by name.",
            "enum": [
              "curated",
              "direct",
              "both"
            ],
            "type": "string"
          },
          "sort": {
            "description": "Ranking key, one of: tvlUsd, netApy, apy, avgApy, avgNetApy, dailyApy, dailyNetApy, name, curator, fee, liquidityUsd, idleAssetsUsd, realAssetsUsd (default tvlUsd). Only tvlUsd, apy and netApy are served by BOTH generations. `name`, `curator`, `fee` and the four trailing-average keys (avgApy, avgNetApy, dailyApy, dailyNetApy) are V1-ONLY; `liquidityUsd`, `idleAssetsUsd` and `realAssetsUsd` are V2-ONLY. A key the selected generation cannot serve is rejected by name, naming the `version` that can, rather than being swapped for a key that exists. Applied server-side over ALL matches within each generation.",
            "enum": [
              "tvlUsd",
              "netApy",
              "apy",
              "avgApy",
              "avgNetApy",
              "dailyApy",
              "dailyNetApy",
              "name",
              "curator",
              "fee",
              "liquidityUsd",
              "idleAssetsUsd",
              "realAssetsUsd"
            ],
            "type": "string"
          },
          "suppliesMarketIds": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Comma list or array of up to 20 64-hex MARKET ids; keeps only vaults that supply at least one listed market. This is the 'which vaults are exposed to this market' question - use it to find the curated routes into a market you already picked, or to size who else is exposed when a market looks unsafe. V1-ONLY: Morpho's V2 filter input has no equivalent, so it is rejected by name unless `version` is 'v1'."
          },
          "version": {
            "description": "Which vault generation to read, one of: both (default), v1, v2. V1 is MetaMorpho with a single global timelock and no gating; V2 adds per-function timelocks, a management fee and transfer gates. At 'both' the two are queried separately and merged into one exact ranking, which bounds paging to a single window. Anything else is rejected by name.",
            "enum": [
              "v1",
              "v2",
              "both"
            ],
            "type": "string"
          }
        },
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "morpho",
      "publicName": "morpho__vaults_discover",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Screen Morpho vaults",
      "toolId": "morpho.vaults.discover"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 3149,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "chain": {
            "anyOf": [
              {
                "minLength": 1,
                "type": "string"
              },
              {
                "minimum": 1,
                "type": "integer"
              }
            ],
            "description": "The chain to read on. Chain slug/alias, or the numeric chain id `TokenFind` returns (e.g. `base` or `8453`). Required and single, because a balance and an approval exist per chain: the same token symbol on two chains is two different contracts with two different allowances, so a guessed chain answers confidently about the wrong one."
          },
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "tokenAddress": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            ],
            "description": "Comma-separated ERC-20 CONTRACT addresses, or an array of the same. Symbols are rejected: several tokens on one chain answer to the same symbol, and an allowance read against the wrong contract is a false safety signal. At most 12 per call, rejected by name rather than trimmed, because each token costs a balance read plus one read per Morpho spender. The native sentinel address is accepted and folded into the native balance. Omit it entirely for the native balance alone."
          },
          "walletAddress": {
            "description": "The ONE account address to read, 0x-prefixed and 40 hex. Any address works; no key is used and nothing is unlocked.",
            "minLength": 1,
            "type": "string"
          }
        },
        "required": [
          "walletAddress",
          "chain"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "morpho",
      "publicName": "morpho__wallet_balance_get",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Read on-chain balances for Morpho tokens",
      "toolId": "morpho.wallet.balance"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1249,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "chain": {
            "anyOf": [
              {
                "minLength": 1,
                "type": "string"
              },
              {
                "minimum": 1,
                "type": "integer"
              }
            ],
            "description": "Chain slug or numeric id (e.g. 'ethereum', 'arbitrum', 42161). One call reads ONE chain; an unsupported chain is rejected by name."
          },
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "ids": {
            "description": "Comma-separated asset ids to price - bare 0x addresses or 'chainId-0xaddress' composites, at most 50 per call. An id belonging to a different chain than `chain` is rejected by name. Omit to page through the chain's assets instead.",
            "type": "string"
          },
          "limit": {
            "description": "Rows per page when no ids are given: whole number 1-100 (default 50). The response reports total, hasMore and nextOffset.",
            "type": "number"
          },
          "offset": {
            "description": "Row offset for paging, whole number ≥ 0 (default 0). Pair it with the returned nextOffset.",
            "type": "number"
          },
          "type": {
            "description": "Optional asset-class filter: PENDLE_LP, SY, PT, YT (comma-separated for several). Anything else is rejected by name. Most useful when paging without `ids`.",
            "type": "string"
          }
        },
        "required": [
          "chain"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "pendle",
      "publicName": "pendle__asset_prices_get",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Read Pendle asset prices",
      "toolId": "pendle.prices.assets"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1755,
      "destructiveHint": true,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "amountIn": {
            "description": "Amount of the payment token in human-readable units.",
            "minLength": 1,
            "type": "string"
          },
          "chain": {
            "anyOf": [
              {
                "minLength": 1,
                "type": "string"
              },
              {
                "minimum": 1,
                "type": "integer"
              }
            ],
            "description": "Chain slug or id - one of Pendle's 11 chains (e.g. 'ethereum', 'arbitrum', 'base', 'bsc')."
          },
          "dryRun": {
            "description": "Preview without executing.",
            "type": "boolean"
          },
          "market": {
            "description": "The Pendle MARKET (LP) CONTRACT ADDRESS - the LP anchor liquidity is added to or removed from.",
            "minLength": 1,
            "type": "string"
          },
          "slippageBps": {
            "description": "Slippage tolerance in basis points (default 100 = 1%).",
            "minimum": 0,
            "type": "integer"
          },
          "tokenIn": {
            "description": "The payment token CONTRACT ADDRESS to deposit (ERC-20; use WETH for ETH).",
            "minLength": 1,
            "type": "string"
          }
        },
        "required": [
          "chain",
          "market",
          "tokenIn",
          "amountIn"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "pendle",
      "publicName": "pendle__lp_add",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Add single-token Pendle liquidity",
      "toolId": "pendle.lp.add"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1271,
      "destructiveHint": true,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "amountIn": {
            "description": "Amount of the payment token to deposit, in human-readable units.",
            "minLength": 1,
            "type": "string"
          },
          "chain": {
            "anyOf": [
              {
                "minLength": 1,
                "type": "string"
              },
              {
                "minimum": 1,
                "type": "integer"
              }
            ],
            "description": "Chain slug or id - one of Pendle's 11 chains (e.g. 'ethereum', 'arbitrum', 'base', 'bsc')."
          },
          "dryRun": {
            "description": "true = quote only. Prices both output legs, runs every fund-safety check including a separate price floor per leg, records the authorization this tool needs, and broadcasts NOTHING. Required before the real call.",
            "type": "boolean"
          },
          "market": {
            "description": "The Pendle MARKET (LP) CONTRACT ADDRESS - the market IS its LP token. Find it with pendle__markets_discover or read it from pendle__market_get.",
            "minLength": 1,
            "type": "string"
          },
          "slippageBps": {
            "description": "Slippage tolerance in whole basis points (default 100 = 1%; maximum 1000 = 10%). A fractional, negative or larger value is REJECTED, never clamped. It bounds BOTH output legs. The dry run and the execute must pass the SAME value (or omit it on both).",
            "minimum": 0,
            "type": "integer"
          },
          "tokenIn": {
            "description": "The payment token CONTRACT ADDRESS to deposit (ERC-20; pass the chain's wrapped-native token for native exposure).",
            "minLength": 1,
            "type": "string"
          }
        },
        "required": [
          "chain",
          "market",
          "tokenIn",
          "amountIn"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "pendle",
      "publicName": "pendle__lp_add_keep_yt",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Add Pendle liquidity and keep the YT",
      "toolId": "pendle.lp.addKeepYt"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 971,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "amountIn": {
            "description": "Human-readable amount - add: the payment token amount; remove: the LP token amount to burn.",
            "minLength": 1,
            "type": "string"
          },
          "chain": {
            "anyOf": [
              {
                "minLength": 1,
                "type": "string"
              },
              {
                "minimum": 1,
                "type": "integer"
              }
            ],
            "description": "Chain slug or id - one of Pendle's 11 chains (e.g. 'ethereum', 'arbitrum', 'base', 'bsc')."
          },
          "direction": {
            "description": "'add' (token → LP) or 'remove' (LP → token).",
            "minLength": 1,
            "type": "string"
          },
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "market": {
            "description": "The Pendle MARKET (LP) CONTRACT ADDRESS - the LP anchor liquidity is added to or removed from.",
            "minLength": 1,
            "type": "string"
          },
          "slippageBps": {
            "description": "Slippage tolerance in basis points (default 100 = 1%).",
            "minimum": 0,
            "type": "integer"
          },
          "tokenIn": {
            "description": "ADD only: the payment token CONTRACT ADDRESS to deposit (ERC-20; use WETH for ETH).",
            "type": "string"
          },
          "tokenOut": {
            "description": "REMOVE only: the output token CONTRACT ADDRESS. Defaults to the market's underlying asset.",
            "type": "string"
          }
        },
        "required": [
          "chain",
          "direction",
          "market",
          "amountIn"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "pendle",
      "publicName": "pendle__lp_quote",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Quote a Pendle liquidity add or remove",
      "toolId": "pendle.lp.quote"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1747,
      "destructiveHint": true,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "amountIn": {
            "description": "Amount of the LP token to remove in human-readable units.",
            "minLength": 1,
            "type": "string"
          },
          "chain": {
            "anyOf": [
              {
                "minLength": 1,
                "type": "string"
              },
              {
                "minimum": 1,
                "type": "integer"
              }
            ],
            "description": "Chain slug or id - one of Pendle's 11 chains (e.g. 'ethereum', 'arbitrum', 'base', 'bsc')."
          },
          "dryRun": {
            "description": "Preview without executing.",
            "type": "boolean"
          },
          "market": {
            "description": "The Pendle MARKET (LP) CONTRACT ADDRESS - the LP anchor liquidity is added to or removed from.",
            "minLength": 1,
            "type": "string"
          },
          "slippageBps": {
            "description": "Slippage tolerance in basis points (default 100 = 1%).",
            "minimum": 0,
            "type": "integer"
          },
          "tokenOut": {
            "description": "The output token CONTRACT ADDRESS. Defaults to the market's underlying asset.",
            "type": "string"
          }
        },
        "required": [
          "chain",
          "market",
          "amountIn"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "pendle",
      "publicName": "pendle__lp_remove",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Remove Pendle liquidity to one token",
      "toolId": "pendle.lp.remove"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1304,
      "destructiveHint": true,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "amountIn": {
            "description": "Amount of the LP (market) token to burn, in human-readable units.",
            "minLength": 1,
            "type": "string"
          },
          "chain": {
            "anyOf": [
              {
                "minLength": 1,
                "type": "string"
              },
              {
                "minimum": 1,
                "type": "integer"
              }
            ],
            "description": "Chain slug or id - one of Pendle's 11 chains (e.g. 'ethereum', 'arbitrum', 'base', 'bsc')."
          },
          "dryRun": {
            "description": "true = quote only. Prices both output legs, runs every fund-safety check including a separate price floor per leg, records the authorization this tool needs, and broadcasts NOTHING. Required before the real call.",
            "type": "boolean"
          },
          "market": {
            "description": "The Pendle MARKET (LP) CONTRACT ADDRESS - the market IS its LP token. Find it with pendle__markets_discover or read it from pendle__market_get.",
            "minLength": 1,
            "type": "string"
          },
          "slippageBps": {
            "description": "Slippage tolerance in whole basis points (default 100 = 1%; maximum 1000 = 10%). A fractional, negative or larger value is REJECTED, never clamped. It bounds BOTH output legs. The dry run and the execute must pass the SAME value (or omit it on both).",
            "minimum": 0,
            "type": "integer"
          },
          "tokenOut": {
            "description": "The TOKEN output leg's CONTRACT ADDRESS (ERC-20; pass wrapped native, never native). Defaults to the market's underlying asset. The other leg is always the market's own PT and is not a parameter.",
            "type": "string"
          }
        },
        "required": [
          "chain",
          "market",
          "amountIn"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "pendle",
      "publicName": "pendle__lp_remove_dual",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Remove Pendle liquidity to two tokens",
      "toolId": "pendle.lp.removeDual"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1927,
      "destructiveHint": true,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "amountIn": {
            "description": "Amount of the SOURCE token in human-readable units (e.g. '1.5', not raw base units).",
            "minLength": 1,
            "type": "string"
          },
          "chain": {
            "anyOf": [
              {
                "minLength": 1,
                "type": "string"
              },
              {
                "minimum": 1,
                "type": "integer"
              }
            ],
            "description": "Chain slug or id - one of Pendle's 11 chains (e.g. 'ethereum', 'arbitrum', 'base', 'bsc'). Both legs are on ONE chain; none of these tools bridges."
          },
          "dryRun": {
            "description": "true = quote only. Prices the move, runs every fund-safety check including the per-leg price floor, records the authorization this tool needs, and broadcasts NOTHING. Required before the real call.",
            "type": "boolean"
          },
          "market": {
            "description": "The Pendle MARKET ADDRESS whose LP you hold and whose PT you want (the market IS the LP token).",
            "minLength": 1,
            "type": "string"
          },
          "pt": {
            "description": "OPTIONAL check: the PT contract address you expect to receive. Must be this market's own PT - a PT from any other market is refused by name. Omit it and Vex uses the market's PT.",
            "type": "string"
          },
          "slippageBps": {
            "description": "Slippage tolerance in whole basis points (default 100 = 1%; maximum 1000 = 10%). A fractional, negative or larger value is REJECTED, never clamped. The dry run and the execute must pass the SAME value (or omit it on both).",
            "minimum": 0,
            "type": "integer"
          }
        },
        "required": [
          "chain",
          "market",
          "amountIn"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "pendle",
      "publicName": "pendle__lp_to_pt",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Convert Pendle liquidity into PT",
      "toolId": "pendle.lp.toPt"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1314,
      "destructiveHint": true,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "amountIn": {
            "description": "Amount of the SOURCE token in human-readable units (e.g. '1.5', not raw base units).",
            "minLength": 1,
            "type": "string"
          },
          "chain": {
            "anyOf": [
              {
                "minLength": 1,
                "type": "string"
              },
              {
                "minimum": 1,
                "type": "integer"
              }
            ],
            "description": "Chain slug or id - one of Pendle's 11 chains (e.g. 'ethereum', 'arbitrum', 'base', 'bsc'). Both legs are on ONE chain; none of these tools bridges."
          },
          "dryRun": {
            "description": "true = quote only. Prices the move, runs every fund-safety check including the per-leg price floor, records the authorization this tool needs, and broadcasts NOTHING. Required before the real call.",
            "type": "boolean"
          },
          "fromMarket": {
            "description": "The Pendle MARKET ADDRESS whose LP you hold (the market IS the LP token). May be MATURED.",
            "minLength": 1,
            "type": "string"
          },
          "slippageBps": {
            "description": "Slippage tolerance in whole basis points (default 100 = 1%; maximum 1000 = 10%). A fractional, negative or larger value is REJECTED, never clamped. The dry run and the execute must pass the SAME value (or omit it on both).",
            "minimum": 0,
            "type": "integer"
          },
          "toMarket": {
            "description": "The Pendle MARKET ADDRESS to move the liquidity into. Must be ACTIVE - find one with pendle__markets_discover.",
            "minLength": 1,
            "type": "string"
          }
        },
        "required": [
          "chain",
          "fromMarket",
          "toMarket",
          "amountIn"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "pendle",
      "publicName": "pendle__lp_transfer",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Move Pendle liquidity between markets",
      "toolId": "pendle.lp.transfer"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1079,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "asset": {
            "description": "PT, YT or LP CONTRACT ADDRESS, 0x-prefixed 40-hex. This endpoint covers those three only - it does not price SY or ordinary tokens. pendle__market_get lists a market's legs.",
            "minLength": 1,
            "type": "string"
          },
          "chain": {
            "anyOf": [
              {
                "minLength": 1,
                "type": "string"
              },
              {
                "minimum": 1,
                "type": "integer"
              }
            ],
            "description": "Chain slug or numeric id (e.g. 'ethereum', 'base', 8453). An unsupported chain is rejected by name."
          },
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "from": {
            "description": "Window start as an ISO-8601 instant (e.g. '2026-07-20T00:00:00Z'). Omit to let Pendle choose the window. Combined with timeFrame it must cover at most 1440 candles.",
            "type": "string"
          },
          "timeFrame": {
            "description": "Candle size: 'hour', 'day' (default) or 'week'. Anything else is rejected by name.",
            "type": "string"
          },
          "to": {
            "description": "Window end as an ISO-8601 instant. Must be later than `from`. Omit for 'up to now'.",
            "type": "string"
          }
        },
        "required": [
          "chain",
          "asset"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "pendle",
      "publicName": "pendle__market_candles_get",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Read Pendle asset price candles",
      "toolId": "pendle.market.candles"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1057,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "chain": {
            "anyOf": [
              {
                "minLength": 1,
                "type": "string"
              },
              {
                "minimum": 1,
                "type": "integer"
              }
            ],
            "description": "Chain slug or numeric id - one of Pendle's supported chains (e.g. 'ethereum', 'arbitrum', 'base', 'bsc', 1, 42161). An unsupported chain is rejected by name. The same address on another chain is a different asset."
          },
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "market": {
            "description": "Market (LP) CONTRACT ADDRESS, 0x-prefixed 40-hex. Pass exactly ONE of market, pt or yt - passing two is rejected by name rather than resolved.",
            "type": "string"
          },
          "pt": {
            "description": "Principal-token CONTRACT ADDRESS, 0x-prefixed 40-hex. Use this when you hold or are pricing a PT and do not know its market.",
            "type": "string"
          },
          "yt": {
            "description": "Yield-token CONTRACT ADDRESS, 0x-prefixed 40-hex. Use this when you hold or are pricing a YT and do not know its market.",
            "type": "string"
          }
        },
        "required": [
          "chain"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "pendle",
      "publicName": "pendle__market_get",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Read one Pendle market",
      "toolId": "pendle.market.get"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 735,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "chain": {
            "anyOf": [
              {
                "minLength": 1,
                "type": "string"
              },
              {
                "minimum": 1,
                "type": "integer"
              }
            ],
            "description": "Chain slug or numeric id (e.g. 'ethereum', 'arbitrum', 42161). An unsupported chain is rejected by name."
          },
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "fields": {
            "description": "Comma-separated series fields. Default: impliedApy,underlyingApy,tvl. Selectable: impliedApy, underlyingApy, maxApy, baseApy, tvl, ptPrice, ytPrice, lpPrice, tradingVolume, pendleApy, aggregatedApy, swapFeeApy, ytFloatingApy, totalPt, totalSy, totalSupply, totalActiveSupply. An unknown field is rejected by name - Pendle serves only these. Ask for fewer fields to spend fewer compute units.",
            "type": "string"
          },
          "from": {
            "description": "Window start as an ISO-8601 instant (e.g. '2026-07-20T00:00:00Z'). Omit to let Pendle choose the window. Combined with timeFrame it must cover at most 1440 points.",
            "type": "string"
          },
          "market": {
            "description": "Market (LP) CONTRACT ADDRESS, 0x-prefixed 40-hex. Not the PT or YT - resolve those to their market with pendle__market_get first.",
            "minLength": 1,
            "type": "string"
          },
          "timeFrame": {
            "description": "Resolution: 'hour', 'day' (default) or 'week'. Anything else is rejected by name.",
            "type": "string"
          },
          "to": {
            "description": "Window end as an ISO-8601 instant. Must be later than `from`. Omit for 'up to now'.",
            "type": "string"
          }
        },
        "required": [
          "chain",
          "market"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "pendle",
      "publicName": "pendle__market_history_get",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Read a Pendle market's history",
      "toolId": "pendle.market.history"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1732,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "chain": {
            "anyOf": [
              {
                "minLength": 1,
                "type": "string"
              },
              {
                "minimum": 1,
                "type": "integer"
              }
            ],
            "description": "Chain slug or numeric id (e.g. 'ethereum', 'arbitrum', 42161). An unsupported chain is rejected by name."
          },
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "limit": {
            "description": "Levels to show per side: whole number 1-50 (default 10); above that it is rejected by name. The response always reports how many levels exist, so nothing is dropped silently.",
            "type": "number"
          },
          "market": {
            "description": "Market (LP) CONTRACT ADDRESS, 0x-prefixed 40-hex. Not the PT or YT - pendle__market_get resolves either to its market.",
            "minLength": 1,
            "type": "string"
          },
          "precision": {
            "description": "Decimal places the implied-APY axis is rounded to: whole number 0-3 (default 2). Provider-required; anything outside the range is rejected by name. Coarser precision merges nearby levels into fewer, larger ones.",
            "type": "number"
          }
        },
        "required": [
          "chain",
          "market"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "pendle",
      "publicName": "pendle__market_orderbook_get",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Read Pendle limit-order depth",
      "toolId": "pendle.orderbook"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 2097,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "categories": {
            "description": "Comma-separated Pendle category ids to KEEP (e.g. 'stables,points,ethena').",
            "type": "string"
          },
          "chainIds": {
            "description": "Comma-separated chains to scope to (e.g. 'ethereum,arbitrum,base'), or 'all'. Each entry is a chain slug/alias or a numeric chain id. Omit for every Pendle chain. An unsupported chain is rejected by name.",
            "type": "string"
          },
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "excludeCategories": {
            "description": "Comma-separated Pendle category ids to EXCLUDE (e.g. 'points' to skip points-farming markets).",
            "type": "string"
          },
          "expiryAfter": {
            "description": "Only markets expiring after this ISO-8601 date.",
            "type": "string"
          },
          "expiryBefore": {
            "description": "Only markets expiring before this ISO-8601 date (e.g. '2027-01-01').",
            "type": "string"
          },
          "fields": {
            "description": "Comma-separated row field groups to keep: identity, apy, liquidity, expiry, legs, points, protocols - or 'all' (default). Use it to keep large result sets small.",
            "type": "string"
          },
          "includeMatured": {
            "description": "Include EXPIRED/matured markets (default false). Matured markets can only be redeemed or removed, never bought.",
            "type": "boolean"
          },
          "isNew": {
            "description": "Only markets Pendle flags as new (true), or only established ones (false).",
            "type": "boolean"
          },
          "isPrime": {
            "description": "Only markets Pendle flags as prime (true), or only non-prime (false).",
            "type": "boolean"
          },
          "limit": {
            "description": "Max markets to return (default 20). There is NO hidden ceiling - hasMore and nextOffset report what is left.",
            "type": "number"
          },
          "maxDaysToExpiry": {
            "description": "Maximum whole days until maturity - find the nearest maturities.",
            "type": "number"
          },
          "maxImpliedApyPercent": {
            "description": "Maximum implied APY as a PERCENT (25 = 25%). Useful to exclude implausible headline rates.",
            "type": "number"
          },
          "maxLiquidityUsd": {
            "description": "Maximum market liquidity in USD.",
            "type": "number"
          },
          "minDaysToExpiry": {
            "description": "Minimum whole days until maturity. Negative values match already-matured markets.",
            "type": "number"
          },
          "minImpliedApyPercent": {
            "description": "Minimum implied APY as a PERCENT (5 = 5%), not a fraction.",
            "type": "number"
          },
          "minLiquidityUsd": {
            "description": "Minimum market liquidity in USD - screen out thin markets you could not exit.",
            "type": "number"
          },
          "offset": {
            "description": "Row offset for paging (default 0). Pair with the returned nextOffset.",
            "type": "number"
          },
          "order": {
            "description": "'desc' (default) or 'asc'.",
            "type": "string"
          },
          "sort": {
            "description": "Ranking key: liquidity (default), impliedApy, aggregatedApy, underlyingApy, maxBoostedApy, tvl, volume, expiry, name. Anything else is rejected by name.",
            "type": "string"
          },
          "underlyingSymbol": {
            "description": "Substring match on the underlying asset symbol or market name (e.g. 'usde', 'weth', 'susd').",
            "type": "string"
          }
        },
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "pendle",
      "publicName": "pendle__markets_discover",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Screen Pendle yield markets",
      "toolId": "pendle.yields"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1664,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "chain": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "minimum": 1,
                "type": "integer"
              }
            ],
            "description": "Optional chain slug or numeric id (e.g. 'ethereum', 42161) to narrow the list to ONE chain. Omit it to see every chain's rewards in one call - there is no chainIds list here, and no 'all' literal to pass. An unsupported chain is rejected by name."
          },
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          }
        },
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "pendle",
      "publicName": "pendle__merkle_rewards_list",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "List claimable Pendle merkle rewards",
      "toolId": "pendle.rewards.merkle"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1675,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "chainIds": {
            "description": "Comma-separated chains to scope to, or 'all'. Each entry is a chain slug/alias or a numeric chain id. Omit for every chain the dashboard returns.",
            "type": "string"
          },
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "fields": {
            "description": "Comma-separated leg field groups to keep: identity, balance, value, expiry, accrued - or 'all' (default).",
            "type": "string"
          },
          "includeAccrued": {
            "description": "Include accrued unclaimed interest and rewards per leg (default true). Pendle caches these up to 24h.",
            "type": "boolean"
          },
          "kinds": {
            "description": "Comma-separated position kinds to keep: pt, yt, lp, sy. Omit for all four. Cross-chain PT legs are always reported separately.",
            "type": "string"
          },
          "minValueUsd": {
            "description": "Server-side minimum USD value per position; smaller ones are not returned at all. Use it to drop dust. Default: unfiltered.",
            "type": "number"
          },
          "redeemableOnly": {
            "description": "Only legs in state matured_redeemable - the matured PTs ready to redeem (default false).",
            "type": "boolean"
          },
          "sort": {
            "description": "'value' (default), 'expiry' or 'chain'. Anything else is rejected by name.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "pendle",
      "publicName": "pendle__positions_get",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Value this wallet's Pendle positions",
      "toolId": "pendle.position.value"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1677,
      "destructiveHint": true,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "amountIn": {
            "description": "Amount of tokenIn in human-readable units.",
            "minLength": 1,
            "type": "string"
          },
          "chain": {
            "anyOf": [
              {
                "minLength": 1,
                "type": "string"
              },
              {
                "minimum": 1,
                "type": "integer"
              }
            ],
            "description": "Chain slug or id - one of Pendle's 11 chains (e.g. 'ethereum', 'arbitrum', 'base', 'bsc')."
          },
          "dryRun": {
            "description": "Preview without executing.",
            "type": "boolean"
          },
          "slippageBps": {
            "description": "Slippage tolerance in basis points (default 100 = 1%).",
            "minimum": 0,
            "type": "integer"
          },
          "tokenIn": {
            "description": "Input token CONTRACT ADDRESS (ERC-20; use WETH for ETH). Buy: the payment token. Sell: the PT address.",
            "minLength": 1,
            "type": "string"
          },
          "tokenOut": {
            "description": "Output token CONTRACT ADDRESS. Buy: the PT address. Sell: the payment token.",
            "minLength": 1,
            "type": "string"
          }
        },
        "required": [
          "chain",
          "tokenIn",
          "tokenOut",
          "amountIn"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "pendle",
      "publicName": "pendle__pt_buy",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Buy a Pendle principal token",
      "toolId": "pendle.pt.buy"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1011,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "amountIn": {
            "description": "Amount of tokenIn in human-readable units.",
            "minLength": 1,
            "type": "string"
          },
          "chain": {
            "anyOf": [
              {
                "minLength": 1,
                "type": "string"
              },
              {
                "minimum": 1,
                "type": "integer"
              }
            ],
            "description": "Chain slug or id - one of Pendle's 11 chains (e.g. 'ethereum', 'arbitrum', 'base', 'bsc')."
          },
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "slippageBps": {
            "description": "Slippage tolerance in basis points (default 100 = 1%).",
            "minimum": 0,
            "type": "integer"
          },
          "tokenIn": {
            "description": "Input token address (payment token for a buy; PT address for a sell/redeem).",
            "minLength": 1,
            "type": "string"
          },
          "tokenOut": {
            "description": "Output token address (PT for a buy; payment/underlying for a sell/redeem).",
            "minLength": 1,
            "type": "string"
          }
        },
        "required": [
          "chain",
          "tokenIn",
          "tokenOut",
          "amountIn"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "pendle",
      "publicName": "pendle__pt_quote",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Quote a Pendle PT trade",
      "toolId": "pendle.pt.quote"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1969,
      "destructiveHint": true,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "amountIn": {
            "description": "Amount of PT to redeem in human-readable units.",
            "minLength": 1,
            "type": "string"
          },
          "chain": {
            "anyOf": [
              {
                "minLength": 1,
                "type": "string"
              },
              {
                "minimum": 1,
                "type": "integer"
              }
            ],
            "description": "Chain slug or id - one of Pendle's 11 chains (e.g. 'ethereum', 'arbitrum', 'base', 'bsc')."
          },
          "dryRun": {
            "description": "Preview without executing.",
            "type": "boolean"
          },
          "slippageBps": {
            "description": "Slippage tolerance in basis points (default 100 = 1%).",
            "minimum": 0,
            "type": "integer"
          },
          "tokenIn": {
            "description": "The matured PT CONTRACT ADDRESS to redeem.",
            "minLength": 1,
            "type": "string"
          }
        },
        "required": [
          "chain",
          "tokenIn",
          "amountIn"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "pendle",
      "publicName": "pendle__pt_redeem",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Redeem a matured Pendle principal token",
      "toolId": "pendle.pt.redeem"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1306,
      "destructiveHint": true,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "amountIn": {
            "description": "Amount of the SOURCE token in human-readable units (e.g. '1.5', not raw base units).",
            "minLength": 1,
            "type": "string"
          },
          "chain": {
            "anyOf": [
              {
                "minLength": 1,
                "type": "string"
              },
              {
                "minimum": 1,
                "type": "integer"
              }
            ],
            "description": "Chain slug or id - one of Pendle's 11 chains (e.g. 'ethereum', 'arbitrum', 'base', 'bsc'). Both legs are on ONE chain; none of these tools bridges."
          },
          "dryRun": {
            "description": "true = quote only. Prices the move, runs every fund-safety check including the per-leg price floor, records the authorization this tool needs, and broadcasts NOTHING. Required before the real call.",
            "type": "boolean"
          },
          "fromPt": {
            "description": "The PT CONTRACT ADDRESS you currently hold (the source maturity). May belong to a MATURED market. Read it from pendle__market_get as the market's `pt` field.",
            "minLength": 1,
            "type": "string"
          },
          "slippageBps": {
            "description": "Slippage tolerance in whole basis points (default 100 = 1%; maximum 1000 = 10%). A fractional, negative or larger value is REJECTED, never clamped. The dry run and the execute must pass the SAME value (or omit it on both).",
            "minimum": 0,
            "type": "integer"
          },
          "toPt": {
            "description": "The PT CONTRACT ADDRESS to roll INTO (the destination maturity). Must belong to an ACTIVE market - find one with pendle__markets_discover.",
            "minLength": 1,
            "type": "string"
          }
        },
        "required": [
          "chain",
          "fromPt",
          "toPt",
          "amountIn"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "pendle",
      "publicName": "pendle__pt_rollover",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Roll a Pendle PT into a later expiry",
      "toolId": "pendle.pt.rollover"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1729,
      "destructiveHint": true,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "amountIn": {
            "description": "Amount of tokenIn in human-readable units.",
            "minLength": 1,
            "type": "string"
          },
          "chain": {
            "anyOf": [
              {
                "minLength": 1,
                "type": "string"
              },
              {
                "minimum": 1,
                "type": "integer"
              }
            ],
            "description": "Chain slug or id - one of Pendle's 11 chains (e.g. 'ethereum', 'arbitrum', 'base', 'bsc')."
          },
          "dryRun": {
            "description": "Preview without executing.",
            "type": "boolean"
          },
          "slippageBps": {
            "description": "Slippage tolerance in basis points (default 100 = 1%).",
            "minimum": 0,
            "type": "integer"
          },
          "tokenIn": {
            "description": "Input token CONTRACT ADDRESS (ERC-20; use WETH for ETH). Buy: the payment token. Sell: the PT address.",
            "minLength": 1,
            "type": "string"
          },
          "tokenOut": {
            "description": "Output token CONTRACT ADDRESS. Buy: the PT address. Sell: the payment token.",
            "minLength": 1,
            "type": "string"
          }
        },
        "required": [
          "chain",
          "tokenIn",
          "tokenOut",
          "amountIn"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "pendle",
      "publicName": "pendle__pt_sell",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Sell a Pendle principal token",
      "toolId": "pendle.pt.sell"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1824,
      "destructiveHint": true,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "amountIn": {
            "description": "Amount of the payment token in human-readable units.",
            "minLength": 1,
            "type": "string"
          },
          "chain": {
            "anyOf": [
              {
                "minLength": 1,
                "type": "string"
              },
              {
                "minimum": 1,
                "type": "integer"
              }
            ],
            "description": "Chain slug or id - one of Pendle's 11 chains (e.g. 'ethereum', 'arbitrum', 'base', 'bsc')."
          },
          "dryRun": {
            "description": "Preview without executing.",
            "type": "boolean"
          },
          "pt": {
            "description": "The market's PT (principal token) CONTRACT ADDRESS - the anchor that resolves the market and its YT.",
            "minLength": 1,
            "type": "string"
          },
          "slippageBps": {
            "description": "Slippage tolerance in basis points (default 100 = 1%).",
            "minimum": 0,
            "type": "integer"
          },
          "tokenIn": {
            "description": "The payment token CONTRACT ADDRESS to spend (ERC-20; use WETH for ETH).",
            "minLength": 1,
            "type": "string"
          }
        },
        "required": [
          "chain",
          "pt",
          "tokenIn",
          "amountIn"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "pendle",
      "publicName": "pendle__py_mint",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Mint a Pendle PT and YT pair",
      "toolId": "pendle.py.mint"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 992,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "amountIn": {
            "description": "Human-readable amount - mint: the payment token amount; redeem: the PT+YT pair amount to burn.",
            "minLength": 1,
            "type": "string"
          },
          "chain": {
            "anyOf": [
              {
                "minLength": 1,
                "type": "string"
              },
              {
                "minimum": 1,
                "type": "integer"
              }
            ],
            "description": "Chain slug or id - one of Pendle's 11 chains (e.g. 'ethereum', 'arbitrum', 'base', 'bsc')."
          },
          "direction": {
            "description": "'mint' (token → PT+YT) or 'redeem' (pre-expiry PT+YT → token).",
            "minLength": 1,
            "type": "string"
          },
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "pt": {
            "description": "The market's PT (principal token) CONTRACT ADDRESS - the anchor that resolves the market and its YT.",
            "minLength": 1,
            "type": "string"
          },
          "slippageBps": {
            "description": "Slippage tolerance in basis points (default 100 = 1%).",
            "minimum": 0,
            "type": "integer"
          },
          "tokenIn": {
            "description": "MINT only: the payment token CONTRACT ADDRESS to spend (ERC-20; use WETH for ETH).",
            "type": "string"
          },
          "tokenOut": {
            "description": "REDEEM only: the output token CONTRACT ADDRESS. Defaults to the market's underlying asset.",
            "type": "string"
          }
        },
        "required": [
          "chain",
          "direction",
          "pt",
          "amountIn"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "pendle",
      "publicName": "pendle__py_quote",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Quote a Pendle PT/YT mint or redeem",
      "toolId": "pendle.py.quote"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1706,
      "destructiveHint": true,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "amountIn": {
            "description": "Amount of the PT+YT pair to burn in human-readable units (equal PT and YT).",
            "minLength": 1,
            "type": "string"
          },
          "chain": {
            "anyOf": [
              {
                "minLength": 1,
                "type": "string"
              },
              {
                "minimum": 1,
                "type": "integer"
              }
            ],
            "description": "Chain slug or id - one of Pendle's 11 chains (e.g. 'ethereum', 'arbitrum', 'base', 'bsc')."
          },
          "dryRun": {
            "description": "Preview without executing.",
            "type": "boolean"
          },
          "pt": {
            "description": "The market's PT (principal token) CONTRACT ADDRESS - the anchor that resolves the market and its YT.",
            "minLength": 1,
            "type": "string"
          },
          "slippageBps": {
            "description": "Slippage tolerance in basis points (default 100 = 1%).",
            "minimum": 0,
            "type": "integer"
          },
          "tokenOut": {
            "description": "The output token CONTRACT ADDRESS. Defaults to the market's underlying asset.",
            "type": "string"
          }
        },
        "required": [
          "chain",
          "pt",
          "amountIn"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "pendle",
      "publicName": "pendle__py_redeem",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Redeem a Pendle PT and YT pair early",
      "toolId": "pendle.py.redeem"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 2296,
      "destructiveHint": true,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "chain": {
            "anyOf": [
              {
                "minLength": 1,
                "type": "string"
              },
              {
                "minimum": 1,
                "type": "integer"
              }
            ],
            "description": "Chain slug or id - one of Pendle's 11 chains (e.g. 'ethereum', 'arbitrum', 'base', 'bsc')."
          },
          "dryRun": {
            "description": "Preview the positions that would be claimed - including eligibleMarkets and skippedMarkets - without executing.",
            "type": "boolean"
          },
          "market": {
            "description": "Optional MARKET CONTRACT ADDRESS to scope the claim to one market - the way to reach a market the unscoped sweep reported in skippedMarkets (no cap applies to a scoped claim). Omit to sweep up to 10 held markets on the chain.",
            "type": "string"
          }
        },
        "required": [
          "chain"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "pendle",
      "publicName": "pendle__rewards_claim",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Claim accrued Pendle rewards",
      "toolId": "pendle.claim"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1003,
      "destructiveHint": true,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "amountIn": {
            "description": "Amount of the payment token in human-readable units.",
            "minLength": 1,
            "type": "string"
          },
          "chain": {
            "anyOf": [
              {
                "minLength": 1,
                "type": "string"
              },
              {
                "minimum": 1,
                "type": "integer"
              }
            ],
            "description": "Chain slug or id - one of Pendle's 11 chains (e.g. 'ethereum', 'arbitrum', 'base', 'bsc')."
          },
          "dryRun": {
            "description": "true = quote only. Prices the wrap, runs every fund-safety check including the price floor, records the authorization this tool needs, and broadcasts NOTHING. Required before the real call.",
            "type": "boolean"
          },
          "slippageBps": {
            "description": "Slippage tolerance in whole basis points (default 100 = 1%; maximum 1000 = 10%). A fractional, negative or larger value is REJECTED, never clamped. The dry run and the execute must pass the SAME value (or omit it on both).",
            "minimum": 0,
            "type": "integer"
          },
          "sy": {
            "description": "The SY (Standardised Yield) CONTRACT ADDRESS. Read it from pendle__market_get as the market's `sy` field, or from the `deliveredAsset` a pendle__pt_redeem fallback reported.",
            "minLength": 1,
            "type": "string"
          },
          "tokenIn": {
            "description": "The payment token CONTRACT ADDRESS to wrap - the INPUT leg (ERC-20; pass the chain's wrapped-native token for native exposure).",
            "minLength": 1,
            "type": "string"
          }
        },
        "required": [
          "chain",
          "sy",
          "tokenIn",
          "amountIn"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "pendle",
      "publicName": "pendle__sy_mint",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Wrap a token into Pendle SY",
      "toolId": "pendle.sy.mint"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1063,
      "destructiveHint": true,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "amountIn": {
            "description": "Amount of SY to unwrap, in human-readable units.",
            "minLength": 1,
            "type": "string"
          },
          "chain": {
            "anyOf": [
              {
                "minLength": 1,
                "type": "string"
              },
              {
                "minimum": 1,
                "type": "integer"
              }
            ],
            "description": "Chain slug or id - one of Pendle's 11 chains (e.g. 'ethereum', 'arbitrum', 'base', 'bsc')."
          },
          "dryRun": {
            "description": "true = quote only. Prices the wrap, runs every fund-safety check including the price floor, records the authorization this tool needs, and broadcasts NOTHING. Required before the real call.",
            "type": "boolean"
          },
          "slippageBps": {
            "description": "Slippage tolerance in whole basis points (default 100 = 1%; maximum 1000 = 10%). A fractional, negative or larger value is REJECTED, never clamped. The dry run and the execute must pass the SAME value (or omit it on both).",
            "minimum": 0,
            "type": "integer"
          },
          "sy": {
            "description": "The SY (Standardised Yield) CONTRACT ADDRESS. Read it from pendle__market_get as the market's `sy` field, or from the `deliveredAsset` a pendle__pt_redeem fallback reported.",
            "minLength": 1,
            "type": "string"
          },
          "tokenOut": {
            "description": "The token CONTRACT ADDRESS to receive - the OUTPUT leg (ERC-20; pass the chain's wrapped-native token for native exposure).",
            "minLength": 1,
            "type": "string"
          }
        },
        "required": [
          "chain",
          "sy",
          "tokenOut",
          "amountIn"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "pendle",
      "publicName": "pendle__sy_redeem",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Unwrap Pendle SY back to a token",
      "toolId": "pendle.sy.redeem"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1857,
      "destructiveHint": true,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "amountIn": {
            "description": "Amount of tokenIn in human-readable units.",
            "minLength": 1,
            "type": "string"
          },
          "chain": {
            "anyOf": [
              {
                "minLength": 1,
                "type": "string"
              },
              {
                "minimum": 1,
                "type": "integer"
              }
            ],
            "description": "Chain slug or id - one of Pendle's 11 chains (e.g. 'ethereum', 'arbitrum', 'base', 'bsc')."
          },
          "dryRun": {
            "description": "Preview without executing.",
            "type": "boolean"
          },
          "slippageBps": {
            "description": "Slippage tolerance in basis points (default 100 = 1%).",
            "minimum": 0,
            "type": "integer"
          },
          "tokenIn": {
            "description": "Input token CONTRACT ADDRESS (ERC-20; use WETH for ETH). Buy: the payment token. Sell: the YT address.",
            "minLength": 1,
            "type": "string"
          },
          "tokenOut": {
            "description": "Output token CONTRACT ADDRESS. Buy: the YT address. Sell: the payment token.",
            "minLength": 1,
            "type": "string"
          }
        },
        "required": [
          "chain",
          "tokenIn",
          "tokenOut",
          "amountIn"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "pendle",
      "publicName": "pendle__yt_buy",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Buy a Pendle yield token",
      "toolId": "pendle.yt.buy"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1083,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "amountIn": {
            "description": "Amount of tokenIn in human-readable units.",
            "minLength": 1,
            "type": "string"
          },
          "chain": {
            "anyOf": [
              {
                "minLength": 1,
                "type": "string"
              },
              {
                "minimum": 1,
                "type": "integer"
              }
            ],
            "description": "Chain slug or id - one of Pendle's 11 chains (e.g. 'ethereum', 'arbitrum', 'base', 'bsc')."
          },
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "slippageBps": {
            "description": "Slippage tolerance in basis points (default 100 = 1%).",
            "minimum": 0,
            "type": "integer"
          },
          "tokenIn": {
            "description": "Input token address (payment token for a buy; YT address for a sell).",
            "minLength": 1,
            "type": "string"
          },
          "tokenOut": {
            "description": "Output token address (YT for a buy; payment token for a sell).",
            "minLength": 1,
            "type": "string"
          }
        },
        "required": [
          "chain",
          "tokenIn",
          "tokenOut",
          "amountIn"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "pendle",
      "publicName": "pendle__yt_quote",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Quote a Pendle YT trade",
      "toolId": "pendle.yt.quote"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1784,
      "destructiveHint": true,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "amountIn": {
            "description": "Amount of tokenIn in human-readable units.",
            "minLength": 1,
            "type": "string"
          },
          "chain": {
            "anyOf": [
              {
                "minLength": 1,
                "type": "string"
              },
              {
                "minimum": 1,
                "type": "integer"
              }
            ],
            "description": "Chain slug or id - one of Pendle's 11 chains (e.g. 'ethereum', 'arbitrum', 'base', 'bsc')."
          },
          "dryRun": {
            "description": "Preview without executing.",
            "type": "boolean"
          },
          "slippageBps": {
            "description": "Slippage tolerance in basis points (default 100 = 1%).",
            "minimum": 0,
            "type": "integer"
          },
          "tokenIn": {
            "description": "Input token CONTRACT ADDRESS (ERC-20; use WETH for ETH). Buy: the payment token. Sell: the YT address.",
            "minLength": 1,
            "type": "string"
          },
          "tokenOut": {
            "description": "Output token CONTRACT ADDRESS. Buy: the YT address. Sell: the payment token.",
            "minLength": 1,
            "type": "string"
          }
        },
        "required": [
          "chain",
          "tokenIn",
          "tokenOut",
          "amountIn"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "pendle",
      "publicName": "pendle__yt_sell",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Sell a Pendle yield token",
      "toolId": "pendle.yt.sell"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1619,
      "destructiveHint": true,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "dryRun": {
            "description": "true simulates the claim and reports what it would pay without signing anything. Omit it to actually claim, which spends gas and is irreversible.",
            "type": "boolean"
          },
          "token": {
            "description": "The pools.fun token's contract ADDRESS. Symbols are not unique on this launchpad and copycats are routinely live, so the address is the only identity that means one token.",
            "minLength": 1,
            "type": "string"
          }
        },
        "required": [
          "token"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "pools",
      "publicName": "pools__fees_claim",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Claim pools.fun creator fees",
      "toolId": "pools.claim_fees"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 4428,
      "destructiveHint": true,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "dryRun": {
            "description": "true simulates the claim and reports what it would pay without signing anything. Omit it to actually claim, which spends gas.",
            "type": "boolean"
          },
          "simulateOnly": {
            "description": "true runs every check a real claim runs, including the gas estimate over the exact bytes, and then stops without opening a key or recording anything. Returns the would-be transaction with executed: false. Use it to prove the path; use dryRun to ask what a claim would pay.",
            "type": "boolean"
          },
          "tokenAddress": {
            "description": "The pools.fun token's contract ADDRESS. Symbols are not unique on this launchpad and copycats are routinely live, so the address is the only identity that means one token. Find fees-to-holders tokens with pools__tokens_discover using holderRewards: true.",
            "minLength": 1,
            "type": "string"
          },
          "walletAddress": {
            "description": "ONLY valid with dryRun: true, where it reads what a DIFFERENT holder is owed - earned(account) and claim() simulated from that address are public views over public chain state. On a real claim it is refused by name, because the distributor pays whoever signs and there is no way to claim into another address.",
            "type": "string"
          }
        },
        "required": [
          "tokenAddress"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "pools",
      "publicName": "pools__holder_rewards_claim",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Claim pools.fun holder rewards",
      "toolId": "pools.holder_rewards_claim"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 3726,
      "destructiveHint": true,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "dryRun": {
            "description": "true simulates the distribute and reports whether it would succeed, what the distributor would move, the bounty rule and a gas ceiling, without signing anything. Omit it to actually distribute.",
            "type": "boolean"
          },
          "simulateOnly": {
            "description": "true runs every check a real distribute runs, including the gas estimate over the exact bytes, and then stops without opening a key or recording anything. Returns the would-be transaction with executed: false.",
            "type": "boolean"
          },
          "tokenAddress": {
            "description": "The pools.fun token's contract ADDRESS whose distributor to push. Find fees-to-holders tokens with pools__tokens_discover using holderRewards: true, and check whether one has work waiting with pools__holder_rewards_get.",
            "minLength": 1,
            "type": "string"
          }
        },
        "required": [
          "tokenAddress"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "pools",
      "publicName": "pools__holder_rewards_distribute",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Distribute pools.fun holder rewards",
      "toolId": "pools.holder_rewards_distribute"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 2102,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "tokenAddress": {
            "description": "Contract address of the pools.fun token whose holder rewards to read. Resolve a name with pools__tokens_search first, or find fees-to-holders tokens with pools__tokens_discover using holderRewards: true.",
            "minLength": 1,
            "type": "string"
          },
          "walletAddress": {
            "description": "The holder to read, as an EVM address. Defaults to the user's own selected wallet, which is the usual question. Any address may be passed because earned(account) is a public view over public chain state; the amounts are read from the distributor either way.",
            "type": "string"
          }
        },
        "required": [
          "tokenAddress"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "pools",
      "publicName": "pools__holder_rewards_get",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Read pools.fun holder rewards",
      "toolId": "pools.holder_rewards"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1698,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "limit": {
            "description": "Rows returned, 1-200. Defaults to 50. This bound is Vex's, not the launchpad's - the endpoint returns everything in one body - and every row past it is reachable with offset.",
            "type": "number"
          },
          "offset": {
            "description": "Zero-based index into the filtered list, for the next page. Pass back the nextOffset from a previous reply and keep query and pricingMode identical, or the offset walks a different list.",
            "type": "number"
          },
          "pricingMode": {
            "description": "Keep only pairs on this factory pricing mode. SIGNED_STOCK lists the pairs whose launch needs a signed price quote inside the factory's 30-to-120-second window; CHAINLINK_STOCK lists the feed-priced ones whose launch carries an empty attestation. The filter is refused by name when the factory reads did not answer, because filtering on a mode nothing was read for would return a list that looks authoritative and is not.",
            "enum": [
              "NONE",
              "CORE_CHAINLINK",
              "CHAINLINK_STOCK",
              "SIGNED_STOCK"
            ],
            "type": "string"
          },
          "query": {
            "description": "Case-insensitive substring matched against both the stock symbol and its company name, applied by Vex after the launchpad's full list is fetched. At most 64 characters. Use it to check whether one stock is launchable rather than paging the whole list.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "pools",
      "publicName": "pools__launch_assets_list",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "List pools.fun launchable stocks",
      "toolId": "pools.launch_assets"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 3774,
      "destructiveHint": true,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "holderRewards": {
            "description": "Set true to direct this token's CREATOR FEE STREAM to its own holders instead of to the launching wallet. Locked at launch and IRREVERSIBLE: the launchpad deploys a rewards distributor for the token and the creator receives nothing from trading fees, ever. Vex proves this before signing by reading the gateway's own FEES_TO_HOLDERS sentinel constants live and refusing unless the transaction carries exactly the sentinel for the mode you asked for; no address can be supplied here or anywhere on this surface. Omit or set false for the ordinary launch, where the fee stream goes to the user's session wallet.",
            "type": "boolean"
          },
          "holderRewardsMode": {
            "description": "Which asset the holders are paid in when holderRewards is true: token (default - the launched token, bought back from the fees), paired (the asset the pool trades against), or both. Refused by name when holderRewards is not true, and refused by name when the launch suite does not expose that mode's sentinel - the V2 suite has the token mode only.",
            "enum": [
              "token",
              "paired",
              "both"
            ],
            "type": "string"
          },
          "imageId": {
            "description": "IN-APP ONLY: the identifier of a picture the user already staged in the app's image locker, listed by launchpads__images_list (one locker, shared by every launchpad). The agent can never create one, only name one the locker already holds. Over the Vex Studio MCP surface this parameter is REFUSED BY NAME and imagePath is used instead. Optional here; pools__launch_execute REQUIRES a picture.",
            "type": "string"
          },
          "imagePath": {
            "description": "VEX STUDIO MCP ONLY: the path of an image file INSIDE the current project (PNG, JPEG, WebP or GIF), which Vex reads itself and publishes. A URL is never accepted on any surface, because the launchpad writes the picture's location on chain and a URL could serve different bytes tomorrow. In the Vex app this parameter is REFUSED BY NAME and imageId is used instead. Optional here; pools__launch_execute REQUIRES a picture.",
            "type": "string"
          },
          "name": {
            "description": "The token's full display name, 1 to 64 characters, exactly as it should appear on the launchpad and in the pinned metadata.",
            "minLength": 1,
            "type": "string"
          },
          "pairedAsset": {
            "description": "Which asset the new pool trades against: weth (default), usdg, or stock for one of the tokenised stocks the launch factory allows. A stock pair REQUIRES pairedStockAddress naming which stock, and it is the launch factory - not this build - that decides whether that address is launchable and how it is priced: list the allowed stocks and their pricing mode with pools__launch_assets_list. A stock-paired pool's liquidity after listing is UNKNOWN at launch time; the illiquidity badge on pools.fun is a fact about pools that already trade and says nothing about a pair being created now.",
            "enum": [
              "weth",
              "usdg",
              "stock"
            ],
            "type": "string"
          },
          "pairedStockAddress": {
            "description": "The 0x address of the tokenised stock to pair against, REQUIRED when pairedAsset is stock and refused by name on any other pair. Take it from pools__launch_assets_list, which reads the factory's own allowedPairedAsset and pricingModeFor for every listed stock. A SIGNED_STOCK pair is priced by a backend-signed quote the factory accepts only 30 to 120 seconds after it was observed, so such a launch must be prepared, confirmed and broadcast inside that window and is re-prepared automatically if it lapses.",
            "type": "string"
          },
          "prebuy": {
            "description": "Optional same-transaction first buy, in HUMAN decimal ETH (for example \"0.01\") - not wei. Autonomous launches support an ETH prebuy only, and only on a WETH pair (the gateway refuses a native dev buy against any other pair); a USDG or stock-paired prebuy needs an approval leg and is available through the desktop form instead.",
            "type": "string"
          },
          "simulateOnly": {
            "description": "Set true to run the WHOLE launch path and stop at the edge of signing: the launchpad prepares real calldata, the chain is read at one anchored block, the launch is simulated from the user's wallet, all 15 pre-signing checks run, and the gas is estimated over those exact bytes - then it returns the would-be launch (predicted token address, calldata fingerprint, value, fees, gas bound) with launched: false. NO wallet key is opened, no authorization is created and nothing is broadcast, so no token exists afterwards. One side effect DOES happen: preparing a launch pins a metadata object with the launchpad and mines a new salt, so a real launch prepared later has a different salt and a different token address. Use it to show a user exactly what a launch would do before they commit.",
            "type": "boolean"
          },
          "symbol": {
            "description": "The ticker, 1 to 16 characters. Symbols are NOT unique on pools.fun and copycats are routinely live, so this is a label rather than an identity.",
            "minLength": 1,
            "type": "string"
          }
        },
        "required": [
          "name",
          "symbol"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "pools",
      "publicName": "pools__launch_execute",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Launch a token on pools.fun",
      "toolId": "pools.launch_execute"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1257,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "holderRewards": {
            "description": "Set true to direct this token's CREATOR FEE STREAM to its own holders instead of to the launching wallet. Locked at launch and IRREVERSIBLE: the launchpad deploys a rewards distributor for the token and the creator receives nothing from trading fees, ever. Vex proves this before signing by reading the gateway's own FEES_TO_HOLDERS sentinel constants live and refusing unless the transaction carries exactly the sentinel for the mode you asked for; no address can be supplied here or anywhere on this surface. Omit or set false for the ordinary launch, where the fee stream goes to the user's session wallet.",
            "type": "boolean"
          },
          "holderRewardsMode": {
            "description": "Which asset the holders are paid in when holderRewards is true: token (default - the launched token, bought back from the fees), paired (the asset the pool trades against), or both. Refused by name when holderRewards is not true, and refused by name when the launch suite does not expose that mode's sentinel - the V2 suite has the token mode only.",
            "enum": [
              "token",
              "paired",
              "both"
            ],
            "type": "string"
          },
          "imageId": {
            "description": "IN-APP ONLY: the identifier of a picture the user already staged in the app's image locker, listed by launchpads__images_list (one locker, shared by every launchpad). The agent can never create one, only name one the locker already holds. Over the Vex Studio MCP surface this parameter is REFUSED BY NAME and imagePath is used instead. Optional here; pools__launch_execute REQUIRES a picture.",
            "type": "string"
          },
          "imagePath": {
            "description": "VEX STUDIO MCP ONLY: the path of an image file INSIDE the current project (PNG, JPEG, WebP or GIF), which Vex reads itself and publishes. A URL is never accepted on any surface, because the launchpad writes the picture's location on chain and a URL could serve different bytes tomorrow. In the Vex app this parameter is REFUSED BY NAME and imageId is used instead. Optional here; pools__launch_execute REQUIRES a picture.",
            "type": "string"
          },
          "name": {
            "description": "The token's full display name, 1 to 64 characters, exactly as it should appear on the launchpad and in the pinned metadata.",
            "minLength": 1,
            "type": "string"
          },
          "pairedAsset": {
            "description": "Which asset the new pool trades against: weth (default), usdg, or stock for one of the tokenised stocks the launch factory allows. A stock pair REQUIRES pairedStockAddress naming which stock, and it is the launch factory - not this build - that decides whether that address is launchable and how it is priced: list the allowed stocks and their pricing mode with pools__launch_assets_list. A stock-paired pool's liquidity after listing is UNKNOWN at launch time; the illiquidity badge on pools.fun is a fact about pools that already trade and says nothing about a pair being created now.",
            "enum": [
              "weth",
              "usdg",
              "stock"
            ],
            "type": "string"
          },
          "pairedStockAddress": {
            "description": "The 0x address of the tokenised stock to pair against, REQUIRED when pairedAsset is stock and refused by name on any other pair. Take it from pools__launch_assets_list, which reads the factory's own allowedPairedAsset and pricingModeFor for every listed stock. A SIGNED_STOCK pair is priced by a backend-signed quote the factory accepts only 30 to 120 seconds after it was observed, so such a launch must be prepared, confirmed and broadcast inside that window and is re-prepared automatically if it lapses.",
            "type": "string"
          },
          "prebuy": {
            "description": "Optional same-transaction first buy, in HUMAN decimal ETH (for example \"0.01\") - not wei. Autonomous launches support an ETH prebuy only, and only on a WETH pair (the gateway refuses a native dev buy against any other pair); a USDG or stock-paired prebuy needs an approval leg and is available through the desktop form instead.",
            "type": "string"
          },
          "symbol": {
            "description": "The ticker, 1 to 16 characters. Symbols are NOT unique on pools.fun and copycats are routinely live, so this is a label rather than an identity.",
            "minLength": 1,
            "type": "string"
          }
        },
        "required": [
          "name",
          "symbol"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "pools",
      "publicName": "pools__launch_preview",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Price a pools.fun launch",
      "toolId": "pools.launch_preview"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1253,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "holderRewards": {
            "description": "Set true to direct this token's CREATOR FEE STREAM to its own holders instead of to the launching wallet. Locked at launch and IRREVERSIBLE: the launchpad deploys a rewards distributor for the token and the creator receives nothing from trading fees, ever. Vex proves this before signing by reading the gateway's own FEES_TO_HOLDERS sentinel constants live and refusing unless the transaction carries exactly the sentinel for the mode you asked for; no address can be supplied here or anywhere on this surface. Omit or set false for the ordinary launch, where the fee stream goes to the user's session wallet.",
            "type": "boolean"
          },
          "holderRewardsMode": {
            "description": "Which asset the holders are paid in when holderRewards is true: token (default - the launched token, bought back from the fees), paired (the asset the pool trades against), or both. Refused by name when holderRewards is not true, and refused by name when the launch suite does not expose that mode's sentinel - the V2 suite has the token mode only.",
            "enum": [
              "token",
              "paired",
              "both"
            ],
            "type": "string"
          },
          "imageId": {
            "description": "IN-APP ONLY: the identifier of a picture the user already staged in the app's image locker, listed by launchpads__images_list (one locker, shared by every launchpad). The agent can never create one, only name one the locker already holds. Over the Vex Studio MCP surface this parameter is REFUSED BY NAME and imagePath is used instead. Optional here; pools__launch_execute REQUIRES a picture.",
            "type": "string"
          },
          "imagePath": {
            "description": "VEX STUDIO MCP ONLY: the path of an image file INSIDE the current project (PNG, JPEG, WebP or GIF), which Vex reads itself and publishes. A URL is never accepted on any surface, because the launchpad writes the picture's location on chain and a URL could serve different bytes tomorrow. In the Vex app this parameter is REFUSED BY NAME and imageId is used instead. Optional here; pools__launch_execute REQUIRES a picture.",
            "type": "string"
          },
          "name": {
            "description": "The token's full display name, 1 to 64 characters, exactly as it should appear on the launchpad and in the pinned metadata.",
            "minLength": 1,
            "type": "string"
          },
          "pairedAsset": {
            "description": "Which asset the new pool trades against: weth (default), usdg, or stock for one of the tokenised stocks the launch factory allows. A stock pair REQUIRES pairedStockAddress naming which stock, and it is the launch factory - not this build - that decides whether that address is launchable and how it is priced: list the allowed stocks and their pricing mode with pools__launch_assets_list. A stock-paired pool's liquidity after listing is UNKNOWN at launch time; the illiquidity badge on pools.fun is a fact about pools that already trade and says nothing about a pair being created now.",
            "enum": [
              "weth",
              "usdg",
              "stock"
            ],
            "type": "string"
          },
          "pairedStockAddress": {
            "description": "The 0x address of the tokenised stock to pair against, REQUIRED when pairedAsset is stock and refused by name on any other pair. Take it from pools__launch_assets_list, which reads the factory's own allowedPairedAsset and pricingModeFor for every listed stock. A SIGNED_STOCK pair is priced by a backend-signed quote the factory accepts only 30 to 120 seconds after it was observed, so such a launch must be prepared, confirmed and broadcast inside that window and is re-prepared automatically if it lapses.",
            "type": "string"
          },
          "prebuy": {
            "description": "Optional same-transaction first buy, in HUMAN decimal ETH (for example \"0.01\") - not wei. Autonomous launches support an ETH prebuy only, and only on a WETH pair (the gateway refuses a native dev buy against any other pair); a USDG or stock-paired prebuy needs an approval leg and is available through the desktop form instead.",
            "type": "string"
          },
          "symbol": {
            "description": "The ticker, 1 to 16 characters. Symbols are NOT unique on pools.fun and copycats are routinely live, so this is a label rather than an identity.",
            "minLength": 1,
            "type": "string"
          }
        },
        "required": [
          "name",
          "symbol"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "pools",
      "publicName": "pools__launch_request_form",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Ask the user to confirm a pools.fun launch",
      "toolId": "pools.launch_request_form"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1371,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "cursor": {
            "description": "Opaque pagination token: pass back the nextCursor from a previous reply to fetch the following page. Keep the other arguments identical between pages, or the cursor walks a different result set.",
            "type": "string"
          },
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "includeClaimable": {
            "description": "true also SIMULATES a fee claim for the first few rows and reports what it would pay, in two separate assets with their decimals. Costs a couple of chain reads per row, so it is off by default; a row that was not simulated reports claimable: null, which means NOT MEASURED rather than nothing to claim.",
            "type": "boolean"
          },
          "limit": {
            "description": "Maximum launches returned, 1-100. Defaults to 25, newest first.",
            "type": "number"
          },
          "platform": {
            "description": "Which launcher to look under: all (default), poolsfun, or sushi. The default covers both so a launch is not missed because it went through the other launcher.",
            "enum": [
              "poolsfun",
              "sushi",
              "all"
            ],
            "type": "string"
          }
        },
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "pools",
      "publicName": "pools__my_launches_list",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "List this wallet's pools.fun launches",
      "toolId": "pools.my_launches"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1006,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "aggregate": {
            "description": "How many timeframe units make one candle, 1-24 (default 1). Combine with timeframe: minute plus 15 is a quarter-hour candle, hour plus 4 is a four-hour candle.",
            "type": "number"
          },
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "limit": {
            "description": "How many candles to return, 1-1000 (default 30). Ask for the window the analysis needs rather than the maximum.",
            "type": "number"
          },
          "timeframe": {
            "description": "Base unit of one candle: minute, hour (default) or day. The real candle span is this multiplied by aggregate.",
            "enum": [
              "minute",
              "hour",
              "day"
            ],
            "type": "string"
          },
          "tokenAddress": {
            "description": "Contract address of the pools.fun token whose price history to read. Resolve it with pools__tokens_search first - a symbol is not an identity on this launchpad.",
            "minLength": 1,
            "type": "string"
          }
        },
        "required": [
          "tokenAddress"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "pools",
      "publicName": "pools__token_candles_list",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Read pools.fun token candles",
      "toolId": "pools.candles"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1411,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "tokenAddress": {
            "description": "Contract address of the token to inspect. Resolve a name to an address with pools__tokens_search first, since symbols repeat across this launchpad.",
            "minLength": 1,
            "type": "string"
          }
        },
        "required": [
          "tokenAddress"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "pools",
      "publicName": "pools__token_get",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Read one pools.fun token",
      "toolId": "pools.token"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 2552,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "cursor": {
            "description": "Opaque pagination token: pass back the nextCursor from a previous reply to fetch the following page. Keep every other filter identical between pages or the cursor walks a different result set.",
            "type": "string"
          },
          "deployerAddress": {
            "description": "Keep only tokens deployed by this wallet address, matched server-side. Use it to see everything one launcher has created.",
            "type": "string"
          },
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "feeRecipientAddress": {
            "description": "Keep only tokens whose creator fee stream is directed at this wallet address, matched server-side. On pools.fun the fee recipient can differ from the deployer, so this answers who EARNS from a token rather than who deployed it.",
            "type": "string"
          },
          "holderRewards": {
            "description": "true keeps only tokens that stream their fees to holders (fees to holders, opted in at launch and locked from then on). Same opt-in switch as vexAttested: only true is accepted, false or omitted means unfiltered, and the complement cannot be requested. Rows that match carry holderRewardsMode (token, paired or both) and holderRewardsDistributor. Combining it with vexAttested asks for tokens that are BOTH, which matched nothing when this was measured - that is a fact about the market, not a rejected filter.",
            "type": "boolean"
          },
          "limit": {
            "description": "Maximum rows returned, 1-100 (the provider's own cap). Omitting it leaves the page size to the provider. Use the returned cursor to walk further rather than asking for more than the cap.",
            "type": "number"
          },
          "live": {
            "description": "When true, restrict the list to the provider's live feed of recently active tokens instead of the whole market. Default false, which lists everything matching the other filters.",
            "type": "boolean"
          },
          "maxAgeHours": {
            "description": "Keep only tokens launched within this many hours. This is the fresh-launch filter on pools.fun: there is no bonding curve or graduation stage to filter on, so age is what separates a brand new token from an established one.",
            "type": "number"
          },
          "maxMarketCapUsd": {
            "description": "Keep only tokens whose market capitalisation in US dollars is at most this. Pair with minMarketCapUsd to bracket a size band; the figure is display-grade.",
            "type": "number"
          },
          "minMarketCapUsd": {
            "description": "Keep only tokens whose market capitalisation in US dollars is at least this. The figure is the provider's own display-grade estimate, not an executable valuation.",
            "type": "number"
          },
          "minTxCount24h": {
            "description": "Keep only tokens with at least this many trades in the last 24 hours. A whole number; useful for separating tokens with real flow from ones whose market cap moved on a single fill.",
            "type": "number"
          },
          "minVolUsd": {
            "description": "Keep only tokens whose traded volume in US dollars over volTimeframe is at least this. It REQUIRES volTimeframe - a volume floor without a window has no meaning and is rejected by name rather than silently applied to some default window.",
            "type": "number"
          },
          "order": {
            "description": "Ranking direction: desc (default, biggest or newest first) or asc. Pair it with sortBy - asc on deployedAt walks the oldest launches, desc gives the freshest.",
            "enum": [
              "desc",
              "asc"
            ],
            "type": "string"
          },
          "platform": {
            "description": "Which launcher to list, poolsfun (default, the pools.fun PartyFactory), sushi (the older SushiLaunchpad on the same chain), or all. These are two different launchers whose tokens sit side by side on Robinhood Chain; the row echoes which one produced it.",
            "enum": [
              "poolsfun",
              "sushi",
              "all"
            ],
            "type": "string"
          },
          "query": {
            "description": "Free-text match against token names and symbols, applied by the launchpad alongside every other filter here. Reach for it when a name has to be combined with filters - a name plus an age or volume floor, say. When the name is all you have, the dedicated pools name-lookup tool is the shorter path.",
            "type": "string"
          },
          "sortBy": {
            "description": "Server-side ranking key. Accepts marketCapUsd (default), vol1m, vol5m, vol1h, vol6h, vol24h, txCount24h, priceChange1m, priceChange5m, priceChange1h, priceChange6h, priceChange24h, lastTradeAt, deployedAt. Sort by deployedAt for the newest launches and by vol1h or txCount24h for what is actually being traded right now.",
            "enum": [
              "marketCapUsd",
              "vol1m",
              "vol5m",
              "vol1h",
              "vol6h",
              "vol24h",
              "txCount24h",
              "priceChange1m",
              "priceChange5m",
              "priceChange1h",
              "priceChange6h",
              "priceChange24h",
              "lastTradeAt",
              "deployedAt"
            ],
            "type": "string"
          },
          "vexAttested": {
            "description": "true keeps only tokens the launchpad marks as carrying a Vex attestation. This is an OPT-IN SWITCH, not a two-sided filter: the launchpad accepts only true, so false (or omitting it) means the filter is not applied and there is no way to ask for the tokens WITHOUT an attestation. The flag is the launchpad's own claim about its index; the attestation itself is a signature Vex published for a launch it made.",
            "type": "boolean"
          },
          "volTimeframe": {
            "description": "The window the minVolUsd floor is measured over: 1m, 5m, 1h, 6h or 24h. Supply it together with minVolUsd; on its own it filters nothing and is rejected.",
            "enum": [
              "1m",
              "5m",
              "1h",
              "6h",
              "24h"
            ],
            "type": "string"
          }
        },
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "pools",
      "publicName": "pools__tokens_discover",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Screen pools.fun tokens",
      "toolId": "pools.tokens"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1313,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "cursor": {
            "description": "Opaque pagination token: pass back the nextCursor from a previous reply to fetch the following page. Keep the other arguments identical between pages, or the cursor walks a different result set.",
            "type": "string"
          },
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "holderRewards": {
            "description": "true narrows the search to tokens that stream their fees to holders. Same opt-in switch as vexAttested. Matching rows carry holderRewardsMode and holderRewardsDistributor.",
            "type": "boolean"
          },
          "limit": {
            "description": "Maximum matches returned, 1-100. Defaults to 10, which is enough to expose copycat symbols sharing the name.",
            "type": "number"
          },
          "platform": {
            "description": "Which launcher to search: all (default here, so a name lookup cannot miss the other launcher), poolsfun, or sushi. Narrow it only when the user has already said which launcher they mean.",
            "enum": [
              "poolsfun",
              "sushi",
              "all"
            ],
            "type": "string"
          },
          "query": {
            "description": "The name or symbol fragment to look for, 1 to 64 characters. Matching is done by the launchpad across both token names and symbols.",
            "minLength": 1,
            "type": "string"
          },
          "vexAttested": {
            "description": "true narrows the search to tokens the launchpad marks as carrying a Vex attestation. An opt-in switch: the launchpad accepts only true, so false or omitted means unfiltered and the tokens without the mark cannot be requested. Useful for picking the right token out of several sharing a symbol.",
            "type": "boolean"
          }
        },
        "required": [
          "query"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "pools",
      "publicName": "pools__tokens_search",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Search pools.fun tokens",
      "toolId": "pools.search"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 2478,
      "destructiveHint": true,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "amountRaw": {
            "description": "Amount to bridge, in raw atomic units of fromToken (e.g. USDC has 6 decimals, so 1 USDC = \"1000000\"). Raw base units as an integer string (not human decimals) - read the token's decimals from `TokenFind` first.",
            "minLength": 1,
            "type": "string"
          },
          "dryRun": {
            "description": "If true, fetch the route without signing.",
            "type": "boolean"
          },
          "fromChain": {
            "anyOf": [
              {
                "minLength": 1,
                "type": "string"
              },
              {
                "minimum": 1,
                "type": "integer"
              }
            ],
            "description": "Source chain. Chain slug/alias, or the numeric chain id `TokenFind` returns (e.g. `base` or `8453`). Robinhood Chain is `robinhood` / `4663`."
          },
          "fromToken": {
            "description": "Source token address, or native ETH/native.",
            "minLength": 1,
            "type": "string"
          },
          "slippageBps": {
            "description": "Basis points (1 bps = 0.01%). Default 100 = 1%. Vex caps this at 1000 (10%) and REJECTS a higher value rather than clamping it.",
            "minimum": 0,
            "type": "integer"
          },
          "toChain": {
            "anyOf": [
              {
                "minLength": 1,
                "type": "string"
              },
              {
                "minimum": 1,
                "type": "integer"
              }
            ],
            "description": "Destination chain. Chain slug/alias, or the numeric chain id `TokenFind` returns (e.g. `base` or `8453`). Robinhood Chain is `robinhood` / `4663`."
          },
          "toToken": {
            "description": "Destination token address, or native ETH/native.",
            "minLength": 1,
            "type": "string"
          }
        },
        "required": [
          "fromChain",
          "fromToken",
          "toChain",
          "toToken",
          "amountRaw"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "relay",
      "publicName": "relay__bridge_execute",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Execute a Relay cross-chain bridge",
      "toolId": "relay.bridge"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 2094,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "amountRaw": {
            "description": "Amount to bridge, in raw atomic units of fromToken (e.g. USDC has 6 decimals, so 1 USDC = \"1000000\"). Raw base units as an integer string (not human decimals) - read the token's decimals from `TokenFind` first.",
            "minLength": 1,
            "type": "string"
          },
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "fromChain": {
            "anyOf": [
              {
                "minLength": 1,
                "type": "string"
              },
              {
                "minimum": 1,
                "type": "integer"
              }
            ],
            "description": "Source chain. Chain slug/alias, or the numeric chain id `TokenFind` returns (e.g. `base` or `8453`). Robinhood Chain is `robinhood` / `4663`."
          },
          "fromToken": {
            "description": "Source token address, or native ETH/native.",
            "minLength": 1,
            "type": "string"
          },
          "slippageBps": {
            "description": "Basis points (1 bps = 0.01%). Default 100 = 1%. Vex caps this at 1000 (10%) and REJECTS a higher value rather than clamping it.",
            "minimum": 0,
            "type": "integer"
          },
          "toChain": {
            "anyOf": [
              {
                "minLength": 1,
                "type": "string"
              },
              {
                "minimum": 1,
                "type": "integer"
              }
            ],
            "description": "Destination chain. Chain slug/alias, or the numeric chain id `TokenFind` returns (e.g. `base` or `8453`). Robinhood Chain is `robinhood` / `4663`."
          },
          "toToken": {
            "description": "Destination token address, or native ETH/native.",
            "minLength": 1,
            "type": "string"
          }
        },
        "required": [
          "fromChain",
          "fromToken",
          "toChain",
          "toToken",
          "amountRaw"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "relay",
      "publicName": "relay__bridge_quote_get",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Quote a Relay cross-chain bridge",
      "toolId": "relay.quote.get"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1980,
      "destructiveHint": true,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "borrowAmountRaw": {
            "description": "Borrow this many raw atomic units of the vault's debt token (scale = the vault's borrowTokenDecimals). Mutually exclusive with repayAmountRaw/repayAll.",
            "type": "string"
          },
          "depositAmountRaw": {
            "description": "Add this many raw atomic units of the vault's collateral token (scale = the vault's supplyTokenDecimals). Mutually exclusive with withdrawAmountRaw/withdrawAll.",
            "type": "string"
          },
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "market": {
            "description": "Which Borrow market: \"main\" or \"ethena\" (default \"main\"). Must match the vault's own market.",
            "type": "string"
          },
          "positionId": {
            "description": "Existing position id to adjust - from solana__lend_borrow_positions_list. Omit (or 0) to create a NEW position.",
            "type": "number"
          },
          "repayAll": {
            "description": "Repay ALL debt on this position, including accrued-interest dust. Mutually exclusive with borrowAmountRaw/repayAmountRaw.",
            "type": "boolean"
          },
          "repayAmountRaw": {
            "description": "Repay this many raw atomic units of debt (scale = the vault's borrowTokenDecimals). Mutually exclusive with borrowAmountRaw/repayAll.",
            "type": "string"
          },
          "vaultId": {
            "description": "Vault to operate on - from solana__lend_borrow_vaults_list, which also gives the two legs' decimals every amount param below is expressed in.",
            "type": "number"
          },
          "withdrawAll": {
            "description": "Withdraw ALL collateral from this position. Mutually exclusive with depositAmountRaw/withdrawAmountRaw.",
            "type": "boolean"
          },
          "withdrawAmountRaw": {
            "description": "Remove this many raw atomic units of collateral (scale = the vault's supplyTokenDecimals). Mutually exclusive with depositAmountRaw/withdrawAll.",
            "type": "string"
          }
        },
        "required": [
          "vaultId"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "solana",
      "publicName": "solana__lend_borrow_operate",
      "readOnlyHint": false,
      "requiresEnv": "JUPITER_API_KEY",
      "title": "Adjust a Jupiter Lend borrow position",
      "toolId": "solana.lend.borrowOperate"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1107,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "market": {
            "description": "Which Borrow market: \"main\" or \"ethena\" (default \"main\"). An unrecognized value is rejected.",
            "type": "string"
          },
          "vaultIds": {
            "description": "Comma-separated vault-id allow-list to filter positions to. Omit for all positions.",
            "type": "string"
          },
          "walletAddress": {
            "description": "The wallet account to read, as a Solana address. Defaults to the session's selected Solana wallet.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "solana",
      "publicName": "solana__lend_borrow_positions_list",
      "readOnlyHint": true,
      "requiresEnv": "JUPITER_API_KEY",
      "title": "List Jupiter Lend borrow positions",
      "toolId": "solana.lend.borrowPositions"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1325,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "market": {
            "description": "Which Borrow market: \"main\" or \"ethena\" (default \"main\"). Vault ids are scoped per market - the same vaultId in a different market is a different vault. An unrecognized value is rejected.",
            "type": "string"
          },
          "vaultIds": {
            "description": "Comma-separated vault-id allow-list (matches the provider's own numeric vault id). Omit for all vaults.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "solana",
      "publicName": "solana__lend_borrow_vaults_list",
      "readOnlyHint": true,
      "requiresEnv": "JUPITER_API_KEY",
      "title": "Screen Jupiter Lend borrow vaults",
      "toolId": "solana.lend.borrowVaults"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 955,
      "destructiveHint": true,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "amountRaw": {
            "description": "Amount to deposit, in RAW atomic units of the asset as an integer string, never human decimals (e.g. USDC has 6 decimals, so 1 USDC = \"1000000\") - read the asset's decimals from solana__lend_earn_rates_list's assetDecimals, or from TokenFind, before building it.",
            "minLength": 1,
            "type": "string"
          },
          "asset": {
            "description": "Token address to deposit.",
            "minLength": 1,
            "type": "string"
          },
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          }
        },
        "required": [
          "asset",
          "amountRaw"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "solana",
      "publicName": "solana__lend_earn_deposit",
      "readOnlyHint": false,
      "requiresEnv": "JUPITER_API_KEY",
      "title": "Deposit into a Jupiter Lend earn vault",
      "toolId": "solana.lend.deposit"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 702,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "walletAddress": {
            "description": "The wallet ACCOUNT to read, as a Solana address (not a token mint or a position key). Defaults to the session's selected Solana wallet - omit it to read your own. Under session scope a DIFFERENT address is rejected.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "solana",
      "publicName": "solana__lend_earn_positions_list",
      "readOnlyHint": true,
      "requiresEnv": "JUPITER_API_KEY",
      "title": "List Jupiter Lend earn positions",
      "toolId": "solana.lend.positions"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 952,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "assets": {
            "description": "Comma-separated list of asset identifiers to filter to - matches the underlying asset mint address, Earn share-token (jlToken) mint, symbol, or provider lending id. Case-insensitive for symbol. Omit for all markets.",
            "type": "string"
          },
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "minSupplyRate": {
            "description": "Only return markets whose base supply APY percent is at or above this value, e.g. 3 for 3%.",
            "type": "number"
          },
          "minTotalRate": {
            "description": "Only return markets whose combined (supply + rewards) APY percent is at or above this value, e.g. 5 for 5%.",
            "type": "number"
          }
        },
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "solana",
      "publicName": "solana__lend_earn_rates_list",
      "readOnlyHint": true,
      "requiresEnv": "JUPITER_API_KEY",
      "title": "Screen Jupiter Lend earn markets",
      "toolId": "solana.lend.rates"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1336,
      "destructiveHint": true,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "amountRaw": {
            "description": "Amount to withdraw, in RAW atomic units of the asset as an integer string, never human decimals (e.g. USDC has 6 decimals, so 1 USDC = \"1000000\") - see solana__lend_earn_positions_list for your current balance and the asset's decimals. Mutually exclusive with withdrawAll - provide exactly one, never both and never neither.",
            "type": "string"
          },
          "asset": {
            "description": "Token address to withdraw.",
            "minLength": 1,
            "type": "string"
          },
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "withdrawAll": {
            "description": "Exit the ENTIRE Earn position for this asset, dust-free (redeems the position's full share balance rather than an underlying amount). Mutually exclusive with amountRaw - provide exactly one, never both. Never pass withdrawAll: false; omit it and pass amountRaw instead.",
            "type": "boolean"
          }
        },
        "required": [
          "asset"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "solana",
      "publicName": "solana__lend_earn_withdraw",
      "readOnlyHint": false,
      "requiresEnv": "JUPITER_API_KEY",
      "title": "Withdraw from a Jupiter Lend earn vault",
      "toolId": "solana.lend.withdraw"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 976,
      "destructiveHint": true,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "amountUsdc": {
            "description": "Amount to spend, in whole/fractional USDC dollars (human-readable, e.g. 10 for $10) - not raw atomic units.",
            "type": "number"
          },
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "marketId": {
            "description": "Market ID (from solana__predict_events_discover or solana__predict_events_search).",
            "minLength": 1,
            "type": "string"
          },
          "side": {
            "description": "Side: yes or no.",
            "minLength": 1,
            "type": "string"
          }
        },
        "required": [
          "marketId",
          "side",
          "amountUsdc"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "solana",
      "publicName": "solana__predict_buy",
      "readOnlyHint": false,
      "requiresEnv": "JUPITER_API_KEY",
      "title": "Buy Jupiter prediction shares",
      "toolId": "solana.predict.buy"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 936,
      "destructiveHint": true,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "positionPubkey": {
            "description": "Position public key (from solana__predict_positions_list).",
            "minLength": 1,
            "type": "string"
          }
        },
        "required": [
          "positionPubkey"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "solana",
      "publicName": "solana__predict_claim",
      "readOnlyHint": false,
      "requiresEnv": "JUPITER_API_KEY",
      "title": "Claim a resolved Jupiter prediction payout",
      "toolId": "solana.predict.claim"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 870,
      "destructiveHint": true,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "minSellPriceSlippageBps": {
            "description": "Slippage tolerance applied to EACH sell in this batch, in basis points (0-1000; e.g. 100 = 1%). Vex caps slippage at 1000 bps (10%) and rejects anything above rather than clamping. Required by the provider - there is no default, so pick a value you accept across every position closed.",
            "minimum": 0,
            "type": "integer"
          }
        },
        "required": [
          "minSellPriceSlippageBps"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "solana",
      "publicName": "solana__predict_close_all",
      "readOnlyHint": false,
      "requiresEnv": "JUPITER_API_KEY",
      "title": "Close every Jupiter prediction position",
      "toolId": "solana.predict.closeAll"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 684,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "eventId": {
            "description": "Event ID (from solana__predict_events_discover or solana__predict_events_search).",
            "minLength": 1,
            "type": "string"
          },
          "includeMarkets": {
            "description": "Include the event's nested markets array (default false - lean).",
            "type": "boolean"
          }
        },
        "required": [
          "eventId"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "solana",
      "publicName": "solana__predict_event_get",
      "readOnlyHint": true,
      "requiresEnv": "JUPITER_API_KEY",
      "title": "Read one Jupiter prediction event",
      "toolId": "solana.predict.event"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1104,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "category": {
            "description": "Category filter: all, crypto, sports, politics, esports, culture, economics, or tech. Omit for all categories.",
            "type": "string"
          },
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "filter": {
            "description": "Status filter: trending, live, new, or upcoming (not yet open for trading).",
            "type": "string"
          },
          "includeMarkets": {
            "description": "Include each event's nested markets array (default false - lean; use solana__predict_event_get for one event's markets).",
            "type": "boolean"
          },
          "limit": {
            "description": "Max results, 1-100 (default 20).",
            "type": "number"
          },
          "offset": {
            "description": "Skip first N results for pagination.",
            "type": "number"
          },
          "provider": {
            "description": "Provider: kalshi, polymarket, or bisonfi (Jupiter Forecast). Default polymarket.",
            "type": "string"
          },
          "sortBy": {
            "description": "Sort by: volume or beginAt.",
            "type": "string"
          },
          "sortDirection": {
            "description": "Sort direction: asc or desc.",
            "type": "string"
          },
          "subcategory": {
            "description": "Comma-separated subcategory filter.",
            "type": "string"
          },
          "tags": {
            "description": "Free-text tag filter, e.g. soccer.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "solana",
      "publicName": "solana__predict_events_discover",
      "readOnlyHint": true,
      "requiresEnv": "JUPITER_API_KEY",
      "title": "Browse Jupiter prediction events",
      "toolId": "solana.predict.events"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1152,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "includeMarkets": {
            "description": "Include each event's nested markets array (default false - lean; use solana__predict_event_get for one event's markets).",
            "type": "boolean"
          },
          "limit": {
            "description": "Max results, 1-20 (default 20). The provider ignores this param upstream - Vex enforces the requested count locally.",
            "type": "number"
          },
          "provider": {
            "description": "Provider: kalshi, polymarket, or bisonfi (Jupiter Forecast). Default polymarket.",
            "type": "string"
          },
          "query": {
            "description": "Search keyword or phrase, e.g. \"bitcoin\" or \"super bowl\".",
            "minLength": 1,
            "type": "string"
          }
        },
        "required": [
          "query"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "solana",
      "publicName": "solana__predict_events_search",
      "readOnlyHint": true,
      "requiresEnv": "JUPITER_API_KEY",
      "title": "Search Jupiter prediction events",
      "toolId": "solana.predict.search"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 885,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "limit": {
            "description": "Max results, 1-100.",
            "type": "number"
          },
          "metric": {
            "description": "Ranking metric: pnl, volume, or win_rate.",
            "minLength": 1,
            "type": "string"
          },
          "period": {
            "description": "Period: all_time, weekly, or monthly.",
            "minLength": 1,
            "type": "string"
          }
        },
        "required": [
          "period",
          "metric",
          "limit"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "solana",
      "publicName": "solana__predict_leaderboard_list",
      "readOnlyHint": true,
      "requiresEnv": "JUPITER_API_KEY",
      "title": "Rank Jupiter prediction traders",
      "toolId": "solana.predict.leaderboards"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 685,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "marketId": {
            "description": "Market ID (from solana__predict_events_discover, solana__predict_events_search, or an event's nested markets).",
            "minLength": 1,
            "type": "string"
          }
        },
        "required": [
          "marketId"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "solana",
      "publicName": "solana__predict_market_get",
      "readOnlyHint": true,
      "requiresEnv": "JUPITER_API_KEY",
      "title": "Read one Jupiter prediction market",
      "toolId": "solana.predict.market"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 627,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "orderPubkey": {
            "description": "Order public key (from solana__predict_orders_list).",
            "minLength": 1,
            "type": "string"
          }
        },
        "required": [
          "orderPubkey"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "solana",
      "publicName": "solana__predict_order_get",
      "readOnlyHint": true,
      "requiresEnv": "JUPITER_API_KEY",
      "title": "Read one live Jupiter prediction order",
      "toolId": "solana.predict.order"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 505,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "orderPubkey": {
            "description": "Order public key (from solana__predict_orders_list).",
            "minLength": 1,
            "type": "string"
          }
        },
        "required": [
          "orderPubkey"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "solana",
      "publicName": "solana__predict_order_status_get",
      "readOnlyHint": true,
      "requiresEnv": "JUPITER_API_KEY",
      "title": "Read a Jupiter prediction order's fill status",
      "toolId": "solana.predict.orderStatus"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 708,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "marketId": {
            "description": "Market ID.",
            "minLength": 1,
            "type": "string"
          }
        },
        "required": [
          "marketId"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "solana",
      "publicName": "solana__predict_orderbook_get",
      "readOnlyHint": true,
      "requiresEnv": "JUPITER_API_KEY",
      "title": "Read Jupiter prediction order-book depth",
      "toolId": "solana.predict.orderbook"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1041,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "limit": {
            "description": "Max results, 1-100 (default 20).",
            "type": "number"
          },
          "offset": {
            "description": "Skip first N results for pagination.",
            "type": "number"
          },
          "walletAddress": {
            "description": "The wallet ACCOUNT to read, as a Solana address (not a token mint or a position key). Defaults to the session's selected Solana wallet - omit it to read your own. Under session scope a DIFFERENT address is rejected.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "solana",
      "publicName": "solana__predict_orders_list",
      "readOnlyHint": true,
      "requiresEnv": "JUPITER_API_KEY",
      "title": "List Jupiter prediction orders",
      "toolId": "solana.predict.orders"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 883,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "count": {
            "description": "Number of data points to return, 1-100 (Vex owner-wide cap; provider default 10 when omitted).",
            "type": "number"
          },
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "interval": {
            "description": "Interval: 24h, 1w, or 1m.",
            "minLength": 1,
            "type": "string"
          },
          "walletAddress": {
            "description": "The wallet ACCOUNT to read, as a Solana address (not a token mint or a position key). Defaults to the session's selected Solana wallet - omit it to read your own. Under session scope a DIFFERENT address is rejected.",
            "type": "string"
          }
        },
        "required": [
          "interval"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "solana",
      "publicName": "solana__predict_pnl_history_get",
      "readOnlyHint": true,
      "requiresEnv": "JUPITER_API_KEY",
      "title": "Read Jupiter prediction realized-PnL history",
      "toolId": "solana.predict.pnlHistory"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 625,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "positionPubkey": {
            "description": "Position public key (from solana__predict_positions_list).",
            "minLength": 1,
            "type": "string"
          }
        },
        "required": [
          "positionPubkey"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "solana",
      "publicName": "solana__predict_position_get",
      "readOnlyHint": true,
      "requiresEnv": "JUPITER_API_KEY",
      "title": "Read one Jupiter prediction position",
      "toolId": "solana.predict.position"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1018,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "isYes": {
            "description": "Filter by side - true for YES, false for NO.",
            "type": "boolean"
          },
          "limit": {
            "description": "Max results, 1-100 (default 20).",
            "type": "number"
          },
          "marketId": {
            "description": "Filter by external market ID.",
            "type": "string"
          },
          "marketPubkey": {
            "description": "Filter by on-chain market account.",
            "type": "string"
          },
          "offset": {
            "description": "Skip first N results for pagination.",
            "type": "number"
          },
          "walletAddress": {
            "description": "The wallet ACCOUNT to read, as a Solana address (not a token mint or a position key). Defaults to the session's selected Solana wallet - omit it to read your own. Under session scope a DIFFERENT address is rejected.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "solana",
      "publicName": "solana__predict_positions_list",
      "readOnlyHint": true,
      "requiresEnv": "JUPITER_API_KEY",
      "title": "List open Jupiter prediction positions",
      "toolId": "solana.predict.positions"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 708,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "walletAddress": {
            "description": "The wallet ACCOUNT to read, as a Solana address (not a token mint or a position key). Defaults to the session's selected Solana wallet - omit it to read your own. Under session scope a DIFFERENT address is rejected.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "solana",
      "publicName": "solana__predict_profile_get",
      "readOnlyHint": true,
      "requiresEnv": "JUPITER_API_KEY",
      "title": "Read a Jupiter prediction trader profile",
      "toolId": "solana.predict.profile"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1112,
      "destructiveHint": true,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "positionPubkey": {
            "description": "Position public key (from solana__predict_positions_list).",
            "minLength": 1,
            "type": "string"
          }
        },
        "required": [
          "positionPubkey"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "solana",
      "publicName": "solana__predict_sell",
      "readOnlyHint": false,
      "requiresEnv": "JUPITER_API_KEY",
      "title": "Sell a Jupiter prediction position",
      "toolId": "solana.predict.sell"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 723,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "includeMarkets": {
            "description": "Include each recommended event's nested markets array (default false - lean).",
            "type": "boolean"
          },
          "provider": {
            "description": "Provider: kalshi, polymarket, or bisonfi (Jupiter Forecast). Default polymarket.",
            "type": "string"
          },
          "walletAddress": {
            "description": "The wallet ACCOUNT to base recommendations on, as a Solana address - any wallet, not only your own.",
            "minLength": 1,
            "type": "string"
          }
        },
        "required": [
          "walletAddress"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "solana",
      "publicName": "solana__predict_suggested_events_list",
      "readOnlyHint": true,
      "requiresEnv": "JUPITER_API_KEY",
      "title": "Suggest Jupiter prediction events for a wallet",
      "toolId": "solana.predict.suggestedEvents"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1009,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "id": {
            "description": "Filter to a single history event ID.",
            "type": "number"
          },
          "limit": {
            "description": "Max results, 1-100 (default 20).",
            "type": "number"
          },
          "offset": {
            "description": "Skip first N results for pagination.",
            "type": "number"
          },
          "positionPubkey": {
            "description": "Filter to one position's history.",
            "type": "string"
          },
          "walletAddress": {
            "description": "The wallet ACCOUNT to read, as a Solana address (not a token mint or a position key). Defaults to the session's selected Solana wallet - omit it to read your own. Under session scope a DIFFERENT address is rejected.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "solana",
      "publicName": "solana__predict_trade_history_list",
      "readOnlyHint": true,
      "requiresEnv": "JUPITER_API_KEY",
      "title": "Read Jupiter prediction trade history",
      "toolId": "solana.predict.history"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 906,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "limit": {
            "description": "Max results, 1-100. Required - this feed has no upstream owner/market scope, so an explicit window is required to avoid an unbounded response.",
            "type": "number"
          },
          "offset": {
            "description": "Skip first N results for pagination.",
            "type": "number"
          }
        },
        "required": [
          "limit"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "solana",
      "publicName": "solana__predict_trades_list",
      "readOnlyHint": true,
      "requiresEnv": "JUPITER_API_KEY",
      "title": "Browse the global Jupiter prediction trade feed",
      "toolId": "solana.predict.trades"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 604,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          }
        },
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "solana",
      "publicName": "solana__predict_trading_status_get",
      "readOnlyHint": true,
      "requiresEnv": "JUPITER_API_KEY",
      "title": "Check Jupiter prediction trading status",
      "toolId": "solana.predict.tradingStatus"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 578,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          }
        },
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "solana",
      "publicName": "solana__predict_vault_get",
      "readOnlyHint": true,
      "requiresEnv": "JUPITER_API_KEY",
      "title": "Read the Jupiter prediction protocol vault",
      "toolId": "solana.predict.vaultInfo"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1222,
      "destructiveHint": true,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "amountIn": {
            "description": "Amount to swap. In HUMAN decimal units (e.g. \"1.5\") - not wei, lamports, or any other base unit. Converted to base units exactly, with no floating-point step: a value carrying more precision than the input side holds is REJECTED, never silently rounded.",
            "minLength": 1,
            "type": "string"
          },
          "computeUnitPricePercentile": {
            "description": "Priority-fee strategy: medium (25th percentile), high (default, 50th), or veryHigh (75th). This is the LEVER for priority-fee cost - Jupiter picks the actual price per compute unit at the percentile you name, and Vex refuses to sign a build whose resulting priority fee exceeds 10,000,000 lamports (0.01 SOL). If a swap is refused for that reason, lower this and re-quote. Priority prices are congestion-driven and swing by an order of magnitude within minutes, so a fresh quote at the SAME setting often fits too - the refusal is not a property of the pair.",
            "type": "string"
          },
          "dexes": {
            "description": "Comma-separated DEX allow-list. Mutually exclusive with excludeDexes.",
            "type": "string"
          },
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "excludeDexes": {
            "description": "Comma-separated DEX deny-list. Mutually exclusive with dexes.",
            "type": "string"
          },
          "forJitoBundle": {
            "description": "Exclude DEXes incompatible with Jito bundles (default false).",
            "type": "boolean"
          },
          "maxAccounts": {
            "description": "Max account count in the route (1-64, default 64) - trims transaction size at the cost of route choice.",
            "type": "number"
          },
          "slippageBps": {
            "description": "Slippage tolerance in basis points (1 bps = 0.01%); default 100 = 1%, which fits deep, liquid pairs. It sets otherAmountThreshold, the minimum output the swap enforces once it executes, and is the ONLY price protection on the trade - the execute does NOT re-check the price against the quote. Pass the identical value to quote and execute (a mismatch blocks the execute). On a thin or volatile pair (new mints, memecoins, small pools) 100 bps often fails. Which failure you get depends on tipLamports. On the DEFAULT tipped lane (tipLamports omitted = 1,000,000) Jupiter forwards the transaction without simulating it, so a slippage failure ALWAYS lands on-chain FAILED: the network fee is spent, nothing is swapped, and the reason appears on the activity row rather than in this call's result - this call returns \"broadcast, confirmation pending\". Only tipLamports: 0 takes the RPC lane, which simulates first and can refuse for free with status \"rejected_before_broadcast\". Either way the remedy is the same: re-quote with a higher slippageBps and execute with the identical value. Read a slippage failure as \"this pair needs more tolerance\", not as \"this pair is untradeable\". Vex caps it at 1000 (10%) and REJECTS anything above rather than clamping. Raise it in steps - no tolerance is known to fit a given pair in advance, and every increase widens the worst-case price you accept.",
            "minimum": 0,
            "type": "integer"
          },
          "tipLamports": {
            "description": "SOL tip in lamports for /tx/v1/submit landing (default 1,000,000 = 0.001 SOL; max 10,000,000 = 0.01 SOL).",
            "type": "number"
          },
          "tokenIn": {
            "description": "Input token symbol or mint - the side you sell.",
            "minLength": 1,
            "type": "string"
          },
          "tokenOut": {
            "description": "Output token symbol or mint - the side you buy.",
            "minLength": 1,
            "type": "string"
          },
          "wrapAndUnwrapSol": {
            "description": "Auto wrap/unwrap native SOL (default true). It also decides WHICH SOL balance the input side spends, and the two are different balances: tokenIn \"SOL\" always means the wallet's own native lamports, while tokenIn set to the explicit wrapped-SOL mint So11111111111111111111111111111111111111112 with wrapAndUnwrapSol false means an existing wrapped-SOL token account. The explicit mint WITH wrapping enabled is REJECTED by name as ambiguous, and is never silently resolved to either balance.",
            "type": "boolean"
          }
        },
        "required": [
          "tokenIn",
          "tokenOut",
          "amountIn"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "solana",
      "publicName": "solana__swap_execute",
      "readOnlyHint": false,
      "requiresEnv": "JUPITER_API_KEY",
      "title": "Execute a Jupiter swap",
      "toolId": "solana.swap.execute"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 3052,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "amountIn": {
            "description": "Amount to swap. In HUMAN decimal units (e.g. \"1.5\") - not wei, lamports, or any other base unit. Converted to base units exactly, with no floating-point step: a value carrying more precision than the input side holds is REJECTED, never silently rounded.",
            "minLength": 1,
            "type": "string"
          },
          "computeUnitPricePercentile": {
            "description": "Priority-fee strategy: medium (25th percentile), high (default, 50th), or veryHigh (75th). This is the LEVER for priority-fee cost - Jupiter picks the actual price per compute unit at the percentile you name, and Vex refuses to sign a build whose resulting priority fee exceeds 10,000,000 lamports (0.01 SOL). If a swap is refused for that reason, lower this and re-quote. Priority prices are congestion-driven and swing by an order of magnitude within minutes, so a fresh quote at the SAME setting often fits too - the refusal is not a property of the pair.",
            "type": "string"
          },
          "dexes": {
            "description": "Comma-separated DEX allow-list. Mutually exclusive with excludeDexes.",
            "type": "string"
          },
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "excludeDexes": {
            "description": "Comma-separated DEX deny-list. Mutually exclusive with dexes.",
            "type": "string"
          },
          "forJitoBundle": {
            "description": "Exclude DEXes incompatible with Jito bundles (default false).",
            "type": "boolean"
          },
          "maxAccounts": {
            "description": "Max account count in the route (1-64, default 64) - trims transaction size at the cost of route choice.",
            "type": "number"
          },
          "slippageBps": {
            "description": "Slippage tolerance in basis points (1 bps = 0.01%); default 100 = 1%, which fits deep, liquid pairs. It sets otherAmountThreshold, the minimum output the swap enforces once it executes, and is the ONLY price protection on the trade - the execute does NOT re-check the price against the quote. Pass the identical value to quote and execute (a mismatch blocks the execute). On a thin or volatile pair (new mints, memecoins, small pools) 100 bps often fails. Which failure you get depends on tipLamports. On the DEFAULT tipped lane (tipLamports omitted = 1,000,000) Jupiter forwards the transaction without simulating it, so a slippage failure ALWAYS lands on-chain FAILED: the network fee is spent, nothing is swapped, and the reason appears on the activity row rather than in this call's result - this call returns \"broadcast, confirmation pending\". Only tipLamports: 0 takes the RPC lane, which simulates first and can refuse for free with status \"rejected_before_broadcast\". Either way the remedy is the same: re-quote with a higher slippageBps and execute with the identical value. Read a slippage failure as \"this pair needs more tolerance\", not as \"this pair is untradeable\". Vex caps it at 1000 (10%) and REJECTS anything above rather than clamping. Raise it in steps - no tolerance is known to fit a given pair in advance, and every increase widens the worst-case price you accept.",
            "minimum": 0,
            "type": "integer"
          },
          "tipLamports": {
            "description": "SOL tip in lamports for /tx/v1/submit landing (default 1,000,000 = 0.001 SOL; max 10,000,000 = 0.01 SOL).",
            "type": "number"
          },
          "tokenIn": {
            "description": "Input token symbol or mint - the side you sell.",
            "minLength": 1,
            "type": "string"
          },
          "tokenOut": {
            "description": "Output token symbol or mint - the side you buy.",
            "minLength": 1,
            "type": "string"
          },
          "wrapAndUnwrapSol": {
            "description": "Auto wrap/unwrap native SOL (default true). It also decides WHICH SOL balance the input side spends, and the two are different balances: tokenIn \"SOL\" always means the wallet's own native lamports, while tokenIn set to the explicit wrapped-SOL mint So11111111111111111111111111111111111111112 with wrapAndUnwrapSol false means an existing wrapped-SOL token account. The explicit mint WITH wrapping enabled is REJECTED by name as ambiguous, and is never silently resolved to either balance.",
            "type": "boolean"
          }
        },
        "required": [
          "tokenIn",
          "tokenOut",
          "amountIn"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "solana",
      "publicName": "solana__swap_quote",
      "readOnlyHint": true,
      "requiresEnv": "JUPITER_API_KEY",
      "title": "Quote a Jupiter swap",
      "toolId": "solana.swap.quote"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 637,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "mints": {
            "description": "Comma-separated mint addresses. Provide this OR queries, not both.",
            "type": "string"
          },
          "queries": {
            "description": "Comma-separated token symbols, names, or mint addresses to resolve and price (e.g. \"SOL,BONK\"). Provide this OR mints, not both.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "solana",
      "publicName": "solana__token_prices_get",
      "readOnlyHint": true,
      "requiresEnv": "JUPITER_API_KEY",
      "title": "Read Solana token prices",
      "toolId": "solana.prices"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1123,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "category": {
            "description": "Category: recent (freshly launched tokens, newest first - measured live: ages 10-175 s since mint, createdAt on every row; use for brand-new/fresh tokens), toptrending (most price movement), toptraded (highest volume), toporganicscore (highest real/organic activity), verified (Jupiter-verified), lst (liquid staking), stocks (tokenized equities, e.g. Ondo, Remora).",
            "enum": [
              "toptrending",
              "toptraded",
              "toporganicscore",
              "recent",
              "lst",
              "verified",
              "stocks"
            ],
            "type": "string"
          },
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "interval": {
            "description": "Time interval: 5m, 1h, 6h, 24h.",
            "enum": [
              "5m",
              "1h",
              "6h",
              "24h"
            ],
            "type": "string"
          },
          "limit": {
            "description": "Max rows returned (default 20). Server-side for the top* categories; applied Vex-side for recent/lst/verified/stocks with returned/totalMatched/hasMore accounting - nothing is silently dropped.",
            "type": "number"
          },
          "minLiquidity": {
            "description": "Only include tokens with liquidity (USD) >= this value. Rejected if negative.",
            "type": "number"
          },
          "minOrganicScore": {
            "description": "Only include tokens with organicScore >= this value (0-100). Rejected if out of range.",
            "type": "number"
          },
          "statsInterval": {
            "description": "Which stats window to include per token: 5m, 1h, 6h, or 24h, or all for every window (default: same as interval, or 1h).",
            "enum": [
              "5m",
              "1h",
              "6h",
              "24h",
              "all"
            ],
            "type": "string"
          },
          "verifiedOnly": {
            "description": "Only include Jupiter-verified tokens.",
            "type": "boolean"
          }
        },
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "solana",
      "publicName": "solana__tokens_discover",
      "readOnlyHint": true,
      "requiresEnv": "JUPITER_API_KEY",
      "title": "Discover new and trending Solana tokens",
      "toolId": "solana.tokens.trending"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 835,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "minLiquidity": {
            "description": "Only include tokens with liquidity (USD) >= this value. Rejected if negative.",
            "type": "number"
          },
          "minOrganicScore": {
            "description": "Only include tokens with organicScore >= this value (0-100). Rejected if out of range.",
            "type": "number"
          },
          "query": {
            "description": "Token name, symbol, or mint address. Accepts a comma-separated list of mint addresses (up to 100) for batch lookup.",
            "minLength": 1,
            "type": "string"
          },
          "statsInterval": {
            "description": "Which stats window to include per token: 5m, 1h, 6h, or 24h, or all for every window (default: 1h).",
            "enum": [
              "5m",
              "1h",
              "6h",
              "24h",
              "all"
            ],
            "type": "string"
          },
          "verifiedOnly": {
            "description": "Only include Jupiter-verified tokens.",
            "type": "boolean"
          }
        },
        "required": [
          "query"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "solana",
      "publicName": "solana__tokens_search",
      "readOnlyHint": true,
      "requiresEnv": "JUPITER_API_KEY",
      "title": "Search Solana tokens by name",
      "toolId": "solana.tokens.search"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1442,
      "destructiveHint": true,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "amountIn": {
            "description": "Amount in human-readable units. This is the TOTAL debited: the swap executes on this amount minus Vex's 25 bps fee.",
            "minLength": 1,
            "type": "string"
          },
          "chain": {
            "anyOf": [
              {
                "minLength": 1,
                "type": "string"
              },
              {
                "minimum": 1,
                "type": "integer"
              }
            ],
            "description": "The chain to swap on. Chain slug/alias, or the numeric chain id `TokenFind` returns (e.g. `base` or `8453`). Robinhood Chain is `robinhood` / `4663`."
          },
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "slippageBps": {
            "description": "Slippage tolerance in basis points (1 bps = 0.01%); default 100 = 1%, which fits deep, liquid pairs. It sets the minimum output written into the swap calldata, and is the ONLY price protection on the trade. Must match the uniswap__swap_quote value (or be omitted on both) - a mismatch blocks the execute. On a thin or volatile pair (new listings, memecoins, small pools) 100 bps often fails, in one of two ways. USUALLY it fails for FREE: the router's \"Too little received\" / \"INSUFFICIENT_OUTPUT_AMOUNT\" comes back from the pre-sign gas estimate, so nothing is signed and NO gas is spent - this call returns status \"not_attempted\" with retryable true, and the activity row records failure code slippage. LESS OFTEN the pool moves after that estimate passes: the router REVERTS once mined, the row records mined_revert, and the gas IS spent. Either is the signal to retry with more tolerance, not to give up on the pair: re-quote with a higher slippageBps and pass the same value here. Vex caps it at 1000 (10%) and REJECTS anything above rather than clamping. Raise it in steps, starting tight since the usual failure costs nothing - no tolerance is known to fit a given pair in advance, and every increase widens the worst-case price you accept.",
            "minimum": 0,
            "type": "integer"
          },
          "tokenIn": {
            "description": "Input token CONTRACT ADDRESS or native ETH/native. Uniswap has no symbol search.",
            "minLength": 1,
            "type": "string"
          },
          "tokenOut": {
            "description": "Output token CONTRACT ADDRESS or native ETH/native.",
            "minLength": 1,
            "type": "string"
          }
        },
        "required": [
          "chain",
          "tokenIn",
          "tokenOut",
          "amountIn"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "uniswap",
      "publicName": "uniswap__swap_execute",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Execute a Uniswap V2/V3 swap",
      "toolId": "uniswap.swap.execute"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1790,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "amountIn": {
            "description": "Amount in human-readable units. This is the TOTAL debited: the route is priced for this amount minus Vex's 25 bps fee.",
            "minLength": 1,
            "type": "string"
          },
          "chain": {
            "anyOf": [
              {
                "minLength": 1,
                "type": "string"
              },
              {
                "minimum": 1,
                "type": "integer"
              }
            ],
            "description": "The chain to quote on. Chain slug/alias, or the numeric chain id `TokenFind` returns (e.g. `base` or `8453`). Robinhood Chain is `robinhood` / `4663`."
          },
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "slippageBps": {
            "description": "Slippage tolerance in basis points (1 bps = 0.01%); default 100 = 1%, which fits deep, liquid pairs. It pins the minimum output the resulting swap will enforce, and is the ONLY price protection that trade has. Pass the SAME value to uniswap__swap_execute, or omit it on both - a mismatch blocks the execute. On a thin or volatile pair (new listings, memecoins, small pools) 100 bps often fails at execute time, in one of two ways. USUALLY it fails for FREE: the router's \"Too little received\" / \"INSUFFICIENT_OUTPUT_AMOUNT\" comes back from the pre-sign gas estimate, so nothing is signed and NO gas is spent, and the activity row records failure code slippage. LESS OFTEN the pool moves after that estimate passes: the router REVERTS once mined, the row records mined_revert, and the gas IS spent. After either, re-quote with a higher slippageBps rather than abandoning the pair - and since the usual failure costs nothing, a tighter tolerance is the cheap thing to try first. Vex caps it at 1000 (10%) and REJECTS anything above rather than clamping; every increase widens the worst-case price you accept.",
            "minimum": 0,
            "type": "integer"
          },
          "tokenIn": {
            "description": "Input token CONTRACT ADDRESS or native ETH/native. Uniswap has no symbol search - resolve a symbol to its address first.",
            "minLength": 1,
            "type": "string"
          },
          "tokenOut": {
            "description": "Output token CONTRACT ADDRESS or native ETH/native.",
            "minLength": 1,
            "type": "string"
          }
        },
        "required": [
          "chain",
          "tokenIn",
          "tokenOut",
          "amountIn"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "uniswap",
      "publicName": "uniswap__swap_quote",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Quote a Uniswap V2/V3 route",
      "toolId": "uniswap.swap.quote"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 2973,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "aggregate": {
            "description": "How many buckets each candle spans, and the legal set depends on the timeframe: minute accepts 1, 5 or 15; hour accepts 1, 4 or 12; day accepts only 1. Default 1. An illegal pairing is refused by name before the call is made.",
            "type": "number"
          },
          "beforeTimestampSeconds": {
            "description": "Walk backwards: return buckets strictly before this unix-seconds mark. The reply hands you the value to use for the next page, and pages neither skip nor overlap.",
            "type": "number"
          },
          "currency": {
            "description": "Denominate the candles in usd (default) or in the pool's quote token. Applies to the chart provider only; the curve sources price in VIRTUAL and refuse this by name.",
            "enum": [
              "usd",
              "token"
            ],
            "type": "string"
          },
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "id": {
            "description": "Numeric Virtuals agent id, exactly as virtuals__agents_discover returns it.",
            "type": "number"
          },
          "limit": {
            "description": "Candles to return (default 100, max 1000). Out of range is rejected by name, never clamped.",
            "type": "number"
          },
          "source": {
            "description": "Which history to read: auto (default) picks by lifecycle and chain, geckoterminal reads the indexed pool, tape reads the provider's curve trade feed, onchain reads the curve pair's own Swap logs. A source that cannot serve the agent is refused by name.",
            "enum": [
              "auto",
              "geckoterminal",
              "tape",
              "onchain"
            ],
            "type": "string"
          },
          "timeframe": {
            "description": "Bucket size: minute, hour (default) or day. The same three values whichever source answers.",
            "enum": [
              "minute",
              "hour",
              "day"
            ],
            "type": "string"
          }
        },
        "required": [
          "id"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "virtuals",
      "publicName": "virtuals__agent_candles_list",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Read a Virtuals agent's price candles",
      "toolId": "virtuals.candles"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1365,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "id": {
            "description": "Numeric Virtuals agent id, exactly as virtuals__agents_discover returns it (e.g. 96200 for VEX). The string spelling \"96200\" is accepted too.",
            "type": "number"
          }
        },
        "required": [
          "id"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "virtuals",
      "publicName": "virtuals__agent_get",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Read one Virtuals agent token",
      "toolId": "virtuals.get"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1677,
      "destructiveHint": true,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "chain": {
            "anyOf": [
              {
                "enum": [
                  "base",
                  "solana",
                  "robinhood",
                  "ethereum"
                ],
                "type": "string"
              },
              {
                "minimum": 1,
                "type": "integer"
              }
            ],
            "description": "Required together with token - Vex must know which BondingV5 to call. Chain slug/alias, or the numeric chain id `TokenFind` returns (e.g. `base` or `8453`)."
          },
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "intentId": {
            "description": "The launch to cancel, as returned by virtuals__agent_launch_execute. Vex resolves the token from its own record, but the CONTRACT still decides who may cancel and what is refunded. Pass this OR token.",
            "type": "string"
          },
          "simulateOnly": {
            "description": "When true, run every check and eth_call the exact cancelLaunch transaction from your wallet address, then return it with `cancelled: false`. No signing key is opened, no activity row is written and nothing is broadcast.",
            "type": "boolean"
          },
          "token": {
            "description": "The agent's ERC-20 contract address, for a launch Vex has no record of. Requires chain. The creator check is the contract's, so a token this wallet did not create is refused by name.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "virtuals",
      "publicName": "virtuals__agent_launch_cancel",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Cancel a Virtuals agent launch",
      "toolId": "virtuals.launch.cancel"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 2276,
      "destructiveHint": true,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "amountIn": {
            "description": "REQUIRED. The VIRTUAL you commit to the launch, as a plain decimal in WHOLE tokens - never wei, never a float. Vex's 25 bps fee comes out of it, so this is exactly what leaves the wallet. What is left goes to the venue and becomes the agent's INITIAL PURCHASE, which is the only amount a cancel would refund. The venue's own launch fee is 0 for the normal immediate launches Vex signs, read live from BondingConfig every time rather than assumed.",
            "minLength": 1,
            "type": "string"
          },
          "antiSniperTaxType": {
            "description": "Which anti-sniper window your agent's curve opens with. One of 0, 1, 2, 3, 4, 5; default 1, which is what the Virtuals app itself sends. 0 = none; 1 = 60 s on buys; 2 = 98 minutes on buys; 3 = 98 minutes on sells; 4 = 98 minutes on both; 5 = 10 minutes on buys. Inside the window the tax starts near 99 percent and decays linearly to zero, and it goes to the venue's anti-sniper vault - not to you. It cannot be changed after launch.",
            "enum": [
              "0",
              "1",
              "2",
              "3",
              "4",
              "5"
            ],
            "type": "string"
          },
          "chain": {
            "anyOf": [
              {
                "enum": [
                  "base",
                  "solana",
                  "robinhood",
                  "ethereum"
                ],
                "type": "string"
              },
              {
                "minimum": 1,
                "type": "integer"
              }
            ],
            "description": "REQUIRED. Where to launch the agent. Vex signs Virtuals launches on base and robinhood only. solana and ethereum answer with the measured reason they are closed: a Solana agent's curve is a Meteora pool the Virtuals BACKEND creates, and Virtuals runs no launch contract on Ethereum. Chain slug/alias, or the numeric chain id `TokenFind` returns (e.g. `base` or `8453`)."
          },
          "cores": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "minItems": 1,
                "type": "array"
              }
            ],
            "description": "REQUIRED. The venue's capability ids for this agent, as whole numbers 0-255, at most 8 of them. Send them as an array ([\"0\", \"1\", \"2\"]) or as a comma-separated string (\"0,1,2\"). BondingV5.preLaunch reverts with InvalidInput on an empty list. Vex does not carry a copy of the venue's core taxonomy - a stale copy would mislabel agents - so read the `cores` block of any existing agent with virtuals__agent_get to see which ids the platform uses."
          },
          "description": {
            "description": "REQUIRED. What the agent is, at most 2000 characters. It is written into contract storage permanently, so length is real gas on both chains and it cannot be edited afterwards.",
            "minLength": 1,
            "type": "string"
          },
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "imageId": {
            "description": "The staged picture, in the Vex app. REQUIRED on this surface. It must ALREADY be published: call launchpads__image_publish with the imageId first (it asks for your approval, because publishing makes the bytes fetchable by anyone), then launch. The URL written on chain is the content-addressed one that publish returned, so the picture cannot change after you approve it. You cannot pass a URL of your own.",
            "type": "string"
          },
          "imagePath": {
            "description": "The picture, over the Vex Studio MCP surface: a path to an image file inside THIS project. REQUIRED on that surface, where there is no image locker. Vex reads the bytes without following symlinks, publishes them to its content-addressed host and writes that hash URL on chain. You cannot pass a URL of your own.",
            "type": "string"
          },
          "links": {
            "description": "Optional social and website links minted into the agent's on-chain record, as an object with any of `twitter`, `telegram`, `youtube` and `website`. The contract stores them as a fixed four-slot list in that order, so an omitted one is written as an empty string. https only - http and URLs carrying credentials are refused by name, because the strings are written to permanent public storage and rendered as links by every explorer.",
            "type": "object"
          },
          "name": {
            "description": "REQUIRED. The agent's display name, at most 64 characters. THE TOKEN ON CHAIN IS NOT ALWAYS NAMED THIS: BondingV5 appends \" by Virtuals\" unless you pass nameSuffix: \"none\", so the ERC-20 the wallet ends up holding may carry a longer name than the string you pass. The preview shows the exact on-chain name as `agent.onChainName`; read it before approving.",
            "minLength": 1,
            "type": "string"
          },
          "nameSuffix": {
            "description": "Whether BondingV5 appends \" by Virtuals\" to the token name. Default \"by_virtuals\", which is the venue's own default and what its app sends; \"none\" sets the contract's skip-suffix flag and launches the bare name. Either way the preview shows the resulting on-chain name.",
            "enum": [
              "by_virtuals",
              "none"
            ],
            "type": "string"
          },
          "previewId": {
            "description": "REQUIRED for a real launch, and omitted for simulateOnly: pass the previewId virtuals__agent_launch_preview returned. It is single-use: the launch it authorizes retires it, so a second launch cannot run against one plan a person approved. It lapses a few minutes after the preview, because a plan priced against a chain that has moved is not the plan you read.",
            "type": "string"
          },
          "simulateOnly": {
            "description": "When true, stop at the edge of signing: re-read the chain, rebuild the exact transactions and eth_call each of them from your wallet address, then return them with `launched: false`. No signing key is opened, NO PREVIEW IS CLAIMED, no activity row is written and nothing is broadcast. Omit previewId when you use it. The preLaunch leg reverts in the simulation whenever the allowance leg above it has not run yet, by construction, and that is reported rather than hidden.",
            "type": "boolean"
          },
          "symbol": {
            "description": "REQUIRED. The token symbol - the venue's own `ticker_` argument - letters and digits only, at most 16 characters. It is uppercased before it is encoded, and the uppercased form is what the approval shows and what goes on chain.",
            "minLength": 1,
            "type": "string"
          }
        },
        "required": [
          "chain",
          "name",
          "symbol",
          "description",
          "cores",
          "amountIn"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "virtuals",
      "publicName": "virtuals__agent_launch_execute",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Launch a Virtuals agent",
      "toolId": "virtuals.launch.execute"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 2023,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "amountIn": {
            "description": "REQUIRED. The VIRTUAL you commit to the launch, as a plain decimal in WHOLE tokens - never wei, never a float. Vex's 25 bps fee comes out of it, so this is exactly what leaves the wallet. What is left goes to the venue and becomes the agent's INITIAL PURCHASE, which is the only amount a cancel would refund. The venue's own launch fee is 0 for the normal immediate launches Vex signs, read live from BondingConfig every time rather than assumed.",
            "minLength": 1,
            "type": "string"
          },
          "antiSniperTaxType": {
            "description": "Which anti-sniper window your agent's curve opens with. One of 0, 1, 2, 3, 4, 5; default 1, which is what the Virtuals app itself sends. 0 = none; 1 = 60 s on buys; 2 = 98 minutes on buys; 3 = 98 minutes on sells; 4 = 98 minutes on both; 5 = 10 minutes on buys. Inside the window the tax starts near 99 percent and decays linearly to zero, and it goes to the venue's anti-sniper vault - not to you. It cannot be changed after launch.",
            "enum": [
              "0",
              "1",
              "2",
              "3",
              "4",
              "5"
            ],
            "type": "string"
          },
          "chain": {
            "anyOf": [
              {
                "enum": [
                  "base",
                  "solana",
                  "robinhood",
                  "ethereum"
                ],
                "type": "string"
              },
              {
                "minimum": 1,
                "type": "integer"
              }
            ],
            "description": "REQUIRED. Where to launch the agent. Vex signs Virtuals launches on base and robinhood only. solana and ethereum answer with the measured reason they are closed: a Solana agent's curve is a Meteora pool the Virtuals BACKEND creates, and Virtuals runs no launch contract on Ethereum. Chain slug/alias, or the numeric chain id `TokenFind` returns (e.g. `base` or `8453`)."
          },
          "cores": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "items": {
                  "type": "string"
                },
                "minItems": 1,
                "type": "array"
              }
            ],
            "description": "REQUIRED. The venue's capability ids for this agent, as whole numbers 0-255, at most 8 of them. Send them as an array ([\"0\", \"1\", \"2\"]) or as a comma-separated string (\"0,1,2\"). BondingV5.preLaunch reverts with InvalidInput on an empty list. Vex does not carry a copy of the venue's core taxonomy - a stale copy would mislabel agents - so read the `cores` block of any existing agent with virtuals__agent_get to see which ids the platform uses."
          },
          "description": {
            "description": "REQUIRED. What the agent is, at most 2000 characters. It is written into contract storage permanently, so length is real gas on both chains and it cannot be edited afterwards.",
            "minLength": 1,
            "type": "string"
          },
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "imageId": {
            "description": "The staged picture, in the Vex app. REQUIRED on this surface. It must ALREADY be published: call launchpads__image_publish with the imageId first (it asks for your approval, because publishing makes the bytes fetchable by anyone), then launch. The URL written on chain is the content-addressed one that publish returned, so the picture cannot change after you approve it. You cannot pass a URL of your own.",
            "type": "string"
          },
          "imagePath": {
            "description": "The picture, over the Vex Studio MCP surface: a path to an image file inside THIS project. REQUIRED on that surface, where there is no image locker. Vex reads the bytes without following symlinks, publishes them to its content-addressed host and writes that hash URL on chain. You cannot pass a URL of your own.",
            "type": "string"
          },
          "links": {
            "description": "Optional social and website links minted into the agent's on-chain record, as an object with any of `twitter`, `telegram`, `youtube` and `website`. The contract stores them as a fixed four-slot list in that order, so an omitted one is written as an empty string. https only - http and URLs carrying credentials are refused by name, because the strings are written to permanent public storage and rendered as links by every explorer.",
            "type": "object"
          },
          "name": {
            "description": "REQUIRED. The agent's display name, at most 64 characters. THE TOKEN ON CHAIN IS NOT ALWAYS NAMED THIS: BondingV5 appends \" by Virtuals\" unless you pass nameSuffix: \"none\", so the ERC-20 the wallet ends up holding may carry a longer name than the string you pass. The preview shows the exact on-chain name as `agent.onChainName`; read it before approving.",
            "minLength": 1,
            "type": "string"
          },
          "nameSuffix": {
            "description": "Whether BondingV5 appends \" by Virtuals\" to the token name. Default \"by_virtuals\", which is the venue's own default and what its app sends; \"none\" sets the contract's skip-suffix flag and launches the bare name. Either way the preview shows the resulting on-chain name.",
            "enum": [
              "by_virtuals",
              "none"
            ],
            "type": "string"
          },
          "symbol": {
            "description": "REQUIRED. The token symbol - the venue's own `ticker_` argument - letters and digits only, at most 16 characters. It is uppercased before it is encoded, and the uppercased form is what the approval shows and what goes on chain.",
            "minLength": 1,
            "type": "string"
          }
        },
        "required": [
          "chain",
          "name",
          "symbol",
          "description",
          "cores",
          "amountIn"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "virtuals",
      "publicName": "virtuals__agent_launch_preview",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Plan a Virtuals agent launch",
      "toolId": "virtuals.launch.preview"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1889,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "chain": {
            "anyOf": [
              {
                "enum": [
                  "base",
                  "solana",
                  "robinhood",
                  "ethereum"
                ],
                "type": "string"
              },
              {
                "minimum": 1,
                "type": "integer"
              }
            ],
            "description": "Required together with token - Vex must know which BondingV5 to read. Chain slug/alias, or the numeric chain id `TokenFind` returns (e.g. `base` or `8453`)."
          },
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "intentId": {
            "description": "The launch to check, as returned by virtuals__agent_launch_execute. Gives the fullest answer: Vex's own record plus a fresh chain observation, and the scan is narrowed to the block the pre-launch landed in. Pass this OR token.",
            "type": "string"
          },
          "token": {
            "description": "The agent's ERC-20 contract address, for a launch Vex has no record of. Requires chain. The scan is then bounded by what the node will serve, so a very old launch may answer UNKNOWN rather than a state.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "virtuals",
      "publicName": "virtuals__agent_launch_status",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Check a Virtuals agent launch",
      "toolId": "virtuals.launch.status"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1971,
      "destructiveHint": true,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "acceptAntiSniperTaxPct": {
            "description": "The maximum anti-sniper tax, in WHOLE PERCENT (1-98), you accept on the side you are trading. OMIT IT to accept none, which is the default and refuses any active window. A fresh Virtuals launch is taxed by FRouterV3 starting at 99% and decaying linearly to zero over the window its anti-sniper type defines (60s, 600s or 5880s), so a trade inside that window can lose almost the whole amount. This parameter is consent to a BOUND, not to a value: the quote shows the current percent and the seconds remaining, and the execute refuses if the percent has risen above what you accepted.",
            "type": "number"
          },
          "amountIn": {
            "description": "REQUIRED. A plain decimal amount in WHOLE tokens, never wei and never a float: on a buy it is the total VIRTUAL you commit (Vex's fee comes out of it, so this is exactly what leaves the wallet); on a sell it is the agent tokens you sell.",
            "minLength": 1,
            "type": "string"
          },
          "chain": {
            "anyOf": [
              {
                "enum": [
                  "base",
                  "solana",
                  "robinhood",
                  "ethereum"
                ],
                "type": "string"
              },
              {
                "minimum": 1,
                "type": "integer"
              }
            ],
            "description": "REQUIRED. The chain the agent lives on. Vex signs bonding-curve trades on base and robinhood; solana and ethereum answer with a typed hand-off naming the tool that does trade them. Chain slug/alias, or the numeric chain id `TokenFind` returns (e.g. `base` or `8453`)."
          },
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "proposalId": {
            "description": "REQUIRED for a real execute, and REJECTED as unnecessary reasoning for simulateOnly: pass the proposalId virtuals__agent_trade_quote returned. It is the digest of everything that quote bound - the contracts and their implementations, the side, the amounts, the fee, the taxes and the floor - so passing it proves the trade being executed is the trade that was priced. A stale or mismatched proposalId is refused and nothing is signed. Omit it ONLY together with simulateOnly: true, which signs nothing.",
            "type": "string"
          },
          "side": {
            "description": "REQUIRED. buy spends VIRTUAL and receives the agent token; sell spends the agent token and receives VIRTUAL. The two are priced by different contract paths and are never interchangeable: a buy quote can never authorize a sell.",
            "enum": [
              "buy",
              "sell"
            ],
            "type": "string"
          },
          "simulateOnly": {
            "description": "When true, stop at the edge of signing: re-read the chain, re-price, build the exact transactions and eth_call each of them from your wallet address, then return them with `executed: false`. No signing key is opened, NO QUOTE IS CONSUMED, no activity row is written and nothing is broadcast. It still needs a FRESH QUOTE for the identical parameters, because it is inspecting a real proposal rather than an imaginary one - take the quote first, then simulate with the same chain, token, side, amountIn and slippageBps and no proposalId. A leg that depends on an allowance the wallet does not hold yet reverts in the simulation by construction, and that is reported rather than hidden.",
            "type": "boolean"
          },
          "slippageBps": {
            "description": "Slippage tolerance in basis points (1 bps = 0.01%). Default 100. Vex caps it at 1000 bps and REFUSES a higher value by name rather than clamping it. On a buy it bounds the agent tokens delivered; on a sell it bounds the router's GROSS VIRTUAL output before the curve's taxes, which is what the contract actually compares.",
            "minimum": 0,
            "type": "integer"
          },
          "token": {
            "description": "REQUIRED. The agent's ERC-20 contract address, exactly as virtuals__agents_discover and virtuals__agent_get return it: `preToken` while the agent is still on its bonding curve, `tokenAddress` once it has graduated. The chain decides what is tradable here, not the API row: BondingV5 is re-read for every call.",
            "minLength": 1,
            "type": "string"
          }
        },
        "required": [
          "chain",
          "token",
          "side",
          "amountIn"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "virtuals",
      "publicName": "virtuals__agent_trade_execute",
      "readOnlyHint": false,
      "requiresEnv": null,
      "title": "Trade a Virtuals agent on its bonding curve",
      "toolId": "virtuals.trade.execute"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1576,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "acceptAntiSniperTaxPct": {
            "description": "The maximum anti-sniper tax, in WHOLE PERCENT (1-98), you accept on the side you are trading. OMIT IT to accept none, which is the default and refuses any active window. A fresh Virtuals launch is taxed by FRouterV3 starting at 99% and decaying linearly to zero over the window its anti-sniper type defines (60s, 600s or 5880s), so a trade inside that window can lose almost the whole amount. This parameter is consent to a BOUND, not to a value: the quote shows the current percent and the seconds remaining, and the execute refuses if the percent has risen above what you accepted.",
            "type": "number"
          },
          "amountIn": {
            "description": "REQUIRED. A plain decimal amount in WHOLE tokens, never wei and never a float: on a buy it is the total VIRTUAL you commit (Vex's fee comes out of it, so this is exactly what leaves the wallet); on a sell it is the agent tokens you sell.",
            "minLength": 1,
            "type": "string"
          },
          "chain": {
            "anyOf": [
              {
                "enum": [
                  "base",
                  "solana",
                  "robinhood",
                  "ethereum"
                ],
                "type": "string"
              },
              {
                "minimum": 1,
                "type": "integer"
              }
            ],
            "description": "REQUIRED. The chain the agent lives on. Vex signs bonding-curve trades on base and robinhood; solana and ethereum answer with a typed hand-off naming the tool that does trade them. Chain slug/alias, or the numeric chain id `TokenFind` returns (e.g. `base` or `8453`)."
          },
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "side": {
            "description": "REQUIRED. buy spends VIRTUAL and receives the agent token; sell spends the agent token and receives VIRTUAL. The two are priced by different contract paths and are never interchangeable: a buy quote can never authorize a sell.",
            "enum": [
              "buy",
              "sell"
            ],
            "type": "string"
          },
          "slippageBps": {
            "description": "Slippage tolerance in basis points (1 bps = 0.01%). Default 100. Vex caps it at 1000 bps and REFUSES a higher value by name rather than clamping it. On a buy it bounds the agent tokens delivered; on a sell it bounds the router's GROSS VIRTUAL output before the curve's taxes, which is what the contract actually compares.",
            "minimum": 0,
            "type": "integer"
          },
          "token": {
            "description": "REQUIRED. The agent's ERC-20 contract address, exactly as virtuals__agents_discover and virtuals__agent_get return it: `preToken` while the agent is still on its bonding curve, `tokenAddress` once it has graduated. The chain decides what is tradable here, not the API row: BondingV5 is re-read for every call.",
            "minLength": 1,
            "type": "string"
          }
        },
        "required": [
          "chain",
          "token",
          "side",
          "amountIn"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "virtuals",
      "publicName": "virtuals__agent_trade_quote",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Price a Virtuals bonding-curve trade",
      "toolId": "virtuals.trade.quote"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1331,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "id": {
            "description": "Numeric Virtuals agent id, exactly as virtuals__agents_discover returns it.",
            "type": "number"
          },
          "limit": {
            "description": "Trades to return, newest first (default 30, max 200). Out of range is rejected, not clamped.",
            "type": "number"
          },
          "side": {
            "description": "Which side of the tape: both (default), buys only, or sells only. Applied by the provider.",
            "enum": [
              "both",
              "buys",
              "sells"
            ],
            "type": "string"
          }
        },
        "required": [
          "id"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "virtuals",
      "publicName": "virtuals__agent_trades_list",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Read a Virtuals agent's curve trade tape",
      "toolId": "virtuals.trades"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 2039,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "chain": {
            "anyOf": [
              {
                "enum": [
                  "base",
                  "solana",
                  "robinhood",
                  "ethereum"
                ],
                "type": "string"
              },
              {
                "minimum": 1,
                "type": "integer"
              }
            ],
            "description": "REQUIRED. The one chain to list - Virtuals indexes exactly four: base, solana, robinhood, ethereum. Chain slug/alias, or the numeric chain id `TokenFind` returns (e.g. `base` or `8453`)."
          },
          "createdAfter": {
            "description": "Only agents created at or after this date (YYYY-MM-DD or full ISO).",
            "type": "string"
          },
          "creatorWallet": {
            "description": "The wallet that launched the agent (the row's own walletAddress), case-insensitive.",
            "type": "string"
          },
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "excludeLaunchX": {
            "description": "Drop X_LAUNCH and ACP_LAUNCH agents, as the provider's own UI does by default.",
            "type": "boolean"
          },
          "factory": {
            "description": "The launch contract family. Live BASE counts on 2026-09-04: BONDING_V4 21,630, BONDING_V5 19,473, BONDING 15,326, BONDING_V2 112, VIBES_BONDING_V2 47, ERC20 30, ERC20_PRO 4, BONDING_V3 3, SOL_METEORA 0 (98 on solana). Every ROBOTIC_* member returned ZERO rows: a robotics agent is stored under the plain factory with launchInfo.isRobotics true, so use `isRobotics` for that screen.",
            "enum": [
              "ERC20",
              "BONDING",
              "BONDING_V2",
              "BONDING_V3",
              "BONDING_V4",
              "BONDING_V5",
              "ERC20_PRO",
              "ROBOTIC_BONDING_V2",
              "ROBOTIC_BONDING_V3",
              "ROBOTIC_BONDING_V4",
              "ROBOTIC_ERC20_PRO",
              "VIBES_BONDING_V2",
              "SOL_METEORA"
            ],
            "type": "string"
          },
          "genesisStartsAfter": {
            "description": "Only agents whose linked genesis sale starts at or after this date.",
            "type": "string"
          },
          "genesisStartsBefore": {
            "description": "Only agents whose linked genesis sale starts at or before this date.",
            "type": "string"
          },
          "hasAirdrop": {
            "description": "The launch reserved a non-zero airdrop percentage.",
            "type": "boolean"
          },
          "hasAntiSniperTax": {
            "description": "The launch configured a non-zero anti-sniper tax type. Note this is about CONFIGURATION, not about the window being open right now - read each row's antiSniper block for that.",
            "type": "boolean"
          },
          "hasFounderVideo": {
            "description": "The agent published a founder video pitch.",
            "type": "boolean"
          },
          "hasGenesis": {
            "description": "The agent came through a genesis points sale (its genesis relation is set).",
            "type": "boolean"
          },
          "hasGraduated": {
            "description": "The provider's own definition of graduated: lpCreatedAt is set.",
            "type": "boolean"
          },
          "hasMarginTrading": {
            "description": "The agent is margin-tradable. Zero rows on base and robinhood when last measured.",
            "type": "boolean"
          },
          "hasRevenueConnect": {
            "description": "The agent has a revenue-connect wallet configured.",
            "type": "boolean"
          },
          "hasStaking": {
            "description": "The agent has a staking address or an agent staking contract.",
            "type": "boolean"
          },
          "includeLaunchX": {
            "description": "Keep ONLY X_LAUNCH and ACP_LAUNCH agents. Opposite of excludeLaunchX; send at most one.",
            "type": "boolean"
          },
          "includePriceSeries": {
            "description": "Ask the provider for its 24h price samples and 24h low/high on each row, returned as `priceSeries24h`. These are irregularly spaced samples, not OHLC candles; for real candles on a graduated agent use virtuals__agent_candles_list.",
            "type": "boolean"
          },
          "isDevCommitted": {
            "description": "The developer made the on-platform commitment.",
            "type": "boolean"
          },
          "isProject60days": {
            "description": "The launch is in the 60-day project programme.",
            "type": "boolean"
          },
          "isRobotics": {
            "description": "A robotics agent. This is the working screen for robotics; the ROBOTIC_* factory values match nothing.",
            "type": "boolean"
          },
          "isVerified": {
            "description": "Anti-impersonation badge only - NOT a safety or quality signal. 45 rows carry it on base.",
            "type": "boolean"
          },
          "launchRadarEnabled": {
            "description": "The launch opted into Launch Radar.",
            "type": "boolean"
          },
          "launchedAfter": {
            "description": "Only agents whose bonding pair started trading at or after this date.",
            "type": "string"
          },
          "limit": {
            "description": "Rows to return on this page (default 20, max 100). This is OUR bound, not the provider's - it served a 10,000-row page live - because an agent row is large. Filtering is server-side, so a smaller page is not a smaller search: `totalMatched` says how many rows the filter found and `nextPage` reaches the rest. Out of range is rejected, not clamped.",
            "type": "number"
          },
          "maxHolderCount": {
            "description": "Ceiling on the holder count, for finding agents that are still early.",
            "type": "number"
          },
          "maxLiquidityUsd": {
            "description": "Liquidity ceiling (USD, display-grade).",
            "type": "number"
          },
          "maxMcapInVirtual": {
            "description": "Market cap ceiling, denominated in VIRTUAL, not USD.",
            "type": "number"
          },
          "maxPriceChangePercent24h": {
            "description": "24h change ceiling, in percent. Negative is legal.",
            "type": "number"
          },
          "maxTop10HolderPercentage": {
            "description": "Top-10 concentration ceiling, 0-100 - the usual way to screen out concentrated supply.",
            "type": "number"
          },
          "maxVolume24h": {
            "description": "24h volume ceiling (USD, display-grade).",
            "type": "number"
          },
          "minHolderCount": {
            "description": "Floor on the holder count - the usual screen against an agent nobody holds.",
            "type": "number"
          },
          "minLiquidityUsd": {
            "description": "Liquidity floor (USD, display-grade).",
            "type": "number"
          },
          "minMcapInVirtual": {
            "description": "Market cap floor, denominated in VIRTUAL, not USD.",
            "type": "number"
          },
          "minPriceChangePercent24h": {
            "description": "24h change floor, in percent. Negative is legal: -20 means down no more than 20 percent.",
            "type": "number"
          },
          "minTop10HolderPercentage": {
            "description": "Top-10 concentration floor, 0-100.",
            "type": "number"
          },
          "minVolume24h": {
            "description": "24h volume floor (USD, display-grade).",
            "type": "number"
          },
          "needAcf": {
            "description": "The launch used Automated Capital Formation (the 10 VIRTUAL launch-fee option).",
            "type": "boolean"
          },
          "page": {
            "description": "1-based provider page (default 1). The reply's `nextPage` names the next one.",
            "type": "number"
          },
          "pageSize": {
            "description": "Alias of `limit`, accepted for compatibility - the same knob now that filtering happens server-side. Send either spelling, never both spellings in the same call.",
            "type": "number"
          },
          "query": {
            "description": "Free text or an address. By default it matches name, symbol, tokenAddress and preToken; `searchScope` narrows that to one side or the other.",
            "type": "string"
          },
          "role": {
            "description": "The agent's declared role. Most rows have none at all (9,410 of 10,000 sampled), so this is a narrow screen: ON_CHAIN 3,392 rows chain-wide.",
            "enum": [
              "ENTERTAINMENT",
              "INFORMATION",
              "ON_CHAIN",
              "PRODUCTIVITY",
              "CREATIVE"
            ],
            "type": "string"
          },
          "searchScope": {
            "description": "What `query` matches: text (name and symbol only), address (exact token or preToken match, case-insensitive), or any (default, both). Only meaningful alongside `query`.",
            "enum": [
              "text",
              "address",
              "any"
            ],
            "type": "string"
          },
          "sort": {
            "description": "Alias of sortBy. Send either spelling, never both spellings in the same call.",
            "enum": [
              "circulatingSupply",
              "createdAt",
              "devHoldingPercentage",
              "fdvInVirtual",
              "holderCount",
              "holderCountPercent24h",
              "launchedAt",
              "level",
              "liquidityUsd",
              "lpCreatedAt",
              "mcapInVirtual",
              "mindshare",
              "netVolume24h",
              "priceChangePercent1h",
              "priceChangePercent5m",
              "priceChangePercent6h",
              "priceChangePercent24h",
              "top10HolderPercentage",
              "totalValueLocked",
              "updatedAt",
              "virtualTokenValue",
              "virtualsPoolVol24h",
              "volume1h",
              "volume5m",
              "volume6h",
              "volume24h",
              "mcap",
              "volume",
              "newest",
              "recentGraduation"
            ],
            "type": "string"
          },
          "sortBy": {
            "description": "Order the provider applies. Accepts its 26 sortable attributes directly, plus four keywords kept for compatibility: mcap (= mcapInVirtual, the default), volume (= volume24h), newest (= createdAt) and recentGraduation (= lpCreatedAt, which ALSO restricts the result to graduated agents, because ordering non-graduates by graduation time answers a different question). The provider refuses an attribute it does not have, so an unlisted key is a failed call rather than a default order.",
            "enum": [
              "circulatingSupply",
              "createdAt",
              "devHoldingPercentage",
              "fdvInVirtual",
              "holderCount",
              "holderCountPercent24h",
              "launchedAt",
              "level",
              "liquidityUsd",
              "lpCreatedAt",
              "mcapInVirtual",
              "mindshare",
              "netVolume24h",
              "priceChangePercent1h",
              "priceChangePercent5m",
              "priceChangePercent6h",
              "priceChangePercent24h",
              "top10HolderPercentage",
              "totalValueLocked",
              "updatedAt",
              "virtualTokenValue",
              "virtualsPoolVol24h",
              "volume1h",
              "volume5m",
              "volume6h",
              "volume24h",
              "mcap",
              "volume",
              "newest",
              "recentGraduation"
            ],
            "type": "string"
          },
          "sortDirection": {
            "description": "asc or desc (default desc). Declared as a closed set because the provider ACCEPTS a nonsense direction and silently sorts descending, so a typo would be invisible.",
            "enum": [
              "asc",
              "desc"
            ],
            "type": "string"
          },
          "status": {
            "description": "Lifecycle stage, applied SERVER-SIDE: undergrad (still on the bonding curve), graduated (AVAILABLE, trading on an AMM pool), genesis (in a points sale), or all (default, no filter). An unrecognised value is rejected, not ignored.",
            "enum": [
              "undergrad",
              "graduated",
              "genesis",
              "all"
            ],
            "type": "string"
          },
          "symbol": {
            "description": "Exact ticker match, case-insensitive. Symbols are NOT unique - VEX matched 10 rows.",
            "type": "string"
          },
          "tokenAddress": {
            "description": "Find the agent that owns this address. Matches EITHER the graduated tokenAddress OR the bonding preToken, because the same agent uses different columns before and after graduation.",
            "type": "string"
          },
          "vibesStatus": {
            "description": "The vibes/ICO lane state. PRECOMMIT is the only value the provider matches (19 rows live); anything else returns zero rows there, which is why the set is closed here.",
            "enum": [
              "PRECOMMIT"
            ],
            "type": "string"
          }
        },
        "required": [
          "chain"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "virtuals",
      "publicName": "virtuals__agents_discover",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Screen Virtuals agent tokens",
      "toolId": "virtuals.list"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 2347,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "chain": {
            "anyOf": [
              {
                "enum": [
                  "base",
                  "solana",
                  "robinhood",
                  "ethereum"
                ],
                "type": "string"
              },
              {
                "minimum": 1,
                "type": "integer"
              }
            ],
            "description": "The chain the agent lives on. Creator fees are held by a per-chain contract with its own payout asset and its own swap thresholds, so the chain and the agent must agree. Only base and robinhood have an AgentTaxV2; solana and ethereum are accepted and answered with `supported: false` and the measured reason rather than a schema error. Chain slug/alias, or the numeric chain id `TokenFind` returns (e.g. `base` or `8453`)."
          },
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "id": {
            "description": "The numeric Virtuals agent id, exactly as virtuals__agents_discover returns it. The tool resolves the agent's bonding token from it and additionally cross-checks the contract's creator address against the wallet the provider shows. Give this OR tokenAddress, never both.",
            "type": "number"
          },
          "tokenAddress": {
            "description": "The agent token's CONTRACT address - the bonding token while the agent is still on its curve, the same address after graduation. This is the key AgentTaxV2 accounts by, so it needs no provider lookup and works even when the Virtuals API is down. Give this OR id, never both.",
            "type": "string"
          }
        },
        "required": [
          "chain"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "virtuals",
      "publicName": "virtuals__creator_fees_get",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Read a Virtuals agent creator's fee status",
      "toolId": "virtuals.creator_fees"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 1767,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "chain": {
            "anyOf": [
              {
                "enum": [
                  "base",
                  "solana",
                  "robinhood",
                  "ethereum"
                ],
                "type": "string"
              },
              {
                "minimum": 1,
                "type": "integer"
              }
            ],
            "description": "Only sales whose agent is on this chain. Chain slug/alias, or the numeric chain id `TokenFind` returns (e.g. `base` or `8453`)."
          },
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "limit": {
            "description": "Rows to return on this page (default 20, max 100). This is OUR bound, not the provider's - it served a 10,000-row page live - because an agent row is large. Filtering is server-side, so a smaller page is not a smaller search: `totalMatched` says how many rows the filter found and `nextPage` reaches the rest. Out of range is rejected, not clamped.",
            "type": "number"
          },
          "page": {
            "description": "1-based provider page (default 1). The reply's `nextPage` names the next one.",
            "type": "number"
          },
          "pageSize": {
            "description": "Alias of `limit`, accepted for compatibility - the same knob now that filtering happens server-side. Send either spelling, never both spellings in the same call.",
            "type": "number"
          },
          "sortBy": {
            "description": "id (default), startsAt or endsAt. Closed on purpose: unlike the agents endpoint, this one does NOT validate the sort attribute - an unknown key is accepted and silently ignored.",
            "enum": [
              "id",
              "startsAt",
              "endsAt"
            ],
            "type": "string"
          },
          "sortDirection": {
            "description": "asc or desc (default desc).",
            "enum": [
              "asc",
              "desc"
            ],
            "type": "string"
          },
          "status": {
            "description": "Sale lifecycle. Live counts on 2026-09-04: FINALIZED 145, CANCELLED 33. An unknown value returns zero rows with no error upstream, which is why the set is closed here.",
            "enum": [
              "CANCELLED",
              "FAILED",
              "FINALIZED",
              "PREPROCESSING",
              "PROCESSING",
              "STARTED"
            ],
            "type": "string"
          }
        },
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "virtuals",
      "publicName": "virtuals__genesis_launches_list",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "Browse the Virtuals Genesis calendar",
      "toolId": "virtuals.geneses"
    },
    {
      "alwaysLoad": false,
      "descriptionBytes": 938,
      "destructiveHint": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "chain": {
            "anyOf": [
              {
                "enum": [
                  "base",
                  "solana",
                  "robinhood",
                  "ethereum"
                ],
                "type": "string"
              },
              {
                "minimum": 1,
                "type": "integer"
              }
            ],
            "description": "REQUIRED. The one chain to list - Virtuals indexes exactly four: base, solana, robinhood, ethereum. Chain slug/alias, or the numeric chain id `TokenFind` returns (e.g. `base` or `8453`)."
          },
          "dryRun": {
            "description": "Simulate this call instead of executing it. Runtime control parameter: when true the call is treated as a preview and nothing is broadcast.",
            "type": "boolean"
          },
          "limit": {
            "description": "Rows to return on this page (default 20, max 100). This is OUR bound, not the provider's - it served a 10,000-row page live - because an agent row is large. Filtering is server-side, so a smaller page is not a smaller search: `totalMatched` says how many rows the filter found and `nextPage` reaches the rest. Out of range is rejected, not clamped.",
            "type": "number"
          },
          "page": {
            "description": "1-based provider page (default 1). The reply's `nextPage` names the next one.",
            "type": "number"
          },
          "pageSize": {
            "description": "Alias of `limit`, accepted for compatibility - the same knob now that filtering happens server-side. Send either spelling, never both spellings in the same call.",
            "type": "number"
          }
        },
        "required": [
          "chain"
        ],
        "type": "object"
      },
      "kind": "protocol",
      "namespace": "virtuals",
      "publicName": "virtuals__graduations_list",
      "readOnlyHint": true,
      "requiresEnv": null,
      "title": "List recent Virtuals graduations",
      "toolId": "virtuals.graduations"
    }
  ]
}
